---
phase: 25-forecast-integration-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/cashflow.js, src/utils/cashflow.test.js]
autonomous: true
requirements: [DEBT-04.1, DEBT-04.4]
must_haves:
  truths:
    - "calculateForecast excludes recurrent expenses that are already finished (cycleTotal > 0 && cycleCurrent >= cycleTotal)"
    - "calculateForecast excludes recurrent expenses that are already marked as paid (status === 'paid')"
    - "Daily snapshots in the forecast contain a hasDebtPayment flag"
  artifacts:
    - path: "src/utils/cashflow.js"
      provides: "Forecast calculation logic"
    - path: "src/utils/cashflow.test.js"
      provides: "Verification of filtering logic"
  key_links:
    - from: "src/utils/cashflow.js"
      to: "dailyBalanceSnapshots"
      via: "return value of calculateForecast"
---

<objective>
Update the forecast calculation engine to correctly filter out completed or already-paid recurrent expenses and identify debt payments.

Purpose: Prevent double-counting of debt payments in cash flow projections and provide data for UI indicators.
Output: Refined `calculateForecast` function and supporting unit tests.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/utils/cashflow.js
@src/utils/cashflow.test.js
@src/db/repository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update calculateForecast logic</name>
  <files>src/utils/cashflow.js</files>
  <action>
    Modify `calculateForecast` in `src/utils/cashflow.js`:
    1. In the `recurrentWithEffective` pre-calculation (around line 170), add filtering logic:
       - Skip items where `cycleTotal > 0` and `cycleCurrent >= cycleTotal`.
       - Skip items where `status === 'paid'`.
    2. Within the forecast loop (around line 190):
       - Identify if any `dayRecurrentExpenses` (the filtered set for the current day) has `isDebtPayment === true`.
       - Add `hasDebtPayment: boolean` to the snapshot object pushed to the `snapshots` array.
    
    Ensure `isDebtPayment` is correctly handled (it's a property on the `recurrentExpense` record).
  </action>
  <verify>Code builds and lint passes</verify>
  <done>calculateForecast correctly filters expenses and flags debt payments.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for forecast filtering</name>
  <files>src/utils/cashflow.test.js</files>
  <action>
    Add a new test case in `src/utils/cashflow.test.js` under the `calculateForecast` describe block:
    - "excludes finished or paid recurrent expenses and flags debt payments"
    - Create mock data with:
      - One recurrent expense with `cycleTotal: 1`, `cycleCurrent: 1` (finished).
      - One recurrent expense with `status: 'paid'`.
      - One active debt payment expense (`isDebtPayment: true`).
      - One normal active recurrent expense.
    - Verify that:
      - Finished and paid expenses are NOT included in `expenseTotal`.
      - The active debt payment IS included and its day has `hasDebtPayment: true`.
      - Other days have `hasDebtPayment: false`.
  </action>
  <verify>npm test src/utils/cashflow.test.js</verify>
  <done>Unit tests pass and verify the new logic.</done>
</task>

</tasks>

<verification>
Run unit tests to ensure no regressions in forecast logic and correct filtering of debt payments.
</verification>

<success_criteria>
- calculateForecast excludes finished/paid items.
- snapshots contain hasDebtPayment flag.
- unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/25-forecast-integration-and-polish/25-01-SUMMARY.md`
</output>
