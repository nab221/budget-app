---
phase: 05-pdf-bank-statement-import
plan: 05-01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/utils/pdf-parser.js, src/db/repository.js, src/utils/string-similarity.js]
autonomous: true
requirements: [PDF-01, PDF-04, PDF-05]

must_haves:
  truths:
    - "System can extract text items with [x, y] coordinates from UK bank PDFs"
    - "Scanned/Image-based PDFs are correctly detected as non-text"
    - "Potential duplicates are identified by Date + Amount"
    - "Fuzzy matching suggests categories based on existing transaction history"
  artifacts:
    - path: "src/utils/pdf-parser.js"
      provides: "Coordinate-based extraction engine and bank-specific Regex patterns"
    - path: "src/db/repository.js"
      provides: "findDuplicates and suggestCategory logic"
  key_links:
    - from: "src/utils/pdf-parser.js"
      to: "pdfjs-dist"
      via: "import and worker setup"
    - from: "src/db/repository.js"
      to: "src/utils/string-similarity.js"
      via: "Jaro-Winkler comparison"
---

<objective>
Setup the core PDF parsing engine and data layer enhancements required for bank statement import.

Purpose: Provide the foundational text extraction and transaction matching logic that the UI will consume.
Output: Coordinate-based PDF extractor, initial bank parsers (TSB, Lloyds, Santander), and duplicate/suggestion repository methods.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pdf-bank-statement-import/05-CONTEXT.md
@.planning/phases/05-pdf-bank-statement-import/05-RESEARCH.md
@src/db/repository.js
@src/utils/currency.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Parsing Infrastructure</name>
  <files>package.json, src/utils/pdf-parser.js, src/utils/string-similarity.js</files>
  <action>
    Install `pdfjs-dist`, `string-similarity`, and `date-fns`.
    Create `src/utils/pdf-parser.js` with `pdfjs-dist` worker setup (Vite-compatible `new Worker()`).
    Implement `extractTextFromPdf(file)` utility that:
    1. Returns a structured representation of the text with [x, y] coordinates.
    2. Detects scanned/image-based PDFs (returns early with error if text content is null/empty).
    3. Groups text items by Y-coordinate (rows) and sorts by X-coordinate (columns).
    
    Implement `string-similarity` wrapper in `src/utils/string-similarity.js` (standard Jaro-Winkler or Sorensen-Dice).
  </action>
  <verify>
    `npm test` or mock script that extracts text from a known UK bank PDF fixture.
    Ensure scanned PDF fixture returns "NO_TEXT_LAYER" error.
  </verify>
  <done>PDF engine can extract rows of text from multi-page PDFs with high accuracy.</done>
</task>

<task type="auto">
  <name>Task 2: Initial Parsers & Repository Enhancements</name>
  <files>src/utils/pdf-parser.js, src/db/repository.js</files>
  <action>
    In `src/utils/pdf-parser.js`, implement auto-parsers for:
    - **Lloyds/TSB Credit Card:** Use Regex patterns to find Date, Description, Amount (Out/In).
    - **Santander Current:** Focus on Salary and Direct Debit identification.
    
    In `src/db/repository.js`, add:
    - `findDuplicates(transactions)`: Compare against existing `income`, `fixedSpends`, and `variableSpends` by Date + Amount (converted to pence).
    - `suggestCategory(description)`: Compare `description` against previous unique `(description, categoryId)` pairs using `string-similarity`. Use 0.85 confidence threshold.
    - `updateCategorizationLearningRule(transactions)`: Update internal mapping of descriptions to categories after a successful import.
  </action>
  <verify>
    Unit test for `findDuplicates` returns expected matches for pre-existing DB records.
    Unit test for `suggestCategory` returns correct category for "TESCO STORES" given "TESCO" history.
    Parser tests for Lloyds/TSB/Santander samples correctly extract Date (in ISO) and Amount (in pence).
  </verify>
  <done>Core data layer can identify duplicates and suggest categories for extracted transactions.</done>
</task>

</tasks>

<verification>
1. Verify `pdfjs-dist` worker loads correctly in browser environment (no worker-loader errors).
2. Confirm coordinate-based grouping handles multi-line descriptions common in Lloyds statements.
3. Validate duplicate detection correctly identifies same-day same-amount transactions.
</verification>

<success_criteria>
- Core PDF extractor correctly groups text by rows from UK bank statement fixtures.
- Duplicate detection flags transactions with matching Date and Amount.
- Suggestion engine provides category IDs for merchants matching historical records.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-bank-statement-import/05-01-SUMMARY.md`
</output>
