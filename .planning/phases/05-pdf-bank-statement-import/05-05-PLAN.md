---
phase: 05-pdf-bank-statement-import
plan: 05
type: tdd
wave: 2
depends_on: ["05-04"]
files_modified: [src/utils/pdf-parser.test.js, src/ui/pdf-import.js, .planning/phases/05-pdf-bank-statement-import/05-VERIFICATION.md, .planning/phases/05-pdf-bank-statement-import/05-UAT.md]
autonomous: true
requirements: [PDF-01, PDF-04]
must_haves:
  truths:
    - "Unit tests cover all implemented bank parsers (Lloyds, Santander, Nationwide, Amex, MBNA, TSB)"
    - "User can copy diagnostic debug info to the clipboard"
    - "05-VERIFICATION.md and 05-UAT.md are completed with objective evidence"
  artifacts:
    - path: "src/utils/pdf-parser.test.js"
      provides: "Regression safety for all parsing regex"
    - path: ".planning/phases/05-pdf-bank-statement-import/05-VERIFICATION.md"
      provides: "Requirement traceability and sign-off"
---

<objective>
Stabilize the PDF parsing logic with comprehensive unit tests and diagnostic tools, and formally verify all phase requirements through structured documentation.

Purpose: Guarantee parser accuracy and provide a clear path for troubleshooting future bank statement format changes.
Output: Robust test suite, diagnostic "Copy Debug Info" button, and formal verification docs.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pdf-bank-statement-import/05.1-CONTEXT.md
@src/utils/pdf-parser.js
@src/utils/pdf-parser.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Unit Tests for All Bank Parsers</name>
  <files>src/utils/pdf-parser.test.js</files>
  <action>
    Expand the existing Vitest suite in `src/utils/pdf-parser.test.js`:
    - Add test cases for `parsers.lloydsTsbCredit`, `parsers.santanderCurrent`, `parsers.nationwide`, `parsers.amex`, `parsers.mbna`, and `parsers.tsbMortgage`.
    - Each test should pass mock row data (matching the bank's observed format) and verify the extracted transactions.
    - Specifically verify that `tsbMortgage` correctly calculates Capital Repaid as `Payment - Interest`.
    - Note: If Barclays, HSBC, or NatWest parsers are missing from the code but required by the roadmap, document them as "Deferred/Unsupported" in the verification file unless implementation is trivial.
  </action>
  <verify>Run `npm test src/utils/pdf-parser.test.js` and verify all parsers are tested and passing.</verify>
  <done>All implemented regex patterns are verified against known-good row data samples.</done>
</task>

<task type="auto">
  <name>Task 2: Add Diagnostic Support and Error Boundaries</name>
  <files>src/ui/pdf-import.js</files>
  <action>
    Add troubleshooting tools to the import UI:
    - Implement a "Copy Debug Info" button in the `Manual Mapping` and `Error` modals.
    - This button should copy a JSON string of `this.state.rawPdfRows` to the clipboard (scrubbed or raw, per CONTEXT).
    - Wrap the `handleFileUpload` call in `src/app.js` or the entry point in `pdf-import.js` in a `try-catch` to prevent fatal app crashes.
    - Show a graceful "Parsing Error" screen with the "Copy Debug Info" button if extraction fails.
  </action>
  <verify>Manually trigger a mapping failure or error; click "Copy Debug Info"; verify clipboard content is valid JSON representing the PDF text structure.</verify>
  <done>Developers have a clear path to diagnostic data for resolving layout-specific parsing issues.</done>
</task>

<task type="auto">
  <name>Task 3: Formal Phase Verification and Sign-off</name>
  <files>.planning/phases/05-pdf-bank-statement-import/05-VERIFICATION.md, .planning/phases/05-pdf-bank-statement-import/05-UAT.md, .planning/ROADMAP.md</files>
  <action>
    Create and populate the final verification files:
    - **05-VERIFICATION.md**: List requirements PDF-01 to PDF-05. Map them to automated tests (PDF-01, 04) or manual UAT (PDF-02, 03, 05).
    - **05-UAT.md**: Log a manual test pass for each bank, including the manual mapping fallback.
    - Update `ROADMAP.md` and `STATE.md` to mark Phase 5 and 5.1 as Complete.
  </action>
  <verify>All documents are committed and accurately reflect the verified state of the feature.</verify>
  <done>Phase 5 and 5.1 are formally completed and the project can proceed to Phase 7.</done>
</task>

</tasks>

<verification>
- Vitest suite covers all parser patterns.
- "Copy Debug Info" works correctly in the browser.
- Verification docs provide clear traceability.
</verification>

<success_criteria>
- Zero regression risk for bank parser regex patterns.
- Robust error handling for malformed or scanned PDFs.
- Formal documentation of all "Success Criteria" from the Roadmap.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-bank-statement-import/05-05-SUMMARY.md`
</output>
