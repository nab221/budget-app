---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/db/repository.js
  - src/ui/categories.js
  - src/app.js
autonomous: false
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - CAT-04
must_haves:
  truths:
    - "User can view, add, and delete categories in both Fixed and Variable groups"
    - "The Fixed Spend and Variable Spend category dropdowns populate from the DB immediately"
    - "Category list is persistent across page reloads"
  artifacts:
    - path: "src/db/repository.js"
      provides: "Category data access logic"
    - path: "src/ui/categories.js"
      provides: "Category management UI rendering and event handling"
  key_links:
    - from: "src/ui/categories.js"
      to: "src/db/repository.js"
      via: "calls"
      pattern: "repository\..*Category"
---

<objective>
Implement the Category Management feature, including data repository, UI rendering, and the seeding of default categories.

Purpose: Delivers the first functional business feature of the app and ensures all spending forms have category data available.
Output: Complete Category Management module.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@budget-app.html
@src/db/schema.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Category Repository Layer</name>
  <files>src/db/repository.js</files>
  <action>
    Create a data access layer for categories using Dexie.js.
    - Implement `getCategories()`: Returns all categories from the `categories` table.
    - Implement `addCategory(group, name)`: Adds a new category to the `categories` table.
    - Implement `deleteCategory(id)`: Deletes a category by ID.
    - Implement `seedDefaultCategories()`: Adds the default list (from RESEARCH.md/Legacy) if the table is empty.
  </action>
  <verify>Call `repository.getCategories()` in console and check for results.</verify>
  <done>Category data management is abstracted.</done>
</task>

<task type="auto">
  <name>Task 2: Category UI Management</name>
  <files>src/ui/categories.js, src/app.js</files>
  <action>
    Implement the Category Management UI logic.
    1. Implement a render function for the category settings view (splitting Fixed vs Variable groups).
    2. Add event listeners for adding and deleting categories.
    3. Implement a way to populate the Fixed and Variable spending dropdowns across the app whenever categories change.
    4. Ensure all user-provided strings (category names) are sanitized using `src/ui/render.js:safeHTML` before display.
    5. Initialize the category view and data seeding in `src/app.js`.
  </action>
  <verify>Confirm category addition/deletion works and updates the UI lists and dropdowns correctly.</verify>
  <done>User can fully manage budget categories.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Functional Verification: Category Management</name>
  <what-built>Functional category management system.</what-built>
  <how-to-verify>
    1. Visit the settings tab.
    2. Click "Seed defaults" (or verify they are seeded on first load).
    3. Add a new fixed category called "Streaming Services".
    4. Confirm it appears in the Fixed Categories list and the Fixed Spend dropdown.
    5. Delete a category and confirm it is removed from both lists/dropdowns.
    6. Verify that trying to add an empty name is prevented.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check for proper integration between the repository and UI. Verify XSS protection for category names.
</verification>

<success_criteria>
- Category management fulfills Success Criterion 1 from ROADMAP.md.
- UI uses `safeHTML` for rendering category names.
- Dropdowns reflect current DB state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
