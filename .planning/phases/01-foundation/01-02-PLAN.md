---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - index.html
  - src/app.js
  - css/main.css
  - src/ui/theme.js
  - src/utils/storage.js
  - src/ui/render.js
autonomous: false
requirements:
  - FOUND-03
  - FOUND-04
  - THEME-01
  - THEME-02
must_haves:
  truths:
    - "Dark/Light theme toggle works and preference persists on refresh"
    - "App shell structure matches the original layout in a modular format"
    - "Malicious HTML in rendered strings is sanitized (XSS protection)"
    - "A banner/reminder is shown if storage persistence is denied"
    - "The app.js entry point correctly initializes theme and storage logic on load"
  artifacts:
    - path: "index.html"
      provides: "App Shell"
    - path: "src/ui/theme.js"
      provides: "Persistent theme toggling logic"
    - path: "src/ui/render.js"
      provides: "DOMPurify-wrapped safe rendering"
  key_links:
    - from: "src/ui/render.js"
      to: "DOMPurify"
      via: "import"
      pattern: "import DOMPurify from 'dompurify'"
---

<objective>
Implement the core UI shell, persistent theme toggling, and security layers (XSS protection and storage persistence).

Purpose: Migrates the UI structure, adds required theme capabilities, and secures the app from data loss and injection attacks.
Output: Secure, themeable App Shell.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@budget-app.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style Migration & Theme Logic</name>
  <files>css/main.css, src/ui/theme.js</files>
  <action>
    Extract and modularize styles and theme logic.
    1. Create `css/main.css` containing the CSS variables and global styles from `budget-app.html`.
    2. Enhance CSS variables to support light mode (default in `budget-app.html` is dark-only).
    3. Implement `src/ui/theme.js` to manage the theme preference in `localStorage` and toggle the `data-theme` or `.light-mode` class on `document.documentElement`.
    4. Provide a way to initialize the theme on load.
  </action>
  <verify>Check `localStorage.getItem('theme')` changes after toggling and the UI visually updates.</verify>
  <done>Theme toggling works and is persistent.</done>
</task>

<task type="auto">
  <name>Task 2: App Shell Migration & Security Wiring</name>
  <files>index.html, src/app.js, src/ui/render.js, src/utils/storage.js</files>
  <action>
    Set up the secure rendering engine, the HTML shell, and wire the initialization logic.
    1. Create `src/ui/render.js` implementing a `safeHTML` template tag that uses `DOMPurify.sanitize()` to prevent XSS (as per RESEARCH.md).
    2. Implement `src/utils/storage.js` with `ensurePersistence()` which calls `navigator.storage.persist()` to mitigate Safari ITP data loss (as per Success Criteria 5).
    3. Add a UI reminder/banner in `index.html` (e.g., `#persistence-warning`) that is shown if `ensurePersistence()` returns `false` or if permission is denied.
    4. Refactor `index.html` to be a clean "App Shell" with the header, navigation tabs, and a main container for dynamic content.
    5. Update `src/app.js` to import and call the theme initialization (`src/ui/theme.js`) and the storage persistence check (`src/utils/storage.js`).
    6. Link the stylesheets and the main ESM entry point in `index.html`.
  </action>
  <verify>Verify `DOMPurify` is sanitizing an test string with `<script>` tags when using `safeHTML`. Confirm the persistence warning appears if you manually block storage permission. Check that theme loads correctly on startup via `app.js`.</verify>
  <done>App Shell is secure, themeable, and wired for initialization.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>App Shell with theme toggling and foundational security.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit the app.
    2. Toggle the theme button; confirm visual change and persistence across reloads.
    3. Confirm the browser console shows "Storage persisted: true/false".
    4. Check for layout parity with the original `budget-app.html`.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure no script errors in the console and the theme toggle is fully functional.
</verification>

<success_criteria>
- HTML structure is clean and modular.
- XSS protection is implemented for all future dynamic rendering.
- Theme preference is stored in localStorage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
