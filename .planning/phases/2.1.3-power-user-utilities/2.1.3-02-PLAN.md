# Phase 2.1.3: Power-User Utilities - Plan 02 (Settings & Balance Consolidation)

## Objective
Finalize the "Balance Start Configuration" block in Settings and wire the persistence logic with atomic recalculation triggers.

## Wave: 1
## Depends on: []

## Tasks
- **Task 1: Implement Save Logic in src/ui/render.js (or app.js)**
  - Bind `click` event to `#saveBalanceStartBtn`.
  - Read values from `#balanceStartDate` and `#balanceOpeningAmount`.
  - Convert amount to pence (integer) and save both to `localStorage` using keys from `src/utils/storage.js`.
  - [ ] Modified `src/ui/render.js` (or wherever settings logic is handled).
- **Task 2: Trigger Balance Recalculation**
  - After saving, call `triggerBalanceRecalc()` from `src/db/repository.js`.
  - Update `#balanceStartStatus` with "Saving..." then "Recalculation complete."
  - Dispatch `app:refresh` to update the dashboard.
  - [ ] Verified background recalculation and UI refresh.

## Verification
- Change the start month or opening balance in the Settings tab.
- Click "Save".
- Verify that the "Current Balance" on the Dashboard and the running balance in the Expenses/Income tabs correctly reflect the change.
