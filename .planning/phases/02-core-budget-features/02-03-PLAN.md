---
phase: 02-core-budget-features
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/utils/finance.js, src/ui/debts.js, src/ui/assets.js, src/app.js]
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04, DEBT-05, DEBT-06, ASSET-01, ASSET-02, ASSET-03]
must_haves:
  truths:
    - "Debt tracker accurately calculates UK credit card minimum payments"
    - "Debt statements are logged and displayed in reverse chronological order"
    - "Asset values are logged and included in simple net-worth calculations"
  artifacts:
    - path: "src/utils/finance.js"
      provides: "calcMinPayment and utilization utility functions"
    - path: "src/ui/debts.js"
      provides: "Debt management UI"
    - path: "src/ui/assets.js"
      provides: "Asset management UI"
  key_links:
    - from: "src/ui/debts.js"
      to: "src/utils/finance.js"
      via: "Function call for min payment"
---

<objective>
Implement debt and asset tracking features, including specialized calculations for UK debt rules and statement logging.

Purpose: Allow users to track their balance sheet (debts and assets).
Output: Functional CRUD for debts/statements and assets.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-budget-features/02-RESEARCH.md
@index.html
@src/utils/currency.js
@src/db/repository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Financial Utilities</name>
  <files>src/utils/finance.js</files>
  <action>
    Create `src/utils/finance.js`:
    - Implement `calcMinPayment(balancePence, aprPercent, feesPence)`:
      - Rule: `max(1% balance + interest, 2.25% balance, £5 floor)`.
      - Monthly interest calculation: `(balancePence * (apr / 100)) / 12`.
    - Implement `calcUtilization(balancePence, limitPence)`:
      - Returns percentage.
  </action>
  <verify>Run a node test script with balance=£1000, APR=20%; verify output matches expected UK bank rule.</verify>
  <done>Utility functions for debt calculations are accurate and robust.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Debt and Statement UI</name>
  <files>src/ui/debts.js</files>
  <action>
    Create `src/ui/debts.js`:
    - Implement CRUD for Debt entities (Name, Type, APR, Credit Limit, Current Balance).
    - Implement Statement Logging:
      - Form to log new statements for a specific debt (Date, Amount, Interest, Fees). Adding a statement MUST update the currentBalance of the main debt record.
      - Display history in a table, sorted by date DESC.
    - Integration:
      - Show balance, utilization %, and calculated minimum payment on the debt summary.
    - Standardize money fields via `toPence`.
  </action>
  <verify>Add a debt with £1000 balance and £5000 limit; verify utilization shows 20% and min payment is displayed.</verify>
  <done>Debt tracking with statement history is fully functional.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Asset UI and Integration</name>
  <files>src/ui/assets.js, src/app.js</files>
  <action>
    - Create `src/ui/assets.js`:
      - Implement CRUD for Assets (Name, Type, As of Date, Current Balance).
    - Update `src/app.js`:
      - Import and initialize `debtUI` and `assetUI`.
  </action>
  <verify>Add an asset (e.g., Savings Account, £2000); verify it appears in the Assets tab.</verify>
  <done>Asset tracking is implemented and wired into the main application.</done>
</task>

</tasks>

<verification>
Check debt list for correct calculations. Log multiple statements for a debt and check chronological sorting.
</verification>

<success_criteria>
- Debt tracker displays credit utilization and minimum payments.
- Statement logging works and updates debt balances correctly.
- Asset tracking allows recording of balances over time.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-budget-features/02-03-SUMMARY.md`
</output>
