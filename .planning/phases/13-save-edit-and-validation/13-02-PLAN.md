---
phase: 13-save-edit-and-validation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified: [src/ui/debts.test.js]
autonomous: true
requirements: [ADD-01, ADD-02, ADD-03, EDIT-01, EDIT-02]

must_haves:
  truths:
    - "Test ADD-01 fails: _saveDebt does not call debtRepository.add yet"
    - "Test ADD-02 fails: inline validation errors are not displayed"
    - "Test EDIT-01 fails: _saveDebt does not call debtRepository.update yet"
    - "Test EDIT-02 fails: _populateEditFields does not exist or is not called"
  artifacts:
    - path: "src/ui/debts.test.js"
      provides: "Failing unit tests for Phase 13 requirements"
  key_links:
    - from: "src/ui/debts.test.js"
      to: "src/ui/debts.js"
      via: "unit test execution"
---

<objective>
Create failing unit tests (RED phase of TDD) for the Save, Edit, and Validation requirements of Phase 13.

Purpose: Define expected behaviors for Add/Edit flows and inline validation before implementation.
Output: Updated src/ui/debts.test.js with failing test cases for ADD-01 through EDIT-02.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v2.5-phases/13-save-edit-and-validation/13-RESEARCH.md
@src/ui/debts.js
@src/ui/debts.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for Add mode (ADD-01, ADD-02, ADD-03)</name>
  <files>src/ui/debts.test.js</files>
  <behavior>
    - ADD-01: _saveDebt() in Add mode calls debtRepository.add() with correct payload and calls _closeDebtModal()
    - ADD-02: Empty name triggers _showFieldError and prevents debtRepository.add()
    - ADD-03: openDebtModal() (Add mode) shows empty/reset inputs
  </behavior>
  <action>
    Add a new 'describe("debtUI save and edit", ...)' block to src/ui/debts.test.js.
    Implement the following test cases:
    1. 'ADD-01: _saveDebt() in Add mode saves payload and closes modal'
       - Build full form DOM (name, type, and fieldsets).
       - Set editingId = null.
       - Set values in inputs.
       - Call await debtUI._saveDebt().
       - Expect debtRepository.add to have been called with correct payload.
       - Expect modalUI.close to have been called.
    2. 'ADD-02: validation error on empty name prevents save and shows error span'
       - Build form DOM.
       - Set name input to empty string.
       - Call await debtUI._saveDebt().
       - Expect debtRepository.add NOT to have been called.
       - Expect a .field-error element to exist in the DOM with "Name is required".
    3. 'ADD-03: openDebtModal in Add mode ensures fresh form'
       - Mock modalUI.show to actually set document.body.innerHTML = content.
       - Call await debtUI.openDebtModal(null).
       - Expect inputs (e.g., debtNameInput) to have empty values.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js</automated>
  </verify>
  <done>Tests for ADD-01, ADD-02, and ADD-03 are failing as expected (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing tests for Edit mode (EDIT-01, EDIT-02)</name>
  <files>src/ui/debts.test.js</files>
  <behavior>
    - EDIT-01: _saveDebt() in Edit mode calls debtRepository.update() with correct payload and ID
    - EDIT-02: openDebtModal(id) pre-populates all fields from fetched debt record
  </behavior>
  <action>
    Add the following test cases to the 'debtUI save and edit' block:
    1. 'EDIT-01: _saveDebt() in Edit mode updates existing record and closes modal'
       - Build full form DOM.
       - Set editingId = 123.
       - Set values in inputs.
       - Call await debtUI._saveDebt().
       - Expect debtRepository.update to have been called with (123, payload).
       - Expect modalUI.close to have been called.
    2. 'EDIT-02: openDebtModal(id) pre-populates fields'
       - Mock debtRepository.get to return a debt with known values (including mortgage-specific fields).
       - Mock modalUI.show to set innerHTML.
       - Call await debtUI.openDebtModal(1).
       - Expect input values (name, balance, term, rate) to match fetched record (converting balance via fromPence).
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js</automated>
  </verify>
  <done>Tests for EDIT-01 and EDIT-02 are failing as expected (RED).</done>
</task>

</tasks>

<verification>
Run the tests:
`npx vitest run src/ui/debts.test.js`
Expected: 5 new tests fail, previous Phase 11 & 12 tests (9 total) continue to pass (unless broken by new mocks).
</verification>

<success_criteria>
- src/ui/debts.test.js contains comprehensive tests for Add, Edit, and Inline Validation.
- All new tests fail (RED) because implementation is missing.
- No existing functionality from Phases 11-12 is regressed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-save-edit-and-validation/13-02-SUMMARY.md`
</output>
