---
phase: 31-banking-calendar-recurrence-upgrade
plan: "02"
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/utils/recurrence.js
  - src/utils/recurrence.test.js
  - src/db/schema.js
  - src/db/repository.js
  - index.html
  - src/app.js
autonomous: true
requirements:
  - TECH-03
  - PLAN-03

must_haves:
  truths:
    - "Recurring expenses with paymentAdjustment: 'next-working-day' produce an adjusted predictedPaymentDate on weekend/holiday nominal dates"
    - "Recurring expenses without paymentAdjustment (or 'none') behave identically to v2.7 — no regressions"
    - "IndexedDB schema upgrades cleanly from v18 to v19 on existing data, defaulting paymentAdjustment to 'none'"
    - "The 'Refresh bank holidays' Settings button appears in the Preferences section and calls refreshBankHolidaysCache()"
    - "refreshBankHolidaysCache() is called fire-and-forget on app startup — never blocks render"
    - "All 393+ existing Vitest tests still pass"
  artifacts:
    - path: src/utils/recurrence.js
      provides: "generateInstances and advanceNextDate emit predictedPaymentDate using adjustedPaymentDate"
      contains: "adjustedPaymentDate"
    - path: src/utils/recurrence.test.js
      provides: "Extended test suite covering paymentAdjustment: 'next-working-day' and 'none' cases"
    - path: src/db/schema.js
      provides: "Dexie v19 with paymentAdjustment in recurrentExpenses index string"
      contains: "version(19)"
    - path: src/db/repository.js
      provides: "v19 migration upgrade() setting paymentAdjustment = 'none' for existing records"
    - path: index.html
      provides: "Refresh bank holidays button in Settings Preferences section"
      contains: "refreshBankHolidaysBtn"
    - path: src/app.js
      provides: "Fire-and-forget refreshBankHolidaysCache() call during startup"
  key_links:
    - from: src/utils/recurrence.js
      to: src/utils/banking-calendar.js
      via: "import { adjustedPaymentDate } from './banking-calendar.js'"
      pattern: "adjustedPaymentDate"
    - from: src/db/schema.js
      to: recurrentExpenses
      via: "version(19).stores() index string includes paymentAdjustment"
      pattern: "version\\(19\\)"
    - from: index.html
      to: src/app.js
      via: "button#refreshBankHolidaysBtn click handler calls refreshBankHolidaysCache()"
      pattern: "refreshBankHolidaysBtn"
---

<objective>
Wire the banking calendar into the recurrence engine and schema. Extend `recurrence.js` to honour `paymentAdjustment: 'next-working-day'`, bump the Dexie schema to v19, add a "Refresh bank holidays" button to the Settings panel, and start the cache refresh on app startup.

Purpose: Downstream phases (32, 33, 34) rely on `predictedPaymentDate` in recurrent expenses reflecting the actual bank-processed date, not the nominal calendar date.
Output: Modified recurrence.js, recurrence.test.js, schema.js, repository.js, index.html, app.js — all consistent with the banking-calendar module from Plan 01.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-banking-calendar-recurrence-upgrade/31-CONTEXT.md
@.planning/phases/31-banking-calendar-recurrence-upgrade/31-RESEARCH.md
@.planning/phases/31-banking-calendar-recurrence-upgrade/31-01-SUMMARY.md

<interfaces>
<!-- Contracts from banking-calendar.js (Plan 01 output) -->
From src/utils/banking-calendar.js:
  export function isUKBankHoliday(date)                         // date: Date | 'YYYY-MM-DD' string → boolean
  export function isWorkingDay(date)                            // date: Date | string → boolean
  export function nextWorkingDay(date)                          // date: Date | string → Date
  export function adjustedPaymentDate(nominalDate, adjustment)  // adjustment: 'none'|'next-working-day' → Date
  export async function refreshBankHolidaysCache()              // → void, fire-and-forget

<!-- Existing recurrence.js contracts (src/utils/recurrence.js) -->
From src/utils/recurrence.js:
  export function generateInstances(base, frequency, count) → Array
    // base: recurrentExpense object (has .parentDate, .date, .recurrenceId, .predictedPaymentDate)
    // frequency: 'weekly'|'biweekly'|'monthly'|'quarterly'|'annually'
    // count: number
    // returns: array of instances with date, nextDate, predictedPaymentDate set

  export function advanceNextDate(item) → { nextDate: string, cycleCurrent: number }
    // item: recurrentExpense with .nextDate, .frequency, .isDebtPayment, .cycleTotal, .cycleCurrent

  export const RecurrenceManager = { async checkAndGenerate() → { recurrentExpenses, oneOffExpenses } }

<!-- Current schema (src/db/schema.js at v18) -->
  recurrentExpenses index string at v18:
  '++id, date, categoryId, label, amount, status, frequency, nextDate, predictedPaymentDate, isEssential, cycleTotal, cycleCurrent, endDate, isDebtPayment, linkedStatementId, isRecurring, recurrenceId, parentDate, debtType, isCleared, isReconciled, linkedDebtId'

  v19 must append: , paymentAdjustment
  (full new string shown in Task 2 action)

<!-- app.js startup pattern -->
From src/app.js (lines ~96-104):
  Startup uses Promise.all([...]) pattern.
  RecurrenceManager.checkAndGenerate() is called as fire-and-forget IIFE inside Promise.all.
  validateDataIntegrity() is called after render as .then() chain (line 254).
  refreshBankHolidaysCache() must follow the same fire-and-forget pattern — never awaited in the startup chain.
  Import: add { refreshBankHolidaysCache } from './utils/banking-calendar.js' at top of app.js.

<!-- index.html Settings panel location -->
  "Preferences" section starts at line 376.
  "Haptic Feedback" checkbox is at lines 378-382.
  Insert "Refresh bank holidays" button AFTER the haptic feedback block (before the closing </div> of the Preferences section, line 383).
  Button: <button id="refreshBankHolidaysBtn" class="secondary" style="margin-top:8px">Refresh bank holidays</button>
  Hint text: <div class="hint" style="margin-top:4px;font-size:.75rem">Update the cached UK bank holidays list from GOV.UK (requires internet connection).</div>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend recurrence.js to apply paymentAdjustment</name>
  <files>src/utils/recurrence.js, src/utils/recurrence.test.js</files>
  <behavior>
    Add to recurrence.test.js BEFORE modifying recurrence.js (RED phase):

    - generateInstances with base.paymentAdjustment = 'next-working-day':
      - monthly expense with parentDate '2026-01-01' (bank holiday), count 1:
        instance.date should be '2026-02-01' (nominal, February)
        instance.predictedPaymentDate should be '2026-02-02' (Mon, since 2026-02-01 is a Sunday — but wait, 2026-02-01 is a Sunday → adjustedPaymentDate → '2026-02-02')
        OR pick a test where nominal falls on a known weekend date to make the assertion clear
      - Use nominal date where adjustment is unambiguous: parentDate '2026-03-21' (Saturday), count 1, monthly:
        instance.date = '2026-04-21' (Tuesday — normal working day)
        instance.predictedPaymentDate = '2026-04-21' (no adjustment needed)
        Better: parentDate '2026-03-14' (Saturday in March — actually Sunday), choose parentDate '2026-08-29' (Summer Bank Holiday 2026), count 1, monthly:
        instance.date = '2026-09-29' (Tuesday — working day)
        Simplest approach: test a monthly where the generated date lands on a weekend and confirm predictedPaymentDate is advanced vs date field.

    - generateInstances with base.paymentAdjustment = 'none' or undefined:
      - same as before — instance.date === instance.predictedPaymentDate (no adjustment)

    - advanceNextDate with item.paymentAdjustment = 'next-working-day':
      - returns { nextDate: nominalStr, predictedPaymentDate: adjustedStr, cycleCurrent }
      - nominalStr and adjustedStr differ when nominal lands on weekend/holiday

    - advanceNextDate with item.paymentAdjustment = 'none' or undefined:
      - returns same shape as before — no predictedPaymentDate key change (backward-compatible)
  </behavior>
  <action>
    After RED tests exist, modify recurrence.js:

    1. Add import at top:
       import { adjustedPaymentDate } from './banking-calendar.js';

    2. In generateInstances — after computing dateStr (line 47), replace the predictedPaymentDate assignment:
       BEFORE: if (instance.predictedPaymentDate) instance.predictedPaymentDate = dateStr;
       AFTER:
         const adjustment = base.paymentAdjustment || 'none';
         const adjustedDate = adjustedPaymentDate(nextInstanceDate, adjustment);
         const adjustedDateStr = format(adjustedDate, 'yyyy-MM-dd');
         instance.predictedPaymentDate = adjustedDateStr;
         instance.paymentAdjustment = adjustment;
         // instance.date remains the nominal scheduling anchor — DO NOT set instance.date = adjustedDateStr

    3. In advanceNextDate — after computing nextDate string, add predictedPaymentDate to the return:
       const adjustment = item.paymentAdjustment || 'none';
       const adjustedDate = adjustedPaymentDate(parseISO(nextDate), adjustment);
       const predictedPaymentDate = format(adjustedDate, 'yyyy-MM-dd');
       return { nextDate, predictedPaymentDate, cycleCurrent };

    Critical constraint: instance.date ALWAYS stays as the nominal date (scheduling anchor for drift prevention).
    predictedPaymentDate is the adjusted display/affordability field.
    Do NOT change the RecurrenceManager.checkAndGenerate() logic — it uses generateInstances which already handles adjustment.
  </action>
  <verify>
    <automated>npx vitest run src/utils/recurrence.test.js</automated>
  </verify>
  <done>All recurrence.test.js tests pass. New paymentAdjustment tests are GREEN. npm test shows 393+ tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Schema v19 migration + repository default</name>
  <files>src/db/schema.js, src/db/repository.js</files>
  <action>
    In src/db/schema.js, add after the v18 block:

    db.version(19).stores({
      income: '++id, date, source, amount, categoryId, isCleared, isReconciled',
      recurrentExpenses: '++id, date, categoryId, label, amount, status, frequency, nextDate, predictedPaymentDate, isEssential, cycleTotal, cycleCurrent, endDate, isDebtPayment, linkedStatementId, isRecurring, recurrenceId, parentDate, debtType, isCleared, isReconciled, linkedDebtId, paymentAdjustment',
      oneOffExpenses: '++id, date, categoryId, note, amount, isRecurring, frequency, recurrenceId, parentDate, isCleared, isReconciled',
      debts: '++id, name, debtType, apr, creditLimit, currentBalance, promoEndDate, postPromoApr, originalPrincipal, termMonths, fixedMonthlyPayment, interestRate, earlyRepaymentFee, earlyRepaymentFeeIsPercent, earlyRepaymentAllowed, isInterestOnly',
      statements: '++id, debtId, date, amount, interest, fees, actualPaymentDate, linkedExpenseId',
      assets: '++id, name, type, asOfDate, currentBalance',
      categories: '++id, name, group',
      targets: '++id, bucket, amount',
      netWorthSnapshots: '++id, month, totalAssets, totalDebt, netWorth',
      categoryMappings: '++id, description, categoryId',
      childcareAccounts: '++id, childName, targetMonthlySpend, entitlementStart, isDisabled, openingBalance',
      childcareLedger: '++id, accountId, date, type, amount, runningBalance',
      balanceSnapshots: '++id, month, openingBalance, closingBalance, incomeTotal, expenseTotal',
      dailyBalanceSnapshots: '++id, date, openingBalance, closingBalance, incomeTotal, expenseTotal',
      expectedIncome: '++id, date, source, amount, categoryId, status',
      bankHolidayOverrides: '++id, date, isOpen'
    }).upgrade(async tx => {
      await tx.table('recurrentExpenses').toCollection().modify(item => {
        if (item.paymentAdjustment === undefined) item.paymentAdjustment = 'none';
      });
    });

    In src/db/repository.js — search for any place that creates or updates recurrentExpenses records and ensure `paymentAdjustment` defaults to `'none'` if not provided. Check the existing `addRecurrentExpense` or equivalent function. If there is a default object shape, add: paymentAdjustment: data.paymentAdjustment ?? 'none'. Do NOT change the migration — that's in schema.js.

    Pitfall to avoid (from RESEARCH.md): paymentAdjustment MUST be in the Dexie index string (v19 stores()) or Dexie cannot query/filter on it. Verify the string above contains it before committing.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test passes (393+ tests). No Dexie version mismatch errors. v19 appears in schema.js. The upgrade() block sets paymentAdjustment = 'none' for existing records.</done>
</task>

<task type="auto">
  <name>Task 3: Settings button + app.js startup wire</name>
  <files>index.html, src/app.js</files>
  <action>
    index.html — Preferences section (around line 383, after the haptic feedback block):
    Insert before the closing </div> of the Preferences section:

    <div style="margin-top:10px">
      <button id="refreshBankHolidaysBtn" class="secondary" style="font-size:.85rem">Refresh bank holidays</button>
      <div class="hint" style="margin-top:4px;font-size:.75rem">Update the cached UK bank holidays list from GOV.UK (requires internet connection).</div>
    </div>

    src/app.js — two changes:

    1. Add import at the top with other utils imports:
       import { refreshBankHolidaysCache } from './utils/banking-calendar.js';

    2. In the startup sequence (around line 96-104), add a fire-and-forget IIFE alongside the RecurrenceManager call:
       (async () => {
         try {
           // Check cache age and refresh if >365 days old or missing
           const cacheDate = localStorage.getItem('uk_bank_holidays_cache_date');
           const isStale = !cacheDate || (Date.now() - new Date(cacheDate).getTime()) > 365 * 24 * 60 * 60 * 1000;
           if (isStale) {
             refreshBankHolidaysCache(); // fire-and-forget — no await
           }
         } catch (err) {
           console.warn('[app] Bank holidays cache check failed:', err);
         }
       })(),

    3. After the UI is initialized (after cloudSyncUI.init()), wire the Settings button handler:
       const refreshBankHolidaysBtn = document.getElementById('refreshBankHolidaysBtn');
       if (refreshBankHolidaysBtn) {
         refreshBankHolidaysBtn.addEventListener('click', async () => {
           refreshBankHolidaysBtn.disabled = true;
           refreshBankHolidaysBtn.textContent = 'Refreshing...';
           await refreshBankHolidaysCache();
           refreshBankHolidaysBtn.disabled = false;
           refreshBankHolidaysBtn.textContent = 'Refresh bank holidays';
           notificationUI.success('Bank holidays updated.');
         });
       }

    Constraint: refreshBankHolidaysCache() in the startup path is NEVER awaited — it is fire-and-forget. The button handler awaits it only for UX feedback (disabled state while in progress).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>npm test passes (393+ tests). "Refresh bank holidays" button exists in index.html in the Preferences section. app.js imports refreshBankHolidaysCache and calls it fire-and-forget on startup. Button handler wired after UI init.</done>
</task>

</tasks>

<verification>
Full suite after all tasks:
```
npm test
```
393+ tests pass (all pre-existing tests green, new recurrence.test.js extensions green).

Manual smoke check: open the app in browser, go to Settings tab, verify "Refresh bank holidays" button is visible in the Preferences section. Check browser console — no errors on startup. Confirm localStorage keys exist after startup: `uk_bank_holidays_cache` and `uk_bank_holidays_cache_date`.
</verification>

<success_criteria>
- recurrence.js imports adjustedPaymentDate from banking-calendar.js
- generateInstances produces predictedPaymentDate reflecting banking-day adjustment when paymentAdjustment is 'next-working-day'
- generateInstances leaves instance.date as the nominal scheduling anchor (not adjusted)
- advanceNextDate returns { nextDate, predictedPaymentDate, cycleCurrent }
- Existing recurrentExpenses with no paymentAdjustment are unaffected (default 'none')
- Dexie v19 in schema.js with paymentAdjustment in the recurrentExpenses index string
- Migration upgrade() sets paymentAdjustment = 'none' for all existing records
- "Refresh bank holidays" button renders in Settings Preferences section
- refreshBankHolidaysCache() called fire-and-forget on app startup (cache staleness check)
- npm test: 393+ tests pass, zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/31-banking-calendar-recurrence-upgrade/31-02-SUMMARY.md`
</output>
