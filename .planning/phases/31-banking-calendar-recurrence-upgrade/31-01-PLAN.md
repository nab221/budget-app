---
phase: 31-banking-calendar-recurrence-upgrade
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/banking-calendar.js
  - src/utils/banking-calendar.test.js
autonomous: true
requirements:
  - TECH-02

must_haves:
  truths:
    - "isUKBankHoliday returns true for all bundled 2025-2026 England & Wales holidays"
    - "nextWorkingDay returns the same date when called on an existing working day"
    - "nextWorkingDay skips weekends and bank holidays in sequence (e.g. Christmas 2026 cluster)"
    - "refreshBankHolidaysCache stores a JSON array under 'uk_bank_holidays_cache' and a date under 'uk_bank_holidays_cache_date'"
    - "Functions fall back to the static set when localStorage is empty or unparseable"
    - "maxCachedYear guard emits console.warn for dates beyond the cached range and applies weekend-only logic"
  artifacts:
    - path: src/utils/banking-calendar.js
      provides: "Synchronous banking calendar — isUKBankHoliday, isWorkingDay, nextWorkingDay, adjustedPaymentDate, refreshBankHolidaysCache"
      exports:
        - isUKBankHoliday
        - isWorkingDay
        - nextWorkingDay
        - adjustedPaymentDate
        - refreshBankHolidaysCache
    - path: src/utils/banking-calendar.test.js
      provides: "Vitest test suite — jsdom environment, ≥90% branch coverage"
  key_links:
    - from: src/utils/banking-calendar.js
      to: localStorage
      via: "loadBankHolidays() reads 'uk_bank_holidays_cache', refreshBankHolidaysCache() writes it"
      pattern: "localStorage\\.getItem\\('uk_bank_holidays_cache'\\)"
    - from: src/utils/banking-calendar.js
      to: "https://www.gov.uk/bank-holidays.json"
      via: "refreshBankHolidaysCache() fetches england-and-wales events array"
      pattern: "england-and-wales"
---

<objective>
Create `src/utils/banking-calendar.js` — a synchronous, pure-function module providing UK banking day logic for England & Wales. No DB access, no async dependency chains. This is the foundational module consumed by the recurrence engine (Plan 02) and ultimately by the Pay-Period Affordability Engine (Phase 34).

Purpose: The recurrence engine cannot await DB calls inside Dexie transactions. A synchronous, localStorage-backed holiday set is the only safe option.
Output: `banking-calendar.js` (module) and `banking-calendar.test.js` (Vitest suite, ≥90% branch coverage).
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-banking-calendar-recurrence-upgrade/31-CONTEXT.md
@.planning/phases/31-banking-calendar-recurrence-upgrade/31-RESEARCH.md

<interfaces>
<!-- Pre-existing code the executor must NOT conflict with. -->

From src/utils/cashflow.js (lines 15-16, 31+):
  - Cache key used by cashflow.js: 'bank-holidays-cache'  (different from the new module's keys)
  - Cache format in cashflow.js: { timestamp, dates }  (different format)
  - cashflow.js exports async: fetchHolidays(), isBankHoliday(), isWorkingDay(), nextWorkingDay()
  - The new module is synchronous — NO name collision at runtime (different module files)
  - DO NOT change or import from cashflow.js

New module keys (do not overlap with cashflow.js):
  CACHE_KEY      = 'uk_bank_holidays_cache'          // value: JSON array of YYYY-MM-DD strings
  CACHE_DATE_KEY = 'uk_bank_holidays_cache_date'     // value: ISO date string of last refresh
  CACHE_EXPIRY_DAYS = 365
</interfaces>
</context>

<feature>
  <name>Banking Calendar Utility (synchronous)</name>
  <files>src/utils/banking-calendar.js, src/utils/banking-calendar.test.js</files>
  <behavior>
    RED phase — write tests first, run `npx vitest run src/utils/banking-calendar.test.js` (must fail), then implement.

    Tests to write (// @vitest-environment jsdom at file top, beforeEach: localStorage.clear()):

    isUKBankHoliday(date):
    - new Date('2026-01-01T00:00:00Z') → true  (New Year's Day)
    - new Date('2026-04-03T00:00:00Z') → true  (Good Friday)
    - new Date('2026-12-28T00:00:00Z') → true  (Boxing Day substitute)
    - new Date('2026-03-20T00:00:00Z') → false (normal Friday)
    - string '2026-01-01' → true  (accepts YYYY-MM-DD strings too)

    isWorkingDay(date):
    - '2026-03-20' → true  (Friday, no holiday)
    - '2026-03-21' → false (Saturday)
    - '2026-03-22' → false (Sunday)
    - '2026-01-01' → false (New Year's Day)
    - '2026-04-06' → false (Easter Monday)

    nextWorkingDay(date) — uses getUTCDay(), never getDay():
    - new Date('2026-01-01T00:00:00Z') → '2026-01-02'  (New Year's Day Thu → Fri)
    - new Date('2026-12-25T00:00:00Z') → '2026-12-29'  (Fri=Christmas, Sat, Sun, Mon=Boxing subst → Tue)
    - new Date('2026-03-20T00:00:00Z') → '2026-03-20'  (already working day — same day returned)
    - new Date('2026-03-21T00:00:00Z') → '2026-03-23'  (Saturday → Monday)
    - new Date('2026-04-03T00:00:00Z') → '2026-04-07'  (Good Friday → after Easter Monday)
    - string input '2026-03-21' → '2026-03-23'  (accepts string)
    - safety: max 14 iterations guard prevents infinite loop on corrupted data

    adjustedPaymentDate(nominalDate, adjustment):
    - (date, 'none') → returns same date object (no adjustment)
    - (date, 'next-working-day') → delegates to nextWorkingDay(date)
    - ('2026-03-21', 'next-working-day') → Date for '2026-03-23'

    refreshBankHolidaysCache() (mock fetch):
    - on success: stores array of date strings under 'uk_bank_holidays_cache', stores today's date under 'uk_bank_holidays_cache_date'
    - on fetch failure: does NOT throw; logs console.warn; localStorage unchanged
    - parses data['england-and-wales'].events.map(e => e.date)

    loadBankHolidays() (via isUKBankHoliday):
    - when 'uk_bank_holidays_cache' has a valid JSON array → uses it instead of STATIC_HOLIDAYS
    - when 'uk_bank_holidays_cache' is corrupt JSON → falls back to STATIC_HOLIDAYS silently

    maxCachedYear guard:
    - when date year > max year in static fallback AND cache is empty → console.warn called; function still returns a result (weekend-only logic)
  </behavior>
  <implementation>
    After all tests are RED, implement banking-calendar.js:

    1. Constants:
       - CACHE_KEY = 'uk_bank_holidays_cache'
       - CACHE_DATE_KEY = 'uk_bank_holidays_cache_date'
       - CACHE_EXPIRY_DAYS = 365

    2. STATIC_HOLIDAYS Set — hardcode England & Wales dates:
       2025: 2025-01-01, 2025-04-18, 2025-04-21, 2025-05-05, 2025-05-26, 2025-08-25, 2025-12-25, 2025-12-26
       2026: 2026-01-01, 2026-04-03, 2026-04-06, 2026-05-04, 2026-05-25, 2026-08-31, 2026-12-25, 2026-12-28
       2027: 2027-01-01, 2027-04-02, 2027-04-05, 2027-05-03, 2027-05-31, 2027-08-30, 2027-12-27, 2027-12-28
       (2027 dates are best estimate from GOV.UK pattern; the Settings refresh button corrects them when API data becomes available)

    3. function loadBankHolidays() → returns Set<string>
       - try localStorage.getItem(CACHE_KEY) → JSON.parse → return new Set(array)
       - on error or null → return STATIC_HOLIDAYS

    4. Internal: function _toUTCDateStr(date) → YYYY-MM-DD string using UTC methods

    5. export function isUKBankHoliday(date) → boolean
       - accepts Date or string; normalize to YYYY-MM-DD via _toUTCDateStr
       - return loadBankHolidays().has(dateStr)

    6. export function isWorkingDay(date) → boolean
       - construct Date with T00:00:00Z suffix if string
       - check getUTCDay() !== 0 && !== 6
       - AND !isUKBankHoliday(date)

    7. export function nextWorkingDay(date) → Date
       - clone to new Date; use getUTCDate/setUTCDate for advancement
       - safety counter: max 14 iterations; log console.warn if limit hit
       - while (!isWorkingDay(d)) { d.setUTCDate(d.getUTCDate() + 1); }

    8. export function adjustedPaymentDate(nominalDate, adjustment) → Date
       - if adjustment !== 'next-working-day': return Date object (normalize string if needed)
       - else: return nextWorkingDay(nominalDate)

    9. export async function refreshBankHolidaysCache() → void
       - fetch 'https://www.gov.uk/bank-holidays.json'
       - parse data['england-and-wales'].events.map(e => e.date)
       - localStorage.setItem(CACHE_KEY, JSON.stringify(dates))
       - localStorage.setItem(CACHE_DATE_KEY, new Date().toISOString().split('T')[0])
       - catch: console.warn('[banking-calendar] Failed to refresh...', err)

    maxCachedYear guard: compute from STATIC_HOLIDAYS. If date.getUTCFullYear() > maxYear in loaded set, emit console.warn and use weekend-only check as fallback.

    Anti-patterns to avoid (from RESEARCH.md):
    - NEVER use getDay() — always getUTCDay() for day-of-week checks
    - NEVER JSON.stringify(new Set(...)) — always serialize to Array.from(set) first
    - NEVER block startup — refreshBankHolidaysCache is always fire-and-forget at call site
    - NEVER mutate stored date fields in recurrence — only predictedPaymentDate
  </implementation>
</feature>

<verification>
After GREEN (all tests pass):
```
npx vitest run src/utils/banking-calendar.test.js
```
All tests pass. Then run full suite:
```
npm test
```
393+ tests pass with no regressions.

Coverage check (informational):
```
npx vitest run --coverage src/utils/banking-calendar.test.js
```
Target: ≥90% branch coverage for banking-calendar.js.
</verification>

<success_criteria>
- banking-calendar.test.js written and RED before implementation
- banking-calendar.js implements all exported functions
- nextWorkingDay('2026-01-01') returns Date('2026-01-02')
- nextWorkingDay('2026-12-25') returns Date('2026-12-29')
- nextWorkingDay('2026-03-20') returns Date('2026-03-20') (same day)
- nextWorkingDay('2026-03-21') returns Date('2026-03-23')
- refreshBankHolidaysCache writes correct localStorage keys
- Static fallback used when localStorage empty
- npm test passes (393+ tests, no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/31-banking-calendar-recurrence-upgrade/31-01-SUMMARY.md`
</output>
