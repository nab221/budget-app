---
phase: 05-forecast-and-graph-alignment
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/ui/dashboard.js]
autonomous: false
requirements: [FORC-01]

must_haves:
  truths:
    - "Detailed Forecast table shows 45 rows by default"
    - "Buttons and UI text correctly describe a 45-day window"
    - "Chart and table present identical values for overlapping dates"
  artifacts:
    - path: "src/ui/dashboard.js"
      provides: "Aligned Dashboard orchestration for forecast views"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "calculateForecast"
      via: "params: (today, 45)"
---

<objective>
Align the Dashboard's visual feedback and coordination logic with the new unified 45-day forecast horizon.

Purpose: Eliminate user confusion caused by mismatched date ranges and inconsistent balance figures.
Output: Consistent Dashboard experience across charts and tables.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-forecast-and-graph-alignment/05-01-SUMMARY.md
@src/ui/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Dashboard Orchestration</name>
  <files>src/ui/dashboard.js</files>
  <action>
    - Update `renderDashboard` to pass the newly enriched `rollingData` object to `renderRollingOverviewChart`.
    - Update `renderForecastTable` to:
      - Call `calculateForecast(today, 45)` instead of 90.
      - Update internal loading messages: "Calculating 90-day forecast..." -> "Calculating 45-day forecast...".
      - Handle the `DailySnapshot` objects from the updated `calculateForecast`.
  </action>
  <verify>
    - `grep "calculateForecast(today, 45)" src/ui/dashboard.js` exists.
    - No "90-day" strings remain in `src/ui/dashboard.js`.
  </verify>
  <done>The forecast table and chart now share the same 45-day window.</done>
</task>

<task type="auto">
  <name>Task 2: Final UI Polish & Content Cleanup</name>
  <files>src/ui/dashboard.js</files>
  <action>
    - Find the "Show Detailed 90-Day Forecast" button text and update it to "Show Detailed 45-Day Forecast".
    - Check the `toggleForecastTable` function and ensure any labels or comments referencing "90" are updated to "45".
    - Verify that the values in the 'Detailed Forecast' table perfectly match the values in the 'Rolling Overview' chart for identical dates.
  </action>
  <verify>
    - `grep -i "90-Day" src/ui/dashboard.js` returns no matches.
  </verify>
  <done>The Dashboard UI consistently presents the 45-day horizon.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Unified 45-day forecast with multi-line chart and table alignment.</ what-built>
  <how-to-verify>
    1. Visit the Dashboard.
    2. Check the 'Rolling Financial Overview' chart:
       - Verify three lines: Balance, Income, Expenses.
       - Hover to confirm values.
    3. Toggle the forecast table:
       - Verify it says "Detailed 45-Day Forecast".
       - Compare the first few days' balance, income, and expenses against the chart's future segment.
       - Ensure they are identical.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual verification of the Dashboard view to ensure chart-table parity and UI consistency.
</verification>

<success_criteria>
- Dashboard buttons say "45-Day Forecast".
- Detailed table shows 45 days.
- Visual alignment between chart and table values.
</success_criteria>

<output>
After completion, create `.planning/phases/05-forecast-and-graph-alignment/05-02-SUMMARY.md`
</output>
