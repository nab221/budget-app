---
phase: 19-github-pages-ci-cd-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified: []
autonomous: false
requirements:
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05

must_haves:
  truths:
    - "A push to the `main` branch triggers the GitHub Actions deploy workflow"
    - "The workflow build and deploy jobs both complete green"
    - "The live URL https://nab221.github.io/budget-app/ loads the Budget Console app"
    - "The PWA install prompt appears on the live URL (or the app is already installable)"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD workflow — already correct, no changes needed"
  key_links:
    - from: "main branch push"
      to: "GitHub Actions workflow"
      via: "on.push.branches: [main] trigger in deploy.yml"
      pattern: "push to main"
    - from: "GitHub Actions workflow"
      to: "https://nab221.github.io/budget-app/"
      via: "actions/deploy-pages publishing dist/ artifact"
      pattern: "live URL"
---

<objective>
Push the base-path fix to `main`, enable GitHub Pages in repository Settings (if not already enabled), and confirm the live deployment at `https://nab221.github.io/budget-app/` loads correctly.

Purpose: The code fix alone does nothing until it reaches `origin/main` and GitHub Actions deploys it. This plan closes the loop from code change to live running PWA.

Output: Live app at `https://nab221.github.io/budget-app/` that loads, renders, and is installable as a PWA.
</objective>

<execution_context>
@C:/Users/nab221/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-github-pages-ci-cd-deployment/19-RESEARCH.md
@.planning/phases/19-github-pages-ci-cd-deployment/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit and push vite.config.js change to main</name>
  <files>vite.config.js</files>
  <action>
    Stage and commit the `vite.config.js` change, then push to `origin/main`:

    ```bash
    git add vite.config.js
    git commit -m "feat(deploy): add base '/budget-app/' for GitHub Pages subpath"
    git push origin main
    ```

    After pushing, note the commit SHA. The GitHub Actions workflow will trigger automatically within seconds of the push completing.

    Do NOT push `dist/` — it is in `.gitignore` and CI rebuilds it from source. Do NOT merge to `master` — the workflow triggers on `main` only.
  </action>
  <verify>
    <automated>git log --oneline -1 origin/main 2>/dev/null || git log --oneline -1</automated>
  </verify>
  <done>
    - `vite.config.js` change committed with message referencing base/GitHub Pages
    - `git push origin main` exits 0
    - Local branch is up to date with `origin/main`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GitHub Pages is enabled and live URL loads</name>
  <what-built>
    Claude pushed the `base: '/budget-app/'` fix to `origin/main`. GitHub Actions should have automatically triggered the deploy workflow.
  </what-built>
  <how-to-verify>
    Step 1 — Enable GitHub Pages (if not yet done):
      - Go to https://github.com/nab221/budget-app/settings/pages
      - Under "Build and deployment", set Source to "GitHub Actions"
      - Save. (If it already shows "GitHub Actions", skip this step.)

    Step 2 — Check the workflow run:
      - Go to https://github.com/nab221/budget-app/actions
      - Confirm the most recent workflow run ("Deploy to GitHub Pages") shows a green checkmark
      - If it failed, click into the run and share the error output

    Step 3 — Verify the live URL:
      - Visit https://nab221.github.io/budget-app/ in a browser
      - The Budget Console app should load (not a 404 or blank page)
      - Open browser DevTools > Console — confirm no 404 errors for assets

    Step 4 — Verify PWA install prompt:
      - On Chrome/Edge desktop: check for the install icon in the address bar
      - On mobile: check for "Add to Home Screen" prompt
      - (PWA install may take a few seconds after first page load)
  </how-to-verify>
  <resume-signal>
    Type "approved" if the live URL loads correctly and the workflow ran green.
    Describe any issues if the URL shows 404, blank page, or the workflow failed.
  </resume-signal>
  <action>Human verification step — follow how-to-verify instructions above.</action>
  <verify><automated>echo "manual checkpoint"</automated></verify>
  <done>User types "approved" after confirming: GitHub Pages source is set to GitHub Actions, workflow ran green, live URL loads app without console 404s.</done>
</task>

</tasks>

<verification>
Manual verification at https://nab221.github.io/budget-app/ — app loads, no console 404s, PWA installable.

Automated local check (confirms push completed):
```bash
git log --oneline origin/main | head -3
```
</verification>

<success_criteria>
- `origin/main` contains the `base: '/budget-app/'` commit
- GitHub Actions workflow ran green (both build and deploy jobs)
- https://nab221.github.io/budget-app/ loads the Budget Console app
- No 404 errors in browser DevTools console for assets
- PWA install prompt appears (install icon in browser address bar or mobile prompt)
</success_criteria>

<output>
After completion, create `.planning/phases/19-github-pages-ci-cd-deployment/19-02-SUMMARY.md`
</output>
