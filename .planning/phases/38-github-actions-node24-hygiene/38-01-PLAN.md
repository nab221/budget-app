---
phase: 38-github-actions-node24-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy.yml
  - src/ui/cloud-sync.js
  - src/ui/backup.js
  - src/db/backup.js
  - src/utils/legacy-import.js
  - tests/legacy-import.test.js
  - package.json
  - vitest.config.js
  - .planning/phases/38-github-actions-node24-hygiene/38-coverage-audit.md
autonomous: true
requirements:
  - TECH-01
  - INTEGRITY-02
  - TECH-04
must_haves:
  truths:
    - "CI uses Node 24-compatible actions and remains green without unnecessary churn when already compliant."
    - "Users can run a dedicated v2 import flow from Settings seams already used for local backup/import."
    - "Legacy import defaults to conflict-safe behavior that does not overwrite existing v3 records."
    - "Coverage for phase 31-37 modules is auditable with a reproducible command and documented gap-closure actions."
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Node 24 + setup-node compatibility guard"
      contains: "actions/setup-node with node-version 24"
    - path: "src/utils/legacy-import.js"
      provides: "v2 parse, validate, map, and conflict-safe import orchestration"
      exports: ["parseLegacyBackup", "detectLegacyShape", "validateLegacyData", "mapLegacyToCurrent", "importLegacyData", "runLegacyImport"]
    - path: "tests/legacy-import.test.js"
      provides: "anti-regression tests for mapping and no-overwrite default"
      contains: "conflict skip by default"
    - path: ".planning/phases/38-github-actions-node24-hygiene/38-coverage-audit.md"
      provides: "module-by-module coverage matrix and gap closure checklist for phases 31-37"
      contains: "provider blocker state and remediation"
  key_links:
    - from: "src/ui/cloud-sync.js"
      to: "src/ui/backup.js"
      via: "settings local import action triggers #importFile flow"
      pattern: "settingsLocalImportBtn.*importFile"
    - from: "src/ui/backup.js"
      to: "src/db/backup.js"
      via: "import path currently calls importBackupData"
      pattern: "importBackupData"
    - from: "tests/legacy-import.test.js"
      to: "src/utils/legacy-import.js"
      via: "mapping and conflict-policy assertions"
      pattern: "no overwrite|skip|apr|balance"
---

<objective>
Deliver Phase 38 as an executable hygiene pass: verify or update CI Node 24 posture, implement a safe v2 legacy import flow on real Settings seams, and produce an auditable coverage closure path for modules introduced in phases 31-37.

Purpose: Close TECH-01, INTEGRITY-02, and TECH-04 with minimal risk and explicit no-op behavior where already compliant.
Output: Updated workflow/import code plus coverage audit artifact and test-backed anti-regression guarantees.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<assumptions_dependencies>
- Research indicates `.github/workflows/deploy.yml` already uses Node 24 and `actions/setup-node@v6`; task 1 must verify first, then no-op if still compliant.
- There is no `src/ui/settings.js`; implementation must use existing Settings seams in `src/ui/cloud-sync.js` and `src/ui/backup.js`.
- Current generic import path in `src/db/backup.js` uses merge semantics that can overwrite by id via bulkPut; legacy import must use conflict-safe default skip behavior.
- Coverage audit is currently blocked unless a Vitest coverage provider is present; task 3 must treat missing provider as a blocker and remediate first.
</assumptions_dependencies>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-github-actions-node24-hygiene/38-CONTEXT.md
@.planning/phases/38-github-actions-node24-hygiene/38-RESEARCH.md
@.github/workflows/deploy.yml
@src/ui/cloud-sync.js
@src/ui/backup.js
@src/db/backup.js
@src/utils/supabase-sync.js
@vitest.config.js
@package.json

<interfaces>
From .github/workflows/deploy.yml:
```yml
- uses: actions/setup-node@v6
  with:
    node-version: 24
```

From src/ui/cloud-sync.js:
```js
export const cloudSyncUI = {
  _renderLocalSettingsActions(actionsEl) {
    // renders #settingsLocalImportBtn
    // click handler triggers document.getElementById('importFile')?.click()
  }
}
```

From src/ui/backup.js:
```js
export const backupUI = {
  async handleImport(event) { /* parse envelope JSON and prompt mode */ },
  async executeImport(content) { /* calls importBackupData(...) */ }
}
```

From src/db/backup.js:
```js
export async function importBackupData(data, options = {}) {
  const { mode = 'overwrite', restoreSettings = true } = options;
  // merge path uses table.bulkPut(recordsToImport)
}
```

From src/utils/supabase-sync.js:
```js
export const CLOUD_LAST_SYNC_KEY = 'budget_cloud_last_sync';
export function isConfigured() {}
export function getSupabaseClient() {}
export async function getSession() {}
export async function pushSnapshot() {}
export async function pullSnapshot() {}
```

From vitest.config.js:
```js
export default defineConfig({
  test: { environment: 'jsdom' }
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI Node24 verification/update (idempotent)</name>
  <files>.github/workflows/deploy.yml</files>
  <action>Run a verification-first pass over `.github/workflows/*.yml` and check for Node runtime drift. If all workflows already use Node 24-compatible setup (node-version 24 and setup-node major >= 4), take explicit no-op path and document "already compliant" in execution notes. If drift exists, patch only the minimum lines to restore compliance. Keep change idempotent: rerunning this task must produce zero diff once compliant.</action>
  <verify>
    <automated>rg "actions/setup-node@|node-version" .github/workflows -n</automated>
  </verify>
  <done>All workflows are verified for TECH-01 with either (a) no file changes and explicit no-op evidence, or (b) minimal update to Node 24-compatible configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Legacy v2 import flow and mapping implementation plan</name>
  <files>src/ui/cloud-sync.js, src/ui/backup.js, src/db/backup.js, src/utils/legacy-import.js, tests/legacy-import.test.js</files>
  <behavior>
    - Test 1: valid v2-shaped payload is detected, validated, and mapped into current schema-compatible records.
    - Test 2: debt APR values like "4.9%" normalize correctly and support numeric input fallback.
    - Test 3: on id collision with existing v3 data, default action is skip (no overwrite) and summary reports skipped/conflicts.
    - Test 4: unrecognized payload is rejected with clear incompatibility reasons before any write.
  </behavior>
  <action>Implement dedicated legacy pipeline in `src/utils/legacy-import.js` with parse, shape detection, validation, mapping, and import orchestration. Wire UI entrypoint into existing Settings-local seam in `src/ui/cloud-sync.js` (next to local import actions) and route file content through legacy parser instead of direct generic restore path for v2 files. Keep existing normal backup import behavior intact in `src/ui/backup.js` for non-legacy envelopes. Add anti-regression guardrails in tests asserting conflict-safe default (no overwrite unless explicit override) and summary reporting for imported/skipped/conflicted rows.</action>
  <verify>
    <automated>npx vitest run tests/legacy-import.test.js --reporter=verbose</automated>
  </verify>
  <done>Legacy import flow is reachable from real Settings seams, performs validated v2->current mapping, and enforces no-overwrite-by-default conflict policy with test coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Test coverage audit and gap closure plan for phases 31-37</name>
  <files>package.json, vitest.config.js, .planning/phases/38-github-actions-node24-hygiene/38-coverage-audit.md</files>
  <action>Unblock and execute coverage audit for TECH-04. First, detect whether coverage provider exists; if missing (for example `@vitest/coverage-v8`), treat as blocker and install/configure it before audit. Then run full coverage and extract module-level line coverage for phase 31-37 targets (including missing/unimplemented modules), classifying each as pass, fail, missing, or deferred-with-rationale. Produce `38-coverage-audit.md` with exact gaps and concrete next test files/actions required to bring every in-scope module to >=80% line coverage.</action>
  <verify>
    <automated>npx vitest run --coverage --reporter=verbose</automated>
  </verify>
  <done>Coverage run is reproducible, blocker handling is documented and resolved, and a concrete gap-closure matrix exists for all phase 31-37 modules under TECH-04.</done>
</task>

</tasks>

<verification>
- CI hygiene check: Node runtime settings verified across workflows, with no-op evidence when already compliant.
- Import safety check: run legacy-import unit tests plus targeted collision scenario to confirm no overwrite by default.
- Coverage audit check: successful coverage command output attached to audit notes; failures captured as explicit closure actions.
</verification>

<success_criteria>
- TECH-01 satisfied with verification-first evidence and idempotent workflow state.
- INTEGRITY-02 satisfied through validated legacy import path on existing Settings seams and tested no-overwrite default behavior.
- TECH-04 satisfied by successful coverage tooling execution and documented closure actions for every phase 31-37 target below threshold.
</success_criteria>

<output>
After completion, create `.planning/phases/38-github-actions-node24-hygiene/38-01-SUMMARY.md`.
</output>
