---
phase: 09-tax-free-childcare-tracker
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [index.html, src/app.js, src/ui/childcare.js, src/ui/dashboard.js, src/db/repository.js, src/ui/expenses.js]
autonomous: true
requirements: [CHILD-05, CHILD-01, CHILD-03]
must_haves:
  truths:
    - "New 'Childcare' tab appears in the main navigation"
    - "Dashboard shows a 'Childcare' summary card with account balances and funding gaps"
    - "User can add/edit childcare accounts and log deposits/spending"
    - "One-off expenses from childcare deposits show a 'Tax-free Childcare' badge"
    - "Reconfirmation alert appears if an entitlement period is ending within 7 days"
  artifacts:
    - path: "src/ui/childcare.js"
      provides: "Full UI for managing childcare accounts and ledgers"
    - path: "src/ui/dashboard.js"
      provides: "Dashboard integration for TFC"
  key_links:
    - from: "src/ui/childcare.js"
      to: "childcareRepository"
      via: "API calls for data management"
---

<objective>
Implement the user interface for the Tax-Free Childcare tracker, integrate it into the dashboard summary and net worth calculations, and add budget-level badges for TFC deposits.

Purpose: Provide a user-friendly way to manage childcare funding and see the impact on overall financial health.
Output: New Childcare tab, updated Dashboard with TFC summary, and enhanced Expense list.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tax-free-childcare-tracker/09-CONTEXT.md
@.planning/phases/09-tax-free-childcare-tracker/09-RESEARCH.md
@.planning/phases/09-tax-free-childcare-tracker/09-01-SUMMARY.md
@index.html
@src/app.js
@src/ui/dashboard.js
@src/ui/expenses.js
@src/db/repository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Childcare UI Module</name>
  <files>src/ui/childcare.js, index.html, src/app.js</files>
  <action>
    - **index.html**: Add 'Childcare' tab button to `#mainTabs` and a corresponding `tab-panel` with `#childcareAccountList` and `#childcareLedgerSection` containers.
    - **src/ui/childcare.js**:
      - Implement `childcareUI` module with `init()`, `render()`, and event handlers.
      - `renderAccounts()`: Display cards for each childcare account with current balance, target spend, and "Funding Gap" details.
      - `renderLedger(accountId)`: Display a chronological table of all entries (Deposits, Top-ups, Spending) with running balances.
      - Implement forms for "Add Account", "Log Deposit", and "Log Spending".
      - "Log Deposit" should prompt for a Category (to be used for the main budget expense).
    - **src/app.js**: Import and initialize `childcareUI`.
  </action>
  <verify>
    Open app, click 'Childcare' tab, add an account, log a deposit, and verify the ledger entries and balances appear.
  </verify>
  <done>Dedicated UI for childcare management is functional.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard & Expense Integration</name>
  <files>src/db/repository.js, src/ui/dashboard.js, src/ui/expenses.js</files>
  <action>
    - **src/db/repository.js**: Update `getDashboardData` to:
      1. Fetch all childcare accounts and their latest ledger balance.
      2. Add the sum of childcare balances to `totalAssets`.
      3. Return a `childcareSummary` object containing per-account balances and funding gaps (using `calculateFundingGap` utility).
    - **src/ui/dashboard.js**:
      1. Add a new summary card: "Childcare Assets" (sum of all accounts).
      2. Add a specialized "Childcare Funding" section or card that shows each child's balance vs target and the "Deposit £X to cover next month" suggestion.
      3. Implement a reconfirmation alert: If `entitlementStart` + 3 months is within 7 days of today, show a warning badge or message.
    - **src/ui/expenses.js**: Update `renderOneOff` to detect expenses with the "Tax-free Childcare:" note prefix and render a colored badge.
  </action>
  <verify>
    - Verify Dashboard 'Total Assets' includes childcare balances.
    - Verify 'Childcare Funding' card shows correct gaps.
    - Verify Expense list shows 'Tax-free Childcare' badge for deposits.
  </verify>
  <done>Childcare data is integrated into the core dashboard and budget views.</done>
</task>

<task type="auto">
  <name>Task 3: UX Polish & Final Wiring</name>
  <files>src/ui/childcare.js, src/ui/render.js</files>
  <action>
    - Ensure all monetary values use `formatGBP`.
    - Use `safeHTML` for all dynamic HTML generation.
    - Add a "Delete Account" confirmation that warns about losing ledger history.
    - Ensure "Log Deposit" form defaults to today's date and "Childcare" category (if found).
  </action>
  <verify>
    Final end-to-end check of the feature.
  </verify>
  <done>Feature polished and follows project conventions.</done>
</task>

</tasks>

<verification>
- Verify account creation and ledger tracking.
- Verify dashboard reflects childcare assets in Net Worth.
- Verify funding gap suggestions match the 20% top-up math.
- Verify reconfirmation alerts appear when appropriate.
</verification>

<success_criteria>
- Dashboard shows current TFC balances and funding gaps.
- Net Worth correctly includes TFC balances.
- Top-up math is accurate and handles quarterly caps.
- Budget integration (badges) is visible.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tax-free-childcare-tracker/09-02-SUMMARY.md`
</output>
