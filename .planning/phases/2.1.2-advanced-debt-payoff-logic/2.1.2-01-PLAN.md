---
phase: 2.1.2-advanced-debt-payoff-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/utils/finance.test.js]
autonomous: true
requirements: [DEBT-11, DEBT-13]
must_haves:
  truths:
    - "Schema v15 includes isInterestOnly for debts"
    - "New tests cover interest-only loans, ERC fees, and strategy comparisons"
  artifacts:
    - path: "src/db/schema.js"
      provides: "v15 database schema"
      contains: "isInterestOnly"
    - path: "src/utils/finance.test.js"
      provides: "Test coverage for simulateLoanPayoff"
      contains: "describe('simulateLoanPayoff')"
---

<objective>
Update the database schema to support interest-only debts and establish a comprehensive TDD foundation for the simulation logic.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema.js
@src/utils/finance.js
@src/utils/finance.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema to v15</name>
  <files>src/db/schema.js</files>
  <action>
    Modify `src/db/schema.js` to increment the version to 15.
    Add `isInterestOnly` to the `debts` table store.
    Include an `upgrade` block for v15 that initializes `isInterestOnly` to `false` for all existing debts.
  </action>
  <verify>
    <automated>node -e "import('./src/db/schema.js').then(s => console.log(s.db.versions.find(v => v._cfg.version === 15) ? 'v15 found' : 'v15 missing'))"</automated>
  </verify>
  <done>Schema v15 is defined and exports `isInterestOnly` for debts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add simulateLoanPayoff tests</name>
  <files>src/utils/finance.test.js</files>
  <behavior>
    - Repayment Loan: Principal reduces over time based on fixed payment.
    - Interest-Only Loan: Principal stays constant if extra payment is 0.
    - ERC Fee (Fixed): Fee is charged correctly when overpayment exceeds 10% allowance.
    - ERC Fee (Percent): Percentage fee is calculated correctly on the excess amount.
    - ERC Impact: Verify fee reduces the effective principal reduction (adds to balance).
    - Strategies: Compare 'term-reduction' and 'payment-reduction' results.
  </behavior>
  <action>
    Append a new `describe('simulateLoanPayoff')` block to `src/utils/finance.test.js`.
    Define test cases for the behaviors listed above.
    The tests for interest-only and ERC impact may fail initially as the implementation will follow in the next plan.
  </action>
  <verify>
    <automated>npx vitest src/utils/finance.test.js --run</automated>
  </verify>
  <done>Comprehensive test suite exists for loan payoff simulation.</done>
</task>

</tasks>

<verification>
Check that `src/db/schema.js` correctly defines version 15 and `src/utils/finance.test.js` has the new test cases.
</verification>

<success_criteria>
- Schema v15 exists in `schema.js`.
- `finance.test.js` contains `simulateLoanPayoff` tests.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1.2-advanced-debt-payoff-logic/2.1.2-01-SUMMARY.md`
</output>
