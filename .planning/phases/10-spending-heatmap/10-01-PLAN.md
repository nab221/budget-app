---
phase: 10-spending-heatmap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/repository.js, src/ui/heatmap.js, index.html, src/ui/dashboard.js, css/main.css]
autonomous: true
requirements: [HMP-01, HMP-02, HMP-03]
must_haves:
  truths:
    - "The Dashboard displays a 'Spending Heatmap' section below summary cards"
    - "A 52x7 grid appears for the current year, with cells representing days"
    - "Spending data is aggregated correctly from one-off and paid recurrent expenses"
  artifacts:
    - path: "src/db/repository.js"
      provides: "getYearlyDailySpending"
    - path: "src/ui/heatmap.js"
      provides: "Heatmap rendering engine (renderSpendingHeatmap)"
    - path: "index.html"
      provides: "Heatmap container and canvas"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "src/db/repository.js"
      via: "getYearlyDailySpending"
    - from: "src/ui/dashboard.js"
      to: "src/ui/heatmap.js"
      via: "renderSpendingHeatmap"
---

<objective>
Establish the foundation for the Spending Heatmap by creating the data aggregation layer, the core rendering engine using Canvas 2D, and integrating the basic grid into the Dashboard layout.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-spending-heatmap/10-CONTEXT.md
@src/db/repository.js
@src/ui/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Daily Spending Aggregation</name>
  <files>src/db/repository.js</files>
  <action>
    Add an exported function `getYearlyDailySpending(year)` to `src/db/repository.js`. 
    - Fetch all `oneOffExpenses` and `recurrentExpenses` for the given year.
    - Filter `recurrentExpenses` to only include those with `status === 'paid'`.
    - Group by `date` (YYYY-MM-DD).
    - For each day, calculate: `total` (sum of amounts in pence), `topCategory` (name of the category with the highest spend that day), and `topCategoryAmount`.
    - Also fetch category names from `categoryRepository` to resolve IDs.
    - Return an object mapping `date` strings to their daily stats.
  </action>
  <verify>
    <automated>node -e "import { getYearlyDailySpending } from './src/db/repository.js'; getYearlyDailySpending('2026').then(d => console.log(Object.keys(d).length))"</automated>
  </verify>
  <done>Repository method exists and returns correct daily totals and top categories for a given year.</done>
</task>

<task type="auto">
  <name>Task 2: Create Heatmap Engine and UI Container</name>
  <files>src/ui/heatmap.js, index.html, css/main.css</files>
  <action>
    1. Create `src/ui/heatmap.js` with `renderSpendingHeatmap(canvasId, year, dailyData, options)`.
       - Implement 52x7 grid logic using Canvas 2D.
       - Use `Sunday-Saturday` for rows, weeks for columns.
       - Support basic coloring (e.g., all spend > 0 as green, 0 as gray).
       - Add labels for Months (Jan, Feb...) and Days (Mon, Wed, Fri).
    2. Add `#spendingHeatmapContainer` to `index.html` below the summary cards in the dashboard tab.
    3. Add heatmap-specific styles to `css/main.css` including the `.heatmap-tooltip` styling.
  </action>
  <verify>
    <automated>ls src/ui/heatmap.js &amp;&amp; grep "spendingHeatmapContainer" index.html</automated>
  </verify>
  <done>Heatmap rendering engine created and layout container added to Dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Heatmap to Dashboard and Privacy Mode</name>
  <files>src/ui/dashboard.js</files>
  <action>
    Update `renderDashboard` in `src/ui/dashboard.js` to:
    - Fetch yearly spending data for the current year using Task 1's method.
    - Call `renderSpendingHeatmap` from Task 2.
    - Ensure the heatmap is hidden if no data exists or appropriately handled.
    - Confirm `Privacy Mode` (blurring) works automatically since it's a `canvas` element (CSS handles it), but ensure it's within a `.chart-container` to allow hover-to-unblur.
  </action>
  <verify>
    <automated>grep "renderSpendingHeatmap" src/ui/dashboard.js</automated>
  </verify>
  <done>Heatmap is rendered on Dashboard with real data and responds to Privacy Mode.</done>
</task>

</tasks>

<verification>
- Heatmap container appears on Dashboard.
- Canvas grid is drawn correctly (52 columns).
- Privacy Mode blurs the heatmap.
- Data reflects actual recorded expenses.
</verification>

<success_criteria>
- Dashboard shows a heatmap grid for the current year.
- Cells with spending are colored differently from empty cells.
- Grid is responsive (scrollable) on mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/10-spending-heatmap/10-01-SUMMARY.md`
</output>
