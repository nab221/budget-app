---
phase: 2.1.1-security-persistence-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/security.js, src/db/schema.js, src/ui/file-sync.js, index.html]
autonomous: true
requirements: [SEC-01, SYNC-01, SYNC-02, SYNC-03]

must_haves:
  truths:
    - "UUID generation is centralized and has a fallback for non-secure contexts."
    - "Persistence banner only shows when storage is non-persistent AND no file is synced."
    - "Header hint text dynamically reflects the sync state (filename or storage status)."
    - "Terminology 'Disconnect File' is used consistently in UI and code."
  artifacts:
    - path: "src/utils/security.js"
      provides: "Centralized generateUUID function"
    - path: "src/ui/file-sync.js"
      provides: "Refined banner and header hint logic"
  key_links:
    - from: "src/db/schema.js"
      to: "src/utils/security.js"
      via: "import { generateUUID }"
---

<objective>
Implement security fallbacks for UUID generation and polish the persistence/sync user experience.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v2.1-REQUIREMENTS.md
@src/utils/security.js
@src/db/schema.js
@src/ui/file-sync.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize UUID Generation</name>
  <files>src/utils/security.js, src/db/schema.js</files>
  <action>
    - Add `export function generateUUID()` to `src/utils/security.js`. 
    - Implementation: Use `crypto.randomUUID()` if available; otherwise, use a robust fallback (e.g., using `crypto.getRandomValues` to construct a RFC4122-compliant UUID, or the current Math.random fallback as a secondary).
    - Update `src/db/schema.js`: Import `generateUUID` from `../utils/security.js` and replace the local `generateUUID` helper in the v12 upgrade block.
  </action>
  <verify>
    - `grep "generateUUID" src/utils/security.js`
    - `grep "import.*generateUUID" src/db/schema.js`
  </verify>
  <done>UUID generation is centralized and used in schema migrations.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Persistence UX and Terminology</name>
  <files>src/ui/file-sync.js, index.html</files>
  <action>
    - Update `refreshPersistenceWarning` in `src/ui/file-sync.js` to ensure the banner (id="persistence-warning") is ONLY visible when `ensurePersistence()` is false AND `SyncManager.getFileName()` is null.
    - Update `updateFileSyncToolbar`:
      - When a file is connected: Set header hint to "Auto-saving to [fileName]".
      - When no file is connected: Set header hint to "Local Storage (IndexedDB) • Persistence: [Active/Inactive]".
      - Use `ensurePersistence()` status to determine the "Active/Inactive" text.
    - Rename any remaining "Reset Persistence" occurrences to "Disconnect File" in button labels and confirmation messages.
  </action>
  <verify>
    - Check `src/ui/file-sync.js` for "Disconnect File" string.
    - Verify `refreshPersistenceWarning` logic matches requirements.
  </verify>
  <done>Banner logic is refined, header hints are dynamic, and terminology is standardized.</done>
</task>

</tasks>

<success_criteria>
- UUIDs are generated safely in all environments.
- Persistence banner behavior aligns with v2.0 UI specifications.
- Header text provides clear feedback on data safety status.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1.1-security-persistence-polish/2.1.1-01-SUMMARY.md`
</output>
