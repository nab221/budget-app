---
phase: 39.1-income-sources-tab
plan: 03
type: execute
wave: 3
depends_on: ["39.1-01", "39.1-02"]
files_modified:
  - index.html
  - src/app.js
  - src/ui/income-spending-settings.js
autonomous: false
requirements:
  - PLAN-06-ext
must_haves:
  truths:
    - "A 'Pay Sources' tab button appears in the main navigation bar"
    - "Clicking the tab renders the income-sources panel without console errors"
    - "Settings no longer shows an Income Sources section"
    - "Settings still shows Spending Buckets section correctly"
  artifacts:
    - path: "index.html"
      provides: "Tab nav button data-tab=income-sources + tab-panel data-panel=income-sources"
      contains: "data-tab=\"income-sources\""
    - path: "src/app.js"
      provides: "incomeSourcesUI import, init, and renderAll wiring"
      contains: "incomeSourcesUI"
    - path: "src/ui/income-spending-settings.js"
      provides: "Buckets-only settings module (income sources section removed)"
  key_links:
    - from: "index.html"
      to: "src/app.js"
      via: "data-tab=income-sources matches panelId === 'income-sources' in renderAll()"
      pattern: "income-sources"
    - from: "src/app.js"
      to: "src/ui/income-sources.js"
      via: "import + incomeSourcesUI.init() + incomeSourcesUI.render()"
      pattern: "incomeSourcesUI"
    - from: "src/ui/income-spending-settings.js"
      to: "src/db/repository.js"
      via: "spendingBucketRepository only (incomeSourceRepository import removed)"
      pattern: "spendingBucketRepository"
---

<objective>
Wire the new incomeSourcesUI module into the app: add the navigation tab and panel to index.html, register the module in app.js, and clean up the duplicate income sources section from Settings.

Purpose: The module (Plan 02) exists but is unreachable until navigation and initialization are wired. The Settings cleanup removes duplicate CRUD that now lives in the dedicated tab.

Output:
- New "Pay Sources" tab visible in the nav bar and functional
- src/app.js imports and initializes incomeSourcesUI
- src/ui/income-spending-settings.js renders only Spending Buckets
- Human verification that the tab renders and Settings is clean
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39.1-income-sources-tab/39.1-RESEARCH.md
@.planning/phases/39.1-income-sources-tab/39.1-02-SUMMARY.md
@src/app.js
@index.html
@src/ui/income-spending-settings.js

<interfaces>
<!-- Exact insertion points for the executor. -->

index.html — existing tab nav (lines ~44–53):
```html
<button class="tab" data-tab="childcare" aria-label="Childcare"><span class="tab-label">Childcare</span></button>
<button class="tab" data-tab="settings" aria-label="Settings"><span class="tab-label">Settings</span></button>
```
Insert new tab button BEFORE settings button.

index.html — existing childcare panel (lines ~338–345):
```html
<div class="tab-panel" data-panel="childcare">
  <div id="childcareSummary"></div>
  <div id="childcareAccountList"></div>
  <div id="childcareLedgerSection" style="display:none"></div>
</div>
<!-- SETTINGS follows -->
```
Insert new tab panel AFTER childcare panel and BEFORE settings panel.

src/app.js — renderAll() switch block (line ~138):
```javascript
if (panelId === 'childcare') renderTasks.push(childcareUI.render());
if (panelId === 'settings') { ... }
```
Add `if (panelId === 'income-sources') renderTasks.push(incomeSourcesUI.render());` after childcare line.

src/app.js — parallel init block (line ~259):
```javascript
childcareUI.init(),
```
Add `incomeSourcesUI.init(),` alongside childcareUI.init().

src/ui/income-spending-settings.js — render() method:
Currently calls: `const [sources, buckets] = await Promise.all([incomeSourceRepository.getAll(), spendingBucketRepository.getAll()])`
Then: `container.innerHTML = _renderSourcesSection(sources) + _renderBucketsSection(buckets)`
Then: `this._bindSourceEvents(container, sources)` + `this._bindBucketEvents(container, buckets)`

After cleanup: `const buckets = await spendingBucketRepository.getAll()`
Then: `container.innerHTML = _renderBucketsSection(buckets)`
Then: `this._bindBucketEvents(container, buckets)` only.

Also remove from income-spending-settings.js:
- `import { incomeSourceRepository } from '../db/repository.js'` (keep spendingBucketRepository)
- `import { getNextIncomeEvent } from '../utils/income.js'` (no longer needed here)
- Private functions: _renderSourcesSection, _renderSourceRow, _renderSourcesSection, _bindSourceEvents, _bindSourceFormEvents, _incomeSourceForm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab nav + panel to index.html, wire app.js</name>
  <files>index.html, src/app.js</files>
  <action>
    **index.html changes (two locations, atomic):**

    1. In the `#mainTabs` div, insert a new tab button immediately before the settings button:
    ```html
    <button class="tab" data-tab="income-sources" aria-label="Pay Sources"><span class="tab-label">Pay Sources</span></button>
    ```
    Tab label is "Pay Sources" (Claude's discretion from CONTEXT.md — avoids collision with existing "Income" tab; fits 6-char truncation rule: "Pay So").

    2. After the closing `</div>` of the childcare tab-panel and before the settings tab-panel comment, insert:
    ```html
    <!-- PAY SOURCES -->
    <div class="tab-panel" data-panel="income-sources">
      <div id="incomeSourcesSummary"></div>
      <div id="incomePendingConfirmations"></div>
      <div id="incomeSourceList"></div>
    </div>
    ```

    **src/app.js changes (three locations):**

    1. Add import at top alongside childcareUI import:
    ```javascript
    import { incomeSourcesUI } from './ui/income-sources.js';
    ```

    2. In renderAll(), after the childcare line, add:
    ```javascript
    if (panelId === 'income-sources') renderTasks.push(incomeSourcesUI.render());
    ```

    3. In the parallel init block (Promise.all), alongside childcareUI.init():
    ```javascript
    incomeSourcesUI.init(),
    ```

    **Critical check (from RESEARCH.md Pitfall 5):** The data-tab value in index.html MUST be `income-sources` (with hyphen). The app.js check MUST be `panelId === 'income-sources'` (same string). Confirm both use the hyphenated form — no camelCase mismatch.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Full vitest suite still passes; index.html contains data-tab="income-sources"; app.js imports and initializes incomeSourcesUI</done>
</task>

<task type="auto">
  <name>Task 2: Remove income sources section from Settings</name>
  <files>src/ui/income-spending-settings.js</files>
  <action>
    Edit src/ui/income-spending-settings.js to remove all income-source related code while keeping all spending-bucket code intact.

    **Remove from imports:**
    - Remove `incomeSourceRepository` from the repository import (keep `spendingBucketRepository`)
    - Remove `getNextIncomeEvent` from the income.js import entirely (the import line becomes unused)

    **Remove these private functions entirely:**
    - `_renderSourceRow(source)` — the per-row renderer
    - `_renderSourcesSection(sources)` — the section HTML builder
    - `_bindSourceEvents(container, sources)` — the event binder for the sources section
    - `_bindSourceFormEvents(form, sources)` — the sub-form event binder
    - `_incomeSourceForm(source)` — the add/edit form builder (if defined separately)

    **Modify render() method:**
    Replace the current implementation:
    ```javascript
    // BEFORE
    const [sources, buckets] = await Promise.all([
      incomeSourceRepository.getAll(),
      spendingBucketRepository.getAll()
    ]);
    container.innerHTML = _renderSourcesSection(sources) + _renderBucketsSection(buckets);
    this._bindSourceEvents(container, sources);
    this._bindBucketEvents(container, buckets);
    ```
    With:
    ```javascript
    // AFTER
    const buckets = await spendingBucketRepository.getAll();
    container.innerHTML = _renderBucketsSection(buckets);
    this._bindBucketEvents(container, buckets);
    ```

    **Do NOT remove:** _renderBucketsSection, _bindBucketEvents, _renderBucketRow, _bucketForm, spendingBucketRepository, and the helpers escHtml, penceToStr, strToPence, todayStr, formatDate (the new income-sources.js also uses these — they are standalone helpers, not shared imports).

    After editing, verify the file still exports `incomeSpendingSettings` with `init()` and `render()` intact.

    This change makes the income-spending-settings.test.js Test B (no income sources in Settings) go GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/income-spending-settings.test.js tests/income-sources.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files pass GREEN; income-spending-settings.test.js Test B (no income sources heading) now passes; full vitest suite passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — tab navigation, CRUD, and Settings cleanup</name>
  <action>Run npm run build then open the app and verify the Pay Sources tab and Settings cleanup manually per the steps below.</action>
  <what-built>
    - New "Pay Sources" tab in the main navigation bar
    - Tab panel with three container divs (summary, pending confirmations, source list)
    - incomeSourcesUI wired into app.js init and renderAll
    - Settings no longer shows Income Sources section
    - Settings still shows Spending Buckets correctly
  </what-built>
  <how-to-verify>
    1. Run `npm run build` — build must succeed with no errors
    2. Open the app (npm run dev or open dist/index.html)
    3. Check nav bar: "Pay Sources" tab appears between Childcare and Settings
    4. Click "Pay Sources" tab:
       - Panel loads without console errors
       - If income sources exist (configured in Phase 33 Settings), they appear as a list
       - If no sources: "No income sources configured yet" message shown
    5. Click "Add Source" (or equivalent button):
       - Inline form appears with: Name, Amount, Pay Date Rule (select), Pay Date Day (hidden until nth-of-month), Active checkbox
       - Submit a source (e.g. "Test Salary", £3000, last-working-day, active)
       - Source appears in the list; form closes
    6. Click "Settings" tab:
       - "Income Sources" section is GONE
       - "Spending Buckets" section is still present and functional
    7. Navigate back to "Pay Sources":
       - Source added in step 5 is still there
       - If today's date is close to the configured payday: a confirmation card appears
    8. Open DevTools Console — no errors during any of the above steps
  </how-to-verify>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All automated tests pass; human confirms Pay Sources tab navigable, CRUD works, Settings shows Spending Buckets only, no console errors</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full automated suite green before checkpoint. Human checkpoint verifies tab renders, CRUD works, Settings is clean.

Automated: `npx vitest run`
Build check: `npm run build`
</verification>

<success_criteria>
- index.html: data-tab="income-sources" tab button + data-panel="income-sources" panel present
- src/app.js: imports incomeSourcesUI; includes in renderAll() and init block
- src/ui/income-spending-settings.js: renders Spending Buckets only; no incomeSourceRepository import
- All tests in income-sources.test.js GREEN
- All tests in income-spending-settings.test.js GREEN (both Test A and Test B)
- Full vitest suite passes (686+ tests)
- Human verify: tab is navigable, CRUD functional, no console errors, Settings is clean
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-income-sources-tab/39.1-03-SUMMARY.md`
</output>
