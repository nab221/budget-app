---
phase: 46-income-card-edit-delete-and-unconfirm-functionality
plan: 03
type: execute
wave: 3
depends_on:
  - 46-02
files_modified: []
autonomous: false
requirements:
  - INCOME-06
  - INCOME-07
  - INCOME-08
  - INCOME-09

must_haves:
  truths:
    - "User can click Edit on an income source card and the inline edit form opens pre-populated"
    - "User can click Delete on an income source card, confirm the dialog, and the card is removed"
    - "Confirmed income entry in modal shows the actual saved amount and date"
    - "User can click Edit on a confirmed entry, change date/amount, save, and see updated values"
    - "User can click Unconfirm on a confirmed entry, confirm the dialog, and entry reverts to pending"
  artifacts:
    - path: "src/ui/income-sources.js"
      provides: "All Phase 46 functionality live in browser"
  key_links:
    - from: "Income tab card grid"
      to: "inline edit form"
      via: "Edit button click — no modal, opens form above card grid"
      pattern: "edit-source"
    - from: "Income modal confirmed entry"
      to: "Edit/Unconfirm inline row"
      via: "Received badge row with Edit + Unconfirm buttons visible"
      pattern: "showEditIncomePrompt|unconfirmIncomeEntry"
---

<objective>
Human browser verification: confirm all four Phase 46 requirements are working correctly in the live app.

Purpose: Vitest covers functional contracts; browser verification confirms UX correctness — form pre-population, dialog flow, modal refresh, and cross-tab sync with Transactions.
Output: User confirms INCOME-06 through INCOME-09 all pass.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/46-income-card-edit-delete-and-unconfirm-functionality/46-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 wrote failing test stubs for INCOME-06..09.
    Plan 02 implemented:
    - Fixed income card Edit/Delete button delegation (removed inline stopPropagation, moved to delegation handler)
    - Confirmed income entries now show: "Received £X on D Mon YYYY" + inline Edit + Unconfirm buttons
    - window.showEditIncomePrompt: opens inline date+amount form pre-filled with existing values
    - window.saveEditedIncomeEntry: calls incomeRepository.update() and refreshes modal
    - window.unconfirmIncomeEntry: window.confirm + incomeRepository.delete() + refreshes modal
  </what-built>
  <how-to-verify>
    Open the app in a browser (run dev server if needed: `npm run dev`).

    **INCOME-06: Card Edit button**
    1. Navigate to the Income tab
    2. Find an income source card (e.g. Salary)
    3. Click the "Edit" button on the card (top-right of the card)
    4. EXPECTED: The inline "Edit Income Source" form appears above the card grid, pre-populated with the source's name, amount, rule, and day — NOT the income entries modal

    **INCOME-06: Card Delete button**
    5. Click the "Delete" button on a card you are willing to delete (or add a test source first)
    6. EXPECTED: A browser confirm dialog appears: "Delete this income source?"
    7. Click Cancel — card remains. Click OK on a test source — card disappears

    **INCOME-07: Confirmed entry shows amount + date**
    8. Open an income source modal by clicking the card body (not Edit/Delete)
    9. Find an entry that has already been confirmed (shows as "Received")
    10. EXPECTED: The badge reads e.g. "Received £2,450.00 on 25 Mar 2026" (actual saved values)
    11. EXPECTED: Small "Edit" and "Unconfirm" buttons appear on the same row as the badge

    **INCOME-08: Edit confirmed entry**
    12. Click the "Edit" button next to a confirmed entry
    13. EXPECTED: The row expands to show date and amount inputs pre-filled with the confirmed values
    14. Change the amount to a slightly different value (e.g. add £1)
    15. Click "Save"
    16. EXPECTED: Modal refreshes, confirmed entry now shows the new amount in the badge
    17. Open Transactions tab — the income entry should show the updated amount

    **INCOME-09: Unconfirm a confirmed entry**
    18. Back in the Income modal, click "Unconfirm" on a confirmed entry
    19. EXPECTED: A browser confirm dialog appears
    20. Click OK
    21. EXPECTED: Modal refreshes, entry now shows the "Confirm" button (pending state)
    22. Transactions tab no longer shows that income entry

    Report: "approved" if all 4 requirements pass, or describe specific failures.
  </how-to-verify>
  <resume-signal>Type "approved" if INCOME-06 through INCOME-09 all pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval gates this plan. All four INCOME-06..09 requirements confirmed in browser.
</verification>

<success_criteria>
User reports "approved" — all four Phase 46 requirements verified:
- INCOME-06: Card Edit/Delete buttons work without throwing errors
- INCOME-07: Confirmed entries show saved amount and date
- INCOME-08: Edit flow saves via incomeRepository.update and modal refreshes
- INCOME-09: Unconfirm reverts entry to pending state and syncs Transactions tab
</success_criteria>

<output>
After completion, create `.planning/phases/46-income-card-edit-delete-and-unconfirm-functionality/46-03-SUMMARY.md`
</output>
