---
phase: 09-swipe-gesture-system
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/ui/expenses.js]
autonomous: true
requirements: [SWP-01, SWP-05]
must_haves:
  truths:
    - "Expenses rows include the red delete affordance"
    - "Left-swiping a row reveals the delete button"
    - "Tapping the revealed delete button triggers row deletion"
  artifacts:
    - path: "src/ui/expenses.js"
      provides: "Wired swipe-to-delete logic"
  key_links:
    - from: "src/ui/expenses.js"
      to: "src/utils/gestures.js"
      via: "SwipeHandler instantiation"
    - from: "src/ui/expenses.js"
      to: "window.deleteExpense"
      via: "callback trigger"
---

<objective>
Integrate swipe-to-delete into the Expenses tab.

Purpose: Allow users to delete expenses with a left-swipe gesture and a confirmation tap.
Output: Updated src/ui/expenses.js with gesture integration and row markup enhancements.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/ui/expenses.js
@src/utils/gestures.js
@css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Swipe Markup to Rows</name>
  <files>src/ui/expenses.js</files>
  <action>
    Modify `expensesUI.render()` to include the swipe affordance markup within each `tr`.
    
    Inside the row generation loop:
    - Add `class="swipe-row"` to the `tr`.
    - Insert `<div class="swipe-action-right" onclick="deleteExpense(${item.id}, '${item.type}')">Delete</div>` as a child of the `tr` or first `td`.
    - Ensure the row has a stable `data-id` and `data-type`.
  </action>
  <verify>
    <automated>grep -q "swipe-action-right" src/ui/expenses.js</automated>
  </verify>
  <done>Row markup includes swipe affordance.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Swipe Logic in Expenses UI</name>
  <files>src/ui/expenses.js</files>
  <action>
    Wire up the SwipeHandler to the Expenses list.
    
    In `expensesUI.init()` or a new `setupGestures()` method:
    - Use delegation on the `expenseBody` to handle touch events for all rows.
    - Instantiate `SwipeHandler` for the swiped row.
    - In `onSwipe(dX)`: apply `transform: translateX(dX)` to the row.
    - In `onThresholdCross()`: trigger 'threshold' haptic.
    - In `onEnd(dX, isMet)`: if `isMet` and direction is left, snap to -80px (revealed state). If not, snap to 0px.
    
    Ensure `SwipeHandler` correctly identifies which row is being swiped using `e.target.closest('tr')`.
  </action>
  <verify>
    <automated>grep -q "SwipeHandler" src/ui/expenses.js</automated>
  </verify>
  <done>SwipeHandler is integrated and controls row transformations.</done>
</task>

</tasks>

<verification>
Manual verification on mobile (or Chrome emulation) to ensure swiping left reveals the red button and tapping it triggers deletion.
</verification>

<success_criteria>
- Left-swipe reveals delete button.
- Snap back works if threshold is not met.
- Delete button is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/09-swipe-gesture-system/09-02-SUMMARY.md`
</output>
