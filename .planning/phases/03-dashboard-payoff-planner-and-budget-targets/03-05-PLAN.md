---
phase: 03-dashboard-payoff-planner-and-budget-targets
plan: 05
type: execute
wave: 3
depends_on: ["03-04"]
files_modified: [src/ui/payoff.js, src/app.js]
autonomous: true
requirements: [BT-01, BT-02, BT-03, PAY-05]
must_haves:
  truths:
    - "Users can select a source debt and enter promotional terms to model a balance transfer."
    - "The modeler correctly shows cost comparison: Interest vs BT Fee."
    - "A clear recommendation is provided for the monthly payment needed to clear the debt within the promo period."
  artifacts:
    - path: "src/ui/payoff.js"
      provides: "Balance transfer modeling component"
      contains: "renderBTModeler"
  key_links:
    - from: "src/ui/payoff.js"
      to: "modelBalanceTransfer"
      via: "utility call"
      pattern: "modelBalanceTransfer"
---

<objective>
Implement the balance transfer modeling feature and finalize the payoff planner.

Purpose: Help users decide if a balance transfer is cost-effective.
Output: Integrated BT modeler in the payoff planner and a final verified Phase 3 feature set.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/utils/finance.js
@src/ui/payoff.js

<interfaces>
From src/utils/finance.js (after Plan 01):
```javascript
export function modelBalanceTransfer(debt, promoMonths, feePercent);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Balance Transfer Modeler UI</name>
  <files>src/ui/payoff.js</files>
  <action>
    1. Update `src/ui/payoff.js` to include a "Balance Transfer Modeler" section.
    2. Inputs needed:
       - Source Debt (Dropdown of all current credit card/loan debts).
       - Promotional Period (Months).
       - Transfer Fee (Percentage).
    3. Use `modelBalanceTransfer` from `src/utils/finance.js` to calculate results when inputs change.
  </action>
  <verify>
    Check if 'src/ui/payoff.js' contains logic for the BT modeler inputs and calculation call.
  </verify>
  <done>
    Payoff planner includes an interactive balance transfer modeling tool.
  </done>
</task>

<task type="auto">
  <name>Task 2: BT Comparison and Recommendations</name>
  <files>src/ui/payoff.js</files>
  <action>
    1. Display results from `modelBalanceTransfer`:
       - Transfer Fee (in GBP).
       - Total Cost (BT) vs Total Interest Saved (Current).
       - Recommended Monthly Payment to clear the balance within the 0% period.
    2. Add clear visual indicators (e.g., "Saves £250" in Green or "Costs £50 more" in Red).
  </action>
  <verify>
    Manual verification: Entering BT terms for a debt provides accurate cost comparison in the UI.
  </verify>
  <done>
    Balance transfer modeling provides actionable financial recommendations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase 3 Final Verification</name>
  <files>src/app.js</files>
  <action>
    1. Perform a final review of all Phase 3 success criteria (DASH-01..05, PAY-01..05, BT-01..03).
    2. Check performance: ensure simulations with 10+ debts are still responsive on mobile-sized viewports.
    3. Fix any final UI inconsistencies or edge cases (e.g., zero-interest debts).
  </action>
  <verify>
    Check against Phase 3 success criteria in ROADMAP.md.
  </verify>
  <done>
    Phase 3 features are fully implemented, verified, and integrated.
  </done>
</task>

</tasks>

<verification>
- Balance Transfer modeler is functional.
- Recommendations for clearing debt within promo windows are accurate.
- All Phase 3 requirements are met.
</verification>

<success_criteria>
- Users can make data-driven decisions about balance transfers.
- Full Phase 3 feature suite is ready for user testing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-payoff-planner-and-budget-targets/03-05-SUMMARY.md`
</output>
