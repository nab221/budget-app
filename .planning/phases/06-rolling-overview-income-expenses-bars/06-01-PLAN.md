---
phase: 06-rolling-overview-income-expenses-bars
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/cashflow.js, src/utils/cashflow.test.js]
autonomous: true
requirements: [DASH-04, ANAL-04]

must_haves:
  truths:
    - "Daily data can be aggregated into weekly bins"
    - "Daily data can be aggregated into monthly bins"
    - "Income and expenses are summed within each bin"
    - "Balance reflects the closing value of each bin"
  artifacts:
    - path: "src/utils/cashflow.js"
      provides: "Aggregation logic for rolling overview data"
  key_links:
    - from: "src/utils/cashflow.js"
      to: "date-fns"
      via: "import { startOfWeek, startOfMonth... }"
---

<objective>
Implement the data aggregation logic to bin daily financial data into weekly and monthly periods.

Purpose: Provide the backend support for the flexible time views in the Rolling Overview chart.
Output: A new utility function `aggregateRollingOverview` in `src/utils/cashflow.js` with comprehensive tests.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rolling-overview-income-expenses-bars/06-RESEARCH.md
@src/utils/cashflow.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement binning logic in cashflow.js</name>
  <files>src/utils/cashflow.js, src/utils/cashflow.test.js</files>
  <behavior>
    - `aggregateRollingOverview(data, binning)` takes daily data object { labels, balance, income, expenses } and mode ('D', 'W', 'M').
    - If mode is 'D', returns data unchanged.
    - If mode is 'W', bins by start of ISO week (Monday).
    - If mode is 'M', bins by start of month.
    - Income and expenses are summed per bin.
    - Balance is the *last* available balance in the bin.
    - TodayIndex is recalculated to point to the bin containing the current date.
  </behavior>
  <action>
    - Export `aggregateRollingOverview` in `src/utils/cashflow.js`.
    - Use `date-fns` functions (`startOfWeek`, `startOfMonth`, `format`, `parseISO`) for reliable date grouping.
    - Follow the implementation strategy in 06-RESEARCH.md.
    - Ensure `todayIndex` is correctly identified in the binned result (which bin contains today's date).
  </action>
  <verify>
    <automated>npm test -- src/utils/cashflow.test.js</automated>
  </verify>
  <done>Aggregation logic implemented and passes all unit tests for Daily, Weekly, and Monthly binning.</done>
</task>

<task type="auto">
  <name>Task 2: Update getDailyRollingData to support binning</name>
  <files>src/utils/cashflow.js</files>
  <action>
    - Update `getDailyRollingData(targetMonth)` to accept an optional `binning` parameter (default 'D').
    - After generating daily data, call `aggregateRollingOverview` if binning is not 'D'.
    - Ensure the return format remains consistent for chart consumers.
  </action>
  <verify>
    <automated>npm test -- src/utils/cashflow.test.js</automated>
  </verify>
  <done>Data utility updated to provide binned data upon request.</done>
</task>

</tasks>

<verification>
Check that `aggregateRollingOverview` handles edge cases like empty data or date ranges spanning multiple years correctly.
</verification>

<success_criteria>
- Unit tests pass for all aggregation modes.
- Balance correctly reflects closing balance of the period.
- Totals for income/expenses are accurate sums of the period.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rolling-overview-income-expenses-bars/06-01-SUMMARY.md`
</output>
