---
phase: 10-advanced-debt-and-payoff
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [src/ui/debts.js, src/ui/dashboard.js]
autonomous: true
requirements: [DEBT-07, DEBT-09]

must_haves:
  truths:
    - "Clicking a debt card opens the edit modal"
    - "Dashboard shows debt impact as % of income"
    - "Dashboard warns about promos expiring within 60 days"
  artifacts:
    - path: "src/ui/debts.js"
      provides: "Edit modal and card click handler"
    - path: "src/ui/dashboard.js"
      provides: "Debts Repayment panel"
---

<objective>
Implement debt editing via card interaction and enhance the dashboard with a proactive debt repayment impact panel and alerts.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-debt-and-payoff/10-01-SUMMARY.md
@src/ui/debts.js
@src/ui/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Debt Editing via Card Click</name>
  <files>src/ui/debts.js</files>
  <action>
    - Modify debt card rendering in `src/ui/debts.js` to add a click listener to the entire card.
    - DO NOT add a separate "Edit" button; clicking the card is the trigger.
    - Implement an "Edit Debt" modal:
        - Use existing modal patterns (`templateUI.showModal`).
        - Form should include: Name, Balance (optional/display), Credit Limit, APR, Promo End Date, and Post-Promo APR.
        - Handle save using `debtRepository.update`.
    - Update the debt card display to show "Promo ends: [Date]" if applicable.
  </action>
  <verify>
    Manual check: Clicking a debt card opens the modal with pre-filled data.
    Saving changes reflects immediately in the UI.
  </verify>
  <done>Debt editing implemented via card-click interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Repayment Panel & Alerts</name>
  <files>src/ui/dashboard.js</files>
  <action>
    - Create `renderDebtRepaymentPanel(debts, income)` in `src/ui/dashboard.js`.
    - Calculate `Debt Impact %`: `(Total Min Payments + Extra Monthly Payment) / Total Income * 100`.
    - Extra Monthly Payment should be fetched from `localStorage` (preference key defined in RESEARCH.md).
    - Implement "Promo Expiring" alerts:
        - Check if any debt has `promoEndDate` within the next 60 days from today.
        - Display a warning badge/message if true.
    - Add this panel to the dashboard view, preferably below the summary grid.
  </action>
  <verify>
    Dashboard displays the correct Debt Impact percentage.
    Warning appears if a debt is set to expire in < 60 days.
  </verify>
  <done>Dashboard enhanced with repayment metrics and promo expiration alerts.</done>
</task>

</tasks>

<verification>
Verify editing flow and dashboard panel calculations manually.
</verification>

<success_criteria>
- Debt cards are clickable and open edit modal.
- Promo dates and post-promo APRs are editable and saved.
- Dashboard shows "(Min + Extra) / Income %" metric.
- 60-day promo expiration warnings appear on dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-debt-and-payoff/10-02-SUMMARY.md`
</output>
