---
phase: 10-advanced-debt-and-payoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js, src/utils/finance.js, src/utils/finance.test.js]
autonomous: true
requirements: [DEBT-08, PAY-08]

must_haves:
  truths:
    - "Simulation accounts for 0% promo periods correctly"
    - "Simulation history includes principal and interest split"
    - "Schema v8 successfully tracks promo end dates and post-promo APR"
  artifacts:
    - path: "src/db/schema.js"
      provides: "Schema v8 definition"
      contains: "promoEndDate, postPromoApr"
    - path: "src/utils/finance.js"
      provides: "Enhanced simulation engine"
      exports: ["simulatePayoff", "calcMinPayment"]
  key_links:
    - from: "src/utils/finance.js"
      to: "src/utils/finance.test.js"
      via: "unit tests"
---

<objective>
Update the data layer and core simulation logic to handle 0% promotional APRs and provide detailed payment history snapshots.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-debt-and-payoff/10-RESEARCH.md
@src/db/schema.js
@src/db/repository.js
@src/utils/finance.js
@src/utils/finance.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema to v8 & Repository</name>
  <files>src/db/schema.js, src/db/repository.js</files>
  <action>
    - Increment schema version to 8 in `src/db/schema.js`.
    - Add `promoEndDate` and `postPromoApr` to the `debts` table store definition.
    - Update `debtRepository` in `src/db/repository.js`:
        - Ensure `add` and `update` methods handle `promoEndDate` (ISO string) and `postPromoApr` (integer pence representation of percentage if needed, though APR is usually stored as float/number). 
        - Ensure data conversion for these new fields is consistent with existing fields.
  </action>
  <verify>
    Run `npm run build` to ensure no syntax errors.
    (Manual verification of migration will happen in next plan's UI tasks).
  </verify>
  <done>Schema v8 defined and repository updated to support promo fields.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Simulation Engine for Promos & History</name>
  <files>src/utils/finance.js</files>
  <action>
    - Update `calcMinPayment` to accept optional `promoEndDate` and `postPromoApr`. If a reference date is before `promoEndDate`, use 0% APR for calculation.
    - Refactor `simulatePayoff` to:
        - Determine effective APR per debt per simulation month based on `promoEndDate`.
        - Handle the "Rate Jump" when `promoEndDate` is exceeded.
        - Capture a 12-month rolling history (or full history) containing:
            - `month` (index or date)
            - `totalInterestCharged`
            - `totalPrincipalPaid`
            - `payments`: Array of `{ debtId, amount, interestCharged, principalPaid, isRateJump }`.
        - Use the iterative loop pattern from RESEARCH.md.
        - Tie-breaker for equal APR: Smallest Balance.
  </action>
  <verify>
    `npx vitest run src/utils/finance.test.js` (expected to fail until Task 3).
  </verify>
  <done>Simulation engine supports 0% promos and returns detailed monthly snapshots.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Advanced Finance Tests</name>
  <files>src/utils/finance.test.js</files>
  <action>
    - Add test cases to `src/utils/finance.test.js`:
        - `calcMinPayment` with active 0% promo (should only reflect principal reduction/floor).
        - `simulatePayoff` with a debt having a 3-month 0% promo (verify £0 interest for 3 months, then jump).
        - Verify the structure of the returned history object (presence of `principalPaid`, `interestCharged`, and `isRateJump`).
        - Verify tie-breaker logic (Smallest Balance).
  </action>
  <verify>
    npx vitest run src/utils/finance.test.js
  </verify>
  <done>Comprehensive unit tests pass for the enhanced simulation engine.</done>
</task>

</tasks>

<verification>
Automated unit tests for finance logic cover all promo and history requirements.
</verification>

<success_criteria>
- Schema v8 is defined.
- `simulatePayoff` handles 0% promos and APR jumps correctly.
- `simulatePayoff` returns a detailed history object for UI consumption.
- `npx vitest run src/utils/finance.test.js` passes 100%.
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-debt-and-payoff/10-01-SUMMARY.md`
</output>
