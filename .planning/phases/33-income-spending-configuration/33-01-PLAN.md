---
phase: 33-income-spending-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.js
  - src/db/repository.js
  - src/utils/income.js
  - src/ui/income-spending-settings.js
  - src/app.js
  - src/utils/supabase-sync.js
  - tests/income.test.js
autonomous: true
requirements:
  - PLAN-04
  - PLAN-06
  - TECH-06

must_haves:
  truths:
    - "Income sources are stored and managed as an unbounded collection with no schema, repository, or UI cap"
    - "Each income source shows its next projected payday using Phase 31 banking-calendar adjustment"
    - "Spending buckets are persisted separately, seeded once, and remain configuration-only inputs to later affordability work"
    - "Phase 33 exposes a collection-based income-event handoff for Phase 34 and does not introduce a singular global payDay"
    - "Phase 33 remains scoped to Settings configuration and projection helpers; no affordability dashboard UI is added here"
  artifacts:
    - path: "src/db/schema.js"
      provides: "Schema version bump from the real current baseline with incomeSources and spendingBuckets stores"
      contains: "incomeSources"
    - path: "src/db/repository.js"
      provides: "CRUD, ordering, default seeding, and active-source accessors for Phase 33 stores"
      contains: "spendingBuckets"
    - path: "src/utils/income.js"
      provides: "Collection-based projected income-event helpers for downstream phases"
      contains: "getUpcomingIncomeEvents"
    - path: "src/ui/income-spending-settings.js"
      provides: "Settings renderer for row-based income sources and spending buckets"
      contains: "Income Sources"
    - path: "tests/income.test.js"
      provides: "Coverage for payday derivation, multi-source ordering, empty-state behavior, and handoff shape"
      contains: "getNextIncomeEvent"
  key_links:
    - from: "src/utils/income.js"
      to: "src/utils/banking-calendar.js"
      via: "adjusted payday derivation"
      pattern: "adjustedPaymentDate|nextWorkingDay"
    - from: "src/app.js"
      to: "src/ui/income-spending-settings.js"
      via: "Settings tab render/init wiring"
      pattern: "incomeSpendingSettings"
    - from: "src/utils/income.js"
      to: "Phase 34 affordability helpers"
      via: "collection-based event contract"
      pattern: "getUpcomingIncomeEvents"
---

<objective>
Implement Phase 33 as a focused configuration-and-projection phase for income sources and spending buckets.

Purpose: Add sync-safe Settings persistence and projected payday helpers for an arbitrary number of income sources, preserve the Phase 31 banking-calendar dependency, and hand Phase 34 a collection-based income-event contract instead of a singular payDay model.

Output: A working Settings flow for unbounded income sources and spending buckets, plus tested projection helpers that produce sorted upcoming income events for downstream affordability logic.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-income-spending-configuration/33-RESEARCH.md
@.planning/phases/33-income-spending-configuration/33-CONTEXT.md
@.planning/phases/34-pay-period-affordability-engine/34-CONTEXT.md
</execution_context>

<assumptions_dependencies>
- Phase 31 is a hard dependency; reuse the shipped banking-calendar utilities and do not hand-roll payday adjustment logic here
- Phase 33 stays in Settings plus pure helper space; do not add pay-period navigator, affordability calculations, balance entry, or childcare UI
- Income sources are explicitly unbounded; removing a visible "max 2" message is not enough if any slot-based schema, repository, or helper assumption remains
- Phase 34 consumes a merged collection of income events across active sources; this plan must not create or preserve a singular global `payDay` abstraction
- If cloud snapshot/backup already serializes Dexie tables generically, preserve that implementation and prove coverage with tests rather than redundant allowlist code
</assumptions_dependencies>

<context>
@src/app.js
@src/db/schema.js
@src/db/repository.js
@src/utils/banking-calendar.js
@src/utils/supabase-sync.js

<interfaces>
From src/app.js Settings rendering:
```javascript
if (panelId === 'settings') {
  renderTasks.push(categoryUI.render());
  renderTasks.push(targetsUI.renderTargetSettings());
}
```

From src/utils/supabase-sync.js cloud snapshot path:
```javascript
const tableData = Object.fromEntries(
  await Promise.all(db.tables.map(async (t) => [t.name, await t.toArray()]))
);
```

Required Phase 34 handoff contract to implement in src/utils/income.js:
```javascript
export function getNextIncomeEvent(source, fromDate) {
  // -> { sourceId, sourceName, amount, nominalDate, adjustedDate }
}

export function getUpcomingIncomeEvents(sources, fromDate, limit) {
  // -> Array<{ sourceId, sourceName, amount, nominalDate, adjustedDate }>
  // sorted by adjustedDate ascending across all active sources
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add collection-based Phase 33 stores and repository support</name>
  <files>src/db/schema.js, src/db/repository.js, src/utils/supabase-sync.js</files>
  <behavior>
    - Adding a third or fourth income source requires no schema or repository changes beyond repeated rows
    - `payDateDay` validation is enforced only for `nth-of-month` and rejects missing, non-integer, or out-of-range values
    - Spending buckets seed defaults exactly once when the store is empty
    - Cloud snapshot/restore includes the new stores without special-casing a single source or a singular payDay field
  </behavior>
  <action>
    In `src/db/schema.js`, bump from **Dexie v19** (the confirmed real current baseline — `src/db/schema.js`, last `db.version()` call), not the roadmap's synthetic v15 label, and add dedicated `incomeSources` and `spendingBuckets` stores. Keep income-source configuration separate from posted income or expected-income ledger tables.

    In `src/db/repository.js`, add CRUD helpers plus ordering support for both stores, default bucket seeding, and repository-level validation for `payDateRule` / `payDateDay`. Support 0..N rows with active/inactive state and display order; do not introduce `primaryIncome`, `secondaryIncome`, or any two-slot equivalent.

    Inspect `src/utils/supabase-sync.js`. If the snapshot path is still generic over `db.tables`, preserve that behavior and only touch the file if a store allowlist or similar Phase-33-specific registration gap actually exists. If explicit registration is required, add both new stores together.

    Anti-cap regression guard: before closing Task 1, run a grep to confirm no two-slot (primaryIncome/secondaryIncome) patterns, no `MAX_SOURCES`, and no literal `2` used as a source limit have been introduced in `src/db/schema.js` or `src/db/repository.js`.
  </action>
  <verify>
    <automated>npx vitest run src/db/repository.test.js</automated>
    <manual>Inspect `src/utils/supabase-sync.js` snapshot path: confirm `db.tables.map(...)` covers all stores generically (no per-store allowlist required for incomeSources/spendingBuckets). If allowlist logic is present, verify both stores are registered and add a targeted test to `src/utils/supabase-sync.test.js` (or equivalent) to confirm their inclusion.</manual>
  </verify>
  <done>
    - Phase 33 persistence supports arbitrary income-source counts with no product or UI cap baked into the data layer
    - Spending buckets seed once and remain separate configuration inputs
    - Sync/backup coverage for the new stores is either implemented or explicitly proven unnecessary by tests against the generic snapshot path
    - No two-slot naming (primaryIncome, secondaryIncome), no MAX_SOURCES constant, and no literal source-count cap of 2 exists anywhere in src/db/schema.js or src/db/repository.js (confirmed by grep before closing)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement projected income-event helpers and Phase 34 handoff contract</name>
  <files>src/utils/income.js, tests/income.test.js</files>
  <behavior>
    - `getNextIncomeEvent(source, fromDate)` returns a normalized projected event with nominal and adjusted dates
    - `getUpcomingIncomeEvents(sources, fromDate, limit)` merges and sorts events across 3+ active sources by adjusted date ascending
    - Inactive sources are ignored and empty active-source input returns an empty collection or null next event cleanly
    - No helper exports or data shapes rely on a singular `payDay` property or single-income assumption
  </behavior>
  <action>
    Create `src/utils/income.js` as a pure helper module that derives nominal paydays from each income source rule and then routes every projected date through the Phase 31 banking-calendar utility. Implement `getNextIncomeEvent(source, fromDate)` and `getUpcomingIncomeEvents(sources, fromDate, limit)` using collection semantics throughout.

    Use these normalized event objects as the explicit Phase 34 handoff contract:
    - `sourceId`
    - `sourceName`
    - `amount`
    - `nominalDate`
    - `adjustedDate`

    In `tests/income.test.js`, cover `nth-of-month`, `last-day`, `last-working-day`, bank-holiday/weekend adjustment, 3+ source ordering, and no-income-source behavior. The tests must prove Phase 34 receives a collection of events rather than a single payDay value.
  </action>
  <verify>
    <automated>npx vitest run tests/income.test.js --coverage</automated>
  </verify>
  <done>
    - Phase 34 has a tested collection-based income-event contract built on adjusted dates
    - Payday projection depends on Phase 31 utilities rather than duplicate date logic
    - Multi-source behavior is proven for 3+ sources and empty-state handling
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Settings UI for configuration-only Phase 33 flows</name>
  <files>src/ui/income-spending-settings.js, src/app.js</files>
  <action>
    Add a dedicated Settings module for Phase 33 and wire it into the existing Settings render flow in `src/app.js`. Render row-based sections for `Income Sources` and `Spending Buckets` with add, edit, delete, and ordering support using the new repository helpers.

    Each income-source row must display the next projected nominal date and adjusted payday derived from `src/utils/income.js`. Keep the UI unbounded with no warnings or blocking for the third or later source. Empty-state rendering is valid.

    Keep this task strictly scoped to Settings configuration and projection display. Do not add dashboard affordability UI, pay-period navigation, or current-balance entry work here.
  </action>
  <verify>
    <automated>npx vitest run src/ui/income-spending-settings.test.js --reporter=verbose</automated>
    <manual>Manually verify in the Settings UI: add a 3rd income source after 2 already exist — no warning, no block, no disabled add-button. Confirm the 3rd source shows a projected adjusted payday derived from income.js. Confirm removing a source does not corrupt displayOrder of remaining rows.</manual>
  </verify>
  <done>
    - Settings can manage arbitrary income sources and spending buckets end-to-end
    - Projected payday display is visible in Settings and uses adjusted dates
    - Phase 33 UI remains configuration-only and does not leak Phase 34 affordability concerns
  </done>
</task>

</tasks>

<verification>
- `incomeSources` supports 0, 1, 2, and 3+ configured rows without any cap-specific branch or copy
- `getUpcomingIncomeEvents()` returns a merged collection sorted by adjusted date across all active sources
- `payDateDay` validation fails only for invalid `nth-of-month` rules and does not block other rule types
- Spending buckets seed once, remain editable, and survive snapshot/restore flows
- No Phase 33 artifact introduces or depends on a singular global `payDay`
- Existing Vitest suites remain green after the Phase 33 changes
</verification>

<success_criteria>
- No artificial cap on income-source count exists anywhere in the Phase 33 implementation
- Phase 31 banking-calendar helpers remain the sole source of payday adjustment behavior
- Phase 33 ships only configuration and projection work, not affordability UI
- Phase 34 receives a stable collection-based handoff contract built on income-event arrays
- Tests cover the main validation, ordering, empty-state, and sync/restore behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/33-income-spending-configuration/33-01-SUMMARY.md`
</output>
