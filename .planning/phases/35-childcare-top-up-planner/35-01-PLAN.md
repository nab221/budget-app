---
phase: 35-childcare-top-up-planner
plan: 01
type: execute
wave: 1
depends_on:
  - 34-01
files_modified:
  - src/db/schema.js
  - src/db/repository.js
  - src/db/repository.test.js
  - src/utils/childcare.js
  - src/utils/childcare.test.js
  - src/ui/childcare.js
  - src/ui/childcare.test.js
  - src/utils/affordability.js
  - src/utils/affordability.test.js
  - src/ui/dashboard.js
  - src/utils/supabase-sync.test.js
autonomous: true
requirements:
  - CHILD-01
  - CHILD-02
  - CHILD-03
  - TECH-06
must_haves:
  truths:
    - "Each childcare account can store a provider list with monthly/termly costs and show a required top-up this period."
    - "Required top-up per account uses provider monthly-equivalent totals, current account balance, and pending government bonus, floored at zero."
    - "Childcare entitlement period is clearly shown per account in the Childcare tab account-card view."
    - "Affordability committed-outgoings logic receives childcare top-up line items through a stable contract and includes them in period output."
    - "Cloud snapshot behavior continues to include new childcare data without adding redundant explicit allowlist plumbing when db.tables generic sync already covers it."
    - "Phase 35 remains focused on providers/top-up/affordability integration and does not expand into CSV/reporting or unrelated milestone features."
    - "No income-source cap logic is introduced as part of childcare work."
  artifacts:
    - path: "src/db/schema.js"
      provides: "Next-version Dexie migration for childcareProviders tied to childcareAccounts"
      contains: "childcareProviders store and upgrade defaults"
    - path: "src/db/repository.js"
      provides: "Childcare provider CRUD + required top-up seam methods for account and aggregate outputs"
      exports: ["childcareRepository"]
    - path: "src/utils/childcare.js"
      provides: "Pure provider monthly-equivalent and required top-up calculators"
      exports: ["monthlyEquivalentFromProvider", "calculateRequiredTopUp"]
    - path: "src/ui/childcare.js"
      provides: "Providers section and required top-up display in account cards + entitlement display"
      contains: "Add/edit/delete provider controls and per-account required top-up"
    - path: "src/utils/affordability.js"
      provides: "Affordability integration contract for childcare top-up line items"
      exports: ["normalizeChildcareTopUps", "includeChildcareTopUpsInCommittedOutgoings"]
    - path: "src/utils/supabase-sync.test.js"
      provides: "Regression checks that generic db.tables snapshot path includes childcareProviders"
      contains: "no explicit new allowlist dependency needed"
  key_links:
    - from: "src/ui/childcare.js"
      to: "src/db/repository.js"
      via: "account-card render loop reads providers + required top-up per account"
      pattern: "getAccountProviders|getRequiredTopUpForAccount"
    - from: "src/db/repository.js"
      to: "src/utils/childcare.js"
      via: "repository delegates formula math to pure helper utilities"
      pattern: "monthlyEquivalentFromProvider|calculateRequiredTopUp"
    - from: "src/utils/affordability.js"
      to: "src/ui/dashboard.js"
      via: "dashboard affordability pipeline consumes childcareTopUps contract"
      pattern: "childcareTopUps|includeChildcareTopUpsInCommittedOutgoings"
    - from: "src/utils/supabase-sync.js"
      to: "src/db/schema.js"
      via: "generic db.tables snapshot enumerates all stores including childcareProviders"
      pattern: "db\\.tables\\.map"
---

<objective>
Implement Phase 35 as a focused childcare top-up planner: add childcare providers, compute required top-up per account, and integrate top-up outputs into affordability committed outgoings.

Purpose: Answer "how much childcare top-up is required this period" and make that amount visible in the childcare and affordability flows without introducing out-of-scope reporting features.
Output: Schema + repository seams with tests, childcare UI provider/top-up rendering, and validated affordability integration contract with anti-regression coverage.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<assumptions_dependencies>
- Phase dependency: Phase 34 affordability view context is the integration target for CHILD-02 behavior.
- Scope lock for this plan: childcare providers + required top-up display + affordability integration only.
- Out of scope unless roadmap requirement is explicitly revised: broad childcare CSV export/reporting and standalone tfcTransactions subsystem.
- Schema versioning rule: at execution time, read the real latest db.version(N) in src/db/schema.js and add exactly N+1 for this migration; do not assume historical roadmap numbering.
- Cloud sync baseline: current sync snapshots all stores via db.tables generic behavior; verify this path covers childcareProviders and preserve it, rather than adding redundant explicit store registration plumbing.
- Phase self-containment: define provider model directly in this phase (childcareProviders store + repository contract); do not rely on any provider model from Phase 33.
- Anti-regression guardrails are mandatory: no income-source cap logic changes, no phase-boundary leakage into unrelated milestone features.
</assumptions_dependencies>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-childcare-top-up-planner/35-CONTEXT.md
@.planning/phases/35-childcare-top-up-planner/35-RESEARCH.md
@.planning/phases/34-pay-period-affordability-engine/34-CONTEXT.md
@src/db/schema.js
@src/db/repository.js
@src/ui/childcare.js
@src/utils/childcare.js
@src/utils/supabase-sync.js
@src/ui/dashboard.js

<interfaces>
From src/db/repository.js:
```javascript
export const childcareRepository = {
  async getAccounts() {}
  async getAccount(id) {}
  async saveAccount(account) {}
  async getLedger(accountId) {}
  async getBalance(accountId) {}
  async addLedgerEntry(entry) {}
  async deleteLedgerEntry(id, accountId) {}
}
```

From src/utils/childcare.js:
```javascript
export function calculateTopUp(depositAmountPence, remainingCapPence)
export function getEntitlementPeriod(entitlementStart, targetDate)
export function calculateFundingGap(targetSpendPence, currentBalancePence)
```

From src/ui/childcare.js:
```javascript
async _renderAccounts(accounts) {
  const cardPromises = accounts.map(async (account) => {
    const balance = await childcareRepository.getBalance(account.id);
    const { gap } = calculateFundingGap(account.targetMonthlySpend || 0, balance);
    // account-card HTML render path
  });
}
```

From src/utils/supabase-sync.js:
```javascript
const tableData = Object.fromEntries(
  await Promise.all(db.tables.map(async (t) => [t.name, await t.toArray()]))
);
```

From Phase 34 context contract (.planning/phases/34-pay-period-affordability-engine/34-CONTEXT.md):
```javascript
// affordability-window integration should include childcare top-up line items
// in committed outgoings/timeline before projected closing-balance calculation.
```

Required CHILD-02 integration contract for this phase:
```javascript
// Produced by childcare domain
[{ accountId, childName, requiredTopUpPence, description }]

// Consumed by affordability domain
normalizeChildcareTopUps(topUps) -> Array<{ date, description, amount }>
includeChildcareTopUpsInCommittedOutgoings(baseRows, topUps) -> mergedRows
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema + repository provider seams and required top-up formula with TDD coverage</name>
  <files>src/db/schema.js, src/db/repository.js, src/db/repository.test.js, src/utils/childcare.js, src/utils/childcare.test.js</files>
  <behavior>
    - Test 1: New childcareProviders records are account-scoped and CRUD operations return only providers for the target account.
    - Test 2: monthlyEquivalentFromProvider returns amount for monthly providers and amount/3 for termly providers.
    - Test 3: calculateRequiredTopUp uses max(0, providerMonthlyEquivalentTotal - currentBalance - pendingGovernmentBonus).
    - Test 4: Required top-up never becomes negative when balance + pending bonus exceeds periodic spend.
    - Test 5: Repository aggregate contract returns both per-account top-up rows and total childcare top-up pence for affordability.
    - Test 6: Income-source behavior is unchanged (no cap or limit logic introduced by childcare changes).
  </behavior>
  <action>Create the childcareProviders Dexie store in src/db/schema.js using a migration that bumps from the actual latest schema version at execution time (read current last db.version and add +1). In src/db/repository.js, add provider CRUD and query seams under childcareRepository, plus required top-up contract methods that delegate formula math to src/utils/childcare.js helpers (do not duplicate formula logic in UI). Add/extend tests first (RED->GREEN) in repository and childcare utility tests, including the explicit anti-regression check that no income-source cap logic is added or changed. Keep implementation strictly focused on CHILD-01 data/model needs; do not add tfcTransactions, CSV/reporting, or unrelated milestone entities.</action>
  <verify>
    <automated>npm run test -- src/db/repository.test.js src/utils/childcare.test.js</automated>
  </verify>
  <done>Schema includes childcareProviders at the real next version, repository exposes provider and required-top-up seams with passing tests, and formula behavior satisfies CHILD-01 without touching unrelated income-source constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Extend childcare UI with providers section and required top-up + entitlement display</name>
  <files>src/ui/childcare.js, src/ui/childcare.test.js, src/db/repository.js</files>
  <action>Within the existing account-card render loop in src/ui/childcare.js, add a Providers subsection per account (list + add/edit/delete controls) sourced from new childcareRepository provider seams, and render a prominent "Required top-up this period" amount per account. Surface entitlement period details per account using getEntitlementPeriod() as part of the visible card content (not only near-expiry alerts). Keep UI scope bounded to childcare tab account cards and provider controls; do not expand into broad monthly reporting tables or CSV/export surfaces in this phase. Add/extend UI tests in src/ui/childcare.test.js to cover provider rendering, top-up value rendering, and entitlement period visibility.</action>
  <verify>
    <automated>npm run test -- src/ui/childcare.test.js src/utils/childcare.test.js</automated>
  </verify>
  <done>Childcare tab shows per-account providers and required top-up values in account cards, entitlement period is clearly visible, and UI tests pass for providers/top-up/entitlement rendering behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement affordability integration contract and validate sync/phase-boundary regressions</name>
  <files>src/utils/affordability.js, src/utils/affordability.test.js, src/ui/dashboard.js, src/utils/supabase-sync.test.js</files>
  <behavior>
    - Test 1: normalizeChildcareTopUps transforms childcare top-up contract rows into committed-outgoing line items with stable fields.
    - Test 2: includeChildcareTopUpsInCommittedOutgoings merges childcare items into affordability rows without mutating unrelated rows.
    - Test 3: Total committed outgoings include childcare top-ups exactly once.
    - Test 4: Cloud snapshot path still includes childcareProviders through generic db.tables table enumeration.
    - Test 5: No explicit cloud-sync store allowlist registration is added when generic path already covers childcareProviders.
    - Test 6: No unrelated milestone features (CSV/reporting/import/export/navigation redesign) are pulled into this phase implementation.
  </behavior>
  <action>Create or extend src/utils/affordability.js with a narrow, explicit CHILD-02 integration contract for childcare top-up rows and wire it into dashboard affordability rendering in src/ui/dashboard.js. If affordability logic currently resides outside src/utils/affordability.js, add a thin adapter module there and keep existing computation ownership intact. Validate TECH-06 by testing that generic db.tables snapshot behavior already captures the new childcareProviders store; preserve this behavior and avoid adding redundant explicit allowlist/store-registration plumbing unless tests prove generic coverage is broken. Add regression tests that assert phase boundaries (no CSV/reporting or unrelated feature additions) and document the validation outcome in the summary artifact.</action>
  <verify>
    <automated>npm run test -- src/utils/affordability.test.js src/utils/supabase-sync.test.js src/ui/childcare.test.js</automated>
  </verify>
  <done>Affordability contract consumes childcare top-up outputs and includes them in committed outgoings, TECH-06 behavior is validated via generic sync coverage tests without redundant allowlist wiring, and anti-regression checks pass.</done>
</task>

</tasks>

<verification>
- Focused implementation tests:
  - npm run test -- src/db/repository.test.js src/utils/childcare.test.js src/ui/childcare.test.js src/utils/affordability.test.js src/utils/supabase-sync.test.js
- Anti-regression gates (must be explicitly checked):
  - No income-source cap logic introduced or modified by this phase.
  - No unnecessary explicit cloud-sync store registration added when db.tables generic sync already includes childcareProviders.
  - No phase-boundary leakage into unrelated milestone features (CSV/reporting/navigation redesign/legacy import).
- Full suite sanity:
  - npm test
</verification>

<success_criteria>
- CHILD-01: childcareProviders store and account-scoped provider CRUD exist, monthly/termly normalization is correct, and required top-up formula is accurate with floor-at-zero behavior.
- CHILD-03: entitlement period is clearly displayed per account in childcare UI.
- CHILD-02: childcare top-up outputs are integrated into affordability committed outgoings through a tested contract and visible in dashboard affordability output.
- TECH-06: cloud sync coverage for childcareProviders is verified against current generic db.tables snapshot behavior, and no redundant sync plumbing is introduced.
- Phase remains tightly scoped to childcare providers/top-up/affordability integration with no broad reporting/export feature creep.
</success_criteria>

<output>
After completion, create .planning/phases/35-childcare-top-up-planner/35-01-SUMMARY.md
</output>
