# Phase 2.1.3: Power-User Utilities - Plan 01 (Manual Recurrence Trigger)

## Objective
Add a manual way to force recurrence generation from the Expenses UI to bridge the gap for users who want to planning ahead.

## Wave: 1
## Depends on: []

## Tasks
- **Task 1: Add Trigger Button to index.html**
  - Insert `<button id="triggerRecurrenceBtn" class="ghost sm">🔁 Trigger Recurrence</button>` into the Expenses tab toolbar (near `#addExpenseBtn`).
  - [ ] Modified `index.html`.
- **Task 2: Bind Trigger Logic in src/ui/expenses.js**
  - Add event listener to `#triggerRecurrenceBtn`.
  - Implement a wrapper that disables the button, calls `window.templateUI.manualTrigger()`, and then re-enables it.
  - Ensure UI refreshes after completion.
  - [ ] Modified `src/ui/expenses.js`.

## Verification
- Click the "Trigger Recurrence" button in the Expenses tab.
- Verify that the recurrence check modal appears.
- Verify that pending recurring items are generated.
