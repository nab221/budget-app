# Summary: Phase 2.1.3 - Plan 01 (Manual Recurrence Trigger)

## Objective
Add a manual way to force recurrence generation from the Expenses UI to bridge the gap for users who want to planning ahead.

## Completed Tasks
- **Task 1: Add Trigger Button to index.html**
  - Inserted `<button id="triggerRecurrenceBtn" class="ghost sm">🔁 Trigger Recurrence</button>` into the Expenses tab toolbar (near `#addExpenseBtn`).
  - [x] Modified `index.html`.
- **Task 2: Bind Trigger Logic in src/ui/expenses.js**
  - Added event listener to `#triggerRecurrenceBtn`.
  - Implemented a wrapper that disables the button, calls `window.templateUI.manualTrigger()`, and then re-enables it.
  - [x] Modified `src/ui/expenses.js`.

## Verification
- Clicking the "Trigger Recurrence" button in the Expenses tab successfully invokes the recurrence check.
- UI refreshes correctly after the check is complete.
