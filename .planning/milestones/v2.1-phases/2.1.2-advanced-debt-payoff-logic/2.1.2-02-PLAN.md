---
phase: 2.1.2-advanced-debt-payoff-logic
plan: 02
type: execute
wave: 2
depends_on: [2.1.2-01]
files_modified: [src/utils/finance.js, src/ui/debts.js, src/ui/payoff.js]
autonomous: true
requirements: [DEBT-10, DEBT-11, DEBT-12]
must_haves:
  truths:
    - "Loans/Mortgages correctly simulate as Interest-Only when configured"
    - "ERC fees are correctly deducted from principal reduction in simulation"
    - "Users can toggle Interest-Only in the debt management UI"
  artifacts:
    - path: "src/utils/finance.js"
      provides: "Updated simulateLoanPayoff"
      contains: "isInterestOnly"
    - path: "src/ui/debts.js"
      provides: "Updated debt form with Interest-Only toggle"
      contains: "isInterestOnly"
  key_links:
    - from: "src/ui/payoff.js"
      to: "src/utils/finance.js"
      via: "simulateLoanPayoff call"
---

<objective>
Implement interest-only logic and ERC refinement in simulation, and expose these features through the user interface.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/utils/finance.js
@src/ui/debts.js
@src/ui/payoff.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update simulateLoanPayoff logic</name>
  <files>src/utils/finance.js</files>
  <behavior>
    - If `debt.isInterestOnly` is true, the `scheduledPayment` should be equal to the `interestCharged` (unless the loan is nearing £0).
    - If `fee > 0`, ensure the `debt.balance` reflects the added cost: `debt.balance = Math.max(0, debt.balance + interestCharged + fee - totalPaid)`.
    - Principal paid calculation should correctly reflect the balance reduction: `principalPaid = initialBalance - debt.balance + interestCharged + fee`.
  </behavior>
  <action>
    Modify `simulateLoanPayoff` in `src/utils/finance.js` to implement the behavior listed above.
    Ensure all tests from Plan 01 Task 2 now pass.
  </action>
  <verify>
    <automated>npx vitest src/utils/finance.test.js --run</automated>
  </verify>
  <done>Simulation logic handles interest-only and ERC fees correctly.</done>
</task>

<task type="auto">
  <name>Task 2: UI - Add Interest-Only toggle to Debts tab</name>
  <files>src/ui/debts.js</files>
  <action>
    Update `src/ui/debts.js` to:
    - In `renderDebtForm`, add an "Interest-Only" checkbox to the `loanOnlyFields` section.
    - In `handleSaveDebt`, read the value of the "Interest-Only" checkbox and include it in the payload.
    - In `render`, display "Interest-Only" on debt cards if the property is true.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('src/ui/debts.js', 'utf8'); console.log(content.includes('isInterestOnly') ? 'isInterestOnly found' : 'isInterestOnly missing')"</automated>
  </verify>
  <done>Users can manage interest-only loans through the UI.</done>
</task>

<task type="auto">
  <name>Task 3: UI - Update Payoff Planner</name>
  <files>src/ui/payoff.js</files>
  <action>
    Review `src/ui/payoff.js` and ensure the simulation results for interest-only debts are correctly displayed.
    If necessary, update labels in the "Loan & Mortgage Payoff" comparison or schedule table to mention interest-only where appropriate.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('src/ui/payoff.js', 'utf8'); console.log('Checked for UI consistency')"</automated>
  </verify>
  <done>Payoff planner reflects interest-only logic in its projections.</done>
</task>

</tasks>

<verification>
Check that the payoff planner correctly simulates interest-only loans and users can toggle this feature in the debts tab.
</verification>

<success_criteria>
- Tests in `finance.test.js` pass with the new logic.
- UI form in `debts.js` contains the new toggle.
- Payoff planner results reflect the configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1.2-advanced-debt-payoff-logic/2.1.2-02-SUMMARY.md`
</output>
