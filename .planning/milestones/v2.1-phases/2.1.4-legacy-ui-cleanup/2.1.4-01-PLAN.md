---
phase: 2.1.4-legacy-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, src/ui/pdf-import.js, src/app.js]
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "Redundant Import button is removed from Income tab"
    - "PDF Import 'Upload Another' button works correctly"
    - "Legacy initialization code is removed from app.js"
  artifacts:
    - path: "index.html"
      provides: "Updated UI without legacy button"
    - path: "src/ui/pdf-import.js"
      provides: "Corrected element references"
  key_links:
    - from: "src/ui/pdf-import.js"
      to: "stmtPdfFile"
      via: "document.getElementById"
---

<objective>
Cleanup legacy UI elements and initialization logic to ensure codebase parity with v2.0 architecture.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant Income tab import button</name>
  <files>index.html</files>
  <action>
    Remove the button at line 98 in index.html:
    `<button class="ghost sm" onclick="document.getElementById('pdfImportFile').click()">📄 Import Bank Statement</button>`
    
    This button references an ID that no longer exists and has been superseded by the Debts tab import.
  </action>
  <verify>grep "pdfImportFile" index.html should return no matches for the button.</verify>
  <done>Income tab no longer shows the 'Import Bank Statement' button.</done>
</task>

<task type="auto">
  <name>Task 2: Fix orphaned reference in pdf-import.js</name>
  <files>src/ui/pdf-import.js</files>
  <action>
    In `renderImportSummary`, update the 'Upload Another' button (line 483) to reference 'stmtPdfFile' instead of 'pdfImportFile'.
    
    Change:
    `document.getElementById('pdfImportFile').click()`
    To:
    `document.getElementById('stmtPdfFile').click()`
    
    This ensures that the "Upload Another" flow continues to work using the active file input.
  </action>
  <verify>Clicking 'Upload Another' in the PDF import summary modal triggers the file selection dialog.</verify>
  <done>PDF import flow correctly references the active stmtPdfFile input.</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup app.js legacy initialization</name>
  <files>src/app.js</files>
  <action>
    Remove the commented-out legacy initialization lines (approx lines 188-189):
    // await expectedIncomeUI.init(); // REMOVED in v2.0
    // await pdfImportUI.init(); // REMOVED in v2.0
  </action>
  <verify>Confirm lines are removed from src/app.js.</verify>
  <done>app.js is free of orphaned legacy initialization comments.</done>
</task>

</tasks>

<success_criteria>
1. 'Import Bank Statement' button is gone from Income tab.
2. PDF Import success modal successfully triggers a new upload.
3. app.js contains no legacy 'REMOVED in v2.0' comments.
</success_criteria>
