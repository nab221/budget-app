# Summary: Phase 2.1.4 - Plan 01 (Legacy UI Cleanup & Final Parity)

## Objective
Cleanup legacy UI elements and initialization logic to ensure codebase parity with v2.0 architecture.

## Completed Tasks
- **Task 1: Remove redundant Income tab import button**
  - Removed the redundant "Import Bank Statement" button from `index.html`.
  - [x] Modified `index.html`.
- **Task 2: Fix orphaned reference in pdf-import.js**
  - Updated the "Upload Another" button in the PDF import summary modal to reference `stmtPdfFile` instead of the legacy `pdfImportFile`.
  - [x] Modified `src/ui/pdf-import.js`.
- **Task 3: Cleanup app.js legacy initialization**
  - Removed commented-out legacy initialization lines for `expectedIncomeUI` and `pdfImportUI`.
  - [x] Modified `src/app.js`.

## Verification
- `grep` searches confirmed that `pdfImportFile` is no longer present in `index.html` or `src/ui/pdf-import.js`.
- `src/app.js` is free of orphaned legacy initialization comments.
- PDF Import flow correctly references the active `stmtPdfFile` input.
