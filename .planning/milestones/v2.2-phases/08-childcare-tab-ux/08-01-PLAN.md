---
phase: 08-childcare-tab-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js]
autonomous: true
requirements: [CHILD-03]
---

<objective>
Update the database schema and repository logic to support an opening balance for childcare accounts. This ensures the ledger running total starts from a user-defined initial value.

Purpose: Foundation for accurate historical tracking in childcare accounts.
Output: Schema v14 and updated repository logic.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/schema.js
@src/db/repository.js

<interfaces>
From src/db/repository.js:
```typescript
export const childcareRepository = {
  async getAccounts(): Promise<Array<Account>>;
  async saveAccount(account: Account): Promise<number>;
  async _recalculateBalances(accountId: number): Promise<void>;
  async getLedger(accountId: number): Promise<Array<Entry>>;
  async getBalance(accountId: number): Promise<number>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema to v14</name>
  <files>src/db/schema.js</files>
  <action>
    - Increment schema version to 14.
    - Add `openingBalance` field to the `childcareAccounts` store in version 14.
    - Implement an upgrade path for v14 to initialize `openingBalance: 0` for all existing accounts.
    - Use integer pence for the database field.
  </action>
  <verify>
    node -e "import('./src/db/schema.js').then(m => console.log(m.db.versions.length))" (verify version 14 exists)
  </verify>
  <done>Schema version 14 defined with openingBalance field and migration logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Opening Balance Logic in Repository</name>
  <files>src/db/repository.js</files>
  <action>
    - Update `childcareRepository.saveAccount` to handle the `openingBalance` field, ensuring it is converted to pence using `toPence()`.
    - Modify `childcareRepository._recalculateBalances(accountId)` to fetch the account's `openingBalance` and use it as the starting value for the `balance` accumulator.
    - Ensure `_recalculateBalances` is called within `saveAccount` if an update is performed (to ensure running totals update if the opening balance is changed).
  </action>
  <verify>
    Verify that _recalculateBalances starts with account.openingBalance.
  </verify>
  <done>Repository correctly incorporates opening balance into ledger calculations.</done>
</task>

</tasks>

<verification>
- Check schema version in browser dev tools (IndexedDB).
- Manually verify that changing an account's opening balance (via console for now) updates the ledger running balances.
</verification>

<success_criteria>
- Schema is at version 14.
- `childcareAccounts` has `openingBalance` field.
- `_recalculateBalances` uses `openingBalance` as the baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/08-childcare-tab-ux/08-01-SUMMARY.md`
</output>
