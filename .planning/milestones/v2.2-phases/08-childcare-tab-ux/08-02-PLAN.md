---
phase: 08-childcare-tab-ux
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/ui/childcare.js]
autonomous: false
requirements: [CHILD-01, CHILD-02, CHILD-03]
must_haves:
  truths:
    - "Clicking a childcare card opens the ledger view."
    - "Changing the opening balance in the edit modal updates the ledger running total."
    - "Edit button is accessible from both the card and the ledger header."
  artifacts:
    - path: "src/ui/childcare.js"
      provides: "Updated UI rendering and form logic"
  key_links:
    - from: "childcareCard.onclick"
      to: "childcareViewLedger(id)"
      via: "event listener"
---

<objective>
Update the Childcare UI to improve navigation and expose the new opening balance field. This includes making cards clickable, adding a consistent edit action, and updating modals.

Purpose: Improve usability and complete the ledger-focused UX.
Output: Enhanced Childcare tab with clickable cards and edit modals.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-childcare-tab-ux/08-01-SUMMARY.md
@src/ui/childcare.js

<interfaces>
From src/ui/childcare.js:
```javascript
export const childcareUI = {
  _activeAccountId: null,
  async render();
  async _renderAccounts();
  async _renderLedger(accountId);
  _showAddAccountModal();
  async _handleAddAccount();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clickable Cards & Edit Modal</name>
  <files>src/ui/childcare.js</files>
  <action>
    - Update `_renderAccounts()`: 
        - Add `cursor: pointer` to the account cards.
        - Add an `onclick` to the card body that triggers `childcareViewLedger(id)`.
        - Ensure clicking buttons (Delete/View Ledger) doesn't bubble up or conflict.
    - Implement `_showEditAccountModal(accountId)`:
        - Fetch existing account data.
        - Re-use/Adapt the Add modal HTML to pre-fill values.
        - Include "Opening Balance (£)" field.
    - Add "Edit" button to the card header (near the child's name).
  </action>
  <verify>
    Visually check that cards have hover states and clicking them opens the ledger.
  </verify>
  <done>Childcare cards are clickable and have an edit action in the header.</done>
</task>

<task type="auto">
  <name>Task 2: Opening Balance Field & Ledger Actions</name>
  <files>src/ui/childcare.js</files>
  <action>
    - Update `_showAddAccountModal()` to include the "Opening Balance (£)" input.
    - Update `_handleAddAccount()` to read `openingBalance` from the modal and pass it to `saveAccount()`.
    - Rename/Refactor `_handleAddAccount()` to a more generic `_handleSaveAccount(id = null)` to support both Create and Update.
    - Update `_renderLedger(accountId)`:
        - Add an "Edit Account" button to the ledger header area (next to "Back to Accounts").
  </action>
  <verify>
    Verify that saving a new account with an opening balance results in a ledger starting at that value.
  </verify>
  <done>Opening balance is editable and ledger actions are improved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refined Childcare UX with clickable cards, opening balance, and edit functionality.</what-built>
  <how-to-verify>
    1. Open the Childcare tab.
    2. Click an account card anywhere (except the delete button). Ledger should open.
    3. Click "Edit" on a card or within the ledger.
    4. Set an "Opening Balance" of £100.00.
    5. Save and check that the ledger's first running balance reflects the £100.00 (or sums correctly with transactions).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
- Manually test account creation with non-zero opening balance.
- Manually test editing an existing account's opening balance.
- Verify that clicking cards works as expected.
</verification>

<success_criteria>
- Click-to-open ledger works on account cards.
- Edit modal is functional and accessible from both views.
- Opening balance is correctly displayed, saved, and reflected in the ledger.
</success_criteria>

<output>
After completion, create `.planning/phases/08-childcare-tab-ux/08-02-SUMMARY.md`
</output>
