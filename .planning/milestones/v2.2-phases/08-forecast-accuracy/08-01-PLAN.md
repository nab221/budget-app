# Phase 08: Forecast Accuracy — Stabilization Wave 2

## Goal
Inject holiday/weekend awareness into the `getDailyRollingData` aggregator in `src/utils/cashflow.js` and ensure logic consistency with `calculateBalanceChain`.

## Tasks

### 08-01-01: Update `getDailyRollingData` to use `nextWorkingDay` for projected recurrent items
- [ ] ACC-01: Inject holiday/weekend awareness into the `getDailyRollingData` aggregator in `src/utils/cashflow.js`.
- [ ] ACC-01-a: Use `nextWorkingDay(item.nextDate, true)` for projected recurrent items.

### 08-01-02: Ensure consistent balance logic between `calculateBalanceChain` and the Rolling graph
- [ ] ACC-02: Check `calculateBalanceChain` and ensure `getDailyRollingData` follows the same cumulative logic.

## Success Criteria
- [ ] `getDailyRollingData` uses `nextWorkingDay` for forecasting recurrent expenses.
- [ ] Daily rolling graph values match the calculated balance chain for same periods.
- [ ] All tests pass.
