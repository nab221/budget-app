---
phase: 06-debts-panel-ux
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/ui/debts.js]
autonomous: true
requirements: [DEBT-06]
---

<objective>
Implement the inline ledger for debt cards and update statement lifecycle logic to support inline refreshing.

Purpose: Seamless access to statement history without leaving the debts view.
Output: Working inline statement ledger with live updates upon CRUD actions.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-debts-panel-ux/06-01-SUMMARY.md
@src/ui/debts.js

<interfaces>
From src/ui/debts.js:
```javascript
export const debtUI = {
  toggleLedger(debtId),
  renderInlineStatements(debtId),
  handleSaveStatement(),
  deleteStatement(id, debtId),
  renderStatements(debtId) // Legacy global render
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement inline ledger logic</name>
  <files>src/ui/debts.js</files>
  <action>
    Modify `src/ui/debts.js` to:
    - Add `toggleLedger(debtId)` to `debtUI`. This function should toggle the `hidden` class on the `ledger-${debtId}` element and call `renderInlineStatements(debtId)` if showing.
    - Add `renderInlineStatements(debtId)` to `debtUI`. This function should:
      - Fetch statements for the specific debt.
      - Render a compact table or list into the `ledger-${debtId}` container.
      - Use styling that distinguishes the ledger from the card itself (e.g., slightly indented, bg-alt color).
      - Include Edit and Delete buttons for each statement within the list.
    - Ensure global "Manage Statements" section is either removed or updated to not conflict.
  </action>
  <verify>
    Verify that clicking a debt card toggles the inline statement history.
  </verify>
  <done>
    Statement history is accessible inline within each debt card.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update statement lifecycle for inline refresh</name>
  <files>src/ui/debts.js</files>
  <action>
    Modify statement management handlers in `src/ui/debts.js`:
    - Update `handleSaveStatement()` to call `renderInlineStatements(debtId)` after a successful save.
    - Update `window.deleteStatement` to call `renderInlineStatements(debtId)` after deletion.
    - Ensure `toggleStmtForm()` doesn't cause unexpected scrolling away from the inline view.
    - Remove the `document.getElementById('statementSection').classList.add('hidden')` logic if it's no longer used.
  </action>
  <verify>
    Adding or deleting a statement should update the inline view immediately without refreshing the whole page.
  </verify>
  <done>
    Statement operations reflect changes immediately in the inline ledger.
  </done>
</task>

</tasks>

<verification>
Perform full CRUD lifecycle on a statement and confirm the inline ledger updates as expected.
</verification>

<success_criteria>
1. Inline ledger displays correctly when debt card is clicked.
2. Saving a new statement refreshes the specific inline ledger.
3. Deleting a statement refreshes the specific inline ledger.
</success_criteria>

<output>
After completion, create `.planning/phases/06-debts-panel-ux/06-02-SUMMARY.md`
</output>
