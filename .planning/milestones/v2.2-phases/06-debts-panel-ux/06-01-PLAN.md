---
phase: 06-debts-panel-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/debts.js]
autonomous: true
requirements: [DEBT-05, DEBT-07]
---

<objective>
Refactor the Debts Panel structure to group debts by type and redesign the debt cards for improved management efficiency.

Purpose: Clearer visual organization and better placement of management actions.
Output: Grouped debt sections and redesigned cards with collocated actions.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-debts-panel-ux/06-CONTEXT.md
@src/ui/debts.js

<interfaces>
From src/ui/debts.js:
```javascript
export const debtUI = {
  editingId: null,
  editingStmtId: null,
  async init(),
  setupEventListeners(),
  toggleDebtForm(show = true),
  renderDebtForm(),
  handleSaveDebt(),
  editDebt(id),
  render(),
  renderStatements(debtId)
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement debt grouping by type</name>
  <files>src/ui/debts.js</files>
  <action>
    Modify `render()` in `src/ui/debts.js` to:
    - Group debts retrieved from `debtRepository.getAll()` by `debtType`.
    - Handle three groups: "Credit Cards" (`credit-card`), "Loans & Mortgages" (`loan` and `mortgage`), and any others.
    - Render distinct section headers for each group within the `debtList` container.
    - Ensure each section has its own grid/container for the cards.
    - Maintain "No debts tracked yet" hint if the entire list is empty.
  </action>
  <verify>
    npm test (if applicable) or manual check: Debt cards should be visually separated by headers.
  </verify>
  <done>
    Debts are grouped with headers for "Credit Cards" and "Loans & Mortgages".
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign debt card template</name>
  <files>src/ui/debts.js</files>
  <action>
    Update the card template within `render()`:
    - Collocate Edit (✏️) and Delete (🗑) buttons in the top-right of the card (within the header/actions row).
    - Use icons or compact labels for these buttons.
    - Change the card's `onclick` handler to call `debtUI.toggleLedger(debt.id)` instead of `editDebt`.
    - Wrap the "body" of the card in a div that handles the `onclick` but excludes the action buttons (use `event.stopPropagation()` on buttons).
    - Add a placeholder div with id `ledger-${debt.id}` within or immediately following the card for the inline ledger.
  </action>
  <verify>
    Verify Edit/Delete buttons are together in the header and card click calls toggleLedger (which may be undefined until next plan).
  </verify>
  <done>
    Card header contains collocated actions; card body click target is updated.
  </done>
</task>

</tasks>

<verification>
Check visual grouping of debts and ensure card buttons are correctly positioned.
</verification>

<success_criteria>
1. Debts are grouped by type with visible headers.
2. Edit and Delete buttons are sitting next to each other in the card header.
3. Card click logic is shifted from "Edit" to "Toggle Ledger".
</success_criteria>

<output>
After completion, create `.planning/phases/06-debts-panel-ux/06-01-SUMMARY.md`
</output>
