---
phase: 02-dashboard-chart-merge-and-simplification
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, src/db/repository.js, src/utils/finance.js]
autonomous: true
requirements: [DASH-01, DASH-04, DASH-05, DASH-06, DASH-07]
must_haves:
  truths:
    - "Redundant dashboard sections (daily cards, budget progress, net worth history) are removed from index.html"
    - "New unified chart container is present in index.html"
    - "getRollingFinancialData provides a 12-month window of income/expense data"
  artifacts:
    - path: "index.html"
      provides: "Updated UI structure"
    - path: "src/db/repository.js"
      provides: "getRollingFinancialData function"
    - path: "src/utils/finance.js"
      exports: ["recurrentFallsInMonth"]
---

<objective>
Prepare the data layer and update the dashboard HTML structure for the new unified "Rolling Financial Overview" chart.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
@src/db/repository.js
@src/utils/finance.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.html structure</name>
  <files>index.html</files>
  <action>
    - Remove `#trendsChartContainer`.
    - Remove `#cashflowForecastSection`.
    - Remove `#cashflowChartContainer`.
    - Remove the `grid2` section containing `#dashboardProgress` and `#dashboardSnapshots` (at the bottom of the Dashboard card).
    - Add a new chart container after the header in the Dashboard section:
      ```html
      <div class="chart-container" id="rollingOverviewChartContainer">
        <h3 class="chart-title">Rolling Financial Overview</h3>
        <canvas id="rollingOverviewChart"></canvas>
      </div>
      ```
  </action>
  <verify>
    - Open index.html and verify the old containers are gone.
    - Verify `#rollingOverviewChart` is present.
  </verify>
  <done>index.html reflects the simplified dashboard layout.</done>
</task>

<task type="auto">
  <name>Task 2: Export recurrentFallsInMonth utility</name>
  <files>src/utils/finance.js</files>
  <action>
    Export the `recurrentFallsInMonth` function so it can be used by the repository for forecasting.
  </action>
  <verify>
    - Check src/utils/finance.js for `export function recurrentFallsInMonth`.
  </verify>
  <done>recurrentFallsInMonth is exported.</done>
</task>

<task type="auto">
  <name>Task 3: Implement getRollingFinancialData in repository.js</name>
  <files>src/db/repository.js</files>
  <action>
    Implement `getRollingFinancialData(targetMonth)`:
    - Calculate a 12-month window: 9 months before `targetMonth`, `targetMonth` itself, and 2 months after.
    - For each month:
      - If month < current month (today's month):
        - Income: Sum actual `db.income` for that month.
        - Expenses: Sum `db.recurrentExpenses` (using `recurrentFallsInMonth`) + `db.oneOffExpenses` (starting with that month).
      - If month >= current month:
        - Income: Sum `db.expectedIncome` for that month.
        - Expenses: Sum `db.recurrentExpenses` (using `recurrentFallsInMonth`). (Optionally include one-off expenses if they exist for current/future).
      - Set `isForecast: month >= currentMonth`.
    - Return an array of `{ month, income, expenses, isForecast }`.
    
    Ensure `recurrentFallsInMonth` is imported from `../utils/finance.js`.
  </action>
  <verify>
    - Verify the function logic correctly distinguishes between history and forecast.
  </verify>
  <done>getRollingFinancialData is implemented and exported.</done>
</task>

</tasks>

<success_criteria>
- Dashboard HTML is simplified and contains the new chart canvas.
- Repository provides the necessary data for a rolling 12-month overview.
</success_criteria>
