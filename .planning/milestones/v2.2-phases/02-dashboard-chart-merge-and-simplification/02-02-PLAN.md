---
phase: 02-dashboard-chart-merge-and-simplification
plan: 02-02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/ui/charts.js, src/ui/dashboard.js]
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]
must_haves:
  truths:
    - "Chart displays history with solid lines and forecast with dashed lines"
    - "Chart uses Green for Income and Red/Orange for Expenses"
    - "Dashboard integration calls the new repository function and renders the new chart"
    - "Unused legacy dashboard functions are removed"
  artifacts:
    - path: "src/ui/charts.js"
      provides: "renderRollingOverviewChart function"
    - path: "src/ui/dashboard.js"
      provides: "Updated renderDashboard and cleanup"
---

<objective>
Implement the unified "Rolling Financial Overview" chart rendering and integrate it into the dashboard.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/charts.js
@src/ui/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement renderRollingOverviewChart in charts.js</name>
  <files>src/ui/charts.js</files>
  <action>
    Implement `renderRollingOverviewChart(canvasId, data)`:
    - Define Okabe-Ito colors: Green for Income (`#009E73`) and Orange/Red for Expenses (`#D55E00`).
    - Create two datasets: "Income" and "Expenses".
    - Use `segment` in Chart.js to make the line dashed for the forecast portion:
      ```javascript
      segment: {
        borderDash: ctx => data[ctx.p1DataIndex].isForecast ? [6, 4] : undefined
      }
      ```
    - Use `fill: false` (or a light fill) to avoid clutter.
    - Format tooltips using `formatPence`.
  </action>
  <verify>
    - Verify the chart uses dashed lines for forecast points.
    - Verify colors are correct.
  </verify>
  <done>renderRollingOverviewChart is implemented and exported.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate new chart into dashboard.js</name>
  <files>src/ui/dashboard.js</files>
  <action>
    - Update `renderDashboard`:
      - Remove the call to `getSpendingTrends` and `renderTrendsChart`.
      - Remove the call to `renderProgressBars`, `renderSnapshots`, and `renderCashFlowForecast`.
      - Fetch data using `getRollingFinancialData(targetMonth)`.
      - Call `renderRollingOverviewChart('rollingOverviewChart', rollingData)`.
    - Cleanup:
      - Remove unused exports/functions: `renderCashFlowForecast`, `renderProgressBars`, `renderSnapshots`.
      - Remove unused imports if applicable.
  </action>
  <verify>
    - Verify the dashboard only renders the new chart and the summary cards.
    - No console errors on dashboard load.
  </verify>
  <done>renderDashboard is updated and unused code is removed.</done>
</task>

</tasks>

<success_criteria>
- The dashboard displays a single, unified chart for trends and forecasts.
- The chart correctly distinguishes between past and future data using solid and dashed lines.
- Redundant visualizations are removed, simplifying the interface.
</success_criteria>
