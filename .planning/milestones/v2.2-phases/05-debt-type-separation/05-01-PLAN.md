---
phase: 05-debt-type-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js, src/utils/finance.js]
autonomous: true
requirements: [DEBT-01, DEBT-02]
must_haves:
  truths:
    - "Database schema version is 13"
    - "Existing 'credit_card' debts are renamed to 'credit-card' in 'debtType' field"
    - "New loan/mortgage fields are initialized on existing records"
    - "recurrentExpenses table includes debtType and linkedDebtId fields"
  artifacts:
    - path: "src/db/schema.js"
      provides: "Schema v13 with migration logic"
    - path: "src/db/repository.js"
      provides: "Updated debtRepository with auto-generation logic"
  key_links:
    - from: "debtRepository.add"
      to: "recurrentExpenseRepository.bulkAdd"
      via: "Automatic series generation for loans/mortgages"
---

<objective>
Upgrade the database schema to v13 and implement the core logic for debt type separation and automatic installment generation for loans and mortgages.

Purpose: Formalize debt types and ensure installment-based debts (loans/mortgages) are automatically tracked in the budget without manual entry.
Output: Updated schema, migration logic, and enhanced repository functions.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-debt-type-separation/05-RESEARCH.md
@src/db/schema.js
@src/db/repository.js
@src/utils/finance.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema to v13 and Implement Migration</name>
  <files>src/db/schema.js</files>
  <action>
    1. Increment Dexie schema version to 13.
    2. Add new fields to 'debts' store: `debtType`, `originalPrincipal`, `termMonths`, `fixedMonthlyPayment`, `interestRate`, `earlyRepaymentFee`, `earlyRepaymentFeeIsPercent`, `earlyRepaymentAllowed`.
    3. Add new fields to 'recurrentExpenses' store: `debtType`, `linkedDebtId`.
    4. Implement `.upgrade(tx => { ... })` for v13:
       - Map `type` to `debtType`.
       - Rename `credit_card` to `credit-card`.
       - Map `overdraft` to `credit-card` (per research recommendation).
       - Default any missing types to `credit-card`.
       - Initialize new debt fields with zero/null/true defaults.
       - Delete the old `type` field from `debts` records.
       - Initialize `debtType: null` and `linkedDebtId: null` for existing `recurrentExpenses`.
  </action>
  <verify>Run `npm test` or check Dexie version in browser console after reload. Verify data migration with a sample record.</verify>
  <done>Schema v13 is active and data is correctly migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance finance utils with loan calculation</name>
  <files>src/utils/finance.js</files>
  <action>
    Add `calcLoanPayment(principal, annualRate, termMonths)` utility function using the standard amortization formula.
    Formula: P * r * (1 + r)^n / ((1 + r)^n - 1)
    Ensure it handles 0% interest cases (principal / term).
  </action>
  <verify>Unit test or console log check: £10,000 at 5% for 60 months should be approx £188.71.</verify>
  <done>Calculation utility is available for suggesting payments in the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Update debtRepository with Auto-Generation Logic</name>
  <files>src/db/repository.js</files>
  <action>
    1. Update `debtRepository` to handle monetary fields: `originalPrincipal`, `fixedMonthlyPayment`, `earlyRepaymentFee`.
    2. Override `debtRepository.add`:
       - After adding the debt record, if `debtType` is 'loan' or 'mortgage' and `fixedMonthlyPayment` > 0:
         - Generate a series of `recurrentExpenses` for `termMonths`.
         - Use `linkedDebtId: debtId`, `debtType: debtType`, `isDebtPayment: true`, `status: 'pending'`.
         - Use `label: "Payment: [Debt Name]"`.
    3. Override `debtRepository.update`:
       - If `fixedMonthlyPayment` or `termMonths` changed for a loan/mortgage:
         - Delete future instances of the existing series using `recurrentExpenseRepository.deleteSeries`.
         - Re-generate remaining instances from current month onwards.
    4. Override `debtRepository.delete`:
       - Ensure linked `recurrentExpenses` are also deleted.
  </action>
  <verify>Programmatically add a loan debt via console and verify `recurrentExpenses` table is populated with term instances.</verify>
  <done>Debts of type loan/mortgage automatically manage their payment schedules.</done>
</task>

</tasks>

<verification>
Check browser IndexedDB for debts table structure.
Check recurrentExpenses for new installments after adding a loan.
</verification>

<success_criteria>
- Dexie schema version 13 is running.
- Existing debts have 'debtType' field.
- Loans/Mortgages generate recurring expenses automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/05-debt-type-separation/05-01-SUMMARY.md`
</output>
