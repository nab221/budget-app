---
phase: 05-debt-type-separation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/ui/debts.js, src/ui/expenses.js]
autonomous: false
requirements: [DEBT-03, DEBT-04]
must_haves:
  truths:
    - "Debt form shows loan/mortgage fields only when relevant type is selected"
    - "Loan and mortgage cards display distinct icons (💰, 🏠)"
    - "Expenses list displays distinct icons for loan/mortgage payments"
  artifacts:
    - path: "src/ui/debts.js"
      provides: "Updated debt form and card rendering"
    - path: "src/ui/expenses.js"
      provides: "Updated icon logic for debt payments"
  key_links:
    - from: "expensesUI.render"
      to: "item.debtType"
      via: "Icon selection switch statement"
---

<objective>
Update the user interface to support the new debt types and fields, and ensure the expenses list correctly reflects the different debt categories.

Purpose: Provide a tailored management experience for different debt types and visual clarity in the budget.
Output: Updated UI modules for Debts and Expenses.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-debt-type-separation/05-RESEARCH.md
@.planning/phases/05-debt-type-separation/05-01-SUMMARY.md
@src/ui/debts.js
@src/ui/expenses.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Debt Form in UI</name>
  <files>src/ui/debts.js</files>
  <action>
    1. Update `renderDebtForm()`:
       - Update the `select` for Type to use hyphenated values: `credit-card`, `loan`, `mortgage`.
       - Add a container for 'Loan/Mortgage Specific Fields'.
       - Add fields: Original Principal (£), Term (Months), Interest Rate (%), Monthly Payment (£).
       - Add Early Repayment section: Fee (£ or %), Allowed (Checkbox).
       - Ensure `debtTypeInput` change listener toggles the visibility of these fields (only show for loan/mortgage).
    2. Update `handleSaveDebt()`:
       - Extract the new fields and pass them to the repository call.
       - Ensure correct formatting (e.g. `toPence` for monetary fields).
  </action>
  <verify>Manually open 'Add Debt' form, switch to 'Loan' and verify fields appear. Save a loan and check the 'debts' table in DevTools.</verify>
  <done>Debt form supports all new types and fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update Debt Card and Expenses Rendering</name>
  <files>src/ui/debts.js, src/ui/expenses.js</files>
  <action>
    1. In `src/ui/debts.js`:
       - Update the card rendering logic in `render()`.
       - Select icons based on `debtType`: `credit-card` -> 💳, `loan` -> 💰, `mortgage` -> 🏠.
       - Display term and principal information if it's a loan/mortgage.
    2. In `src/ui/expenses.js`:
       - Update the `render()` loop to pick the correct icon for debt payments based on `item.debtType`.
       - Logic: switch/case on `item.debtType` returning the appropriate emoji.
  </action>
  <verify>Check both 'Debts' and 'Expenses' tabs to verify correct icons are displayed for different debt types.</verify>
  <done>User interface correctly visualizes the different debt types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full debt workflow</name>
  <what-built>Full debt type separation workflow.</what-built>
  <how-to-verify>
    1. Visit the Debts tab.
    2. Add a new Loan: Name="Test Loan", Type="Loan", Principal=12000, Term=60, Payment=200.
    3. Verify the loan card shows a 💰 icon and term info.
    4. Switch to Expenses tab.
    5. Verify a recurring expense named "Payment: Test Loan" exists with a 💰 icon.
    6. Edit the loan in Debts tab, change name to "Updated Loan".
    7. Verify the expenses update their labels.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual visual check of the icons and form behaviors.
</verification>

<success_criteria>
- Loan/Mortgage specific fields are visible when selected.
- Correct icons (💳, 💰, 🏠) are used consistently.
- Loan creation auto-generates expense series.
</success_criteria>

<output>
After completion, create `.planning/phases/05-debt-type-separation/05-02-SUMMARY.md`
</output>
