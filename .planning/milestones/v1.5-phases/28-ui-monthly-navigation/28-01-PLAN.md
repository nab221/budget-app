---
phase: 28-ui-monthly-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [css/main.css, src/ui/expenses.js, src/app.js]
autonomous: true
requirements: [UI-MONTH-01, UI-MONTH-02, UI-MONTH-03]
must_haves:
  truths:
    - "Month picker format matches [◄ Prev] [Month Year Dropdown] [Next ►]."
    - "Selected month is persisted per sub-tab in localStorage."
    - "Expenses tab no longer follows the global dashboard picker."
  artifacts:
    - path: "src/ui/expenses.js"
      provides: "Localized month state and picker component"
      contains: "recurrentMonth, oneOffMonth, renderMonthPicker"
---

<objective>
Refactor the Expenses UI to support independent, persistent, and formatted month-based navigation.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/milestones/v1.5-REQUIREMENTS.md
@src/ui/expenses.js
@src/app.js
@css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Styling for Month Picker</name>
  <files>css/main.css</files>
  <action>
    Add the following styles to `css/main.css`:
    ```css
    .month-nav {
      display: flex;
      align-items: center;
      gap: 12px;
      justify-content: center;
      margin-bottom: 20px;
      padding: 10px;
      background: var(--bg-alt);
      border-radius: 8px;
    }
    .month-nav button {
      font-size: 1.1rem;
      padding: 4px 12px;
    }
    .month-nav select {
      font-size: 0.9rem;
      padding: 6px 12px;
      font-weight: 600;
      color: var(--accent);
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 6px;
      min-width: 140px;
      text-align: center;
    }
    ```
  </action>
  <verify>
    Visually check in browser or ensure CSS is added.
  </verify>
  <done>
    Styles for `.month-nav` and its children are present in `css/main.css`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor expensesUI for Localized Months</name>
  <files>src/ui/expenses.js</files>
  <action>
    1. Update `expensesUI` properties:
       - Replace `currentMonth` with `recurrentMonth` and `oneOffMonth`.
       - Initialize them from `localStorage` (`expenses_recurrent_month`, `expenses_oneoff_month`) or default to current YYYY-MM.
    2. Add `initMonths()` helper to load state.
    3. Implement `renderMonthPicker()`:
       - Generates HTML with `◄ Prev`, a `<select>` for Month/Year, and `Next ►`.
       - Dropdown should cover a reasonable range (e.g., current +/- 12 months).
       - Buttons increment/decrement the selected month using `date-fns` logic (or simple string math).
    4. Update `render()` to call `renderMonthPicker()` and inject it above the filters.
    5. Update `renderRecurrent()` and `renderOneOff()` to use the correct month property.
    6. Update `setupEventListeners()`:
       - Bind picker buttons and dropdown change events.
       - Ensure sub-tab switching triggers picker re-render.
  </action>
  <verify>
    `grep` for `recurrentMonth` and `oneOffMonth` in `src/ui/expenses.js`.
    Check that `renderMonthPicker` is defined.
  </verify>
  <done>
    `expensesUI` is independent and uses its own per-sub-tab month state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Decouple from Global Picker</name>
  <files>src/app.js</files>
  <action>
    1. In `src/app.js`, update the global `monthPicker` listener:
       - Remove `expensesUI.render(monthPicker.value)` from the listener. (Expenses will manage their own state).
    2. In the tab-switching logic:
       - When switching to 'expenses', call `expensesUI.render()` without arguments.
  </action>
  <verify>
    Ensure `expensesUI.render` is called correctly in `app.js`.
  </verify>
  <done>
    Expenses tab no longer changes when the global Dashboard picker is used.
  </done>
</task>

</tasks>

<verification>
- Navigate to Expenses. Use the new picker to change months.
- Switch between Recurrent and One-off sub-tabs; verify they remember their respective months.
- Refresh the page; verify months are persisted in `localStorage`.
- Change the global dashboard picker; verify Expenses tab month remains unchanged.
</verification>

<success_criteria>
- Independent month selection for Recurrent vs One-off expenses.
- Picker UI matches requirements exactly.
- Per-tab persistence in `localStorage`.
</success_criteria>

<output>
After completion, create `.planning/phases/28-ui-monthly-navigation/28-01-SUMMARY.md`
</output>
