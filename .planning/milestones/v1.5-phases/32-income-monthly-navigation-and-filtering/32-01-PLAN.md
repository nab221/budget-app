---
phase: 32-income-monthly-navigation-and-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, src/ui/transactions.js]
autonomous: true
requirements: [INC-01, INC-02, INC-03]

must_haves:
  truths:
    - "Income tab has its own month navigation [◄ Prev] [Month Year Dropdown] [Next ►]"
    - "Income tab has a category filter dropdown"
    - "Selected income month persists across page refreshes"
    - "Filtering by category and search works together"
  artifacts:
    - path: "src/ui/transactions.js"
      provides: "Month navigation and category filtering logic for Income"
    - path: "index.html"
      provides: "Containers for Income month picker and category filter"
  key_links:
    - from: "src/ui/transactions.js"
      to: "incomeRepository.getByMonth"
      via: "data fetching"
    - from: "src/ui/transactions.js"
      to: "filterTransactions"
      via: "list filtering"
---

<objective>
Implement independent month navigation and category filtering for the Income tab, mirroring the UX of the Expenses tab.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/transactions.js
@src/ui/expenses.js
@index.html
@src/db/repository.js
@src/utils/filtering.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.html structure for Income tab</name>
  <files>index.html</files>
  <action>
    Modify the Income tab panel (`data-panel="income"`) in `index.html`:
    - Add a container for the month picker (`incMonthPicker`) above the action buttons or after them.
    - Wrap the search input in a container that also includes a placeholder for the category filter (`incCategoryFilterContainer`), similar to how it's done in the Expenses tab.
  </action>
  <verify>
    <automated>grep -q "incMonthPicker" index.html && grep -q "incCategoryFilterContainer" index.html</automated>
  </verify>
  <done>index.html has the necessary containers for the new UI components in the Income tab.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor transactionUI state and event handling</name>
  <files>src/ui/transactions.js</files>
  <action>
    Update `transactionUI` in `src/ui/transactions.js`:
    - Add `selectedCategories: []` to the state.
    - Implement `initMonths()`: load `transaction_month` from `localStorage` or default to the current month (YYYY-MM). Call this in `init()`.
    - Update `setupEventListeners()`:
      - Remove the listener for the global `#monthPicker` (or ensure it doesn't affect `transactionUI.currentMonth` anymore).
      - Add global window functions: `incPrevMonth()`, `incNextMonth()`, and `handleIncMonthChange(e)` to handle navigation, following the `expensesUI` pattern.
    - Implement `toggleCategoryDropdown(show)`, `handleCategoryChange(checkbox)`, and `clearCategoryFilter()` for the category filter.
  </action>
  <verify>
    <automated>grep -q "selectedCategories" src/ui/transactions.js && grep -q "initMonths" src/ui/transactions.js</automated>
  </verify>
  <done>transactionUI has the necessary state and event handlers for independent month navigation and category filtering.</done>
</task>

<task type="auto">
  <name>Task 3: Implement renderMonthPicker and renderCategoryFilter</name>
  <files>src/ui/transactions.js</files>
  <action>
    Implement the following methods in `transactionUI`:
    - `renderMonthPicker()`: Renders the [◄ Prev] [Dropdown] [Next ►] UI into `#incMonthPicker`.
    - `renderCategoryFilter()`: Renders the category filter dropdown into `#incCategoryFilterContainer`.
    - Ensure these are called within the `render()` method.
  </action>
  <verify>
    <automated>grep -q "renderMonthPicker" src/ui/transactions.js && grep -q "renderCategoryFilter" src/ui/transactions.js</automated>
  </verify>
  <done>The Income tab now displays the month picker and category filter.</done>
</task>

<task type="auto">
  <name>Task 4: Refactor renderIncome to use strict month and category filtering</name>
  <files>src/ui/transactions.js</files>
  <action>
    Update `renderIncome(month)` in `src/ui/transactions.js`:
    - Fetch data using `incomeRepository.getByMonth(this.currentMonth)` instead of `getThreeMonthHistory`.
    - Use the `filterTransactions` utility from `../utils/filtering.js` to filter the items based on `this.searchQuery` and `this.selectedCategories`.
    - Update the search input handler in `setupEventListeners` to call `this.render()` instead of `this.renderIncome(this.currentMonth)` (to ensure filters are recalculated correctly if needed, though `renderIncome` handles it).
    - Ensure the total displayed at the bottom reflects the filtered items.
  </action>
  <verify>
    <automated>grep -q "incomeRepository.getByMonth" src/ui/transactions.js && grep -q "filterTransactions" src/ui/transactions.js</automated>
  </verify>
  <done>The Income list is strictly filtered by the selected month and correctly respects both search and category filters.</done>
</task>

</tasks>

<verification>
Check for correct month persistence in localStorage.
Verify that changing the dashboard month picker does NOT affect the Income tab's selected month.
Verify that clicking the Prev/Next buttons in the Income tab updates the list correctly.
Verify that the category filter correctly filters the income entries.
</verification>

<success_criteria>
1. Income tab has independent month picking persisted in localStorage.
2. Income tab has category filtering.
3. Income tab uses strict monthly filtering.
4. Search and category filters work together.
</success_criteria>

<output>
After completion, create `.planning/phases/32-income-monthly-navigation-and-filtering/32-01-SUMMARY.md`
</output>
