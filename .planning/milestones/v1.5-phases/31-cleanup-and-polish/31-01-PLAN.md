# Phase 31-01 PLAN: Cleanup & Polish

## Goal
Remove legacy code and perform final integration polish for the Recurrence system.

## Requirements Traceability
- [x] **CLEANUP-01**: Remove legacy templates from UI.
- [x] **CLEANUP-02**: Standardize `templates.js` into a minimal recurrence helper.
- [x] **POLISH-01**: Ensure app boot triggers recurrence check.

## Implementation Steps

### 1. Refactor `src/ui/templates.js`
- Remove `renderTemplates`, `handleSaveTemplate`, `deleteTemplate`, `editTemplate`.
- Keep `showModal` and `closeModal` (used by `expensesUI`).
- Refactor `manualTrigger` to call `RecurrenceManager.checkAndGenerate()`.

### 2. Update `src/app.js`
- Import `RecurrenceManager` from `../utils/recurrence.js`.
- Add `await RecurrenceManager.checkAndGenerate()` to `initApp`.

### 3. Update `src/ui/expenses.js`
- Remove legacy template imports if any.
- Verify "Check for new instances" button still works via the refactored `templateUI.manualTrigger`.

### 4. Final Verification
- Restart app -> check console logs for recurrence generation.
- Navigate to next month -> verify recurring items appeared.
- Check "Settings" tab -> verify no "Templates" section.

## Review Notes
- I'll check if `Settings` UI needs specific removal logic.
