---
phase: 25-forecast-integration-and-polish
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified: [src/ui/dashboard.js]
autonomous: false
requirements: [DEBT-04.2]
must_haves:
  truths:
    - "Dashboard timeline shows 💳 for days containing a forecasted debt payment"
    - "Marking a debt payment as paid updates the forecast timeline"
  artifacts:
    - path: "src/ui/dashboard.js"
      provides: "Visual indicator for debt payments in forecast"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "dailyBalanceSnapshots"
      via: "day.hasDebtPayment check"
---

<objective>
Update the dashboard's forecast timeline to visually highlight days containing debt payments.

Purpose: Provide the user with a quick visual cue for upcoming debt-related cash flow impacts.
Output: Updated `renderCashFlowForecast` in `src/ui/dashboard.js`.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/dashboard.js
@src/db/repository.js
@src/app.js
@.planning/phases/25-forecast-integration-and-polish/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debt payment indicator to forecast timeline</name>
  <files>src/ui/dashboard.js</files>
  <action>
    Update `renderCashFlowForecast` in `src/ui/dashboard.js`:
    - Within the `timelineDays.map` function (around line 170), identify days with `day.hasDebtPayment === true`.
    - If `hasDebtPayment` is true, add a `💳` icon (emoji) to the day card.
    - Suggested placement: Top right corner of the day card or next to the date label. Use a small absolute-positioned container or simple span.
    
    Example addition to the card template:
    `${day.hasDebtPayment ? '<span style="position:absolute;top:4px;right:4px;font-size:.8rem" title="Debt payment">💳</span>' : ''}`
    (Note: ensure the card container has `position:relative` if using absolute positioning).
  </action>
  <verify>Open dashboard and check for 💳 icon on forecast days with debt payments.</verify>
  <done>Timeline displays the 💳 icon for debt payment days.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Forecast integration for debt payments</what-built>
  <how-to-verify>
    1. Go to the "Debts" tab and ensure you have a debt with a statement and a pending "Min Payment" expense.
    2. Check the "Dashboard" and verify the 💳 icon appears on the day of the min payment in the "Cash Flow Forecast" timeline.
    3. Go back to "Debts", record a payment for that statement.
    4. Verify the dashboard refreshes automatically and the 💳 icon is GONE (because the expense is now `paid` and excluded from the forecast).
    5. Verify the 90-day forecast chart also reflects the change.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual visual check and end-to-end flow verification.
</verification>

<success_criteria>
- Dashboard timeline shows 💳 for debt payments.
- Real-time refresh works when marking as paid.
- Visual polish is consistent with the rest of the dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/25-forecast-integration-and-polish/25-02-SUMMARY.md`
</output>
