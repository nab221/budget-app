---
phase: 21-data-layer-and-repository
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js]
autonomous: true
requirements: [DEBT-01.1, DEBT-01.2, DEBT-01.3]
must_haves:
  truths:
    - "Database version is 11"
    - "statements table contains v11 fields: openingBalance, minimumPayment, paymentDueDate, actualPaymentAmount, actualPaymentDate, linkedExpenseId"
    - "recurrentExpenses table contains v11 fields: isDebtPayment, linkedStatementId"
  artifacts:
    - path: "src/db/schema.js"
      provides: "Database schema version 11"
      contains: "db.version(11)"
  key_links: []
---

<objective>
Update the database schema to version 11, expanding the `statements` and `recurrentExpenses` tables with fields required for the enhanced debt management system.

Purpose: Provide the data structure for linking debt statements to payment expenses.
Output: Updated `src/db/schema.js` with v11 and migration logic.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-data-layer-and-repository/21-RESEARCH.md
@src/db/schema.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Schema Version 11</name>
  <files>src/db/schema.js</files>
  <action>
    Add `db.version(11).stores({...})` to `src/db/schema.js`.
    
    Updated tables:
    - `statements`: Add `openingBalance`, `minimumPayment`, `paymentDueDate`, `actualPaymentAmount`, `actualPaymentDate`, `linkedExpenseId` to the field list. (Note: These are mostly for documentation in the store string, but Dexie uses them for indexing if specified. Use '++id, debtId, date, amount, actualPaymentDate, linkedExpenseId' for indexing).
    - `recurrentExpenses`: Add `isDebtPayment` and `linkedStatementId` to the store string (e.g., '++id, date, categoryId, status, nextDate, predictedPaymentDate, isDebtPayment, linkedStatementId').
    
    Ensure existing stores for other tables are maintained correctly.
  </action>
  <verify>grep "db.version(11)" src/db/schema.js</verify>
  <done>Schema version 11 is defined with the required fields for debt management.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Migration Logic for v11</name>
  <files>src/db/schema.js</files>
  <action>
    Implement `.upgrade(async tx => { ... })` for version 11 in `src/db/schema.js`.
    
    Logic:
    - For `statements`: Initialize `openingBalance` (0), `minimumPayment` (0), `paymentDueDate` (null), `actualPaymentAmount` (null), `actualPaymentDate` (null), and `linkedExpenseId` (null) for existing records.
    - For `recurrentExpenses`: Initialize `isDebtPayment` (false) and `linkedStatementId` (null) for existing records.
    
    Use `tx.table('statements').toCollection().modify(...)` and `tx.table('recurrentExpenses').toCollection().modify(...)`.
  </action>
  <verify>Schema file contains upgrade logic for v11 initializing the new fields.</verify>
  <done>Existing data is safely migrated to v11 with default values for new fields.</done>
</task>

</tasks>

<verification>
Check schema version 11 definition and upgrade logic.
</verification>

<success_criteria>
1. `src/db/schema.js` defines version 11.
2. `statements` store string includes `actualPaymentDate` and `linkedExpenseId` for indexing.
3. `recurrentExpenses` store string includes `isDebtPayment` and `linkedStatementId` for indexing.
4. Upgrade logic initializes all new fields to appropriate defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/21-data-layer-and-repository/21-01-SUMMARY.md`
</output>
