---
phase: 21-data-layer-and-repository
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified: [src/db/repository.js]
autonomous: true
requirements: [DEBT-01.4, DEBT-01.5]
must_haves:
  truths:
    - "addWithExpense() correctly creates a statement and a linked expense within a single transaction"
    - "recordPayment() correctly updates actual payment fields in both statement and linked expense"
    - "Both methods trigger balance and forecast recalculations"
  artifacts:
    - path: "src/db/repository.js"
      provides: "Expanded statementRepository with linked expense support"
      contains: "addWithExpense"
  key_links:
    - from: "statementRepository.addWithExpense"
      to: "db.transaction"
      via: "Dexie transaction"
    - from: "statementRepository.addWithExpense"
      to: "triggerBalanceRecalc"
      via: "Function call"
---

<objective>
Expand `statementRepository` with support for atomic statement/expense creation and payment recording.

Purpose: Implement the business logic for managing debt statement lifecycles and their financial impact.
Output: Enhanced `statementRepository` in `src/db/repository.js`.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-data-layer-and-repository/21-RESEARCH.md
@src/db/repository.js
@src/utils/currency.js
</context>

<interfaces>
From src/db/repository.js:
```javascript
export const statementRepository = createBaseRepository(db.statements, ['amount', 'interest', 'fees']);

export async function triggerBalanceRecalc(date) { ... }
export async function triggerDailyForecastRecalc(date) { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement addWithExpense()</name>
  <files>src/db/repository.js</files>
  <action>
    Convert `statementRepository` into a full object (like `incomeRepository`) and implement `addWithExpense(statementData, debtName)`.
    
    Implementation details:
    1.  Ensure all monetary fields (`amount`, `interest`, `fees`, `openingBalance`, `minimumPayment`) are converted to pence using `toPence`.
    2.  Find the category ID for 'Credit Cards & Loans' using `db.categories`.
    3.  Execute a Dexie transaction ('rw') covering `db.statements` and `db.recurrentExpenses`.
    4.  Add statement: Create record with `statementData`, but set `linkedExpenseId: null`.
    5.  Add expense: Create `recurrentExpenses` record with:
        - `label`: `Min Payment: ${debtName}`
        - `amount`: (minimum payment from statement)
        - `nextDate`: (due date from statement)
        - `categoryId`: (ID from step 2)
        - `status`: 'pending'
        - `isDebtPayment`: true
        - `linkedStatementId`: (ID from step 4)
        - `cycleTotal`: 1
        - `cycleCurrent`: 0
    6.  Update statement: Set `linkedExpenseId` to the expense ID from step 5.
    7.  After transaction: Call `triggerBalanceRecalc(statementData.date)` and `triggerDailyForecastRecalc(statementData.date)`.
  </action>
  <verify>grep "addWithExpense" src/db/repository.js</verify>
  <done>statementRepository can atomically create linked statements and expenses.</done>
</task>

<task type="auto">
  <name>Task 2: Implement recordPayment()</name>
  <files>src/db/repository.js</files>
  <action>
    Implement `recordPayment(statementId, actualAmount, paymentDate)` in `statementRepository`.
    
    Implementation details:
    1.  Convert `actualAmount` to pence.
    2.  Execute a Dexie transaction ('rw') covering `db.statements` and `db.recurrentExpenses`.
    3.  Get statement by `statementId`.
    4.  Update statement: Set `actualPaymentAmount` and `actualPaymentDate`.
    5.  If `linkedExpenseId` exists:
        - Update linked `recurrentExpenses` record: Set `status: 'paid'`, `amount: actualAmountPence`, `cycleCurrent: 1`, and `date: paymentDate`.
    6.  After transaction: Call `triggerBalanceRecalc(paymentDate)` and `triggerDailyForecastRecalc(paymentDate)`.
  </action>
  <verify>grep "recordPayment" src/db/repository.js</verify>
  <done>statementRepository correctly handles payment confirmation for debt statements.</done>
</task>

</tasks>

<verification>
Check implementation of `addWithExpense` and `recordPayment` in `src/db/repository.js`.
</verification>

<success_criteria>
1. `addWithExpense` creates both records atomically and resolves category/label.
2. `recordPayment` updates both records correctly and marks expense as paid.
3. Both methods trigger the required balance/forecast recalculations.
4. Circular link (`linkedExpenseId` <-> `linkedStatementId`) is correctly maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/21-data-layer-and-repository/21-02-SUMMARY.md`
</output>
