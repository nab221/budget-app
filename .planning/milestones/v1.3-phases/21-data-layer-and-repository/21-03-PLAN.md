---
phase: 21-data-layer-and-repository
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified: [src/db/repository.test.js]
autonomous: true
requirements: [DEBT-01.4, DEBT-01.5]
must_haves:
  truths:
    - "Unit tests pass for addWithExpense() and recordPayment()"
    - "Tests verify circular ID linking"
    - "Tests verify status and amount updates in recordPayment"
  artifacts:
    - path: "src/db/repository.test.js"
      provides: "Verification suite for statementRepository"
      contains: "describe('statementRepository'"
  key_links: []
---

<objective>
Update `src/db/repository.test.js` with comprehensive tests for the new `statementRepository` methods.

Purpose: Ensure the data integrity and correctness of the statement/expense linking logic.
Output: Updated `src/db/repository.test.js` with passing tests.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/db/repository.js
@src/db/repository.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Test Mocks</name>
  <files>src/db/repository.test.js</files>
  <action>
    Update the `vi.mock('./schema.js', ...)` block in `src/db/repository.test.js`.
    
    1.  Add `statements: createMockTable()` to the mock db object.
    2.  Ensure `recurrentExpenses` and `categories` tables are correctly mocked and cleared in `beforeEach`.
    3.  Import `statementRepository` and `categoryRepository` from `./repository.js`.
  </action>
  <verify>grep "statements = createMockTable()" src/db/repository.test.js</verify>
  <done>Mocks are updated to support testing of the statement repository.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tests for addWithExpense() and recordPayment()</name>
  <files>src/db/repository.test.js</files>
  <action>
    Add a new `describe('statementRepository', () => { ... })` block with tests:
    
    1.  Test `addWithExpense()`:
        - Verify statement record is created.
        - Verify recurrent expense record is created with correct fields (`isDebtPayment: true`, `cycleTotal: 1`, `status: 'pending'`).
        - Verify `statement.linkedExpenseId` matches the expense ID.
        - Verify `expense.linkedStatementId` matches the statement ID.
        - Verify correct category and label resolution.
    2.  Test `recordPayment()`:
        - Verify `actualPaymentAmount` and `actualPaymentDate` are updated on the statement.
        - Verify linked expense `status` becomes 'paid' and `amount` matches `actualAmount`.
        - Verify `cycleCurrent` becomes 1.
  </action>
  <verify>npm test src/db/repository.test.js</verify>
  <done>Comprehensive tests verify the correctness of the new statement management logic.</done>
</task>

</tasks>

<verification>
Run `npm test src/db/repository.test.js` to ensure all tests pass.
</verification>

<success_criteria>
1. `src/db/repository.test.js` includes mocks for all required tables.
2. `addWithExpense` tests verify atomic creation and circular ID linking.
3. `recordPayment` tests verify state updates across both linked records.
4. All repository tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/21-data-layer-and-repository/21-03-SUMMARY.md`
</output>
