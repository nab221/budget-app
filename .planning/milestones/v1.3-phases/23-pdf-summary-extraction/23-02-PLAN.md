---
phase: 23-pdf-summary-extraction
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified: [src/ui/pdf-import.js]
autonomous: true
requirements: [DEBT-03.1, DEBT-03.4]
must_haves:
  truths:
    - "pdfImportUI correctly identifies and enters 'statement' mode when triggered"
    - "System provides a specialized summary review screen instead of a transaction list in statement mode"
    - "Clear error message or manual entry hint shown if extraction returns no matches"
  artifacts:
    - path: "src/ui/pdf-import.js"
      contains: "handleFileUploadForStatement"
  key_links:
    - from: "src/ui/pdf-import.js"
      to: "src/utils/pdf-parser.js"
      via: "import { extractStatementSummary }"
---

<objective>
Enhance the PDF Import UI to support a dedicated "Statement Mode" that uses the summary extraction logic.

Purpose: Provide a UI wrapper for the statement extraction engine.
Output: A functional PDF import workflow that returns a statement summary object.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/pdf-import.js
@src/utils/pdf-parser.js
@.planning/phases/23-pdf-summary-extraction/23-01-SUMMARY.md

<interfaces>
From src/utils/pdf-parser.js:
```javascript
export function extractStatementSummary(rows) { ... } // Returns { date, openingBalance, amount, minimumPayment, paymentDueDate }
```

From src/ui/pdf-import.js:
```javascript
export const pdfImportUI = {
  state: { transactions: [], ... },
  async handleFileUpload(file) { ... },
  showLoadingModal() { ... },
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Statement Mode state and entry point</name>
  <files>src/ui/pdf-import.js</files>
  <action>
    - Add `mode` (default 'transactions') and `targetDebtId` to `pdfImportUI.state`.
    - Implement `handleFileUploadForStatement(debtId)`:
      - Sets `state.mode = 'statement'` and `state.targetDebtId = debtId`.
      - Triggers a file input click (reusing existing hidden input or creating a specific one).
    - Modify `handleFileUpload(file)`:
      - Branch logic based on `this.state.mode`.
      - If 'statement', call `extractTextFromPdf`, then `extractStatementSummary(rows)`.
  </action>
  <verify>Check for handleFileUploadForStatement function and mode branching in pdf-import.js</verify>
  <done>Entry point for statement mode exists and calls the summary extraction function</done>
</task>

<task type="auto">
  <name>Task 2: Implement Statement Summary Preview</name>
  <files>src/ui/pdf-import.js</files>
  <action>
    - Implement `renderStatementSummaryPreview(extractedData)`:
      - Displays a modal (reusing `templateUI.showModal`) with extracted fields in a clear list.
      - Includes "Opening Balance", "New Balance", "Minimum Payment", etc.
      - Shows "Review and Confirm" instructions.
    - Implement `confirmStatementImport()`:
      - Sends the reviewed data back to `debtUI.prefillStatementForm(data)`.
      - Closes the modal.
    - Add error handling:
      - If `extractedData` contains only nulls, show the manual entry fallback hint.
  </action>
  <verify>Check for renderStatementSummaryPreview and confirmStatementImport in pdf-import.js</verify>
  <done>UI handles the review and confirmation of extracted statement summaries</done>
</task>

</tasks>

<success_criteria>
1. `pdfImportUI` can be triggered in statement mode.
2. In statement mode, it extracts and displays summary data rather than a transaction table.
3. User sees a helpful message if summary data cannot be identified in the PDF.
</success_criteria>

<output>
After completion, create `.planning/phases/23-pdf-summary-extraction/23-02-SUMMARY.md`
</output>
