---
phase: 23-pdf-summary-extraction
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified: [src/ui/debts.js]
autonomous: true
requirements: [DEBT-03.3]
must_haves:
  truths:
    - "'Import PDF' button is present in the Debt statement history section"
    - "Clicking 'Import PDF' initiates the specialized statement import flow"
    - "Completing the PDF import correctly populates the statement entry form"
  artifacts:
    - path: "src/ui/debts.js"
      contains: "prefillStatementForm"
  key_links:
    - from: "src/ui/debts.js"
      to: "src/ui/pdf-import.js"
      via: "window.pdfImportUI.handleFileUploadForStatement"
---

<objective>
Integrate the PDF extraction workflow into the Debt Management UI.

Purpose: Bridge the gap between PDF parsing and data entry.
Output: A seamless user experience for logging debt statements from PDFs.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/debts.js
@src/ui/pdf-import.js
@.planning/phases/23-02-SUMMARY.md

<interfaces>
From src/ui/debts.js:
```javascript
export const debtUI = {
  renderStmtForm() { ... },
  toggleStmtForm(show) { ... },
  // ...
};
```

From src/ui/pdf-import.js:
```javascript
export const pdfImportUI = {
  handleFileUploadForStatement(debtId) { ... },
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Import Button and UI Trigger</name>
  <files>src/ui/debts.js</files>
  <action>
    - Add an "Import PDF" button to the `statementSection` (near the "Log Statement" button).
    - Wire the button to call `window.pdfImportUI.handleFileUploadForStatement(debtId)`.
    - Style the button to fit the existing UI.
  </action>
  <verify>Check for 'Import PDF' button in debts.js and its onclick handler</verify>
  <done>UI has a clear entry point for PDF statement imports</done>
</task>

<task type="auto">
  <name>Task 2: Implement Form Pre-filling Logic</name>
  <files>src/ui/debts.js</files>
  <action>
    - Modify `renderStmtForm(prefillData = null)`:
      - Accepts an optional `prefillData` object.
      - Uses `prefillData` to override default values if provided.
      - Ensures currency amounts from `prefillData` (pence) are converted back to GBP strings (e.g., via `fromPence`).
    - Implement `prefillStatementForm(data)`:
      - Calls `this.toggleStmtForm(true)`.
      - Re-renders the form with `data`.
      - Ensures the form is scrolled into view.
  </action>
  <verify>Check renderStmtForm parameter update and prefillStatementForm implementation in debts.js</verify>
  <done>Debt statement form can be pre-populated with data from external sources (PDF extraction)</done>
</task>

</tasks>

<success_criteria>
1. "Import PDF" button is functional in the Debt view.
2. Extracted statement data correctly pre-fills the statement entry form.
3. User can edit the pre-filled fields and save the statement successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/23-pdf-summary-extraction/23-03-SUMMARY.md`
</output>
