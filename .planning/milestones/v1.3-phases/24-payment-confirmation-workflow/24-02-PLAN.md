---
phase: 24-payment-confirmation-workflow
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [src/ui/debts.js]
autonomous: true
requirements: [DEBT-04.3]

must_haves:
  truths:
    - "Statement history shows both minimum due and actual amount paid."
    - "Actual payment date is visible if it differs from the due date."
    - "Debt statements are clearly linked to their corresponding payments visually."
  artifacts:
    - path: "src/ui/debts.js"
      provides: "Statement history with actual payment details"
  key_links:
    - from: "src/ui/debts.js"
      to: "statement.actualPaymentAmount"
      via: "renderStatements"
---

<objective>
Update the Debt UI to display confirmed payment details in the statement history and ensure a consistent visual experience across tabs.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-payment-confirmation-workflow/24-RESEARCH.md
@src/ui/debts.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Statement History Rendering</name>
  <files>src/ui/debts.js</files>
  <action>
    1. Locate the `renderStatements(debtId)` method.
    2. Modify the row rendering for `minimumPayment` (6th column) to also display `actualPaymentAmount` if present.
       - Use a smaller font size and successful color (green) for the "Paid: £X.XX" text.
    3. Modify the row rendering for `paymentDueDate` (7th column) to also display `actualPaymentDate` if present.
       - Use a smaller font size and successful color (green) for the "On: YYYY-MM-DD" text.
    4. Add a `💳` badge next to the "Min Payment Due" text if it's a debt payment (all statements in this view are debt related, so consistent badges are good).
  </action>
  <verify>
    Grep for 'actualPaymentAmount' and '💳' in src/ui/debts.js
  </verify>
  <done>Statement history list shows actual payment details and consistent iconography.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Comprehensive payment confirmation workflow across Expenses and Debts tabs.</what-built>
  <how-to-verify>
    1. Open the app and go to the "Expenses" tab.
    2. Mark a debt expense as "Paid".
    3. Verify that a modal appears prompting for the actual amount and date.
    4. Confirm the payment and verify the expense row is highlighted/marked as paid.
    5. Go to the "Debts" tab and view the history for that debt.
    6. Verify that the "Actual Amount Paid" and date are visible in the history table.
    7. Go back to "Expenses" and un-mark it as paid.
    8. Verify the "Debts" tab now reflects the payment as being reset (cleared from history columns).
  </how-to-verify>
  <resume-signal>Approved</resume-signal>
</task>

</tasks>

<success_criteria>
1. Statement history columns are expanded to show actual vs minimum payments.
2. Cross-tab synchronization is seamless via the app:refresh mechanism.
3. Iconic representation (💳) is consistent throughout the UI for debt transactions.
</success_criteria>

<output>
After completion, create `.planning/phases/24-payment-confirmation-workflow/24-02-SUMMARY.md`
</output>
