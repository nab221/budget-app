---
phase: 24-payment-confirmation-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/repository.js, src/ui/expenses.js]
autonomous: true
requirements: [DEBT-02.4]

must_haves:
  truths:
    - "Marking a debt payment as paid triggers a specialized modal instead of a simple toggle."
    - "User can enter an actual amount and date for the payment."
    - "Recording a payment updates both the expense and the linked statement."
    - "Toggling off a paid debt expense resets the statement and reverts the expense to the minimum."
  artifacts:
    - path: "src/db/repository.js"
      provides: "resetPayment method for statements"
    - path: "src/ui/expenses.js"
      provides: "Debt payment confirmation modal and toggle interception"
  key_links:
    - from: "src/ui/expenses.js"
      to: "statementRepository.recordPayment"
      via: "Confirmation modal submit"
    - from: "src/ui/expenses.js"
      to: "statementRepository.resetPayment"
      via: "Toggle paid-to-pending for debt expense"
---

<objective>
Implement the specialized debt payment confirmation workflow in the Expenses UI.
Intercepts the standard toggle for debt-related expenses to ensure actual payment amounts are captured and synchronized with debt statements.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-payment-confirmation-workflow/24-RESEARCH.md
@src/db/repository.js
@src/ui/expenses.js
@src/utils/cashflow.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Repository Logic</name>
  <files>src/db/repository.js</files>
  <action>
    1. Add `resetPayment(statementId)` method to `statementRepository`:
       - Use a transaction for `statements` and `recurrentExpenses`.
       - Retrieve the statement to find `linkedExpenseId` and `minimumPayment`.
       - Set `actualPaymentAmount` and `actualPaymentDate` to null on the statement.
       - If `linkedExpenseId` exists, update the expense: `status: 'pending'`, `amount: statement.minimumPayment`, `cycleCurrent: 0`, `date: null`.
       - Trigger recalcs using `triggerBalanceRecalc` and `triggerDailyForecastRecalc`.
    2. Update `recurrentExpenseRepository.markAllAsPaid`:
       - Filter the pending items to skip those where `isDebtPayment` is true.
       - This prevents bulk "Mark Paid" from desynchronizing statements.
  </action>
  <verify>
    node -e "import('./src/db/repository.js').then(r => console.log('resetPayment' in r.statementRepository))"
  </verify>
  <done>statementRepository supports resetPayment and markAllAsPaid ignores debts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Expenses UI Interception and Modal</name>
  <files>src/ui/expenses.js</files>
  <action>
    1. Import `statementRepository` from `../db/repository.js`.
    2. Import `nextWorkingDay` from `../utils/cashflow.js`.
    3. Modify `window.toggleRecurrentStatus`:
       - Check if `item.isDebtPayment` and `item.linkedStatementId` are set.
       - If `currentStatus === 'pending'`, call `expensesUI.showDebtPaymentConfirmation(item)`.
       - If `currentStatus === 'paid'`, confirm with user and call `statementRepository.resetPayment(item.linkedStatementId)`.
    4. Implement `expensesUI.showDebtPaymentConfirmation(item)`:
       - Use `nextWorkingDay(new Date().toISOString().slice(0, 10), true)` to get the suggested payment date.
       - Use `modalUI.show` (or `showModal`) to display a form:
         - Title: "Confirm Debt Payment"
         - Fields: Actual Amount Paid (default to `item.amount`), Payment Date (default to suggested date).
         - Footer buttons: "Record Payment" (primary) and "Cancel" (ghost).
    5. Implement `expensesUI.handleConfirmDebtPayment(itemId, statementId, amount, date)`:
       - Call `statementRepository.recordPayment(statementId, amount, date)`.
       - Close the modal.
       - UI will refresh via `app:refresh` triggered by repository.
  </action>
  <verify>
    Grep for 'isDebtPayment' and 'showDebtPaymentConfirmation' in src/ui/expenses.js
  </verify>
  <done>Expenses UI intercepts debt payment toggles and prompts for confirmation with suggested dates.</done>
</task>

</tasks>

<success_criteria>
1. `statementRepository.resetPayment` correctly reverts both statement and expense records.
2. `toggleRecurrentStatus` triggers the specialized modal for debt payments.
3. The modal suggests the next working UK bank holiday date correctly.
4. Toggling off a paid debt expense resets its state and linked statement.
</success_criteria>

<output>
After completion, create `.planning/phases/24-payment-confirmation-workflow/24-01-SUMMARY.md`
</output>
