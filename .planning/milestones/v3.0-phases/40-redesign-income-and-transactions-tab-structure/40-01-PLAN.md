---
phase: 40-redesign-income-and-transactions-tab-structure
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/transactions-merged.test.js
autonomous: true
requirements:
  - 40-02
  - 40-03
  - 40-04

must_haves:
  truths:
    - "RED test exists asserting renderAll() routes panelId 'transactions' to transactionUI.render()"
    - "RED tests exist asserting merged row list contains both income and expense _rowType entries"
    - "RED test exists asserting renderHeatmap() calls renderSpendingHeatmap for both transactionsIncomeHeatmapContainer and transactionsSpendingHeatmapContainer"
    - "All existing 704+ Vitest tests continue to pass"
  artifacts:
    - path: "tests/transactions-merged.test.js"
      provides: "RED scaffold for 40-02, 40-03, 40-04"
      exports: []
  key_links:
    - from: "tests/transactions-merged.test.js"
      to: "src/ui/transactions.js"
      via: "vi.mock + import"
      pattern: "transactionUI"
---

<objective>
Create the Wave 0 RED test scaffold for Phase 40. These tests must fail before the implementation in Plan 02 and pass after it.

Purpose: Establish Nyquist-compliant automated verification so every implementation commit in Plan 02 has a runnable test gate. The VALIDATION.md marks these as Wave 0 requirements.
Output: tests/transactions-merged.test.js with 3 RED test groups covering requirements 40-02, 40-03, and 40-04.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-redesign-income-and-transactions-tab-structure/40-RESEARCH.md
@.planning/phases/40-redesign-income-and-transactions-tab-structure/40-VALIDATION.md
</context>

<feature>
  <name>transactions-merged test scaffold</name>
  <files>tests/transactions-merged.test.js</files>
  <behavior>
    RED Test Group A — panelId routing (REQ-40-02):
    - renderAll() with panelId='transactions' should call transactionUI.render()
    - renderAll() with panelId='income' should NOT call transactionUI.render() (old key removed)

    RED Test Group B — merged row model (REQ-40-03):
    - transactionUI._buildMergedRows(incomeItems, recurrentItems, oneOffItems) returns array
    - Each income item has _rowType === 'income'
    - Each recurrent expense item has _rowType === 'expense' and type === 'recurrent'
    - Each oneOff expense item has _rowType === 'expense' and type === 'oneoff'
    - Merged array is sorted by displayDate descending
    - Income item has displayLabel from item.source field
    - Expense item has displayLabel from item.label (recurrent) or item.note (oneoff)

    RED Test Group C — dual heatmap containers (REQ-40-04):
    - transactionUI.renderHeatmap() calls renderSpendingHeatmap with container id 'transactionsIncomeHeatmapContainer'
    - transactionUI.renderHeatmap() calls renderSpendingHeatmap with container id 'transactionsSpendingHeatmapContainer'
    - renderHeatmap() does NOT call renderSpendingHeatmap with the OLD id 'incomeTabHeatmapContainer'
  </behavior>
  <implementation>
    Write the test file so that:
    - Group A: mock the DOM (JSDOM already configured via vite.config.js). Spy on transactionUI.render. Create a minimal renderAll() stub that reads dataset.tab from a mocked active tab element. Since app.js is hard to isolate, test the panelId routing logic as a unit test using the source directly — mock the tab element and call the renderAll logic. If app.js is too coupled, test by checking that panelId 'income' no longer matches any render branch (document-level DOM test using existing test patterns from tests/income-sources.test.js as a model).
    - Group B: call transactionUI._buildMergedRows() directly — this is a new pure helper to be added by Plan 02. The test defines the expected contract: input three arrays, output merged sorted array. Since the function doesn't exist yet, these tests will fail with "is not a function" — that is the correct RED state.
    - Group C: mock renderSpendingHeatmap from heatmap.js. Mock the DOM containers transactionsIncomeHeatmapContainer and transactionsSpendingHeatmapContainer. Call transactionUI.renderHeatmap(). Assert mock was called with the new IDs.

    Pattern: follow tests/income-sources.test.js for vi.mock patterns, beforeEach DOM setup, and afterEach cleanup.

    IMPORTANT: After writing the scaffold, run `npm test -- --run tests/transactions-merged.test.js` to confirm all tests are RED. Then run `npm test -- --run` to confirm the full suite (704+ tests) still passes.
  </implementation>
</feature>

<verification>
<automated>npm test -- --run tests/transactions-merged.test.js</automated>
All tests in transactions-merged.test.js must be RED (failing). Full suite must still pass.
Run: npm test -- --run
</verification>

<success_criteria>
- tests/transactions-merged.test.js exists with at least 8 test cases
- All tests in that file FAIL (RED state confirmed)
- Full suite (704+ tests) passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/40-redesign-income-and-transactions-tab-structure/40-01-SUMMARY.md`
</output>
