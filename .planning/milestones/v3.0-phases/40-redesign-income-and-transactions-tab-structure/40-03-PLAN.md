---
phase: 40-redesign-income-and-transactions-tab-structure
plan: "03"
type: execute
wave: 2
depends_on:
  - "40-02"
files_modified: []
autonomous: false
requirements:
  - 40-01
  - 40-02
  - 40-04
  - 40-05

must_haves:
  truths:
    - "User sees 'Income' label on the Pay Sources tab (income source management)"
    - "User sees 'Transactions' label on the tab that shows transaction history"
    - "Transactions tab shows both income (IN) and expense (OUT) rows together"
    - "Debt-linked expense rows navigate to Debts tab on tap — no inline edit"
    - "Non-debt expense rows support swipe-right edit and swipe-left delete"
    - "Dashboard heatmaps appear below the affordability/summary cards, not above them"
    - "Both income and spending heatmaps are visible in the Transactions tab"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 40 changes. Claude has automated everything — this checkpoint confirms the visual and interactive results are correct before the phase is marked complete.

Purpose: Phase 40 changes affect tab labels, merged transaction rendering, IN/OUT tagging, and dashboard layout — all require visual confirmation.
Output: User approval or list of defects for gap closure.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-redesign-income-and-transactions-tab-structure/40-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. "Pay Sources" tab renamed to "Income" (label + aria only, data-tab='income-sources' unchanged)
    2. "Income" tab renamed to "Transactions" (data-tab, data-panel, aria-label, span text all changed)
    3. Transactions tab shows merged IN/OUT rows — income entries tagged green IN, expense entries tagged red OUT
    4. Debt-linked expense rows navigate to Debts tab on tap, with no edit/delete buttons
    5. Both income heatmap and spending heatmap visible in the Transactions tab
    6. Dashboard heatmaps moved to the bottom (below affordability cards and spending analytics grid)
  </what-built>
  <how-to-verify>
    Run `npm run dev` to start the dev server, then open the app.

    Check 1 — Tab labels:
    - Navigate through all tabs in the bottom bar
    - Confirm the tab for income source management shows "Income" (not "Pay Sources")
    - Confirm the tab for transaction history shows "Transactions" (not "Income")
    - Confirm the Income tab (income sources) opens correctly and shows the CRUD list

    Check 2 — Transactions tab merged view:
    - Open the Transactions tab
    - If no data: add one income entry and one expense entry in the same month
    - Confirm both appear in the same table
    - Confirm income rows have a green "IN" pill/badge
    - Confirm expense rows have a red "OUT" pill/badge
    - Confirm rows are sorted by date descending

    Check 3 — Expense row interactions:
    - In the Transactions tab, if a debt-linked expense row exists (any expense from the Debts tab): tap it — should navigate to the Debts tab
    - For a non-debt expense: swipe right → Edit form should open (or edit button present); swipe left → Delete confirmation

    Check 4 — Heatmaps in Transactions tab:
    - Open the Transactions tab and scroll down
    - Confirm an "Income Heatmap" section is visible
    - Confirm a "Spending Heatmap" section is visible (new in this tab)

    Check 5 — Dashboard heatmap position:
    - Open the Dashboard tab
    - Scroll down past the affordability cards and spending analytics grid
    - Confirm the income heatmap and spending heatmap appear BELOW those sections (not above them)
    - Confirm the affordability cards and summary grid are still at the top

    Check 6 — No console errors:
    - Open browser DevTools console
    - Navigate through Dashboard, Transactions, Income (Pay Sources), Expenses tabs
    - Confirm no new errors introduced by this phase
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g. "Check 2 failed — no IN/OUT tags visible")</resume-signal>
</task>

</tasks>

<verification>
Human approval required. All six checks must pass.
</verification>

<success_criteria>
User types "approved" after verifying all six checks. Any failures are documented for gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/40-redesign-income-and-transactions-tab-structure/40-03-SUMMARY.md`
</output>
