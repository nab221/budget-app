---
phase: 16-advanced-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, src/app.js, src/utils/finance.js, src/ui/expenses.js, src/ui/templates.js]
autonomous: true
requirements: [CTRL-01.1, CTRL-01.2]
must_haves:
  truths:
    - "User can set an opening balance in Settings that persists after save"
    - "First month's balance calculation incorporates the manual opening balance"
    - "Clicking 'Call Templates' in Expenses opens the template selection modal"
  artifacts:
    - path: "src/utils/finance.js"
      provides: "Updated balance chain logic"
    - path: "src/ui/templates.js"
      provides: "Manual template trigger method"
---

<objective>
Implement initial opening balance settings and a manual template trigger to improve setup flexibility and recurring data entry.
</objective>

<context>
@index.html
@src/app.js
@src/utils/finance.js
@src/ui/expenses.js
@src/ui/templates.js
@.planning/phases/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update UI and Configuration Persistence</name>
  <files>index.html, src/app.js</files>
  <action>
    - Add 'Opening Balance' number input (id="balanceOpeningAmount") to index.html in the Settings tab, grouped with 'Balance Start Date'.
    - Add 'Call Templates' button (class="ghost sm") to index.html in the Expenses tab near the 'Add Expense' button.
    - Update src/app.js to read/write 'balanceOpeningAmount' from/to localStorage. 
    - Ensure 'balanceOpeningAmount' is saved in pence (convert from decimal input).
    - Trigger "Recalculating..." feedback and full balance recalculation when the Settings save button is clicked.
  </action>
  <verify>
    - Input exists in Settings.
    - Saving a value (e.g., 100.00) results in 'budget_balance_opening_amount: 10000' in localStorage.
  </verify>
  <done>Opening balance UI is functional and persistent.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Opening Balance into Finance Logic</name>
  <files>src/utils/finance.js</files>
  <action>
    - Modify 'calculateBalanceChain' in src/utils/finance.js.
    - Retrieve 'initialOpeningBalance' from localStorage (default to 0 if missing).
    - Add this value to the starting balance of the very first month in the chain (the 'balanceStartDate').
    - Ensure all subsequent months correctly carry forward this initial injection.
  </action>
  <verify>
    - Run unit tests or manual check: Set start date to Jan 2024, opening balance to 500, check if Jan 2024 closing balance is (Income - Expenses + 500).
  </verify>
  <done>Finance logic correctly accounts for the initial opening balance.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Manual Template Trigger</name>
  <files>src/ui/expenses.js, src/ui/templates.js</files>
  <action>
    - In src/ui/templates.js, implement 'templateUI.manualTrigger(monthStr)'.
    - This method should invoke the existing template selection modal logic for the specified month.
    - In src/ui/expenses.js, bind the 'Call Templates' button click event to 'templateUI.manualTrigger' using the currently selected month in the UI.
  </action>
  <verify>
    - Clicking the button in the Expenses tab opens the template selection modal.
  </verify>
  <done>Users can manually trigger recurring templates for any month.</done>
</task>

</tasks>

<success_criteria>
- Opening balance input supports positive and negative values.
- Balance chain recalculates correctly with the new initial value.
- "Call Templates" button is visible and functional in the Expenses tab.
</success_criteria>
