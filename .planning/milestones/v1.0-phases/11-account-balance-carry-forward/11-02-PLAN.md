---
phase: 11-account-balance-carry-forward
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: [src/ui/dashboard.js, src/ui/templates.js, src/ui/charts.js, src/app.js, tests/e2e/balance.spec.js]
autonomous: true
requirements: [BAL-04]
must_haves:
  truths:
    - "Dashboard shows today's balance and month-end forecast."
    - "Balance card turns Red if projected balance drops below zero."
    - "Users can configure and persist 'Balance Start Date' in settings."
---

<objective>
Implement the UI components and end-to-end verification for the Account Balance Carry-Forward system.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Balance Card & Trend Chart UI</name>
  <files>src/ui/dashboard.js, src/ui/templates.js, src/ui/charts.js</files>
  <action>
    1. Implement `renderBalancePanel` to show Running Balance and Forecast.
    2. Add "Negative Balance" logic: set card background to red if any snapshot in the next 3 months is < 0.
    3. Implement `renderBalanceChart` (90-day trend) in `src/ui/charts.js` using dashed lines for projections.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/balance.spec.js --grep "Balance Card"</automated>
  </verify>
  <done>Dashboard UI elements are visible and correctly styled based on data state.</done>
</task>

<task type="auto">
  <name>Task 2: Settings & Start Date Persistence</name>
  <files>index.html, src/app.js</files>
  <action>
    1. Add "Balance Start Date" input to settings.
    2. Persist selection to `localStorage` (key: `budget_balance_start_date`).
    3. Trigger full snapshot recalculation when the date changes.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/balance.spec.js --grep "Settings Persistence"</automated>
  </verify>
  <done>Start date persists across reloads and triggers recalculation on change.</done>
</task>

<task type="auto">
  <name>Task 3: E2E Tests Implementation</name>
  <files>tests/e2e/balance.spec.js</files>
  <action>
    Create a comprehensive Playwright E2E suite covering:
    - Dashboard Balance Card visibility and math accuracy.
    - Negative balance alert (verify red background when a large future expense is added).
    - Forecast chart rendering.
    - "Hard Start Date" persistence and impact on calculations.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/balance.spec.js</automated>
  </verify>
  <done>All UI requirements are verified by automated E2E tests.</done>
</task>

</tasks>
