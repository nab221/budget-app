---
phase: 11-account-balance-carry-forward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js, src/utils/finance.js, src/utils/finance.test.js, src/db/repository.test.js]
autonomous: true
requirements: [BAL-01, BAL-02, BAL-03]
must_haves:
  truths:
    - "Database includes a version 9 schema with a balanceSnapshots table."
    - "The 'Opening Balance' category is present in the database."
    - "calculateBalanceChain correctly sums income and expenses for a month, including projections."
    - "The closing balance of one month correctly becomes the opening balance of the next."
  artifacts:
    - path: "src/db/schema.js"
      provides: "v9 balanceSnapshots table"
    - path: "src/db/repository.js"
      provides: "balanceSnapshotRepository and seeding logic"
    - path: "src/utils/finance.js"
      provides: "calculateBalanceChain engine"
---

<objective>
Implement the core data layer and business logic for the Account Balance Carry-Forward system. This includes the database schema, category seeding, and the carry-forward arithmetic engine.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Seed Category, Schema & Repository</name>
  <files>src/db/schema.js, src/db/repository.js, src/db/repository.test.js</files>
  <action>
    1. Update `src/db/schema.js` to version 9: Add `balanceSnapshots: '++id, month, openingBalance, closingBalance, incomeTotal, expenseTotal'`.
    2. In `src/db/repository.js`, ensure the "Opening Balance" category is provisioned/seeded into the `categories` table if it doesn't exist.
    3. Implement `balanceSnapshotRepository` with methods: `getByMonth`, `save`, `deleteFrom`, and `getLatestSnapshot`.
  </action>
  <verify>
    <automated>npm test src/db/repository.test.js</automated>
  </verify>
  <done>v9 schema active, "Opening Balance" category exists, and repository methods pass tests.</done>
</task>

<task type="auto">
  <name>Task 2: Carry-Forward Calculation Engine</name>
  <files>src/utils/finance.js, src/utils/finance.test.js</files>
  <action>
    Implement `calculateBalanceChain(startDate, horizonMonths = 3)` in `src/utils/finance.js`.
    Logic: Initialize with "Opening Balance" transaction or 0; loop through months to current + horizon; calculate closing = opening + income - expenses; save snapshots.
  </action>
  <verify>
    <automated>npm test src/utils/finance.test.js</automated>
  </verify>
  <done>Calculation engine passes unit tests for arithmetic and carry-forward scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Integration & Recalculation Triggers</name>
  <files>src/db/repository.js</files>
  <action>
    Update `incomeRepository` and `expenseRepository` to call `triggerBalanceRecalc(date)` on modifications. This helper must invalidate snapshots from the modified month onwards and trigger a background recalculation.
  </action>
  <verify>Manual verification via console: update a transaction and check if the `balanceSnapshots` table updates.</verify>
  <done>Data modifications trigger automatic background balance recalculations.</done>
</task>

</tasks>
