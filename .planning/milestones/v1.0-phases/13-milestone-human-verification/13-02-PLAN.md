---
phase: 13-milestone-human-verification
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ui/debts.test.js
autonomous: true
requirements:
  - ADD-01
  - ADD-02
  - ADD-03
  - EDIT-01
  - EDIT-02

must_haves:
  truths:
    - "Five new failing tests exist in debts.test.js covering ADD-01, ADD-02, ADD-03, EDIT-01, EDIT-02"
    - "Each test asserts a specific observable behavior: repository call, error span presence, empty inputs, or pre-populated inputs"
    - "Running npx vitest run src/ui/debts.test.js reports the new tests as RED (failing)"
    - "No existing passing tests are broken"
  artifacts:
    - path: "src/ui/debts.test.js"
      provides: "Failing unit tests for Phase 13 requirements"
      contains: "ADD-01, ADD-02, ADD-03, EDIT-01, EDIT-02"
  key_links:
    - from: "debts.test.js ADD-01 test"
      to: "debtRepository.add mock"
      via: "expect(debtRepository.add).toHaveBeenCalledWith"
    - from: "debts.test.js ADD-02 test"
      to: "DOM .field-error span"
      via: "document.querySelector('.field-error')"
    - from: "debts.test.js EDIT-02 test"
      to: "FIELD_IDS inputs"
      via: "document.getElementById(id).value"
---

<objective>
Write failing unit tests for all five Phase 13 requirements: the Add save path (ADD-01), inline validation (ADD-02), form reset on Add open (ADD-03), the Edit save path (EDIT-01), and Edit field pre-population (EDIT-02).

Purpose: Establish RED state for TDD. Tests define the exact contract that the Phase 13-03 implementation plan must satisfy.
Output: Five new failing tests appended to src/ui/debts.test.js in a new describe block.
</objective>

<execution_context>
@C:/Users/nab221/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v1.0-phases/13-milestone-human-verification/13-RESEARCH.md
@.planning/milestones/v1.0-phases/13-milestone-human-verification/13-VALIDATION.md

<interfaces>
<!-- Key contracts from src/ui/debts.js that tests must target. -->

FIELD_IDS (module-level constant):
```javascript
const FIELD_IDS = {
  name: 'debtNameInput',
  type: 'debtTypeInput',
  ccBalance: 'ccBalanceInput', ccApr: 'ccAprInput', ccLimit: 'ccLimitInput',
  ccMinPayment: 'ccMinPaymentInput', ccPromoEnd: 'ccPromoEndInput', ccPostApr: 'ccPostAprInput',
  mortgagePropertyValue: 'mortgagePropertyValueInput', mortgageBalance: 'mortgageBalanceInput',
  mortgageTerm: 'mortgageTermInput', mortgageRate: 'mortgageRateInput', mortgageErc: 'mortgageErcInput',
  loanOriginal: 'loanOriginalInput', loanBalance: 'loanBalanceInput',
  loanTerm: 'loanTermInput', loanRate: 'loanRateInput',
  otherBalance: 'otherBalanceInput',
}
```

debtUI methods to test (do NOT exist yet — tests will be RED):
- `debtUI._saveDebt()` — async, reads FIELD_IDS inputs, calls debtRepository.add or .update
- `debtUI._populateEditFields(debt)` — sets .value on FIELD_IDS inputs from a debt record
- `debtUI._showFieldError(fieldId, message)` — inserts a `<span class="field-error">` after the field
- `debtUI._clearFieldErrors()` — removes all `.field-error` elements

Existing mock infrastructure in debts.test.js (confirmed present):
- `vi.mock('../db/repository.js')` — debtRepository.add, .update, .get are vi.fn()
- `vi.mock('./render.js')` — modalUI.show is vi.fn() (synchronous, does NOT set innerHTML)
- `vi.mock('../utils/haptics.js')` — triggerHaptic is vi.fn()
- beforeEach sets debtUI.editingId = null, clears document.body.innerHTML

Pattern established in Phase 12 tests: inject fieldset HTML manually into document.body because
modalUI.show is mocked and does not set innerHTML.
</interfaces>
</context>

<feature>
  <name>Phase 13 save, edit, and validation tests (RED)</name>
  <files>src/ui/debts.test.js</files>
  <behavior>
    ADD-01: debtUI._saveDebt() Add mode — given name='Test CC', type='credit-card', ccBalance=500, ccApr=19.9, ccLimit=2000, ccMinPayment=25 set on inputs in document.body; debtUI.editingId=null; debtRepository.add resolves with {id:1} — after calling _saveDebt(), debtRepository.add is called once with an object containing { name: 'Test CC', debtType: 'credit-card', currentBalance: 500, apr: 19.9, creditLimit: 2000 }

    ADD-02: debtUI._saveDebt() with empty name — given name input value is '' and type='credit-card'; after _saveDebt(), debtRepository.add is NOT called and document.querySelector('.field-error') is truthy (error span exists in DOM)

    ADD-03: debtUI.openDebtModal() Add mode — call openDebtModal() with no argument (id=null), then check document.getElementById(FIELD_IDS.name).value === ''; _buildFormHTML returns a template with no value= attributes so all inputs are empty strings by default

    EDIT-01: debtUI._saveDebt() Edit mode — given debtUI.editingId=42, name='Barclays', type='personal-loan', loanOriginal=10000, loanBalance=8000, loanTerm=60, loanRate=5.5 on inputs; debtRepository.update resolves — after _saveDebt(), debtRepository.update is called with (42, objectContaining({ name: 'Barclays', debtType: 'personal-loan', interestRate: 5.5, apr: 5.5 })) and debtRepository.add is NOT called

    EDIT-02: debtUI._populateEditFields(debt) — given a debt object { name:'Barclays', debtType:'personal-loan', currentBalance:800000, originalPrincipal:1000000, termMonths:60, interestRate:5.5, apr:5.5, fixedMonthlyPayment:1500, earlyRepaymentFee:50000, earlyRepaymentFeeIsPercent:false, earlyRepaymentAllowed:true, isInterestOnly:false } (pence-stored fields in pence); after _populateEditFields(debt), document.getElementById(FIELD_IDS.loanBalance).value === '8000' (fromPence(800000)=8000), document.getElementById(FIELD_IDS.loanOriginal).value === '10000'
  </behavior>
  <implementation>
    Tests target methods that do not yet exist — all five tests must fail (RED) after this plan.
    Append a new describe block 'debtUI save, edit, and validation' to the existing test file.
    Use beforeEach to set up document.body.innerHTML with all FIELD_IDS inputs needed per test.
    For ADD-03: must call openDebtModal() which calls modalUI.show (mocked). After show(), inject the form HTML manually (same pattern as Phase 12 tests) then check input values are empty.
    For EDIT-02: inject input elements into document.body and call _populateEditFields(debt) directly.
    Confirm RED: npx vitest run src/ui/debts.test.js must report failures for all five new tests.
    Existing tests must remain GREEN.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for ADD-01, ADD-02, ADD-03 (Add flow RED)</name>
  <files>src/ui/debts.test.js</files>
  <behavior>
    - ADD-01: _saveDebt() Add mode calls debtRepository.add with correct credit-card payload
    - ADD-02: _saveDebt() with empty name does not call add, injects .field-error span into DOM
    - ADD-03: openDebtModal() with no id produces form with empty name input value
  </behavior>
  <action>
    Append a new describe block 'debtUI save, edit, and validation' to src/ui/debts.test.js.

    In this describe block, write a beforeEach that sets document.body.innerHTML to a snippet
    containing all inputs referenced in FIELD_IDS (name, type, ccBalance, ccApr, ccLimit,
    ccMinPayment, ccPromoEnd, ccPostApr, loanOriginal, loanBalance, loanTerm, loanRate,
    mortgagePropertyValue, mortgageBalance, mortgageTerm, mortgageRate, mortgageErc, otherBalance),
    and also resets debtUI.editingId = null and clears all mocks via vi.clearAllMocks().

    ADD-01 test: set document.getElementById('debtNameInput').value = 'Test CC', type = 'credit-card',
    ccBalance = '500', ccApr = '19.9', ccLimit = '2000', ccMinPayment = '25'. Call await debtUI._saveDebt().
    Assert debtRepository.add.mock.calls.length === 1. Assert the first argument contains
    { name: 'Test CC', debtType: 'credit-card', currentBalance: 500, apr: 19.9, creditLimit: 2000 }.

    ADD-02 test: set name input value = '' and type = 'credit-card'. Call await debtUI._saveDebt().
    Assert debtRepository.add was NOT called. Assert document.querySelector('.field-error') is not null.

    ADD-03 test: debtUI.editingId = null. Call await debtUI.openDebtModal(). After openDebtModal
    calls modalUI.show (mocked — does not set innerHTML), manually inject the form HTML by calling
    debtUI._buildFormHTML() and setting document.body.innerHTML = debtUI._buildFormHTML().
    Assert document.getElementById('debtNameInput').value === '' (empty string, not a previous value).
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js 2>&1 | tail -30</automated>
  </verify>
  <done>Three new tests in the 'debtUI save, edit, and validation' describe block exist and FAIL (RED). All previously passing tests remain GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests for EDIT-01, EDIT-02 (Edit flow RED)</name>
  <files>src/ui/debts.test.js</files>
  <behavior>
    - EDIT-01: _saveDebt() Edit mode calls debtRepository.update(editingId, payload) for a personal loan — not debtRepository.add
    - EDIT-02: _populateEditFields(debt) sets input .value properties from a debt record, converting pence fields with fromPence
  </behavior>
  <action>
    Continue in the same 'debtUI save, edit, and validation' describe block from Task 1.

    EDIT-01 test: set debtUI.editingId = 42. Set inputs: name = 'Barclays', type = 'personal-loan',
    loanOriginal = '10000', loanBalance = '8000', loanTerm = '60', loanRate = '5.5'.
    debtRepository.update = vi.fn().mockResolvedValue(undefined).
    Call await debtUI._saveDebt().
    Assert debtRepository.update.mock.calls[0][0] === 42.
    Assert debtRepository.update.mock.calls[0][1] contains
    { name: 'Barclays', debtType: 'personal-loan', interestRate: 5.5, apr: 5.5 }.
    Assert debtRepository.add was NOT called.

    EDIT-02 test: inject all FIELD_IDS inputs into document.body.innerHTML (same as beforeEach).
    Build a debt object in pence: { name: 'Barclays', debtType: 'personal-loan',
    currentBalance: 800000, originalPrincipal: 1000000, termMonths: 60, interestRate: 5.5, apr: 5.5,
    fixedMonthlyPayment: 150000, earlyRepaymentFee: 50000, earlyRepaymentFeeIsPercent: false,
    earlyRepaymentAllowed: true, isInterestOnly: false }.
    Call debtUI._populateEditFields(debt).
    Assert document.getElementById('loanBalanceInput').value === '8000'.
    Assert document.getElementById('loanOriginalInput').value === '10000'.
    Assert document.getElementById('loanRateInput').value === '5.5'.
    Assert document.getElementById('loanTermInput').value === '60'.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js 2>&1 | tail -30</automated>
  </verify>
  <done>Two additional failing tests (EDIT-01, EDIT-02) exist in the describe block. All five new tests are RED. All 11 previously passing tests remain GREEN. Total failing count matches exactly 5 new tests.</done>
</task>

</tasks>

<verification>
After both tasks:
- npx vitest run src/ui/debts.test.js shows exactly 5 new FAILING tests
- All previously passing modal scaffold and type-specific fieldset tests remain GREEN
- No syntax errors in the test file (Vitest can parse it)
- Each failing test targets a method that will be created in 13-03-PLAN.md
</verification>

<success_criteria>
- Five failing tests named ADD-01 through ADD-03 and EDIT-01 through EDIT-02 exist in src/ui/debts.test.js
- Running npx vitest run src/ui/debts.test.js shows RED for all five new tests
- Test assertions directly target the contracts defined in 13-RESEARCH.md (debtRepository.add/update calls, .field-error span, input .value from pence conversion)
- No watch-mode flags used
- Commit: test(13-02): add failing tests for ADD-01 ADD-02 ADD-03 EDIT-01 EDIT-02 RED
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/13-milestone-human-verification/13-02-SUMMARY.md`
</output>
