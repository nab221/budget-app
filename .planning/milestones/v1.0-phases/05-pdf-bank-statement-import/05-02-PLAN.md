---
phase: 05-pdf-bank-statement-import
plan: 05-02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/ui/pdf-import.js, src/ui/templates.js, src/ui/render.js]
autonomous: true
requirements: [PDF-02, PDF-03, PDF-05]

must_haves:
  truths:
    - "User can view a transaction preview table with in-line editing of description and category"
    - "System highlights transactions without a category (e.g., yellow background)"
    - "Detected duplicates are displayed in a separate 'Review Conflicts' section"
    - "User can manually map PDF columns to Date, Description, and Amount if auto-parse fails"
  artifacts:
    - path: "src/ui/pdf-import.js"
      provides: "Preview table UI with in-line editing, bulk actions, and manual mapping"
    - path: "src/ui/templates.js"
      provides: "PDF Import modal and preview table templates"
  key_links:
    - from: "src/ui/pdf-import.js"
      to: "src/utils/pdf-parser.js"
      via: "parse result consumption"
    - from: "src/ui/pdf-import.js"
      to: "src/db/repository.js"
      via: "duplicate detection and suggestion engine calls"
---

<objective>
Build the user interface for PDF bank statement import, including the transaction preview, in-line editing, and manual column mapping.

Purpose: Provide the visual tools for users to review, edit, and confirm transactions before importing into the budget.
Output: PDF Import modal, preview table with bulk actions, conflict review section, and manual mapping fallback.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pdf-bank-statement-import/05-CONTEXT.md
@.planning/phases/05-pdf-bank-statement-import/05-RESEARCH.md
@.planning/phases/05-pdf-bank-statement-import/05-01-SUMMARY.md
@src/ui/templates.js
@src/ui/render.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Transaction Preview UI</name>
  <files>src/ui/pdf-import.js, src/ui/templates.js, src/ui/render.js</files>
  <action>
    Define the PDF Import Modal in `src/ui/templates.js` with a file dropzone.
    Implement `renderPdfPreview(transactions)` in `src/ui/pdf-import.js`:
    1. Create a table with checkboxes, in-line editable descriptions, and category dropdowns.
    2. Highlight rows missing categories (yellow background per CONTEXT.md).
    3. Add "Suggested" badges to categories provided by the suggestion engine.
    4. Implement "Bulk Categorize" action for selected rows.
    5. Add "Import Selected" button that triggers the repository import logic.
  </action>
  <verify>
    Upload a mock bank PDF and verify the table renders with extracted data.
    Ensure editing a description in the table correctly updates the internal state.
    Confirm bulk categorization applies the same category to all checked rows.
  </verify>
  <done>User can review and edit extracted transactions in a clean, interactive table.</done>
</task>

<task type="auto">
  <name>Task 2: Conflict Review & Manual Mapping UI</name>
  <files>src/ui/pdf-import.js, src/ui/templates.js</files>
  <action>
    Implement the "Review Conflicts" section:
    1. Separate transactions flagged as potential duplicates into a distinct UI block below the main preview.
    2. Allow users to manually "Force Import" or "Dismiss" each conflict.
    
    Implement Manual Column Mapping UI:
    1. Add a "Manual Map" button that appears if auto-parse fails or is selected.
    2. Show a data preview (first 3 rows of PDF text) with dropdowns above each column.
    3. Dropdowns must allow mapping to: [Date], [Description], [Amount (Out)], [Amount (In)], [Balance (Optional)].
    4. Re-parse based on mapping and refresh the preview table.
  </action>
  <verify>
    Upload a duplicate PDF and verify conflicting rows appear in the Review section.
    Verify manual mapping correctly parses a custom PDF layout based on user-selected columns.
  </verify>
  <done>System handles duplicate conflicts gracefully and provides a robust manual fallback for unknown bank layouts.</done>
</task>

</tasks>

<verification>
1. Test PDF upload on both Desktop and Mobile (PWA context).
2. Confirm in-line edits are preserved when switching between "Preview" and "Conflicts" views.
3. Validate manual mapping with a "noisy" PDF that has multiple text columns.
</verification>

<success_criteria>
- Users can upload, edit, and confirm transactions from the preview table.
- Highlighted rows correctly draw attention to missing categories.
- Manual mapping allows successful import of unsupported PDF layouts.
- Conflicts are reviewed separately from new transactions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-bank-statement-import/05-02-SUMMARY.md`
</output>
