---
phase: 05-pdf-bank-statement-import
plan: 04
type: execute
wave: 1
depends_on: ["05-03"]
files_modified: [src/app.js, src/ui/pdf-import.js, src/ui/render.js, src/ui/dashboard.js, src/ui/transactions.js, src/ui/subscriptions.js, src/ui/debts.js, src/ui/assets.js]
autonomous: true
requirements: [PDF-02, PDF-03, PDF-05]
must_haves:
  truths:
    - "ReferenceErrors for fixedSpendRepository and toPence are resolved"
    - "Global app:refresh event mechanism is implemented and consumed by all UI modules"
    - "Dashboard and transaction tables refresh immediately after import via the global event"
    - "User stays in the import modal after success to see a summary and can upload another statement"
  artifacts:
    - path: "src/ui/pdf-import.js"
      provides: "Stabilized import UI with post-import summary"
    - path: "src/app.js"
      provides: "Global refreshApp utility"
---

<objective>
Stabilize the PDF import functionality by resolving critical ReferenceErrors, implementing a robust global app refresh mechanism, and improving the post-import user experience.

Purpose: Ensure the feature is robust, reliable, and provides clear feedback to the user without full page reloads.
Output: Fixed imports, global refresh event system, and refined import success flow.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pdf-bank-statement-import/05.1-CONTEXT.md
@src/ui/pdf-import.js
@src/app.js
@src/ui/render.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve ReferenceErrors and Missing Imports</name>
  <files>src/ui/pdf-import.js</files>
  <action>
    Fix critical ReferenceErrors in `src/ui/pdf-import.js`:
    - Add `fixedSpendRepository` to the named imports from `../db/repository.js`.
    - Add `toPence` to the named imports from `../utils/currency.js`.
    - Audit the file for any other missing imports (e.g., `variableSpendRepository`, `incomeRepository` which should already be there but verify).
  </action>
  <verify>Check that the file loads without errors in the browser and that clicking 'Extract Data' in manual mapping doesn't throw `toPence is not defined`.</verify>
  <done>ReferenceErrors are resolved and the import logic has access to all required repositories and utilities.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Global Refresh Mechanism</name>
  <files>src/app.js, src/ui/dashboard.js, src/ui/transactions.js, src/ui/subscriptions.js, src/ui/debts.js, src/ui/assets.js</files>
  <action>
    Implement a `CustomEvent` based refresh system:
    - In `src/app.js`, define `window.app.refreshApp = () => window.dispatchEvent(new CustomEvent('app:refresh'));`.
    - Update `window.app.renderAll` (used by legacy code) to also call `refreshApp()`.
    - In each UI module's `init` or equivalent, add a listener for `app:refresh`.
    - **Dashboard:** In `src/app.js`, ensure the `refreshDashboard` local function is called when `app:refresh` fires.
    - **Transactions/Subs/Debts/Assets:** In their respective `init()` methods, add: `window.addEventListener('app:refresh', () => this.render());`.
    - Ensure `dashboard.js` re-renders correctly when triggered.
  </action>
  <verify>Trigger `window.app.refreshApp()` from the console and observe all visible UI components (Dashboard cards, transaction rows) refreshing their data.</verify>
  <done>The app has a unified signal for data updates that ensures all tabs stay in sync.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Post-Import UX and Summary View</name>
  <files>src/ui/pdf-import.js, src/ui/render.js</files>
  <action>
    Modify `confirmImport()` in `src/ui/pdf-import.js` to improve feedback:
    - After the import loop finishes, call `window.app.refreshApp()`.
    - Instead of `window.templateUI.closeModal()`, call a new method `renderImportSummary(count, skippedCount)`.
    - `renderImportSummary` should display the counts in the modal body and provide two buttons in the footer:
      - **"Upload Another"**: Triggers `document.getElementById('pdfImportFile').click()`.
      - **"Finish"**: Calls `window.templateUI.closeModal()`.
    - Add a `toast(message, type)` function to `src/ui/render.js` for lightweight non-blocking feedback if needed (optional but recommended).
  </action>
  <verify>Perform a PDF import; verify the modal transitions to a summary screen and that "Upload Another" correctly restarts the flow.</verify>
  <done>The import workflow supports multiple consecutive uploads and provides clear success metrics.</done>
</task>

</tasks>

<verification>
- No ReferenceErrors in console during PDF processing.
- UI components refresh automatically after import confirmation.
- Modal lifecycle matches the new "Summary -> Finish/Another" workflow.
</verification>

<success_criteria>
- 100% resolution of reported runtime crashes in the PDF import module.
- Immediate visual consistency across Dashboard and Tables after data modification.
- Streamlined "bulk upload" experience for power users.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-bank-statement-import/05-04-SUMMARY.md`
</output>
