---
phase: 06-cloud-backup
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - index.html
  - src/app.js
  - vite.config.js
autonomous: false
requirements:
  - CLOUD-04

must_haves:
  truths:
    - "index.html loads GIS script from accounts.google.com before </body>"
    - "Cloud Backup section appears in the Settings panel with id=cloudBackupContainer"
    - "app.js imports cloudBackupUI and calls cloudBackupUI.init() during app startup"
    - "Workbox globPatterns in vite.config.js does NOT include the GIS CDN URL"
    - "Service worker does NOT cache accounts.google.com/gsi/client"
    - "User can see both provider cards in Settings tab when running the app"
  artifacts:
    - path: "index.html"
      provides: "GIS script tag + cloudBackupContainer div in settings panel"
      contains: "accounts.google.com/gsi/client"
    - path: "src/app.js"
      provides: "cloudBackupUI import and init call"
      contains: "cloudBackupUI"
    - path: "vite.config.js"
      provides: "Workbox config that excludes external CDN scripts from precache"
  key_links:
    - from: "index.html"
      to: "src/ui/cloud-backup.js"
      via: "cloudBackupUI.render() writes into #cloudBackupContainer"
      pattern: "cloudBackupContainer"
    - from: "src/app.js"
      to: "src/ui/cloud-backup.js"
      via: "import cloudBackupUI + init()"
      pattern: "cloudBackupUI\\.init"
---

<objective>
Wire the cloud backup module into the app: add the GIS script to index.html, add the Cloud Backup section to the Settings panel, import and initialise cloudBackupUI in app.js, and verify the Workbox config does not precache the GIS CDN script.

Purpose: Make the cloud backup UI visible and functional in the running app.
Output: Updated index.html, src/app.js, vite.config.js + human checkpoint verifying the feature works end-to-end.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cloud-backup/06-CONTEXT.md
@.planning/phases/06-cloud-backup/06-02-SUMMARY.md

<interfaces>
<!-- What plan 02 created -->
From src/ui/cloud-backup.js:
```javascript
export const cloudBackupUI = {
  async init() { /* initGoogleDrive() + window.cloudBackupUI = this + this.render() */ },
  render() { /* writes into #cloudBackupContainer */ },
  connect(providerId) {},
  backup(providerId) {},
  restore(providerId) {},
  disconnect(providerId) {}
};
```

From index.html (current Settings panel structure — attach Cloud Backup section after Install App block):
  The settings panel is <div class="tab-panel" data-panel="settings">
  It ends with the Install App bordered section before closing </div>
  Add the Cloud Backup section after Install App section, before the closing </div> of the settings tab-panel.

Cloud Backup HTML to add:
```html
<div style="margin-top:20px; padding-top:20px; border-top:1px solid var(--border)">
  <h3 style="font-size:.9rem;margin-bottom:8px">Cloud Backup</h3>
  <div class="hint" style="margin-bottom:12px">Back up and restore your data via Google Drive or OneDrive. Manual backup only — no automatic sync.</div>
  <div id="cloudBackupContainer">
    <!-- Rendered by cloud-backup.js -->
  </div>
</div>
```

GIS script to add (just before </body>):
```html
<script src="https://accounts.google.com/gsi/client" async defer></script>
```

From vite.config.js (check current globPatterns — ensure only local assets):
  workbox.globPatterns should be: ['**/*.{js,css,html,ico,png,svg,webp,woff,woff2}']
  Must NOT include any external URL patterns.
  The GIS script URL is external — it will NOT be precached automatically (not a local asset).
  Verify the config is correct — if globPatterns already covers only local assets, no change needed.
  Add a comment confirming GIS is intentionally excluded if not already noted.

From src/app.js (current imports — add cloudBackupUI after pdfImportUI):
  import { pdfImportUI } from './ui/pdf-import';
  // Add:
  import { cloudBackupUI } from './ui/cloud-backup.js';

In app.js init/startup section, add cloudBackupUI.init() call after pdfImportUI.init() or backupUI.init().
Also: in the settings tab switch handler, add cloudBackupUI.render() so the card state refreshes when user navigates to Settings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire index.html, app.js, and verify vite.config.js</name>
  <files>index.html, src/app.js, vite.config.js</files>
  <action>
1. Open index.html. In the Settings tab-panel (data-panel="settings"), after the Install App bordered section and before the closing tag of the settings panel, add the Cloud Backup section HTML (see interfaces above). Do not move or alter existing sections.

2. In index.html, add the GIS script tag just before the closing body tag:
   <script src="https://accounts.google.com/gsi/client" async defer></script>
   This loads async — the GIS script will be available before user interacts with Connect button.

3. Open vite.config.js. Find the Workbox configuration. Confirm globPatterns only covers local assets.
   If there is no explicit exclude needed, add a comment near globPatterns:
   // Note: GIS (accounts.google.com/gsi/client) is intentionally NOT precached.
   // Cloud backup features require network — gracefully disabled offline by navigator.onLine checks.
   If runtimeCaching is configured, do NOT add a route for accounts.google.com.

4. Open src/app.js. Add import:
   import { cloudBackupUI } from './ui/cloud-backup.js';

5. In the app initialisation sequence in app.js, add:
   await cloudBackupUI.init();
   Place it after the existing backupUI.init() call (or after pdfImportUI init if that exists).

6. Find the settings tab switch handler in app.js (look for code that calls render functions when the settings tab is activated). Add:
   cloudBackupUI.render();
   alongside the other render calls for the settings tab. This ensures the card reflects current state whenever user opens Settings.

7. Confirm that cloudBackupUI.init() sets window.cloudBackupUI (per plan 02). If it does, do not add a duplicate window assignment in app.js.

Run build to check for errors:
   npm run build
   If build errors appear related to cloud-backup, fix import paths or missing exports.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build succeeds with no errors. index.html contains id="cloudBackupContainer" and the GIS script tag. src/app.js imports cloudBackupUI and calls init(). vite.config.js globPatterns covers only local assets with comment about GIS exclusion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cloud backup UI end-to-end in browser</name>
  <files>none (human verification only)</files>
  <action>
Human verification checkpoint. Run the app and confirm the cloud backup UI works correctly:
1. Run: npm run dev
2. Navigate to Settings tab and scroll to the Cloud Backup section at the bottom.
3. Confirm two cards are visible (Google Drive, OneDrive) each with a Connect button.
4. If VITE_GOOGLE_CLIENT_ID is set in .env: test the full connect/backup/restore/disconnect flow for Google Drive.
5. If no credentials: confirm cards render without console errors on page load.
6. Confirm connected state persists after page reload (localStorage preserved).
7. Confirm GIS script is NOT served from service worker cache (devtools Network tab).
  </action>
  <what-built>
    Complete cloud backup feature:
    - Google Drive and OneDrive provider cards in Settings tab
    - Connect / Disconnect flows with confirmation modals
    - Backup Now and Restore with loading states and inline feedback
    - Connected state (email, last backup timestamp) persists across page reloads
    - Only one provider connectable at a time
    - Backup/Restore disabled when offline
    Note: To test with real OAuth you need VITE_GOOGLE_CLIENT_ID and VITE_ONEDRIVE_CLIENT_ID set in .env. For UI-only verification (no real auth), the cards should render correctly and buttons should be visible.
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open the app in browser. Navigate to Settings tab (last tab: gear icon).
    3. Scroll to the bottom — you should see a "Cloud Backup" section with two cards side by side: "Google Drive" and "OneDrive". Each card has a "Connect" button.
    4. If you have VITE_GOOGLE_CLIENT_ID set in .env: click "Connect" on Google Drive card. Google OAuth popup should open. After auth, card should update to show your email and "Last backup: Never" with Backup Now / Restore / Disconnect buttons.
    5. Click "Backup Now" — should show inline loading then "Backed up just now" (or auth-related error if client ID not registered for this origin).
    6. Reload the page — navigate back to Settings — cloud backup card should still show as connected with your email.
    7. Click "Disconnect" — confirmation modal should appear — confirm — card reverts to unconnected state.
    8. Without .env credentials: verify that the cards render correctly, Connect buttons are present, and no console errors on page load.
    9. Check browser devtools: Network tab — confirm GIS script is loaded from network, not served from service worker cache.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual browser verification required for UI cards and OAuth flows</automated>
  </verify>
  <done>Both provider cards visible in Settings tab, Connect buttons present, no console errors on page load, confirmed by user.</done>
  <resume-signal>Type "approved" if cards render correctly and UI behaves as described. Or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- npm run build exits with code 0
- index.html has GIS script tag before closing body tag
- index.html has id="cloudBackupContainer" in settings panel
- src/app.js imports cloudBackupUI and calls init()
- vite.config.js Workbox globPatterns targets only local assets
- Settings tab shows two cloud provider cards (visual checkpoint)
- Connected state persists after page reload (localStorage)
- Confirmation modals fire for disconnect and restore
</verification>

<success_criteria>
- Build passes
- Both provider cards visible in Settings (Google Drive, OneDrive)
- Connect/Disconnect/Backup/Restore flows work as designed (confirmed via checkpoint)
- localStorage correctly stores cloud_provider, cloud_account_email, cloud_last_backup
- GIS script not served from service worker cache
- Phase 6 requirements CLOUD-01 through CLOUD-04 satisfied
</success_criteria>

<output>
After completion, create .planning/phases/06-cloud-backup/06-03-SUMMARY.md
</output>
