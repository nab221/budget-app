---
phase: 07-milestone-v1.0-polish-and-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/dashboard.js, src/utils/onedrive.js, src/utils/storage.js, src/utils/google-drive.js, src/ui/cloud-backup.js, src/db/schema.js, src/db/backup.js, src/ui/backup.js]
autonomous: true
requirements: [FOUND-04, CLOUD-04, DATA-01, DATA-02, PWA-01, PWA-02, PWA-04, CHART-01, CHART-02, CLOUD-01, CLOUD-02, CLOUD-03]
must_haves:
  truths:
    - "Dashboard Net Worth card renders with 'Risk' badge using safe DOM methods (no innerHTML for dynamic content)"
    - "OneDrive module no longer contains getOneDriveUserEmail export"
    - "Database schema includes clear documentation for version 3 skip"
    - "Cloud backup and local backup both use unified DB import logic"
  artifacts:
    - path: "src/utils/storage.js"
      provides: "Shared storage keys"
      contains: "CLOUD_LAST_BACKUP_KEY"
    - path: "src/db/backup.js"
      provides: "Unified database import transaction"
      exports: ["importBackupData"]
  key_links:
    - from: "src/ui/cloud-backup.js"
      to: "src/db/backup.js"
      via: "importBackupData call"
    - from: "src/ui/backup.js"
      to: "src/db/backup.js"
      via: "importBackupData call"
---

<objective>
Address technical debt identified during the v1.0 milestone audit to improve maintainability, security, and documentation before final release.

Purpose: Harden UI rendering, remove dead code, consolidate constants, and unify critical data restoration logic.
Output: Refactored UI and utility modules with improved consistency and reduced maintenance risk.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
From src/db/schema.js:
```javascript
export const db = new Dexie('BudgetConsoleDB');
```

From src/utils/storage.js (existing):
```javascript
export async function ensurePersistence();
export async function checkPersistence();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden UI Rendering & Schema Documentation</name>
  <files>src/ui/dashboard.js, src/db/schema.js</files>
  <action>
    1. In `src/ui/dashboard.js`:
       - Refactor `renderDashboard` to replace `container.innerHTML = cards.map(...)` with a safer DOM construction pattern (e.g., `document.createElement`).
       - Remove `labelIsHtml: true` from `netWorthLabel` card. Instead, update the card object to support an optional `badge` property (e.g., `{ label: 'Net Worth', badge: { text: 'Risk', ... } }`).
       - Ensure `textContent` is used for all labels and values to maintain XSS protection (FOUND-04).
    2. In `src/db/schema.js`:
       - Add a clear documentation comment between `db.version(2)` and `db.version(4)` explaining that version 3 was skipped during development (audit finding).
  </action>
  <verify>
    - Dashboard still displays all 9 cards correctly.
    - Net Worth badge appears when persistence is denied.
    - No `innerHTML` usage for dynamic card data in `src/ui/dashboard.js`.
    - `grep "version(3)" src/db/schema.js` (or similar) shows documentation comment.
  </verify>
  <done>UI rendering is hardened against XSS and schema jump is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Dead Code Removal & Constant Consolidation</name>
  <files>src/utils/onedrive.js, src/utils/storage.js, src/utils/google-drive.js, src/ui/cloud-backup.js</files>
  <action>
    1. In `src/utils/onedrive.js`:
       - Delete the exported `getOneDriveUserEmail` function (orphaned export).
    2. In `src/utils/storage.js`:
       - Add and export `const CLOUD_LAST_BACKUP_KEY = 'cloud_last_backup';`.
    3. In `src/utils/google-drive.js`, `src/utils/onedrive.js`, and `src/ui/cloud-backup.js`:
       - Import `CLOUD_LAST_BACKUP_KEY` from `src/utils/storage.js`.
       - Remove local definitions of the constant.
       - Ensure all usages of the key reference the imported constant.
  </action>
  <verify>
    - `grep -r "getOneDriveUserEmail" src/` returns no results.
    - `grep "const CLOUD_LAST_BACKUP_KEY" src/utils/storage.js` confirms export.
    - App still correctly tracks and displays the last cloud backup date in Settings.
  </verify>
  <done>Dead code removed and shared constants consolidated to a single source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: Unify Restore Logic</name>
  <files>src/db/backup.js, src/ui/backup.js, src/ui/cloud-backup.js</files>
  <action>
    1. Create `src/db/backup.js`:
       - Implement and export `importBackupData(data)` which wraps the Dexie transaction logic currently duplicated in `backup.js` and `cloud-backup.js`.
       - The function should handle `db.transaction('rw', db.tables, ...)` and `bulkAdd` operations.
    2. In `src/ui/backup.js`:
       - Import `importBackupData` from `src/db/backup.js`.
       - Refactor `executeImport(content)` to use this shared function for the database transaction part. Keep decryption logic in `backup.js`.
    3. In `src/ui/cloud-backup.js`:
       - Import `importBackupData` from `src/db/backup.js`.
       - Refactor `importData(parsed)` to use this shared function.
  </action>
  <verify>
    - Local JSON import (encrypted and unencrypted) still works correctly.
    - Cloud Restore (Google Drive/OneDrive) still works correctly.
    - Code duplication between the two modules is eliminated.
  </verify>
  <done>Database restoration logic is unified into a single, maintainable utility function.</done>
</task>

</tasks>

<verification>
Check all 5 items from the prompt are addressed in the code.
Ensure the app still functions correctly (Dashboard renders, Backups work).
</verification>

<success_criteria>
- No `innerHTML` on dashboard for dynamic labels.
- `getOneDriveUserEmail` is gone.
- `CLOUD_LAST_BACKUP_KEY` is central.
- `importBackupData` is the single point for DB restoration.
- Schema documentation is updated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-milestone-v1.0-polish-and-tech-debt/07-01-SUMMARY.md`
</output>
