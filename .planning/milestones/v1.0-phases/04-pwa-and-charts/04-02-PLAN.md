---
phase: 04-pwa-and-charts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/ui/charts.js, src/ui/dashboard.js, src/db/repository.js, css/main.css, index.html]
autonomous: true
requirements: [CHART-01]
must_haves:
  truths:
    - "Spending trends chart uses a color-blind safe palette (Blue/Orange/Yellow)."
    - "Trend visualization uses a Stacked Area Chart for 12-month spending."
    - "Chart includes floating tooltips for exact £ amounts."
  artifacts:
    - path: "src/ui/charts.js"
      provides: "Configured Chart.js instance with Okabe-Ito palette"
    - path: "src/db/repository.js"
      provides: "getSpendingTrends data aggregator"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "src/ui/charts.js"
      via: "component call"
      pattern: "renderTrendsChart"
---

<objective>
Integrate Chart.js and implement the Stacked Area spending trends chart on the dashboard using a color-blind safe palette.

Purpose: Provide accessible, visual financial history for the user.
Output: Integrated Chart.js, historical data aggregation, and accessible dashboard chart.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pwa-and-charts/04-CONTEXT.md
@src/db/repository.js
@src/ui/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart.js Integration & Data Aggregation</name>
  <files>package.json, src/db/repository.js, src/ui/charts.js</files>
  <action>
    1. Install `chart.js` via npm.
    2. Implement `getSpendingTrends(targetMonth)` in `src/db/repository.js`:
       - Aggregate Income, Fixed, and Variable totals for the last 12 months.
    3. Create `src/ui/charts.js` and define the **Okabe-Ito palette** constants:
       - Income: Blue (#0072B2)
       - Fixed: Orange (#D55E00)
       - Variable: Yellow (#F0E442)
    4. Implement `renderTrendsChart(canvasId, data)` using a `line` chart with `fill: true` and `stacked: true` on the Y-axis.
  </action>
  <verify>
    Verify `getSpendingTrends` returns 12 months of accurate data via node test script.
  </verify>
  <done>
    Chart infrastructure and data aggregation layer are ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Chart UI</name>
  <files>src/ui/dashboard.js, index.html, css/main.css</files>
  <action>
    1. Update `index.html` to add a responsive chart container above the summary grid.
    2. Update `css/main.css` to ensure charts resize correctly on mobile (maintain aspect ratio).
    3. Update `src/ui/dashboard.js` to call `renderTrendsChart` during the dashboard render cycle.
    4. Configure floating tooltips in `charts.js` to show exact £ amounts on hover/tap.
  </action>
  <verify>
    Open dashboard; verify the Stacked Area chart appears with correct colors and tooltips.
  </verify>
  <done>
    Spending trends are visually represented on the main dashboard.
  </done>
</task>

</tasks>

<verification>
- Chart colors match the Okabe-Ito scale.
- Interaction (hover/tap) shows accurate currency values.
- Chart is responsive on mobile screens.
</verification>

<success_criteria>
- Dashboard provides a clear 12-month visual trend.
- Accessible colors are used for all datasets.
- Stacking logic correctly reflects total spending vs income.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-and-charts/04-02-SUMMARY.md`
</output>
