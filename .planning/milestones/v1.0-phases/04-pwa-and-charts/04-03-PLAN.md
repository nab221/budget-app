---
phase: 04-pwa-and-charts
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified: [src/ui/payoff.js, src/ui/dashboard.js, src/app.js, src/ui/pwa-ux.js]
autonomous: true
requirements: [CHART-02, DATA-03, FOUND-03]
must_haves:
  truths:
    - "Debt payoff chart initial view is focused on the next 24 months."
    - "Export reminder appears only if the last backup was >7 days ago."
    - "Dashboard shows a red 'Risk' badge if storage persistence is denied."
  artifacts:
    - path: "src/ui/charts.js"
      provides: "renderDebtPayoffChart component"
    - path: "src/ui/pwa-ux.js"
      provides: "Storage and export reminder logic"
---

<objective>
Implement the debt payoff timeline chart and finalize PWA UX with smart reminders and storage safety indicators.

Purpose: Provide detailed debt projections and ensure data safety via proactive UI feedback.
Output: Debt payoff timeline chart, smart backup reminders, and storage risk indicators.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pwa-and-charts/04-CONTEXT.md
@src/ui/payoff.js
@src/ui/dashboard.js
@src/utils/finance.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debt Payoff Timeline Chart</name>
  <files>src/ui/charts.js, src/ui/payoff.js, index.html</files>
  <action>
    1. Implement `renderDebtPayoffChart(canvasId, projectionData)` in `src/ui/charts.js`.
    2. Configure the chart to:
       - Show line series for each debt balance over time.
       - Use the Okabe-Ito palette for lines.
       - Initial X-axis scale focus on the **next 24 months**.
    3. Update `src/ui/payoff.js` to calculate the balance-over-time series from `simulatePayoff` and call the chart renderer.
    4. Add `<canvas id="payoffChart">` to the Payoff Planner UI in `index.html`.
  </action>
  <verify>
    Verify payoff chart reflects changes in the 'Extra Monthly Payment' input immediately.
  </verify>
  <done>
    Debt payoff timeline visualized with 24-month focus.
  </done>
</task>

<task type="auto">
  <name>Task 2: Data Safety & Smart Reminders</name>
  <files>src/ui/pwa-ux.js, src/ui/dashboard.js, src/app.js</files>
  <action>
    1. Implement `checkStoragePersistence()` in `src/ui/pwa-ux.js`:
       - If `navigator.storage.persist()` is false, signal the dashboard.
    2. Implement `checkExportReminder()` in `src/ui/pwa-ux.js`:
       - Compare `last_export_timestamp` in localStorage with `Date.now()`.
       - If > 7 days, show a reminder message in the header or Settings.
    3. Update `src/ui/dashboard.js` to display a **red "Risk" badge** near the Net Worth card if persistence is not guaranteed.
    4. Add "Ready for Offline" status text to the Settings tab once precaching is confirmed.
  </action>
  <verify>
    Manual verification: Mock a 7-day old timestamp in localStorage and check for the reminder.
  </verify>
  <done>
    User is proactively warned about data safety and guided toward regular backups.
  </done>
</task>

</tasks>

<verification>
- Payoff chart shows balances decreasing to zero.
- "Risk" badge appears only when storage persistence is denied.
- Export reminder appears only when backup is overdue.
</verification>

<success_criteria>
- Long-term debt projections are easily readable on mobile.
- Data safety feedback is clear but non-obtrusive.
- Phase 4 requirements for Android/Windows are fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-and-charts/04-03-SUMMARY.md`
</output>
