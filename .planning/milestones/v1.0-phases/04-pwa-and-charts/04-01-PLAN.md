---
phase: 04-pwa-and-charts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.js, src/app.js, public/icons/icon-192.png, public/icons/icon-512.png, src/ui/pwa-ux.js]
autonomous: true
requirements: [PWA-01, PWA-02, PWA-04]
must_haves:
  truths:
    - "App is installable via a manual button in the Settings tab on Android and Windows."
    - "App works fully offline after first load using service worker precaching."
    - "A subtle 'Update Available' bar appears at the bottom when a new version is detected."
  artifacts:
    - path: "vite.config.js"
      provides: "Vite PWA configuration with manifest"
    - path: "src/ui/pwa-ux.js"
      provides: "Manual installation and update notification logic"
  key_links:
    - from: "src/app.js"
      to: "src/ui/pwa-ux.js"
      via: "init call"
      pattern: "initPWA"
---

<objective>
Setup the PWA foundation using `vite-plugin-pwa` and implement the manual installation and update workflows for Android and Windows.

Purpose: Enable offline-first access and a controlled installation experience.
Output: Configured PWA infrastructure, high-visibility icons, and update notification bar.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pwa-and-charts/04-CONTEXT.md
@.planning/phases/04-pwa-and-charts/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PWA Infrastructure & Manifest</name>
  <files>package.json, vite.config.js, public/icons/icon-192.png, public/icons/icon-512.png</files>
  <action>
    1. Install `vite-plugin-pwa` as a dev dependency.
    2. Configure `vite.config.js` with `VitePWA`:
       - `registerType: 'prompt'` (for the manual update workflow).
       - `manifest`:
         - name: "Budget Console"
         - short_name: "Budget"
         - background_color: "#0b1120"
         - theme_color: "#0b1120"
         - icons: Green symbol on a white circle (192x192, 512x512).
       - `workbox`: `globPatterns: ['**/*.{js,css,html,ico,png,svg}']` for full offline precaching.
    3. Place the green-on-white high-visibility icons in `public/icons/`.
  </action>
  <verify>
    Run `npm run build` and check that `dist/sw.js` and `dist/manifest.webmanifest` are generated.
  </verify>
  <done>
    PWA infrastructure is configured with the correct branding and caching strategy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update UI & Manual Installation Logic</name>
  <files>src/ui/pwa-ux.js, src/app.js, index.html, css/main.css</files>
  <action>
    1. Create `src/ui/pwa-ux.js` to manage PWA state:
       - Use `registerSW` with `onNeedRefresh` to show a subtle bar at the bottom (`#update-bar`).
       - Intercept `beforeinstallprompt` event, prevent default, and store it.
       - Export `initPWA()` to handle these listeners.
       - Implement `installApp()` which triggers the stored prompt.
    2. Update `index.html`:
       - Add a container for the bottom update bar.
       - Add an "Install App" button in the **Settings** tab.
    3. Update `src/app.js`:
       - Call `initPWA()` during initialization.
       - Connect the "Install App" button to `installApp()`.
    4. Update `css/main.css` with styles for the bottom update bar.
  </action>
  <verify>
    Simulate 'beforeinstallprompt' in Chrome DevTools and verify the "Install App" button appears and functions.
  </verify>
  <done>
    Manual installation and update notifications are fully functional.
  </done>
</task>

</tasks>

<verification>
- App loads and functions with no network (checked via DevTools).
- "Install App" button triggers the browser installation prompt.
- Update bar appears when the service worker is updated.
</verification>

<success_criteria>
- Manifest and icons match user preferences (Green on White).
- No automatic browser prompts; install is user-triggered.
- Version updates are handled gracefully via the bottom bar.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-and-charts/04-01-SUMMARY.md`
</output>
