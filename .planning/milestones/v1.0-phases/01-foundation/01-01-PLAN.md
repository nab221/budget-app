---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - index.html
  - .gitignore
  - src/app.js
  - src/utils/currency.js
  - src/utils/currency.test.js
  - src/db/schema.js
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "Calculations like £0.10 + £0.20 result in exactly £0.30 (no float errors)"
    - "The database initializes without errors on first load"
    - "Tests pass for currency conversion and formatting"
    - "A versionchange handler is present to handle IndexedDB schema updates"
  artifacts:
    - path: "src/utils/currency.js"
      provides: "Pence-integer arithmetic helpers"
    - path: "src/db/schema.js"
      provides: "Dexie.js database instance and stores"
    - path: "src/app.js"
      provides: "Main entry point skeleton"
  key_links:
    - from: "src/db/schema.js"
      to: "Dexie"
      via: "import"
      pattern: "import Dexie from 'dexie'"
---

<objective>
Initialize the modular project structure, implement the core financial arithmetic using pence-integers, and set up the Dexie.js database layer.

Purpose: Ensures a stable, bug-free foundation for all financial data and establishes the modern ESM build environment.
Output: Project skeleton, tested currency utilities, and IndexedDB schema.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@budget-app.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Initialization & Tooling</name>
  <files>package.json, index.html, .gitignore, src/app.js</files>
  <action>
    Initialize a new Vite project. 
    1. Create `package.json` with type: "module".
    2. Install dependencies: `dexie`, `dompurify`.
    3. Install devDependencies: `vite`, `vitest`.
    4. Create a basic `index.html` as the ESM entry point.
    5. Create a skeleton `src/app.js`.
    6. Setup `.gitignore` for node_modules and dist.
  </action>
  <verify>Run `npm list` and check for vite/vitest/dexie/dompurify</verify>
  <done>Project environment ready for development.</done>
</task>

<task type="auto">
  <name>Task 2: Money Utility (TDD)</name>
  <files>src/utils/currency.js, src/utils/currency.test.js</files>
  <action>
    Implement pence-integer arithmetic utilities following Pattern 1 from RESEARCH.md.
    - `toPence(pounds)`: Convert string/float to integer pence.
    - `fromPence(pence)`: Convert integer pence to float (for display).
    - `formatGBP(pence)`: Format integer pence as currency string using `Intl.NumberFormat`.
    - Avoid floating point math errors (e.g., use `Math.round` when converting to pence).
    - Create `src/utils/currency.test.js` and write tests for rounding edge cases.
  </action>
  <verify>Run `npx vitest run src/utils/currency.test.js` and ensure all tests pass.</verify>
  <done>Financial arithmetic is reliable and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Database Schema & Setup</name>
  <files>src/db/schema.js</files>
  <action>
    Configure Dexie.js database schema based on Pattern 2 from RESEARCH.md and the legacy `budget-app.html` structure.
    - Initialize `BudgetConsoleDB`.
    - Define version 1 stores: income, fixedSpends, variableSpends, subscriptions, debts, statements, assets, categories.
    - Use clear primary keys and indexes for date-based filtering.
    - Add a `db.on('versionchange')` handler to ensure tabs refresh when the schema updates.
    - Add a `db.on('blocked')` handler to alert users if multiple tabs prevent an upgrade (as per Success Criteria 4).
  </action>
  <verify>Import `db` in a script and check `db.isOpen()` in browser console (or via simple test script).</verify>
  <done>Data layer is ready for feature implementation.</done>
</task>

</tasks>

<verification>
Check that `npm run dev` starts without errors and `npm run test` passes.
</verification>

<success_criteria>
- Project structure follows RESEARCH.md recommendations.
- Currency tests cover floating point pitfalls.
- Dexie schema matches legacy requirements but in a modular file.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
