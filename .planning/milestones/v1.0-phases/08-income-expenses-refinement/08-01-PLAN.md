---
phase: 08-income-expenses-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js, index.html, src/app.js, src/ui/expenses.js, src/ui/transactions.js, src/ui/subscriptions.js]
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04]

must_haves:
  truths:
    - "User can view a single 'Expenses' tab containing both Recurrent and One-off sub-views"
    - "Recurrent expenses are visually split into Essential and Non-essential groups"
    - "Recurrent items show 'Payment X of Y' progress where applicable"
    - "User can mark all recurrent items as paid with a single action"
  artifacts:
    - path: "src/ui/expenses.js"
      provides: "Consolidated expenses UI logic"
    - path: "src/db/schema.js"
      provides: "Dexie version 5 with recurrentExpenses and oneOffExpenses"
  key_links:
    - from: "src/app.js"
      to: "src/ui/expenses.js"
      via: "import and tab click handler"
---

<objective>
Consolidate the fragmented expense tracking (Fixed, Variable, Subscriptions) into a unified "Expenses" tab with "Recurrent" and "One-off" sub-views. This involves a Dexie schema upgrade and a complete UI overhaul of the spending entry and listing components.

Purpose: Simplify the user mental model by grouping transactions by their frequency/nature rather than their technical implementation.
Output: Unified Expenses UI and updated data layer.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-income-expenses-refinement/08-CONTEXT.md
@.planning/phases/08-income-expenses-refinement/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dexie Schema Version 5 & Repository Migration</name>
  <files>src/db/schema.js, src/db/repository.js</files>
  <action>
    Update the database layer to support the new consolidated expense model.
    
    1. In src/db/schema.js:
       - Add db.version(5).stores({ ... })
       - New tables:
         - `recurrentExpenses`: '++id, date, categoryId, label, amount, status, frequency, nextDate, isEssential, cycleTotal, cycleCurrent, endDate'
         - `oneOffExpenses`: '++id, date, categoryId, note, amount'
       - Omit `fixedSpends`, `variableSpends`, and `subscriptions` from the version 5 stores to deprecate them.
    
    2. In src/db/repository.js:
       - Create `recurrentExpenseRepository` and `oneOffExpenseRepository` using the base repository factory.
       - Implement `getByMonth(monthStr)` for both new repositories.
       - Update `categoryRepository.isCategoryInUse` to check the new tables.
       - Update `getSpendingTrends` and `getDashboardData` to fetch and aggregate data from `recurrentExpenses` and `oneOffExpenses` instead of the deprecated tables.
       - Note: For dashboard cards, "Fixed" should now sum `recurrentExpenses` and "Variable" should sum `oneOffExpenses`. "Subscriptions" card can be removed or kept as a subset of recurrent.
  </action>
  <verify>
    npm test (if tests exist) or run in browser console: `db.version` should be 5 and `db.recurrentExpenses` should exist.
  </verify>
  <done>Database schema and data access layer updated to reflect the consolidated expense model.</done>
</task>

<task type="auto">
  <name>Task 2: Unified Expenses UI (Tabs, Forms, Tables)</name>
  <files>index.html, src/app.js, src/ui/expenses.js, src/ui/transactions.js, src/ui/subscriptions.js</files>
  <action>
    Implement the new "Expenses" UI and remove legacy components.
    
    1. Update index.html:
       - Replace the "Fixed Spends", "Variable", and "Subscriptions" tabs with a single "Expenses" tab.
       - Create a new tab panel for "Expenses" with sub-navigation for "Recurrent" and "One-off".
       - Move/Update the forms:
         - Recurrent Form: Needs `isEssential` (checkbox), `frequency` (select), `cycleTotal` (number), `endDate` (date).
         - One-off Form: Same as previous Variable form.
    
    2. Create src/ui/expenses.js:
       - Implement `expensesUI` object to handle the logic for both sub-views.
       - Implement `renderRecurrent()`: Split items into Essential and Non-essential sections.
       - Implement `renderOneOff()`: Simple list similar to Variable.
       - Show totals for Essential vs Non-essential.
    
    3. Update src/app.js:
       - Import `expensesUI`.
       - Update the tab click handler to call `expensesUI.render()` when the "Expenses" tab is selected.
       - Remove references to `subscriptionUI`.
    
    4. Refactor src/ui/transactions.js:
       - Remove Fixed and Variable logic.
       - Keep only Income logic (rename to `src/ui/income.js` if preferred, but updating imports in app.js is required).
    
    5. Delete src/ui/subscriptions.js (deprecated).
  </action>
  <verify>
    Open app: "Expenses" tab should show "Recurrent" and "One-off" views. Adding items to both should work and they should persist in the new tables.
  </verify>
  <done>UI consolidated into a single Expenses tab with functional Recurrent and One-off sub-views.</done>
</task>

<task type="auto">
  <name>Task 3: Recurrent Logic & Refinement</name>
  <files>src/ui/expenses.js, src/db/repository.js</files>
  <action>
    Implement advanced recurrent features and labels.
    
    1. In src/ui/expenses.js:
       - Add labels/badges for cancelable (Non-essential) items.
       - Implement "Payment X of Y" display for items where `cycleTotal > 0`.
       - Add a "Mark Finished" visual state for items where `cycleCurrent >= cycleTotal`.
       - Implement the "Mark all as paid" button:
         - Uses a bulk update to set all 'pending' items to 'paid' for the current month.
         - For items with cycles, increment `cycleCurrent`.
    
    2. In src/db/repository.js (recurrentExpenseRepository):
       - Implement `markAllAsPaid(monthStr)` using Dexie transactions.
  </action>
  <verify>
    Create a recurrent item with 10 cycles. Mark it paid and verify it shows "Payment 1 of 10". Clicking "Mark all as paid" should update all pending items.
  </verify>
  <done>Recurrent items support cycle tracking, cancellation labeling, and bulk payment actions.</done>
</task>

</tasks>

<verification>
Checklist:
- [ ] Expenses tab exists and replaces legacy tabs.
- [ ] Recurrent vs One-off sub-tabs work correctly.
- [ ] Essential/Non-essential totals are displayed.
- [ ] "Payment X of Y" shows correctly for cycled items.
- [ ] Bulk "Mark all as paid" works.
- [ ] Dashboard cards reflect consolidated totals.
</verification>

<success_criteria>
User can manage all their spending through two unified views (Recurrent and One-off) with clear visibility of essential vs non-essential costs and payment progress for cycled items.
</success_criteria>

<output>
After completion, create `.planning/phases/08-income-expenses-refinement/08-01-SUMMARY.md`
</output>
