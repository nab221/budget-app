---
phase: 08-income-expenses-refinement
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [src/db/repository.js, src/ui/income.js, src/ui/transactions.js, src/ui/targets.js, src/ui/dashboard.js, src/app.js]
autonomous: true
requirements: [INC-05]

must_haves:
  truths:
    - "Income tab displays the current month plus the previous 2 months of history"
    - "Income list shows aggregated totals per month"
    - "Budget targets are managed by 'Recurrent' and 'One-off' buckets rather than categories"
    - "Dashboard progress bars reflect bucket-based targets"
  artifacts:
    - path: "src/ui/income.js"
      provides: "3-month income history rendering"
  key_links:
    - from: "src/db/repository.js"
      to: "db.income"
      via: "between() query for sliding window"
---

<objective>
Enhance financial trend visibility by implementing a 3-month income history sliding window and transitioning budget targets from a category-based model to a bucket-based model (Recurrent vs One-off).

Purpose: Provide users with a broader view of their income stability and simplify budget goal setting.
Output: Extended Income view and bucket-based Budget Targets.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-income-expenses-refinement/08-CONTEXT.md
@.planning/phases/08-income-expenses-refinement/08-RESEARCH.md
@.planning/phases/08-income-expenses-refinement/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Income 3-month Sliding Window</name>
  <files>src/db/repository.js, src/ui/income.js, src/ui/transactions.js</files>
  <action>
    Implement the 3-month income history view.
    
    1. In src/db/repository.js (incomeRepository):
       - Implement `getThreeMonthHistory(targetMonthStr)`:
         - Calculate the start date (1st of the month 2 months prior).
         - Use `db.income.where('date').between(start, end, true, true)` to fetch all records in the window.
    
    2. In src/ui/income.js (formerly src/ui/transactions.js):
       - Update `renderIncome()` to use `getThreeMonthHistory`.
       - Group results by month (e.g., "March 2026", "February 2026", "January 2026").
       - Render the groups with per-month totals.
       - Ensure the list remains at the bottom of the Income panel.
  </action>
  <verify>
    Select March in the month picker. The income list should show any income from March, February, and January.
  </verify>
  <done>Income tab displays a 3-month history window with aggregated monthly totals.</done>
</task>

<task type="auto">
  <name>Task 2: Bucket-based Budget Targets</name>
  <files>src/db/repository.js, src/ui/targets.js, src/ui/dashboard.js</files>
  <action>
    Transition budget targets from categories to buckets.
    
    1. In src/db/repository.js:
       - Update `targetRepository`:
         - Implement `getByBucket(bucketName)`.
         - Remove category-specific lookup methods.
       - Update `getDashboardData`:
         - Aggregate spending by bucket ('recurrent' vs 'one-off').
         - Include bucket-based progress data in the return object.
    
    2. In src/ui/targets.js:
       - Overhaul `renderTargetSettings()` to show only two inputs: "Recurrent Monthly Target" and "One-off Monthly Target".
       - Update event listeners to save by bucket.
    
    3. In src/ui/dashboard.js:
       - Update `renderProgressBars()` to display only two bars: Recurrent and One-off, using the new bucket-based targets.
  </action>
  <verify>
    Go to Settings -> Budget Targets. Set a target for Recurrent and One-off. Verify dashboard shows these two progress bars with correct actual spending.
  </verify>
  <done>Budget targets simplified to bucket-level (Recurrent vs One-off) across settings and dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Final Polish & Phase 8 Verification</name>
  <files>src/ui/expenses.js, src/ui/income.js, css/main.css</files>
  <action>
    Perform final UX refinements and verify all phase requirements.
    
    1. UI Polish:
       - Ensure consistent styling for 'Essential' vs 'Non-essential' badges.
       - Check responsive layout for the new Expenses tab.
       - Add tooltips or hints explaining the Recurrent vs One-off split.
    
    2. Verification:
       - Walk through all Phase 8 success criteria in ROADMAP.md.
       - INC-05: 3-month history? (Pass/Fail)
       - EXP-01: Consolidated tabs? (Pass/Fail)
       - EXP-02: Essential/Non-essential flags? (Pass/Fail)
       - EXP-03: Variable cycles (X of Y)? (Pass/Fail)
       - EXP-04: Cancellation labels? (Pass/Fail)
  </action>
  <verify>
    All Phase 8 requirements are satisfied and verified through manual or automated checks.
  </verify>
  <done>Phase 8 complete and verified.</done>
</task>

</tasks>

<verification>
Checklist:
- [ ] Income history window shows 3 months correctly.
- [ ] Monthly totals in income history are accurate.
- [ ] Budget targets work by bucket (Recurrent/One-off).
- [ ] Dashboard progress bars reflect bucket targets.
- [ ] Final UI styling is polished and consistent.
</verification>

<success_criteria>
Financial trends are more visible through extended income history, and budget targets are simpler to manage at the bucket level. All Phase 8 requirements are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-income-expenses-refinement/08-02-SUMMARY.md`
</output>
