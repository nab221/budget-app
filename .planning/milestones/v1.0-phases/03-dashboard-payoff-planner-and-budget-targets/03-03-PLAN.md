---
phase: 03-dashboard-payoff-planner-and-budget-targets
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/ui/dashboard.js, src/ui/targets.js, src/app.js, src/db/repository.js]
autonomous: true
requirements: [DASH-03, DASH-05]
must_haves:
  truths:
    - "Category progress bars on the dashboard accurately show actual spend vs target."
    - "Net worth snapshots are automatically taken once per month on app load."
    - "Snapshot list displays historical net worth trends."
  artifacts:
    - path: "src/ui/targets.js"
      provides: "Budget target management UI"
      exports: ["renderTargetSettings"]
    - path: "src/ui/dashboard.js"
      provides: "Dashboard components (Progress Bars, Snapshots)"
      contains: "renderProgressBars, renderSnapshots"
  key_links:
    - from: "src/app.js"
      to: "src/db/repository.js"
      via: "snapshot trigger call"
      pattern: "checkAndTakeSnapshot"
---

<objective>
Implement budget targets with progress visualization and historical net worth snapshotting.

Purpose: Provide goal-oriented budgeting and long-term trend tracking.
Output: Integrated progress bars and historical net worth data list on the dashboard.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-payoff-planner-and-budget-targets/03-RESEARCH.md
@src/ui/dashboard.js
@src/db/repository.js
@src/app.js

<interfaces>
From src/db/repository.js (after Plan 01):
```javascript
export async function getAll(storeName); // for targets
export async function add(storeName, item);
export async function checkAndTakeSnapshot(); // planned
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Category Budget Targets Management</name>
  <files>src/ui/targets.js, src/db/repository.js</files>
  <action>
    1. Create `src/ui/targets.js`.
    2. Implement a UI (e.g., a modal or dedicated settings section) to:
       - List all spending categories (from `categories` store).
       - Allow setting an optional monthly amount target for each.
       - Store targets in the `targets` Dexie store (created in Plan 01).
    3. Update `repository.js` to ensure targets can be easily retrieved by category.
  </action>
  <verify>
    Check if 'targets' entries are correctly added to IndexedDB.
  </verify>
  <done>
    Users can set and manage per-category budget targets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Progress Bars</name>
  <files>src/ui/dashboard.js</files>
  <action>
    1. Update `renderDashboard` in `src/ui/dashboard.js` to include a "Budget Progress" section.
    2. For each category that has a target:
       - Fetch actual spending for the current month.
       - Render a progress bar showing (Actual / Target) %.
       - Use color coding (Green < 80%, Amber 80-100%, Red > 100%).
  </action>
  <verify>
    Manual verification: Changing spending or targets updates progress bar visualization on the dashboard.
  </verify>
  <done>
    Dashboard provides visual feedback on budget target adherence.
  </done>
</task>

<task type="auto">
  <name>Task 3: Historical Net Worth Snapshots</name>
  <files>src/db/repository.js, src/app.js, src/ui/dashboard.js</files>
  <action>
    1. Implement `checkAndTakeSnapshot()` in `src/db/repository.js`:
       - Check if a snapshot for the current month already exists in `netWorthSnapshots`.
       - If not, calculate current net worth (Assets - Debt) and save a new snapshot entry.
    2. Call `checkAndTakeSnapshot()` in `src/app.js` during initialization.
    3. Add a "Net Worth History" section to the dashboard in `src/ui/dashboard.js` that renders the snapshots as a simple table/list.
  </action>
  <verify>
    node -e "import { checkAndTakeSnapshot } from './src/db/repository.js'; checkAndTakeSnapshot().then(() => console.log('Snapshot check done'));"
  </verify>
  <done>
    Net worth snapshots are taken automatically, and the dashboard displays historical records.
  </done>
</task>

</tasks>

<verification>
- Dashboard shows progress bars for categories with set targets.
- Automatic snapshotting confirmed by checking `netWorthSnapshots` table.
- Dashboard displays list of snapshots.
</verification>

<success_criteria>
- Users can track their spending against custom targets.
- Historical net worth data is captured automatically monthly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-payoff-planner-and-budget-targets/03-03-SUMMARY.md`
</output>
