---
phase: 03-dashboard-payoff-planner-and-budget-targets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js, src/utils/finance.js, src/utils/finance.test.js]
autonomous: true
requirements: [PAY-03, PAY-04, PAY-05, BT-01, BT-02]
must_haves:
  truths:
    - "Payoff simulation correctly rolls over payments from cleared debts to the next priority debt."
    - "UK minimum payment rules are accurately reflected in simulations (Interest + 1% vs 2.25%)."
    - "Balance transfer modeling accurately compares fee costs vs interest savings."
  artifacts:
    - path: "src/db/schema.js"
      provides: "New targets and netWorthSnapshots stores"
      contains: "targets, netWorthSnapshots"
    - path: "src/utils/finance.js"
      provides: "simulatePayoff and modelBalanceTransfer logic"
      exports: ["simulatePayoff", "modelBalanceTransfer"]
  key_links:
    - from: "src/utils/finance.js"
      to: "calcMinPayment"
      via: "function call"
      pattern: "calcMinPayment\("
---

<objective>
Update the data layer and core finance utilities to support payoff simulations, budget targets, and balance transfer modeling.

Purpose: Provides the mathematical and storage foundation for all Phase 3 features.
Output: Updated Dexie schema, expanded repository, and verified finance simulation logic.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-payoff-planner-and-budget-targets/03-RESEARCH.md
@src/db/schema.js
@src/db/repository.js
@src/utils/finance.js

<interfaces>
From src/utils/finance.js:
```javascript
export function calcMinPayment(balancePence, aprPercent, feesPence = 0);
export function calcUtilization(balancePence, limitPence);
```

From src/db/schema.js:
```javascript
export const db = new Dexie('BudgetApp');
db.version(2).stores({
  income: '++id, date, month, categoryId',
  fixedSpends: '++id, date, month, categoryId',
  // ... (others)
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema and Repository for Targets & Snapshots</name>
  <files>src/db/schema.js, src/db/repository.js</files>
  <action>
    1. Update `src/db/schema.js` to version 3.
    2. Add new stores:
       - `targets`: `++id, categoryId, amount`
       - `netWorthSnapshots`: `++id, month, totalAssets, totalDebt, netWorth`
    3. Update `src/db/repository.js` to include CRUD methods for these new stores (getAll, getByMonth for snapshots, add/update/delete for targets).
  </action>
  <verify>
    node -e "import { db } from './src/db/schema.js'; db.open().then(() => { console.log(db.tables.map(t => t.name)); process.exit(0); });" (Check if new tables exist)
  </verify>
  <done>
    Dexie schema version 3 is active and repository methods are available for targets and netWorthSnapshots.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Payoff Simulation Logic</name>
  <files>src/utils/finance.js, src/utils/finance.test.js</files>
  <action>
    1. Implement `simulatePayoff(debts, strategy, extraPaymentPence)` in `src/utils/finance.js`.
    2. The simulation must:
       - Handle 'avalanche' (highest APR first), 'snowball' (lowest balance first), and 'min' (minimums only) strategies.
       - Accurately apply monthly interest and calculate minimum payments using `calcMinPayment`.
       - Implement "rollover": when a debt is cleared, its minimum payment and any allocated extra funds move to the next priority debt.
       - Return an object with `{ totalInterest, monthsToClear, resultsByDebt }`.
    3. Add unit tests in `src/utils/finance.test.js` covering multiple debts and different strategies.
  </action>
  <verify>
    npm test src/utils/finance.test.js
  </verify>
  <done>
    `simulatePayoff` handles all strategies and rollover correctly, verified by unit tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Balance Transfer Modeling Logic</name>
  <files>src/utils/finance.js, src/utils/finance.test.js</files>
  <action>
    1. Implement `modelBalanceTransfer(debt, promoMonths, feePercent)` in `src/utils/finance.js`.
    2. Calculate:
       - `transferFeePence`: `debt.balance * (feePercent / 100)`.
       - `recommendedMonthlyPayment`: `(debt.balance + transferFee) / promoMonths`.
       - `totalCostBT`: `transferFeePence`.
       - `totalCostCurrent`: `simulatePayoff([debt], 'min', 0).totalInterest`.
    3. Add unit tests for BT modeling.
  </action>
  <verify>
    npm test src/utils/finance.test.js
  </verify>
  <done>
    `modelBalanceTransfer` provides accurate cost comparisons and recommendations.
  </done>
</task>

</tasks>

<verification>
- `npm test` passes for all finance utilities.
- Manual verification: Check that `db.version` is 3 in browser console after load.
</verification>

<success_criteria>
- Schema version 3 is implemented and migration is successful.
- Finance utility functions cover all required Phase 3 business logic with high test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-payoff-planner-and-budget-targets/03-01-SUMMARY.md`
</output>
