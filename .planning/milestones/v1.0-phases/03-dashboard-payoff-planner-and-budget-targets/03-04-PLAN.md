---
phase: 03-dashboard-payoff-planner-and-budget-targets
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/ui/payoff.js, src/ui/dashboard.js, src/app.js, index.html]
autonomous: true
requirements: [PAY-01, PAY-02, PAY-04]
must_haves:
  truths:
    - "Users can compare Avalanche, Snowball, and Minimum strategies side-by-side."
    - "Extra monthly payment input reactively updates payoff simulations for all strategies."
    - "Debt-free date countdown on the dashboard updates based on the selected/fastest strategy."
  artifacts:
    - path: "src/ui/payoff.js"
      provides: "Debt payoff strategy visualization"
      exports: ["renderPayoffPlanner"]
    - path: "src/ui/dashboard.js"
      provides: "Debt-free countdown card"
      contains: "renderDebtFreeCountdown"
  key_links:
    - from: "src/ui/payoff.js"
      to: "src/utils/finance.js"
      via: "simulation call"
      pattern: "simulatePayoff"
---

<objective>
Implement the debt payoff planner with multi-strategy comparison and reactive simulation.

Purpose: Help users decide on the best debt payoff strategy.
Output: Side-by-side payoff simulations and a debt-free date countdown on the dashboard.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/utils/finance.js
@src/ui/dashboard.js
@src/app.js

<interfaces>
From src/utils/finance.js (after Plan 01):
```javascript
export function simulatePayoff(debts, strategy, extraPaymentPence);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strategy Comparison View</name>
  <files>src/ui/payoff.js, index.html</files>
  <action>
    1. Create `src/ui/payoff.js`.
    2. Implement `renderPayoffPlanner(containerId)`:
       - Fetch all current debts from `repository.js`.
       - Run `simulatePayoff` for all 3 strategies: 'avalanche', 'snowball', 'min'.
       - Render a side-by-side comparison (Cards or Table) showing: Months to Clear, Total Interest Paid, Total Amount Paid for each.
    3. Add a placeholder in `index.html` (e.g., `<div id="payoff-view"></div>`) and navigation logic to show it.
  </action>
  <verify>
    Check if 'src/ui/payoff.js' exists and contains logic to call 'simulatePayoff' for 3 strategies.
  </verify>
  <done>
    Payoff planner view provides clear strategy comparisons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reactive Extra Monthly Payment Input</name>
  <files>src/ui/payoff.js</files>
  <action>
    1. Add a numeric input for "Extra Monthly Payment" to the Payoff Planner UI.
    2. Add an event listener to the input that re-runs all 3 simulations when the value changes.
    3. Ensure the UI updates immediately to reflect the new timeframes and interest savings.
  </action>
  <verify>
    Manual verification: Entering an extra payment value instantly updates all 3 simulation results in the UI.
  </verify>
  <done>
    Payoff simulations are reactive to user input for extra payments.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dashboard Debt-Free Countdown</name>
  <files>src/ui/dashboard.js, src/ui/payoff.js</files>
  <action>
    1. Update `renderDashboard` in `src/ui/dashboard.js` to include a "Debt-Free Date" card (Requirement DASH-04).
    2. The card should display a countdown (e.g., "15 months until debt-free").
    3. The data should be derived from the fastest payoff strategy (usually Avalanche or Snowball).
  </action>
  <verify>
    Check if Dashboard now contains a card showing the debt-free date.
  </verify>
  <done>
    Dashboard provides visibility into the projected debt-free timeline.
  </done>
</task>

</tasks>

<verification>
- Payoff planner displays comparison for all 3 strategies.
- Changing extra payment updates simulation results.
- Dashboard shows debt-free countdown.
</verification>

<success_criteria>
- Users can clearly see the impact of choosing different payoff strategies.
- Extra payments are accurately modeled and visualized.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-payoff-planner-and-budget-targets/03-04-SUMMARY.md`
</output>
