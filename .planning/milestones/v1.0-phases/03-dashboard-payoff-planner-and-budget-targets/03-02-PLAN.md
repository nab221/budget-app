---
phase: 03-dashboard-payoff-planner-and-budget-targets
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/db/repository.js, src/ui/dashboard.js, src/app.js, index.html]
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "Dashboard displays all 9 summary cards with accurate calculations."
    - "Summary cards update immediately when the period filter (Month/YTD/All) is changed."
  artifacts:
    - path: "src/ui/dashboard.js"
      provides: "Dashboard rendering logic"
      exports: ["renderDashboard"]
    - path: "src/db/repository.js"
      provides: "getDashboardData aggregation method"
      contains: "getDashboardData"
  key_links:
    - from: "src/app.js"
      to: "src/ui/dashboard.js"
      via: "import and call"
      pattern: "renderDashboard"
---

<objective>
Implement the core dashboard UI with 9 summary cards and period-based data aggregation.

Purpose: Provide the central financial overview for the user.
Output: Reactive dashboard UI with 9+ data points and period filtering.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/repository.js
@src/app.js
@index.html

<interfaces>
From src/db/repository.js (after Plan 01):
```javascript
export async function getByMonth(storeName, monthStr);
export async function getAll(storeName);
```

New interfaces planned for this plan:
```javascript
export async function getDashboardData(periodType, targetMonth); 
// periodType: 'month' | 'ytd' | 'all'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Dashboard Data Aggregation</name>
  <files>src/db/repository.js</files>
  <action>
    1. Implement `getDashboardData(periodType, targetMonth)` in `src/db/repository.js`.
    2. Logic:
       - If 'month': aggregate totals for the specific `targetMonth`.
       - If 'ytd': aggregate totals from January of the year in `targetMonth` until `targetMonth`.
       - If 'all': aggregate all data in the database.
    3. Return an object: `{ income, fixed, variable, netPosition, totalSubscriptions, totalDebt, totalAssets, netWorth, fixedToIncomeRatio }`.
    4. Ensure all values are in pence integers.
  </action>
  <verify>
    node -e "import { getDashboardData } from './src/db/repository.js'; getDashboardData('month', '2026-03').then(d => console.log(d));"
  </verify>
  <done>
    Centralized data aggregation for the dashboard is functional and supports period filtering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard UI and 9 Summary Cards</name>
  <files>src/ui/dashboard.js, index.html</files>
  <action>
    1. Create `src/ui/dashboard.js`.
    2. Implement `renderDashboard(containerId, periodType, targetMonth)`:
       - Fetch data using `getDashboardData`.
       - Render 9 summary cards using a clean grid layout.
       - Use `formatGBP` from `src/utils/currency.js` for display.
       - Include: Income, Fixed Expenses, Variable Expenses, Net Position, Subscriptions, Total Debt, Total Assets, Net Worth, Fixed-to-Income Ratio.
    3. Add a placeholder in `index.html` (e.g., `<div id="dashboard-view"></div>`) and navigation logic to show it.
  </action>
  <verify>
    Check if 'src/ui/dashboard.js' exists and contains rendering logic for all 9 cards.
  </verify>
  <done>
    Dashboard UI is implemented with all 9 required cards.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Period Filtering Logic</name>
  <files>src/ui/dashboard.js, src/app.js</files>
  <action>
    1. Add a period selector (Dropdown or Tabs) to the Dashboard UI (Month, YTD, All Time).
    2. Ensure that changing the selector re-renders the dashboard with the correct period data.
    3. Wire the dashboard into the main application initialization in `src/app.js`.
  </action>
  <verify>
    Manual verification: Changing the period selector in the UI updates the card values.
  </verify>
  <done>
    Dashboard is reactive and accurately reflects data for different time periods.
  </done>
</task>

</tasks>

<verification>
- Dashboard shows all 9 summary cards.
- Switching between 'Month', 'YTD', and 'All Time' updates all values.
- Net Position is correctly calculated as Income - (Fixed + Variable).
- Net Worth is correctly calculated as Total Assets - Total Debt.
</verification>

<success_criteria>
- Dashboard provides a comprehensive financial snapshot.
- Data aggregation is efficient and period-aware.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-payoff-planner-and-budget-targets/03-02-SUMMARY.md`
</output>
