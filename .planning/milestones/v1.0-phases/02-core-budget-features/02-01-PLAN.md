---
phase: 02-core-budget-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js, src/utils/security.js]
autonomous: true
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "IndexedDB schema is at version 2 with renamed fields and new tables"
    - "Encryption service correctly handles AES-GCM with salt and IV"
    - "Repositories exist for all new financial entities"
  artifacts:
    - path: "src/db/schema.js"
      provides: "Schema v2 definition"
    - path: "src/utils/security.js"
      provides: "AES-GCM encryption/decryption"
    - path: "src/db/repository.js"
      provides: "Repositories for income, fixed/variable spends, subs, debts, assets, templates"
  key_links:
    - from: "src/db/repository.js"
      to: "src/db/schema.js"
      via: "Dexie table access"
---

<objective>
Update the data layer and implement security foundations to support Phase 2 features.

Purpose: Provide a robust, secure storage architecture for complex financial data.
Output: Schema v2, Unified Repositories, and Encryption Service.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-budget-features/02-RESEARCH.md
@src/db/schema.js
@src/db/repository.js
@src/utils/currency.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Database Schema to v2</name>
  <files>src/db/schema.js</files>
  <action>
    Update Dexie schema to version 2 with the following changes:
    - Rename `fixedSpends.name` to `label`.
    - Rename `variableSpends.name` to `note`.
    - Add default `status: 'pending'` to existing `fixedSpends` in the upgrade path.
    - Define new stores:
      - `income`: '++id, date, source, amount, categoryId'
      - `fixedSpends`: '++id, date, categoryId, label, amount, status'
      - `variableSpends`: '++id, date, categoryId, note, amount'
      - `subscriptions`: '++id, name, amount, categoryId, frequency, nextDate'
      - `recurringTemplates`: '++id, name, amount, categoryId, frequency, type'
      - `debts`: '++id, name, type, apr, creditLimit, currentBalance'
      - `statements`: '++id, debtId, date, amount, interest, fees'
      - `assets`: '++id, name, type, asOfDate, currentBalance'
    
    Ensure `db.on('versionchange')` and `db.on('blocked')` handlers are preserved.
  </action>
  <verify>Run `dexie-export-import` test or inspect DevTools Application tab for IndexedDB version 2.</verify>
  <done>Schema v2 is active and migration logic preserves existing category/fixed/variable data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Encryption Service</name>
  <files>src/utils/security.js</files>
  <action>
    Create a standalone encryption service using the Web Crypto API (SubtleCrypto):
    - `encryptData(data, password)`: AES-GCM 256-bit with PBKDF2 (100,000 iterations, SHA-256).
    - Use a fresh 16-byte salt and 12-byte IV for every operation.
    - Package the result as a Base64 string containing [salt][iv][ciphertext].
    - `decryptData(base64Data, password)`: Reverse the process.
    - Handle decryption failures (wrong password) by throwing a clear error.
  </action>
  <verify>
    Create a small test script/node script that encrypts a JSON object, decrypts it with the same password, and verifies identity, then fails with a wrong password.
  </verify>
  <done>Encryption and decryption work correctly for arbitrary JSON objects using AES-GCM.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Repositories for All Entities</name>
  <files>src/db/repository.js</files>
  <action>
    Expand `src/db/repository.js` to include repository objects for:
    - `incomeRepository`
    - `fixedSpendRepository`
    - `variableSpendRepository`
    - `subscriptionRepository`
    - `debtRepository`
    - `assetRepository`
    - `recurringTemplateRepository`
    
    Each repository must implement standard CRUD: `get(id)`, `getAll()`, `add(data)`, `update(id, data)`, `delete(id)`.
    
    CRITICAL:
    - Use `toPence` in `add`/`update` methods for all money fields.
    - Use `fromPence` when returning data if necessary, or let the UI handle formatting (P1 convention suggests the UI uses `formatGBP`).
    - Implement `getByMonth(monthStr)` (YYYY-MM) for income, fixed, and variable repositories.
  </action>
  <verify>Inspect code for repository pattern consistency; verify `getByMonth` uses `startsWith` index query.</verify>
  <done>Standardized repository access exists for all new entities.</done>
</task>

</tasks>

<verification>
Check version number in IndexedDB via DevTools. Verify that repositories export all required CRUD methods.
</verification>

<success_criteria>
- Dexie schema is successfully upgraded to v2 without data loss.
- Encryption/Decryption utility passes identity test.
- Repositories are ready for UI implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-budget-features/02-01-SUMMARY.md`
</output>
