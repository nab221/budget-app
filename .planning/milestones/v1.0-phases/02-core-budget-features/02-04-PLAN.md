---
phase: 02-core-budget-features
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [src/ui/templates.js, src/ui/backup.js, src/app.js]
autonomous: true
requirements: [REC-01, REC-02, REC-03, REC-04, DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "App prompts for recurring transactions at the start of each month"
    - "Data can be exported as both plain and encrypted JSON"
    - "Imports are password-protected (if encrypted) and require user confirmation before replacement"
  artifacts:
    - path: "src/ui/templates.js"
      provides: "UI for recurring templates and prompting logic"
    - path: "src/ui/backup.js"
      provides: "Export/Import UI and logic"
  key_links:
    - from: "src/ui/backup.js"
      to: "src/utils/security.js"
      via: "Encryption/Decryption calls"
---

<objective>
Implement automation for recurring transactions and secure data portability via encrypted export/import.

Purpose: Save user time with templates and ensure data safety.
Output: Recurring transaction engine and secure backup UI.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-budget-features/02-RESEARCH.md
@src/utils/security.js
@src/db/repository.js
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Recurring Templates and Prompt Logic</name>
  <files>src/ui/templates.js</files>
  <action>
    Create `src/ui/templates.js`:
    - CRUD for `recurringTemplates` (Name, Amount, Category, Frequency, Type).
    - Implement Start-of-Month check:
      - Compare `new Date().toISOString().slice(0, 7)` with `localStorage.getItem('lastPromptedMonth')`.
      - If different, find all templates and check if corresponding entries exist for the current month.
      - Show a modal with "Confirm recurring items" list.
    - Implement `generateFromTemplate(templateId)`:
      - Creates a new record in `fixedSpends` or `variableSpends`.
      - Updates `localStorage.lastPromptedMonth` when complete.
  </action>
  <verify>Set `lastPromptedMonth` to a previous month in console; refresh app and verify the prompt appears with active templates.</verify>
  <done>Templates save time by automating repetitive data entry each month.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Plain and Encrypted Export/Import UI</name>
  <files>src/ui/backup.js</files>
  <action>
    Create `src/ui/backup.js`:
    - Wire `exportBtn` and `importFile` handlers.
    - Plain Export: `JSON.stringify` all tables (Dexie `bulkGet` everything).
    - Encrypted Export:
      - Prompt user for a password.
      - Use `encryptData` from `src/utils/security.js`.
      - Download as `.json` file.
    - Import Logic:
      - detect if JSON is encrypted (look for salt/iv signature).
      - Prompt for password if needed.
      - Validate JSON schema before importing.
      - CRITICAL: User MUST confirm "Replace all current data?" before importing.
      - Use Dexie `clear()` then `bulkAdd()` for each table to ensure total replacement.
  </action>
  <verify>Export data; delete a row; import the file and verify the row returns. Then test with an encrypted file and wrong password.</verify>
  <done>Users can safely move their data between devices and keep secure backups.</done>
</task>

<task type="auto">
  <name>Task 3: Integration and Cleanup</name>
  <files>src/app.js</files>
  <action>
    - Update `src/app.js`:
      - Import and initialize `templateUI` and `backupUI`.
      - Remove Phase 1 temporary "Test Dropdown" from Settings if present.
      - Finalize any shared state between tabs.
  </action>
  <verify>Run the full suite of Phase 2 features and verify no console errors.</verify>
  <done>Phase 2 features are integrated and polished.</done>
</task>

</tasks>

<verification>
Check that encryption works by inspecting an exported file. Verify that importing replaces (not merges) data by adding a new item before import and checking if it's gone after import.
</verification>

<success_criteria>
- Start-of-month prompt correctly identifies due templates.
- Exported files are readable only with the correct password (if encrypted).
- Import process is safe and confirmed by the user.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-budget-features/02-04-SUMMARY.md`
</output>
