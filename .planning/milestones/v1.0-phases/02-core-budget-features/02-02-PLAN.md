---
phase: 02-core-budget-features
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/ui/transactions.js, src/ui/subscriptions.js, src/app.js]
autonomous: true
requirements: [INC-01, INC-02, INC-03, INC-04, FIXED-01, FIXED-02, FIXED-03, FIXED-04, VAR-01, VAR-02, VAR-03, VAR-04, SUB-01, SUB-02, SUB-03, SUB-04]
must_haves:
  truths:
    - "Income, Fixed, and Variable lists show data filtered by the month picker"
    - "User can add, edit, and delete income and expense entries"
    - "Subscriptions display monthly-equivalent costs and handle next-date logic"
  artifacts:
    - path: "src/ui/transactions.js"
      provides: "UI for Income, Fixed, and Variable CRUD"
    - path: "src/ui/subscriptions.js"
      provides: "UI for Subscription CRUD and calculations"
  key_links:
    - from: "src/ui/transactions.js"
      to: "src/db/repository.js"
      via: "Repository method calls"
    - from: "src/ui/subscriptions.js"
      to: "src/utils/currency.js"
      via: "toPence/fromPence conversion"
---

<objective>
Implement the core transaction recording features, including income, expenses, and subscriptions with month-based filtering.

Purpose: Enable users to log their primary financial activities.
Output: Functional CRUD UI for all transaction types and subscriptions.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-budget-features/02-RESEARCH.md
@index.html
@src/ui/render.js
@src/db/repository.js
@src/utils/currency.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Income, Fixed, and Variable CRUD UI</name>
  <files>src/ui/transactions.js</files>
  <action>
    Create `src/ui/transactions.js` to handle the UI for Income, Fixed Spends, and Variable Spends:
    - `init()`: Attach event listeners to "Add" buttons and the global `monthPicker`.
    - `render(month)`: Fetch and render data for the selected month (YYYY-MM).
    - Implement `addTransaction(type, data)`: Uses `incomeRepository`, `fixedSpendRepository`, or `variableSpendRepository`.
    - Implement `deleteTransaction(type, id)`: With confirmation prompt.
    - Implement inline editing for existing entries (or a simple "Edit" prompt/modal).
    - Ensure all money inputs are converted via `toPence` before storage.
    - Ensure `fixedSpends` handle the `status` field (pending/paid).
    - Display monthly totals for each section.
  </action>
  <verify>Add entries for current month and previous month; verify the `monthPicker` correctly filters the lists.</verify>
  <done>Full CRUD for Income, Fixed, and Variable transactions works with month filtering.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Subscription CRUD and Logic</name>
  <files>src/ui/subscriptions.js</files>
  <action>
    Create `src/ui/subscriptions.js`:
    - Implement CRUD UI for subscriptions (Name, Amount, Category, Frequency).
    - Calculate and display "Monthly Equivalent" for non-monthly frequencies (Quarterly: /3, Annual: /12).
    - Implement logic to display "Next Payment Date" and logic to update it (handling Feb 29th/leap year clamping to Feb 28th).
    - Standardize on `toPence` for storage and `formatGBP` for display.
  </action>
  <verify>Add a £120 annual subscription; verify it shows "£10.00 / mo" equivalent.</verify>
  <done>Subscription management is functional with accurate monthly-cost projections.</done>
</task>

<task type="auto">
  <name>Task 3: Wire UI Modules into app.js</name>
  <files>src/app.js</files>
  <action>
    Update `src/app.js` to import and initialize the new UI modules:
    - Import `transactionUI` and `subscriptionUI`.
    - Call `.init()` for both in the main app load.
    - Ensure the global `monthPicker` defaults to the current month (YYYY-MM).
    - Connect the `monthPicker` change event to re-render relevant transaction lists.
  </action>
  <verify>Refresh app; verify current month is selected and clicking "Add" in any tab works.</verify>
  <done>All Phase 2 core transaction UI modules are integrated and responsive to month changes.</done>
</task>

</tasks>

<verification>
Verify that adding an income entry in January doesn't show up when the picker is set to February. Verify that "Paid" status on fixed spends persists.
</verification>

<success_criteria>
- Income, Fixed, and Variable tabs have full CRUD capability.
- Month picker correctly filters transactions across all tabs.
- Subscriptions display both actual amount and monthly equivalent.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-budget-features/02-02-SUMMARY.md`
</output>
