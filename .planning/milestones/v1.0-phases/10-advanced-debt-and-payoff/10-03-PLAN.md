---
phase: 10-advanced-debt-and-payoff
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified: [src/ui/payoff.js]
autonomous: true
requirements: [PAY-06, PAY-07]

must_haves:
  truths:
    - "User can switch and persist payoff strategies"
    - "Payoff table shows 12-month rolling snapshot with principal/interest split"
    - "Rate jumps are visually highlighted in the breakdown table"
  artifacts:
    - path: "src/ui/payoff.js"
      provides: "Interactive planner and breakdown table"
      contains: "localStorage.setItem"
---

<objective>
Finalize the Payoff Planner with interactive strategy selection and a detailed 12-month payment breakdown table.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-advanced-debt-and-payoff/10-02-SUMMARY.md
@src/ui/payoff.js
@src/utils/finance.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interactive Strategy Selection & Persistence</name>
  <files>src/ui/payoff.js</files>
  <action>
    - Add a UI element (e.g., radio toggle or button group) in the Payoff Planner to select the active strategy (`avalanche`, `snowball`, or `min`).
    - Persist the selection to `localStorage` using the key `budget_payoff_preference` as per RESEARCH.md.
    - When the strategy changes, re-render the charts and the detailed breakdown table immediately.
    - Ensure the Dashboard's "Debt-free Countdown" uses this persisted strategy.
  </action>
  <verify>
    Switching strategy updates the months-to-clear and total interest displays instantly.
    Refreshing the page preserves the selected strategy.
  </verify>
  <done>Strategy selection is interactive and persistent.</done>
</task>

<task type="auto">
  <name>Task 2: Detailed 12-Month Payment Breakdown</name>
  <files>src/ui/payoff.js</files>
  <action>
    - Implement a table showing a 12-month rolling snapshot of projected payments.
    - Columns: Month, Total Paid, and for each debt: [Principal | Interest].
    - Use the history object returned by the refactored `simulatePayoff`.
    - Highlight rows where a "Rate Jump" occurs (where `isRateJump` is true for any debt in that month).
    - Ensure the table is responsive or scrollable on mobile.
  </action>
  <verify>
    Table correctly displays the principal/interest split for the first 12 months.
    Rate jumps are visually distinct.
  </verify>
  <done>12-month detailed breakdown table implemented with split interest tracking.</done>
</task>

</tasks>

<verification>
Verify strategy persistence and breakdown table accuracy.
Check "Rate Jump" highlighting with a test debt.
</verification>

<success_criteria>
- Strategy is saved to localStorage and used globally.
- Detailed table shows principal vs interest split.
- 12-month snapshot accurately reflects simulation history.
- Rate jump highlights are visible when a promo expires.
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-debt-and-payoff/10-03-SUMMARY.md`
</output>
