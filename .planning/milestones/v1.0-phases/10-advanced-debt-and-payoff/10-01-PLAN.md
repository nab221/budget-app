---
phase: 10-advanced-debt-and-payoff
plan: 10-01
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/finance.js, src/db/repository.js]
autonomous: true
requirements: [BAL-02]

must_haves:
  truths:
    - "adjustBalance creates an offset transaction in the database"
    - "calculateBalanceChain treats 'Opening Balance' category as a balance seed adjustment in any month"
  artifacts:
    - path: "src/utils/finance.js"
      provides: "Updated balance chain logic to include adjustment entries in every month"
    - path: "src/db/repository.js"
      provides: "adjustBalance function for creating the offset entries"
  key_links:
    - from: "src/db/repository.js:adjustBalance"
      to: "incomeRepository.add / oneOffExpenseRepository.add"
      via: "Creating ledger entries for adjustments"
---

<objective>
Implement the core logic for account balance reconciliation using offset transactions. This plan ensures the balance engine (calculateBalanceChain) supports adjustments at any point in the timeline and provides a repository method to create these adjustments automatically.
</objective>

<execution_context>
@C:/Users/brito/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/utils/finance.js
@src/db/repository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update calculateBalanceChain to handle mid-chain adjustments</name>
  <files>src/utils/finance.js</files>
  <action>
    Modify the calculateBalanceChain function in src/utils/finance.js to treat income with the 'Opening Balance' category (group='system') as a balance seed adjustment in EVERY month, not just the start month.
    
    1. Separate 'Opening Balance' category income entries from 'regularIncome' for all months.
    2. Add the sum of these adjustment entries to the 'openingBalance' of the month before calculating incomeTotal and expenseTotal.
    3. Ensure these adjustment entries are EXCLUDED from the snapshot's 'incomeTotal'.
    4. (Optional/Better) Also check one-off expenses for the 'Opening Balance' category and subtract them from the 'openingBalance' (excluding them from 'expenseTotal').
  </action>
  <verify>npm test tests/balance/balance-ui.test.js (ensure existing tests still pass, or create a temporary script to verify chain math with adjustments)</verify>
  <done>calculateBalanceChain correctly processes 'Opening Balance' entries in any month without double-counting them as regular income/expense.</done>
</task>

<task type="auto">
  <name>Task 2: Implement adjustBalance in repository</name>
  <files>src/db/repository.js</files>
  <action>
    Implement the adjustBalance(enteredAmountPence, date) function in src/db/repository.js.
    
    1. Resolve the current calculated balance for the specified date (or today). You can use a filtered query to calculate the balance as of that date or rely on existing snapshots and pending transactions.
    2. Calculate the difference: offset = enteredAmountPence - currentCalculatedBalance.
    3. If offset is positive: Add an income entry with label "Balance Adjustment", amount=offset, categoryId=openingBalCatId (look it up by name 'Opening Balance').
    4. If offset is negative: Add a one-off expense entry with label "Balance Adjustment", amount=Math.abs(offset), categoryId=openingBalCatId.
    5. Call triggerBalanceRecalc(date) after adding the record.
  </action>
  <verify>Check database state after calling repository.adjustBalance() via a temporary script or console log in tests.</verify>
  <done>Repository has adjustBalance function that creates correct ledger entries based on current balance gap.</done>
</task>

</tasks>

<verification>
Verify that creating an income/expense with the 'Opening Balance' category correctly shifts the running balance in the dashboard without being counted as regular income/expense in the totals.
</verification>

<success_criteria>
- calculateBalanceChain correctly processes mid-chain adjustments.
- adjustBalance(pence, date) creates the necessary ledger entries to reconcile the balance.
</success_criteria>

<output>
After completion, create `.planning/phases/10-advanced-debt-and-payoff/10-01-SUMMARY.md`
</output>
