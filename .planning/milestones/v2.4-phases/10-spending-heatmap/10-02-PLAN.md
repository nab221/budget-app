---
phase: 10-spending-heatmap
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [src/ui/heatmap.js, src/ui/dashboard.js]
autonomous: true
requirements: [HMP-02, HMP-03, HMP-04]
must_haves:
  truths:
    - "Heatmap cells show 4 distinct green intensities based on daily spending quartiles"
    - "A tooltip appears on hover/tap showing specific date details"
    - "A second grid for the prior year appears if data exists"
  artifacts:
    - path: "src/ui/heatmap.js"
      provides: "Tooltip logic, quartile scaling, multi-year support"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "src/ui/heatmap.js"
      via: "re-rendered for multi-year detection"
---

<objective>
Enhance the heatmap with meaningful coloring via quartile scaling, interactive tooltips, and automatic year-over-year grid rendering when historical data is available.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-spending-heatmap/10-CONTEXT.md
@.planning/phases/10-spending-heatmap/10-01-SUMMARY.md
@src/ui/heatmap.js
@src/ui/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Quartile Color Scaling</name>
  <files>src/ui/heatmap.js</files>
  <action>
    - Update `renderSpendingHeatmap` to calculate quartiles from all non-zero daily totals in the provided dataset.
    - If multiple years are rendered, use a shared scale based on all data.
    - Map each cell to 5 levels (0 = no spend, 1 = Q1, 2 = Q2, 3 = Q3, 4 = Q4).
    - Use CSS-variable-derived greens for the intensities to maintain theme consistency.
  </action>
  <verify>
    <automated>grep "quartile" src/ui/heatmap.js</automated>
  </verify>
  <done>Cells are colored based on their relative spending density using quartile scaling.</done>
</task>

<task type="auto">
  <name>Task 2: Interactive Tooltips (Hover/Tap)</name>
  <files>src/ui/heatmap.js</files>
  <action>
    - Implement a mousemove/click listener on the heatmap canvas.
    - Calculate the date corresponding to the cursor position (based oncellSize, gap, labels).
    - Display a floating tooltip (absolute positioned div) with:
        - Date (e.g., "Monday, March 2, 2026")
        - Total spend (£)
        - Top category (if any)
    - Ensure it works with both mouse hover and mobile tap.
    - Style the tooltip to match the app's standard look.
  </action>
  <verify>
    <automated>grep "mousemove" src/ui/heatmap.js</automated>
  </verify>
  <done>Heatmap provides detailed contextual info via an interactive tooltip.</done>
</task>

<task type="auto">
  <name>Task 3: Year-over-Year Support</name>
  <files>src/ui/dashboard.js, src/ui/heatmap.js</files>
  <action>
    - Update Dashboard to check for data in the prior year (currentYear - 1).
    - If expenses exist in the prior year, render a second canvas/grid beneath the current year's grid.
    - The `getYearlyDailySpending` method from Plan 01 should be called twice (or once for a broader range).
    - Ensure labels and layout accommodate two grids cleanly.
  </action>
  <verify>
    <automated>grep "previousYear" src/ui/dashboard.js</automated>
  </verify>
  <done>Heatmap automatically shows the prior year's grid when sufficient data is available.</done>
</task>

</tasks>

<verification>
- Verify colors reflect spending distribution.
- Tooltip displays correct information for selected cells.
- Prior year grid appears when historical data is present.
</verification>

<success_criteria>
- Heatmap uses a 4-level green scale.
- Tooltip is accurate and performs well.
- Multi-year visualization is seamless.
</success_criteria>

<output>
After completion, create `.planning/phases/10-spending-heatmap/10-02-SUMMARY.md`
</output>
