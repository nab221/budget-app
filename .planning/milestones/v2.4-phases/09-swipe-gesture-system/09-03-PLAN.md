---
phase: 09-swipe-gesture-system
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: [src/ui/expenses.js, css/main.css]
autonomous: false
requirements: [SWP-02, SWP-04, SWP-05]
must_haves:
  truths:
    - "Right-swipe reveals the green clear button (in reconciliation mode)"
    - "Reconciled/locked rows are ignored by swipe gestures"
    - "Rows snap back correctly on cancel or threshold failure"
    - "Mobile edge conflicts are prevented"
  artifacts:
    - path: "src/ui/expenses.js"
      provides: "Complete swipe gesture suite"
  key_links:
    - from: "src/ui/expenses.js"
      to: "toggleExpCleared"
      via: "callback trigger"
---

<objective>
Implement right-swipe to clear and refine the gesture experience for mobile.

Purpose: Complete the swipe gesture system with reconciliation support and mobile-focused polish.
Output: Final version of src/ui/expenses.js and css/main.css with all requirements met.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/ui/expenses.js
@src/utils/gestures.js
@css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Swipe-to-Clear</name>
  <files>src/ui/expenses.js</files>
  <action>
    Add support for right-swipe gestures to clear expenses.
    
    - In `expensesUI.render()`: Insert `<div class="swipe-action-left" onclick="toggleExpCleared(${item.id}, '${item.type}', ${isCleared})">Clear</div>` into each `tr`.
    - Note: Only reveal/allow this affordance if `this.reconciliationMode` is true.
    - Update `SwipeHandler` logic in `expensesUI` to handle positive delta (right swipe).
    - If swiped right beyond threshold: snap to +80px (revealed state).
  </action>
  <verify>
    <automated>grep -q "swipe-action-left" src/ui/expenses.js</automated>
  </verify>
  <done>Right-swipe reveals clear button in reconciliation mode.</done>
</task>

<task type="auto">
  <name>Task 2: Protect Reconciled Rows</name>
  <files>src/ui/expenses.js</files>
  <action>
    Modify the gesture initialization to ignore swiping on locked rows.
    
    In the touchstart handler:
    - Check if the targeted row `tr` has the class `.reconciled-row` or if the item is already reconciled.
    - If it is, `return` early and do not instantiate the `SwipeHandler`.
  </action>
  <verify>
    <automated>grep -q "reconciled-row" src/ui/expenses.js</automated>
  </verify>
  <done>Reconciled rows are protected from gestures.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Mobile Experience & Edge Conflicts</name>
  <files>src/ui/expenses.js, css/main.css</files>
  <action>
    Implement mobile-specific refinements to prevent browser conflicts.
    
    - In `SwipeHandler` (src/utils/gestures.js): Check the initial `clientX`. If it's < 20px from either edge of the screen, ignore the swipe to avoid conflicting with iOS "Back" or "Forward" gestures.
    - In `css/main.css`: Add `touch-action: pan-y;` to the `.swipe-row` to prevent the browser from trying to handle horizontal scrolls.
    - Ensure `e.preventDefault()` is called during swipe ONLY when the horizontal threshold (e.g. 10px) is met, allowing vertical scroll to work otherwise.
  </action>
  <verify>
    <automated>grep -q "touch-action: pan-y" css/main.css</automated>
  </verify>
  <done>Mobile edge conflicts and scroll behavior refined.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full swipe gesture system with delete, clear, and protection logic.</what-built>
  <how-to-verify>
    1.  Open Expenses tab.
    2.  Swipe left on a row: red Delete button should appear. Tap it -> haptic + delete confirmation.
    3.  Enter Reconciliation Mode.
    4.  Swipe right on a row: green Clear button should appear. Tap it -> haptic + row highlights green.
    5.  Confirm reconciled (locked) rows do not swipe.
    6.  Confirm rows snap back if swipe threshold not met.
    7.  Confirm haptics fire when crossing the threshold (light tick) and on completion (delete/success pattern).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual verification is critical for this phase given the mobile hardware requirement.
</verification>

<success_criteria>
- All SWP-* requirements from ROADMAP are met.
- No regression in table performance or layout.
- Smooth animations on mobile devices.
</success_criteria>

<output>
After completion, create `.planning/phases/09-swipe-gesture-system/09-03-SUMMARY.md`
</output>
