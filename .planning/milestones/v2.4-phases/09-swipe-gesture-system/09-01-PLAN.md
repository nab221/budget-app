---
phase: 09-swipe-gesture-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/gestures.js, css/main.css, src/utils/haptics.js]
autonomous: true
requirements: [SWP-03, SWP-05]
must_haves:
  truths:
    - "SwipeHandler tracks touch events and calculates delta correctly"
    - "CSS affordances are hidden off-screen by default but visible when row is transformed"
    - "Threshold-cross haptic pattern is available in the haptics utility"
  artifacts:
    - path: "src/utils/gestures.js"
      provides: "Universal SwipeHandler class"
    - path: "css/main.css"
      provides: "Styles for swipe-action affordances"
    - path: "src/utils/haptics.js"
      provides: "New 'threshold' haptic pattern"
  key_links:
    - from: "src/utils/gestures.js"
      to: "src/utils/haptics.js"
      via: "triggerHaptic call"
---

<objective>
Establish the infrastructure for swipe gestures including a reusable SwipeHandler utility, CSS affordances, and haptic refinements.

Purpose: Provide the foundational tools (utility, styles, patterns) needed to implement swipe-to-delete/clear on individual rows.
Output: src/utils/gestures.js, updated css/main.css, and updated src/utils/haptics.js.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/utils/haptics.js
@css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SwipeHandler Utility</name>
  <files>src/utils/gestures.js</files>
  <action>
    Create a SwipeHandler class that handles touch events (touchstart, touchmove, touchend).
    
    It should:
    - Track the horizontal delta (dX) of a touch sequence.
    - Provide callbacks: onSwipe(deltaX), onEnd(deltaX, isThresholdMet), onThresholdCross(direction).
    - Handle a configurable threshold (e.g. 60px).
    - Prevent vertical scrolling once horizontal threshold is crossed (e.g. >10px).
    - Include logic for a small dead-zone to avoid accidental triggers.
    - Prevent edge-swipe conflicts (e.g. ignore swipes starting within 20px of screen edge).
    - Be generic enough to be applied to any element.
  </action>
  <verify>
    <automated>node -e "import('./src/utils/gestures.js').then(m => console.log('Gestures utility exists:', !!m.SwipeHandler))"</automated>
  </verify>
  <done>SwipeHandler class exported and handles touch tracking logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS Foundation for Swipe Affordances</name>
  <files>css/main.css</files>
  <action>
    Add CSS styles for swipe-revealed actions behind table rows.
    
    Styles to add:
    - .swipe-row: position: relative; overflow: visible; (on the tr)
    - .swipe-action-left, .swipe-action-right: position: absolute; top: 0; bottom: 0; display: flex; align-items: center; justify-content: center; width: 80px; z-index: -1; font-size: 0.8rem; font-weight: 600; color: #fff;
    - .swipe-action-left: left: -80px; background: var(--danger); border-radius: 4px 0 0 4px; (revealed on right swipe)
    - .swipe-action-right: right: -80px; background: var(--success); border-radius: 0 4px 4px 0; (revealed on left swipe)
    - .swipe-active: transition: transform 0.2s ease-out; (applied to the tr when it needs to snap back or open)
    
    Note: The 'z-index: -1' only works if the parent (tr) or its cells don't have an opaque background that covers the absolute child. We may need to ensure the row's background doesn't block the reveal.
  </action>
  <verify>
    <automated>grep -q "\.swipe-action-left" css/main.css</automated>
  </verify>
  <done>CSS classes for swipe-reveal are defined.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Haptic Patterns</name>
  <files>src/utils/haptics.js</files>
  <action>
    Add a dedicated 'threshold' pattern to the haptic patterns.
    
    Update HAPTIC_PATTERNS:
    - Add 'threshold': [10] (short light tap for when user crosses the activation line)
    
    This ensures that when a user swiping crosses the threshold, they get a tactile 'tick' before they even let go.
  </action>
  <verify>
    <automated>grep -q "threshold" src/utils/haptics.js</automated>
  </verify>
  <done>Threshold haptic pattern is available.</done>
</task>

</tasks>

<verification>
No logic is wired to the UI yet, but we check that the utility and styles are correctly defined.
</verification>

<success_criteria>
- src/utils/gestures.js exists and exports SwipeHandler.
- css/main.css contains .swipe-action-* styles.
- src/utils/haptics.js includes the 'threshold' pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/09-swipe-gesture-system/09-01-SUMMARY.md`
</output>
