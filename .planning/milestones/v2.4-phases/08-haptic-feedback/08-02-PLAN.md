---
phase: 08-haptic-feedback
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/ui/expenses.js, src/ui/transactions.js, src/app.js]
autonomous: true
requirements: [HAP-01, HAP-02]
must_haves:
  truths:
    - Saving a transaction or income produces a 'success' double-pulse.
    - Deleting an expense or income produces a 'delete' pulse after the confirmation dialog.
    - Validation failures trigger the 'error' pulse synchronously with the alert.
    - Toggling "Cleared" or "Paid" status produces a short 'tap'.
    - Toggling Privacy Mode produces a 'tap'.
  artifacts:
    - src/ui/expenses.js: Integrated triggerHaptic and alertWithHaptic calls.
    - src/ui/transactions.js: Integrated triggerHaptic and alertWithHaptic calls.
  key_links:
    - UI handlers -> src/utils/haptics.js: Tactile feedback on all mutations.
---

# Plan: Phase 8-02 - Core Haptic Feedback Integration

<objective>
Integrate haptic feedback into the main Expenses and Income tabs, and Privacy Mode.
</objective>

<context>
@src/utils/haptics.js
@src/ui/expenses.js
@src/ui/transactions.js
@src/app.js
</context>

<tasks>
<task type="auto">
  <name>Integrate Haptics into Expenses</name>
  <files>src/ui/expenses.js</files>
  <action>
    - Import `triggerHaptic` and `alertWithHaptic`.
    - Replace `alert()` with `alertWithHaptic()` in `handleSaveExpense`.
    - Add `triggerHaptic('success')` to `handleSaveExpense` on success.
    - Add `triggerHaptic('delete')` to `deleteExpense` (after confirmation).
    - Add `triggerHaptic('tap')` to `toggleExpCleared`.
    - Add `triggerHaptic('success')` to `handleMarkAllPaid`.
  </action>
  <verify>Expenses mutations and validation errors trigger appropriate haptic patterns.</verify>
  <done>Tactile feedback is fully integrated into the Expenses tab.</done>
</task>

<task type="auto">
  <name>Integrate Haptics into Income and Transactions</name>
  <files>src/ui/transactions.js</files>
  <action>
    - Import `triggerHaptic` and `alertWithHaptic`.
    - Replace `alert()` with `alertWithHaptic()` in `handleSaveIncome`.
    - Add `triggerHaptic('success')` to `handleSaveIncome` on success.
    - Add `triggerHaptic('delete')` to `deleteIncome` (after confirmation).
    - Add `triggerHaptic('success')` to `handleReconcileIncome`.
  </action>
  <verify>Income mutations and validation errors trigger appropriate haptic patterns.</verify>
  <done>Tactile feedback is fully integrated into the Income tab.</done>
</task>

<task type="auto">
  <name>Integrate Haptics into Privacy Mode</name>
  <files>src/app.js</files>
  <action>
    - In `src/app.js`, import `triggerHaptic`.
    - Add `triggerHaptic('tap')` to the end of `togglePrivacyMode`.
  </action>
  <verify>Toggling Privacy Mode (top-right button) triggers a short tap pulse.</verify>
  <done>Privacy Mode provides tactile confirmation.</done>
</task>
</tasks>

<success_criteria>
- Core data-mutating actions in Expenses and Income provide tactile confirmation.
- Validation failures produce an 'error' vibration pulse.
- Privacy Mode toggle is tactile.
</success_criteria>
