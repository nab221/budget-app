---
phase: 05-forecast-and-graph-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/utils/cashflow.js, src/ui/charts.js]
autonomous: true
requirements: [DASH-03, FORC-01]

must_haves:
  truths:
    - "getDailyRollingData returns balance, income, and expense arrays"
    - "calculateForecast uses a default 45-day horizon"
    - "Rolling Overview chart renders three distinct lines with legend"
  artifacts:
    - path: "src/utils/cashflow.js"
      provides: "Unified forecast and rolling data aggregation"
    - path: "src/ui/charts.js"
      provides: "Multi-line support for Rolling Overview chart"
  key_links:
    - from: "src/utils/cashflow.js"
      to: "calculateForecast"
      via: "logic sharing"
---

<objective>
Unify the forecasting logic in the data layer and enhance the chart component to support multi-line data visualization.

Purpose: Ensure the chart and table use identical calculation engines and the same 45-day horizon.
Output: Refactored data utilities and a multi-line chart component.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-forecast-and-graph-alignment/RESEARCH.md
@src/utils/cashflow.js
@src/ui/charts.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify Forecast Logic and Enrich Data Structure</name>
  <files>src/utils/cashflow.js</files>
  <action>
    - Update `calculateForecast` to default to a 45-day horizon.
    - Refactor `getDailyRollingData` to:
      - Return an enriched object: `{ labels, data: { balance, income, expenses }, todayIndex }`.
      - Use a 45-day forecast window (instead of 60).
      - Ensure the logic for the forecast segment (from `today` onwards) is perfectly aligned with `calculateForecast`.
      - Collect `income` and `expenses` totals for every day (both history and forecast).
    - Avoid code duplication between `getDailyRollingData`'s projection logic and `calculateForecast` by extracting the shared core loop or having one call the other.
  </action>
  <verify>
    - `node -e "import('./src/utils/cashflow.js').then(m => m.getDailyRollingData().then(d => console.log(Object.keys(d.data))))"` contains balance, income, expenses.
    - `node -e "import('./src/utils/cashflow.js').then(m => m.calculateForecast('2026-03-01').then(d => console.log(d.length)))"` returns 45.
  </verify>
  <done>Data layer provides identical, enriched snapshots for both historical and forecast segments.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Rolling Overview Chart for Multi-line Support</name>
  <files>src/ui/charts.js</files>
  <action>
    - Update `renderRollingOverviewChart` to handle the new `data.data` structure: `{ balance, income, expenses }`.
    - Add two new datasets to the chart: "Income" and "Expenses".
    - Use `OKABE_ITO.income` (#0072B2) for Income and `OKABE_ITO.fixed` (#D55E00) or similar for Expenses.
    - Keep "Balance" as the primary line.
    - Enable the legend to allow users to toggle lines.
    - Update tooltips to show all three values for a given date.
    - Ensure the 'today' vertical line/marker still works correctly with the new datasets.
  </action>
  <verify>
    - Chart.js configuration in `renderRollingOverviewChart` includes 3 datasets.
    - `legend: { display: true }` is set.
  </verify>
  <done>Rolling Overview chart visually presents balance, income, and expenses with a clear legend.</done>
</task>

</tasks>

<verification>
1. Run `npm run dev` and check for console errors in `getDailyRollingData` or chart rendering.
2. Verify the data returned by `getDailyRollingData` matches the expected schema.
</verification>

<success_criteria>
- `getDailyRollingData` returns balance, income, and expense arrays.
- `calculateForecast` defaults to 45 days.
- Chart displays three distinct lines.
</success_criteria>

<output>
After completion, create `.planning/phases/05-forecast-and-graph-alignment/05-01-SUMMARY.md`
</output>
