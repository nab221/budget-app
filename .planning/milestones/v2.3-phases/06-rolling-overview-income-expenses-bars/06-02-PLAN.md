---
phase: 06-rolling-overview-income-expenses-bars
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/ui/charts.js, src/ui/dashboard.js, css/main.css]
autonomous: true
requirements: [DASH-04, ANAL-04]

must_haves:
  truths:
    - "Chart displays account balance as a black line"
    - "Chart displays income as green bars"
    - "Chart displays expenses as red bars pointing downwards"
    - "User can toggle between Daily, Weekly, and Monthly views using a segmented control"
    - "Chart correctly aggregates and re-renders when binning is changed"
  artifacts:
    - path: "src/ui/charts.js"
      provides: "Mixed chart implementation (Line + Bar)"
    - path: "src/ui/dashboard.js"
      provides: "Binning selection UI and state management"
    - path: "css/main.css"
      provides: "Styles for the segmented control"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "src/utils/cashflow.js"
      via: "getDailyRollingData with binning parameter"
    - from: "src/ui/charts.js"
      to: "Chart.js"
      via: "BarController and BarElement registration"
---

<objective>
Update the 'Rolling Financial Overview' to a mixed chart (Line + Bars) and add a user interface for selecting time binning.

Purpose: Improve visual clarity of financial trends and allow for long-term zoom-out.
Output: A redesigned chart with a modern segmented control for period selection.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rolling-overview-income-expenses-bars/06-RESEARCH.md
@src/ui/charts.js
@src/ui/dashboard.js
@css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Chart Utility for Mixed Chart support</name>
  <files>src/ui/charts.js</files>
  <action>
    - Import and register `BarController` and `BarElement` from `chart.js`.
    - Modify `renderRollingOverviewChart(canvasId, data)` to:
      1. Use a mixed chart approach (specify `type: 'line'` for balance and `type: 'bar'` for income/expenses datasets).
      2. In the expenses dataset, map values to negative magnitude: `data: expenses.map(e => -Math.abs(e))`.
      3. Set appropriate colors: income (green/blue), expenses (vermilion/red), balance (black).
      4. Ensure `todayIndex` logic for dashed/solid lines still works for the balance line.
      5. Adjust bar spacing/thickness if necessary for readability.
  </action>
  <verify>
    <automated>MISSING — Requires visual verification in next task</automated>
  </verify>
  <done>Chart utility supports mixed Line/Bar charts and correctly negates expense values for visual separation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Binning UI and Integration in Dashboard</name>
  <files>src/ui/dashboard.js, css/main.css</files>
  <action>
    - Add `_selectedBinning` state to `src/ui/dashboard.js` (default 'D').
    - Create a modern segmented control (radio button group) in the Dashboard above the Rolling Chart.
    - Labels: 'D' (Daily), 'W' (Weekly), 'M' (Monthly).
    - Add CSS from 06-RESEARCH.md to `css/main.css` to style the control.
    - Update `renderDashboard()` to pass `_selectedBinning` to `getDailyRollingData()`.
    - Ensure chart re-renders immediately when binning is changed.
  </action>
  <verify>
    <automated>MISSING — Requires manual verification</automated>
  </verify>
  <done>UI for binning selection implemented and connected to the chart data flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>New Rolling Financial Overview with Mixed Chart and Binning</what-built>
  <how-to-verify>
    1. Visit the Dashboard.
    2. Confirm the Rolling Financial Overview shows a black line (balance) with green bars (income) and red downward bars (expenses).
    3. Toggle the binning selector:
       - 'D': Confirm daily granularity.
       - 'W': Confirm weekly aggregation (fewer bars, higher sums).
       - 'M': Confirm monthly aggregation (fewest bars, largest sums).
    4. Hover over bars/points to confirm tooltips show correct values.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Ensure that switching binning does not break the balance line (it should still match the daily balance at the end of each period).
</verification>

<success_criteria>
- User can see income and expenses as bars.
- User can switch between D/W/M binning.
- Chart is responsive and tooltips are accurate.
</success_criteria>

<output>
After completion, create `.planning/phases/06-rolling-overview-income-expenses-bars/06-02-SUMMARY.md`
</output>
