---
phase: 07-code-inconsistencies-inefficiencies
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/ui/charts.js
  - src/utils/cashflow.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "barForecastPlugin and its Chart.register() call are gone from charts.js"
    - "BarController and BarElement are removed from charts.js imports and Chart.register()"
    - "getDailyRollingData signature has no binning parameter"
    - "aggregateRollingOverview function is deleted from cashflow.js"
    - "getRollingFinancialData function is deleted from cashflow.js"
    - "Unused date-fns imports (startOfWeek, startOfMonth, format, parseISO) removed from cashflow.js"
    - "Full test suite remains green after all deletions"
  artifacts:
    - path: "src/ui/charts.js"
      provides: "Clean Chart.js registration — only components actually used by active charts"
    - path: "src/utils/cashflow.js"
      provides: "No dead exports — only getDailyRollingData, calculateForecast, and genuinely-used utilities remain"
  key_links:
    - from: "src/utils/cashflow.js"
      to: "date-fns"
      via: "Import list must contain only imports used by remaining functions"
    - from: "src/ui/charts.js"
      to: "Chart.register()"
      via: "Must not reference BarController, BarElement, or barForecastPlugin after deletion"
---

<objective>
Remove all dead code confirmed in research. This plan executes after bug fixes are verified green, so regressions are caught immediately by the test suite.

Purpose: Eliminate unreachable code that was accumulated during Phases 1–6, reducing cognitive overhead for future maintainers and preventing confusion about which rendering paths are active.
Output: charts.js and cashflow.js with all confirmed-dead code removed.
</objective>

<execution_context>
@C:/Users/nab221/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-code-inconsistencies-inefficiencies/07-CONTEXT.md
@.planning/phases/07-code-inconsistencies-inefficiencies/07-RESEARCH.md
@.planning/phases/07-code-inconsistencies-inefficiencies/07-02-SUMMARY.md
</context>

<interfaces>
<!-- Dead code to remove — exact research-confirmed locations. -->

charts.js dead code (confirmed in research):
- barForecastPlugin object definition: lines 36–58
- Chart.register(barForecastPlugin): line 60
- BarController in import (line 4): remove from named import list
- BarElement in import (line 10): remove from named import list
- BarController in Chart.register(): remove from register call (lines 20-31 range)
- BarElement in Chart.register(): remove from register call

cashflow.js dead code (confirmed in research):
- getDailyRollingData signature: `binning = 'D'` parameter on line 300 — remove parameter
- Dead binning branch at lines 398-402:
  ```javascript
  if (binning !== 'D') {
    return aggregateRollingOverview(result, binning);
  }
  ```
  Remove this block entirely.
- aggregateRollingOverview function: lines 545–618 — delete entire function
- getRollingFinancialData function: lines 447–507 — delete entire function
- date-fns imports in cashflow.js that become unused after aggregateRollingOverview deletion:
  - startOfWeek, startOfMonth, format, parseISO — remove from import (verify each is not used elsewhere first)

Callers confirmed safe (from research):
- getDailyRollingData: only caller is dashboard.js line 95 — no binning arg passed, no change needed in dashboard.js
- aggregateRollingOverview: only callers are the deleted dead branch + cashflow.test.js (describe block already removed in plan 01)
- getRollingFinancialData: zero callers in /src outside cashflow.js itself
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove barForecastPlugin and dead bar imports from charts.js</name>
  <files>src/ui/charts.js</files>
  <action>
    Read src/ui/charts.js in full before editing.

    Step 1 — Remove barForecastPlugin:
    Delete the entire `const barForecastPlugin = { ... }` object definition (lines 36–58 per research).
    Delete the `Chart.register(barForecastPlugin)` call (line 60 per research).

    Step 2 — Remove BarController and BarElement from the import statement at the top of the file.
    Find the named import from 'chart.js' that includes BarController and BarElement. Remove only those two names from the import list. Keep all other imports (CategoryScale, LinearScale, LineController, LineElement, PointElement, Filler, Tooltip, Legend, etc. — whatever active charts use).

    Step 3 — Remove BarController and BarElement from the Chart.register() call.
    Find the Chart.register() call (multi-line, lines 20-31 range). Remove BarController and BarElement from the arguments. Keep all other registered components.

    Do not change any chart rendering functions (renderRollingOverviewChart, renderTrendsChart, etc.).
    Do not change any other Chart.js plugin registrations.

    Run the test suite after editing.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
    Expected: full suite green. No "BarController is not defined" or similar errors.
  </verify>
  <done>charts.js contains no reference to barForecastPlugin, BarController, or BarElement. Chart.register() only includes components used by active charts. Full test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead functions and binning param from cashflow.js</name>
  <files>src/utils/cashflow.js</files>
  <action>
    Read src/utils/cashflow.js in full before editing. Line numbers may have shifted from plan 02 edits — verify exact current positions before deleting.

    Step 1 — Remove binning parameter from getDailyRollingData.
    Find: `export async function getDailyRollingData(targetMonth, binning = 'D')`
    Change to: `export async function getDailyRollingData(targetMonth)`

    Step 2 — Remove the dead binning branch near the end of getDailyRollingData.
    Find and delete:
    ```javascript
    if (binning !== 'D') {
      return aggregateRollingOverview(result, binning);
    }
    ```

    Step 3 — Delete getRollingFinancialData.
    Find `export async function getRollingFinancialData(targetMonth)` and delete the entire function body (the function that was at lines 447–507 before plan 02 edits).

    Step 4 — Delete aggregateRollingOverview.
    Find `export function aggregateRollingOverview(dailyData, binning = 'D')` and delete the entire function body (was at lines 545–618).

    Step 5 — Clean up date-fns imports.
    Open the import block at the top of cashflow.js (lines 13–17 area). Before removing any import, grep for each name across the rest of cashflow.js:
    - `startOfWeek` — if zero occurrences outside aggregateRollingOverview (now deleted), remove from import
    - `startOfMonth` — same check, remove if unused
    - `format` — same check (note: advanceNextDate in recurrence.js uses format, but cashflow.js should be checked independently)
    - `parseISO` — same check

    Remove only the names confirmed unused. Keep any that appear elsewhere in cashflow.js.

    After all deletions, run the test suite. If any "X is not defined" errors appear, those imports were used elsewhere — add them back.

    Ensure the public API of cashflow.js is unchanged: getDailyRollingData, calculateForecast, and other legitimate exports still present and working.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
    Expected: full suite green. No import errors. Balance equality test still passes. No reference to aggregateRollingOverview or getRollingFinancialData remaining.
  </verify>
  <done>
    - getDailyRollingData has no binning parameter and no binning conditional branch
    - aggregateRollingOverview deleted from cashflow.js
    - getRollingFinancialData deleted from cashflow.js
    - date-fns imports trimmed to only what cashflow.js actually uses
    - Full test suite green including balance equality integration test
  </done>
</task>

</tasks>

<verification>
Final phase gate check:

1. Full test suite: `npm test -- --run` — all green
2. No dead code references: confirm these identifiers are gone
   - `grep -n "barForecastPlugin\|aggregateRollingOverview\|getRollingFinancialData" src/ui/charts.js src/utils/cashflow.js` — should return nothing
   - `grep -n "BarController\|BarElement" src/ui/charts.js` — should return nothing
3. Dashboard caller unchanged: `grep -n "getDailyRollingData" src/ui/dashboard.js` — should show call with no binning arg (same as before)
</verification>

<success_criteria>
- barForecastPlugin: deleted from charts.js
- BarController + BarElement: removed from charts.js imports and Chart.register()
- getDailyRollingData: no binning parameter, no binning branch
- aggregateRollingOverview: deleted from cashflow.js
- getRollingFinancialData: deleted from cashflow.js
- Unused date-fns imports: removed from cashflow.js import block
- npm test -- --run: fully green
- dashboard.js: unchanged (no binning arg was ever passed)
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-inconsistencies-inefficiencies/07-03-SUMMARY.md`
</output>
