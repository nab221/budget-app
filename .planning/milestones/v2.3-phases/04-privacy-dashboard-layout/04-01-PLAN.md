---
phase: 04-privacy-dashboard-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/dashboard.js, src/ui/payoff.js, css/main.css]
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - "Dashboard summary cards are blurred when Privacy Mode is enabled"
    - "Savings Rate percentage and savings amount are blurred in Privacy Mode"
    - "All sensitive monetary amounts in the Payoff Planner tab are blurred in Privacy Mode"
    - "Blur is removed on hover for individual blurred elements"
  artifacts:
    - path: "src/ui/dashboard.js"
      provides: "Updated rendering with privacy-blur classes"
    - path: "src/ui/payoff.js"
      provides: "Updated rendering with privacy-blur classes"
    - path: "css/main.css"
      provides: "Enhanced privacy styles"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "body.privacy-enabled"
      via: "CSS selector dependency"
      pattern: ".privacy-blur"
---

<objective>
Implement privacy hardening for the Dashboard and Payoff Planner to ensure no information leakage when Privacy Mode is active.

Purpose: Protect user's sensitive financial data from being visible to bystanders.
Output: Updated Dashboard and Payoff Planner UIs that respect the global Privacy Mode toggle.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/dashboard.js
@src/ui/payoff.js
@css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blur Dashboard Summary Cards & Savings Rate KPI</name>
  <files>src/ui/dashboard.js</files>
  <action>
    - Update `adjustFontSize(el, pence)` to use `el.innerHTML = <span class="privacy-blur">${displayValue}</span>` instead of `el.textContent`. This will cover the main values in summary cards.
    - Update `savingsRateKPI` rendering to wrap the percentage value and the "Current Savings" amount in `<span class="privacy-blur">`.
    - Update the childcare list rendering in `renderDashboard` to wrap the balance in `<span class="privacy-blur">`.
    - Update the `warning` text in summary cards if it contains monetary values (e.g., "Projected £X on Date").
  </action>
  <verify>
    <automated>grep -c "privacy-blur" src/ui/dashboard.js</automated>
  </verify>
  <done>Summary cards and Savings Rate KPI values are wrapped in the privacy-blur class.</done>
</task>

<task type="auto">
  <name>Task 2: Global Privacy Masking for Payoff Planner</name>
  <files>src/ui/payoff.js</files>
  <action>
    - Wrap all monetary values rendered in `renderCreditCardPayoff` and `renderLoanPayoff` in `<span class="privacy-blur">` (specifically the interest amounts and strategy comparison values).
    - Wrap all monetary values in the "Consolidated Payoff Schedule" table in `renderConsolidatedSchedule` in `<span class="privacy-blur">`.
    - Wrap the upfront fee, potential savings, and recommended monthly payment in `renderBTModeler` in `<span class="privacy-blur">`.
    - Ensure the hover-to-reveal behavior works by using the span wrapper.
  </action>
  <verify>
    <automated>grep -c "privacy-blur" src/ui/payoff.js</automated>
  </verify>
  <done>All sensitive monetary amounts in the Payoff Planner are masked via the privacy-blur class.</done>
</task>

<task type="auto">
  <name>Task 3: Enhance Privacy CSS and Chart Masking</name>
  <files>css/main.css</files>
  <action>
    - Ensure `privacy-blur` is applied to containers if needed, but primarily maintain the individual element blurring for interactive reveal.
    - Add a CSS rule to blur charts in Privacy Mode: `body.privacy-enabled canvas { filter: blur(15px); transition: filter 0.3s ease; }` and allow reveal on hover: `body.privacy-enabled .chart-container:hover canvas { filter: blur(0); }`.
  </action>
  <verify>
    <automated>grep -c "body.privacy-enabled canvas" css/main.css</automated>
  </verify>
  <done>Privacy Mode now masks charts and supports interactive reveal for Payoff Planner cards.</done>
</task>

</tasks>

<verification>
Check Dashboard:
1. Toggle Privacy Mode ON.
2. Verify all Summary Card values are blurred.
3. Verify Savings Rate percentage and Savings amount are blurred.
4. Verify Childcare balances are blurred.
5. Verify charts are blurred.
6. Hover over elements to verify they reveal.

Check Payoff Planner:
1. Toggle Privacy Mode ON.
2. Verify all amounts in comparison cards, schedule table, and BT modeler are blurred.
3. Verify charts are blurred.
</verification>

<success_criteria>
- Zero information leakage (monetary values/charts) on Dashboard and Payoff Planner when Privacy Mode is enabled.
- Smooth hover-to-reveal experience for all masked elements.
</success_criteria>

<output>
After completion, create `.planning/phases/04-privacy-dashboard-layout/04-01-SUMMARY.md`
</output>
