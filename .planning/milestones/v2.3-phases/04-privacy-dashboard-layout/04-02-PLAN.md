---
phase: 04-privacy-dashboard-layout
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [index.html, css/main.css]
autonomous: false
requirements: [DASH-02, UX-01]

must_haves:
  truths:
    - "The 'Dashboard' heading (H2) is removed from the dashboard panel"
    - "The Period Selector (Month Picker + View Select) is the first component in the dashboard"
    - "The Summary Grid (cards) follows the Period Selector"
    - "Analytics charts (Rolling Overview + Category/Savings) are at the bottom"
  artifacts:
    - path: "index.html"
      provides: "New dashboard layout structure"
  key_links:
    - from: "src/ui/dashboard.js"
      to: "index.html"
      via: "DOM IDs (dashboardMonthPicker, summaryGrid, rollingOverviewChartContainer)"
      pattern: "getElementById"
---

<objective>
Refine the Dashboard layout by removing redundant titles and reordering components for better flow (controls first, then overview, then analytics).

Purpose: Optimize the Dashboard for quick period-based status checks.
Output: Restructured Dashboard layout in index.html.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder Dashboard Components & Remove Duplicate Title</name>
  <files>index.html</files>
  <action>
    - Open `index.html`.
    - Locate the `[data-panel="dashboard"]` div.
    - Remove the `<div class="card-hd"><h2>Dashboard</h2></div>` block (it is redundant with the active tab).
    - Reorder the remaining blocks to follow this sequence:
      1. Period Selector (div containing `#dashboardMonthPicker` and `#viewSelect`).
      2. Summary Cards (`#summaryGrid`).
      3. Rolling Financial Overview Chart (`#rollingOverviewChartContainer`).
      4. Spending Analytics Section (`grid2` containing Spending Breakdown and Savings Rate).
    - Ensure all container IDs remain identical so `dashboard.js` logic continues to function.
  </action>
  <verify>
    <automated>grep -n "dashboardMonthPicker" index.html</automated>
  </verify>
  <done>Dashboard components are reordered: Period Selector -> Summary Cards -> Charts.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>New Dashboard layout with reordered components.</what-built>
  <how-to-verify>
    1. Open the application.
    2. Go to the Dashboard tab.
    3. Verify that the Month Picker and View Select are at the top.
    4. Verify the Summary Cards follow immediately.
    5. Verify the Rolling Chart and Category Charts are at the bottom.
    6. Verify that switching months/views still works as expected.
    7. Check mobile responsiveness (navigation should still be bottom-bar and layout should stack).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check Dashboard Layout:
1. "Dashboard" H2 is gone.
2. Period Selector is first.
3. Summary Cards are second.
4. Charts are at the bottom.
</verification>

<success_criteria>
- Clearer hierarchy on the Dashboard.
- Controls are easily accessible at the top.
- Redundant information removed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-privacy-dashboard-layout/04-02-SUMMARY.md`
</output>
