# Milestone Requirements Archive: v1.4 Local File Persistence

**Status:** COMPLETED
**Date:** 2026-03-02

## Requirements

### REPO: Repository Refactoring
- [x] **REPO-01**: Consolidate all database writes/mutations to `src/db/repository.js`. (Validated)
- [x] **REPO-02**: Remove all inline Dexie calls from `budget-app.html` and move to repository. (Validated)
- [x] **REPO-03**: Implement a middleware or hook in the repository to trigger sync on every mutation. (Validated)

### FS: File System Handle Management
- [x] **FS-01**: Implement `src/utils/storage.js` utility for picking a file handle. (Validated)
- [x] **FS-02**: Persist the picked file handle in IndexedDB. (Validated)
- [x] **FS-03**: Implement `requestPermission` and `queryPermission` logic for persisted handles. (Validated)
- [x] **FS-04**: Verify handle validity on app load and provide re-authorization flow. (Validated)

### SYNC: Auto-save & Sync Logic
- [x] **SYNC-01**: Implement periodic/triggered export of full database to the selected file handle. (Validated)
- [x] **SYNC-02**: Add a debounce (500ms) to mutation-triggered saves. (Validated)
- [x] **SYNC-03**: Implement resilience logic for cloud-synced folders (retries + backoff). (Validated)
- [x] **SYNC-04**: Ensure atomic-like writes using `createWritable` swap file mechanism. (Validated)

### UI: File Persistence UX
- [x] **UI-01**: Add a "Local File Sync" section in the settings/toolbar. (Validated)
- [x] **UI-02**: Button to "Select Local File" and initialize sync. (Validated)
- [x] **UI-03**: Visual indicator of sync status (Saved, Error, etc). (Validated)
- [x] **UI-04**: "Change File" and "Reset" buttons for handle management. (Validated)

## Traceability

| Requirement | Status | Outcome |
|-------------|--------|---------|
| REPO-01 | ✓ | Fully integrated into `src/db/repository.js`. |
| REPO-02 | ✓ | `budget-app.html` archived to `.bak`. |
| REPO-03 | ✓ | `triggerSync()` hook implemented and verified. |
| FS-01 | ✓ | `checkFileSupport` utility implemented. |
| FS-02 | ✓ | `HandleStore` implemented via native IndexedDB. |
| FS-03 | ✓ | `queryPermission` check before every save. |
| FS-04 | ✓ | Handle loaded and verified on boot in `app.js`. |
| SYNC-01 | ✓ | Full payload exported via `SyncManager.saveToFile`. |
| SYNC-02 | ✓ | 500ms debounce implemented. |
| SYNC-03 | ✓ | Exponential backoff retry logic implemented. |
| SYNC-04 | ✓ | Native `createWritable` mechanism used. |
| UI-01 | ✓ | Modal and toolbar integrated. |
| UI-02 | ✓ | "Select Budget File" workflow functional. |
| UI-03 | ✓ | Status text updates in real-time. |
| UI-04 | ✓ | Change and Reset workflows verified. |

---
*Archived from REQUIREMENTS.md on 2026-03-02*
