---
phase: 19-expected-income-integration
plan: 19-02
type: execute
wave: 2
depends_on: [19-01]
files_modified: [index.html, src/app.js, src/ui/expected-income.js, src/ui/render.js]
autonomous: true
requirements: [EXPI-01.2, EXPI-01.3]
---

<objective>
Create the "Cash Flow Planner" UI to manage expected income predictions.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add Tab and Panel to UI</name>
  <files>index.html, src/app.js, src/ui/render.js</files>
  <action>
    - Add "Cash Flow Planner" tab to `index.html`.
    - Add corresponding panel div to `index.html`.
    - Update `src/app.js` to handle the new tab.
    - Update `src/ui/render.js` to export a registration for the new UI.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Expected Income UI</name>
  <files>src/ui/expected-income.js</files>
  <action>
    Create `src/ui/expected-income.js` with:
    - `renderExpectedIncome()`: Fetches predictions and existing expectedIncome records.
    - If no records exist, show a "Generate Predictions" button that calls `generateExpectedIncomePredictions`.
    - List of expected income with:
        - Confirm: Creates real income record, deletes expected record.
        - Edit: Opens modal (use existing income modal pattern).
        - Delete: Removes from database.
  </action>
</task>
</tasks>
