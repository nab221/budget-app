---
phase: 20-cash-flow-experience
plan: 20-01
type: execute
wave: 1
depends_on: []
files_modified: [src/ui/charts.js, src/ui/dashboard.js, src/app.js]
autonomous: true
requirements: [UI-01.1, UI-01.2, UI-01.3, UI-01.4]
---

<objective>
Enhance the Dashboard with daily cash flow forecast visualizations and critical date alerts.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement Cash Flow Chart</name>
  <files>src/ui/charts.js</files>
  <action>
    Add `renderCashFlowChart(canvasId)` to `src/ui/charts.js`.
    - Fetch 90 days of daily snapshots.
    - Render a line chart (Balance vs Date).
    - Add a horizontal red line at £0.
    - Style points < 0 in red.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Dashboard Forecast Summary</name>
  <files>src/ui/dashboard.js</files>
  <action>
    Add `renderCashFlowForecast()` to `src/ui/dashboard.js`.
    - Identify critical date (first date balance < 0).
    - Identify next income date (confirmed or expected).
    - Render a warning card if a critical date exists.
    - Render a 7-day timeline (horizontal list with icons/amounts).
  </action>
</task>

<task type="auto">
  <name>Task 3: Integrate with App Initialization</name>
  <files>src/app.js, index.html</files>
  <action>
    - Add `cashflowChartContainer` and `cashflowForecastSection` to `index.html`.
    - Update `src/app.js` to call `renderCashFlowForecast()` during dashboard refresh.
  </action>
</task>
</tasks>
