---
phase: 18-forecast-logic
plan: 18-02
type: execute
wave: 2
depends_on: [18-01]
files_modified: [src/db/repository.js]
autonomous: true
requirements: [FORC-01.1]

must_haves:
  truths:
    - "Changes to income/expenses trigger a daily forecast recalculation."
    - "Forecast is saved efficiently to the database via bulk operations."
  artifacts:
    - path: "src/db/repository.js"
      provides: "triggerForecastRecalc function and repository updates"
  key_links:
    - from: "incomeRepository.add"
      to: "triggerForecastRecalc"
      via: "Mutation trigger"
---

<objective>
Update repository to support bulk operations and integrate the forecast trigger into the mutation workflow.

Purpose: To ensure the 90-day forecast is always up-to-date with the latest user data while maintaining high performance.
Output: Integrated forecast triggers in `src/db/repository.js` and an optimized `dailyBalanceRepository`.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-forecast-logic/18-01-SUMMARY.md
@src/db/repository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize dailyBalanceRepository and implement triggerForecastRecalc</name>
  <files>src/db/repository.js</files>
  <action>
    - Add `bulkSave(snapshots)` to `dailyBalanceRepository` using `db.dailyBalanceSnapshots.bulkPut`.
    - Implement `triggerForecastRecalc(date)` in `src/db/repository.js`.
    - This function should:
      1. Determine the start date for recalculation (usually `Math.min(today, date)`).
      2. Call `calculateForecast(startDate, 90)` (lazy-import if needed).
      3. Use `dailyBalanceRepository.bulkSave` to persist snapshots.
      4. Dispatch `app:refresh` event.
  </action>
  <verify>grep "async function triggerForecastRecalc" src/db/repository.js</verify>
  <done>Trigger function and bulk save implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire triggers to repositories</name>
  <files>src/db/repository.js</files>
  <action>
    Add `triggerForecastRecalc(date).catch(() => {})` to:
    - `incomeRepository`: add, update, delete.
    - `recurrentExpenseRepository`: add, update, delete.
    - `oneOffExpenseRepository`: add, update, delete.
    - `expectedIncomeRepository`: add, update, delete.
    
    Ensure it's called after the database operation succeeds.
  </action>
  <verify>
    grep "triggerForecastRecalc" src/db/repository.js | measure-object -line
    # Should see at least 8-10 calls
  </verify>
  <done>All relevant mutations now trigger a daily forecast recalculation.</done>
</task>

</tasks>

<verification>
Manual verification: Add a transaction and check if `triggerForecastRecalc` is called (via console logs or debugger).
</verification>

<success_criteria>
- `triggerForecastRecalc` is present and correctly implemented.
- All income and expense mutations trigger the daily forecast update.
- Performance is maintained via bulk database operations.
</success_criteria>

<output>
After completion, create `.planning/phases/18-forecast-logic/18-02-SUMMARY.md`
</output>
