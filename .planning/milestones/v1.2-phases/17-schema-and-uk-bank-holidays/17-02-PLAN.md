---
phase: 17-schema-and-uk-bank-holidays
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified: [src/utils/cashflow.js, src/utils/cashflow.test.js]
autonomous: true
requirements: [SCHEMA-01.4, SCHEMA-01.5]
must_haves:
  truths:
    - "UK Bank Holidays are fetched from gov.uk API"
    - "Holiday data is cached in localStorage"
    - "isWorkingDay correctly identifies weekends and bank holidays"
    - "nextWorkingDay correctly shifts dates to the next working day"
  artifacts:
    - path: "src/utils/cashflow.js"
      provides: "Holiday logic and caching"
    - path: "src/utils/cashflow.test.js"
      provides: "Unit tests for cashflow utilities"
  key_links:
    - from: "src/utils/cashflow.js"
      to: "https://www.gov.uk/bank-holidays.json"
      via: "fetch()"
    - from: "src/utils/cashflow.js"
      to: "bankHolidayRepository"
      via: "User override check"
---

<objective>
Implement UK Bank Holiday handling with offline caching and working day logic.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-schema-and-uk-bank-holidays/17-CONTEXT.md
@.planning/phases/17-schema-and-uk-bank-holidays/17-01-SUMMARY.md
@src/db/repository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Holiday Logic and Caching</name>
  <files>src/utils/cashflow.js</files>
  <action>
    Create `src/utils/cashflow.js` with the following exports:
    1. `fetchHolidays()`:
       - Fetch `https://www.gov.uk/bank-holidays.json`.
       - Extract `england-and-wales.events.map(e => e.date)`.
       - Store in `localStorage['bank-holidays-cache']` as `{ timestamp: Date.now(), dates: string[] }`.
       - Include a 24-hour cache expiry check before fetching.
    2. `isBankHoliday(dateStr)`:
       - Check `bankHolidayRepository.isOverrideActive(dateStr)` (User overrides).
       - If no override, check `localStorage` cache.
    3. `isWorkingDay(dateStr)`:
       - Returns `false` if Saturday (6) or Sunday (0).
       - Returns `false` if `isBankHoliday(dateStr)` is true.
       - Returns `true` otherwise.
    4. `nextWorkingDay(dateStr)`:
       - Increments date by 1 day until `isWorkingDay` is true.
    
    Make sure to handle potential fetch errors gracefully (e.g. use cached data even if expired if fetch fails).
  </action>
  <verify>
    Check file exports and logic.
  </verify>
  <done>Holiday utility implemented with caching and working day logic.</done>
</task>

<task type="auto">
  <name>Task 2: Unit Tests for Cashflow Utility</name>
  <files>src/utils/cashflow.test.js</files>
  <action>
    Create `src/utils/cashflow.test.js` to verify:
    1. `isWorkingDay` identifies weekends correctly.
    2. `isWorkingDay` identifies bank holidays (mocking localStorage/repository).
    3. `nextWorkingDay` shifts a Friday to a Monday if Monday is not a holiday.
    4. `nextWorkingDay` shifts a Friday to a Tuesday if Monday is a bank holiday.
    5. `isBankHoliday` respects user overrides from `bankHolidayRepository`.
  </action>
  <verify>
    `npm test tests/src/utils/cashflow.test.js` (Wait, check where tests are run).
    Based on file list, tests are in `src/utils/*.test.js` and run via `test-security.js`?
    Actually, I'll use `npm test` if available or `node` for a simple test script if no runner is configured.
    Check `package.json` for test command.
  </verify>
  <done>Unit tests pass for all major holiday/working day scenarios.</done>
</task>

</tasks>

<verification>
Verify `src/utils/cashflow.js` exists and tests pass.
Check `localStorage` for the cache entry after calling `fetchHolidays()`.
</verification>

<success_criteria>
- UK Bank Holidays can be fetched and cached.
- Weekend and Holiday logic correctly identifies working days.
- User overrides are respected in holiday checks.
</success_criteria>

<output>
After completion, create `.planning/phases/17-schema-and-uk-bank-holidays/17-02-SUMMARY.md`
</output>
