---
phase: 17-schema-and-uk-bank-holidays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/schema.js, src/db/repository.js]
autonomous: true
requirements: [SCHEMA-01.1, SCHEMA-01.2, SCHEMA-01.3]
must_haves:
  truths:
    - "Schema v10 is active in IndexedDB"
    - "New tables dailyBalanceSnapshots, expectedIncome, and bankHolidayOverrides exist"
    - "recurrentExpenses have predictedPaymentDate field"
    - "Existing recurrentExpenses are migrated to have predictedPaymentDate = nextDate"
  artifacts:
    - path: "src/db/schema.js"
      provides: "Schema v10 definition and migration"
    - path: "src/db/repository.js"
      provides: "dailyBalanceRepository, expectedIncomeRepository, bankHolidayRepository"
  key_links:
    - from: "src/db/schema.js"
      to: "db.version(10)"
      via: "Dexie version upgrade"
    - from: "src/db/repository.js"
      to: "db.dailyBalanceSnapshots"
      via: "Repository exports"
---

<objective>
Implement Schema v10 and create the necessary repositories to support the Daily Cash Flow Engine.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-schema-and-uk-bank-holidays/17-CONTEXT.md
@src/db/schema.js
@src/db/repository.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Schema v10 and Migrations</name>
  <files>src/db/schema.js</files>
  <action>
    Update `src/db/schema.js` to version 10.
    1. Define `db.version(10).stores({ ... })`.
    2. Add new tables:
       - `dailyBalanceSnapshots: '++id, date, openingBalance, closingBalance, incomeTotal, expenseTotal'`
       - `expectedIncome: '++id, date, source, amount, categoryId, status'`
       - `bankHolidayOverrides: '++id, date, isOpen'`
    3. Update `recurrentExpenses` store definition to include `predictedPaymentDate` (add it to the index string).
    4. Implement `.upgrade(async tx => { ... })` for version 10:
       - Iterate over `recurrentExpenses` and set `predictedPaymentDate` equal to the existing `nextDate` for each record if `predictedPaymentDate` is missing.
    5. Ensure all existing tables from version 9 are maintained in the version 10 stores definition.
  </action>
  <verify>
    node -e "import { db } from './src/db/schema.js'; console.log(db.verno);" (Wait, this is ESM, use a test script or just check if it compiles)
    Alternative: Check for `db.version(10)` in file content.
  </verify>
  <done>Schema v10 defined with correct stores and migration logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add Repositories for New Tables</name>
  <files>src/db/repository.js</files>
  <action>
    Add three new repositories to `src/db/repository.js` using the established patterns:
    1. `dailyBalanceRepository`:
       - `getByDate(date)`: find snapshot for specific date.
       - `save(snapshot)`: upsert snapshot (check if existing by date).
       - `deleteFrom(fromDate)`: delete all snapshots from a specific date onwards.
       - `getLatestSnapshot()`: get the most recent snapshot by date.
    2. `expectedIncomeRepository`:
       - `...createBaseRepository(db.expectedIncome)`
       - `getByMonth(monthStr)`: filter by date startsWith.
    3. `bankHolidayRepository`:
       - `...createBaseRepository(db.bankHolidayOverrides)`
       - `getOverride(date)`: get override for a specific date.
       - `isOverrideActive(date)`: boolean check for isOpen.
    
    Ensure `createBaseRepository` includes `status` in `expectedIncome` if needed, but it's mostly for amount fields. `expectedIncome` has `amount`, so it should use it.
  </action>
  <verify>
    `grep "export const dailyBalanceRepository" src/db/repository.js`
    `grep "export const expectedIncomeRepository" src/db/repository.js`
    `grep "export const bankHolidayRepository" src/db/repository.js`
  </verify>
  <done>Repositories exported and support required operations.</done>
</task>

</tasks>

<verification>
Check `src/db/schema.js` and `src/db/repository.js` for new definitions.
Run any existing database tests to ensure no regressions.
</verification>

<success_criteria>
- Schema v10 is correctly defined in `src/db/schema.js`.
- Migration for `recurrentExpenses` is implemented.
- New repositories are exported from `src/db/repository.js`.
</success_criteria>

<output>
After completion, create `.planning/phases/17-schema-and-uk-bank-holidays/17-01-SUMMARY.md`
</output>
