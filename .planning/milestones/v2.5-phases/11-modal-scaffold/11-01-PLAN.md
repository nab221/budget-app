---
phase: 11-modal-scaffold
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/ui/debts.test.js
autonomous: true
requirements:
  - MODAL-01
  - MODAL-02
  - MODAL-03
  - MODAL-04

must_haves:
  truths:
    - "npx vitest run src/ui/debts.test.js runs without errors (tests fail RED, not crash)"
    - "Four failing tests exist — one per MODAL requirement"
    - "Test file uses jsdom environment via inline directive"
  artifacts:
    - path: "src/ui/debts.test.js"
      provides: "Failing unit tests for MODAL-01, MODAL-02, MODAL-03, MODAL-04"
      exports: []
  key_links:
    - from: "src/ui/debts.test.js"
      to: "src/ui/debts.js"
      via: "vi.mock and import"
      pattern: "import.*debts"
---

<objective>
Create the failing test scaffold for Phase 11 so Plan 02 has a concrete RED baseline to turn GREEN.

Purpose: Nyquist compliance — every implementation task needs an automated verify command. All four MODAL requirements map to `src/ui/debts.test.js`.
Output: One test file with four failing tests (one per MODAL requirement). Tests must run (not crash) and fail for the right reason — the functions under test don't exist yet.
</objective>

<execution_context>
@C:/Users/nab221/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v2.5-phases/11-modal-scaffold/11-RESEARCH.md
@.planning/milestones/v2.5-phases/11-modal-scaffold/11-VALIDATION.md

<interfaces>
<!-- Key APIs the test file must mock and assert against. Extracted from source. -->

From src/ui/render.js (lines 64-135):
```javascript
export const modalUI = {
  elements: { overlay, title, body, footer, close },
  init(),         // sets up close onclick, keydown Esc listener
  show(title, content, footerArray),  // removes 'hidden' from overlay, body.overflow='hidden'
  close()         // adds 'hidden' to overlay, body.overflow=''
};
```

From src/ui/debts.js (current state — functions that will be added by Plan 02):
```javascript
export const debtUI = {
  editingId: null,           // tracks id being edited; null = Add mode
  openDebtModal(id = null),  // to be created in Plan 02
  _closeDebtModal(),         // to be created in Plan 02
};
```

Test environment: Vitest 3.x with jsdom (devDependency already present).
Add `// @vitest-environment jsdom` as the first line of the test file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing test scaffold for MODAL-01 through MODAL-04</name>
  <files>src/ui/debts.test.js</files>
  <behavior>
    - MODAL-01: `openDebtModal()` calls `modalUI.show()` — assert `modalUI.show` was called with a title containing 'Debt' and the overlay becomes visible
    - MODAL-02: After `openDebtModal()`, clicking the overlay element (backdrop) calls `modalUI.close()` — assert close is called on a simulated overlay click
    - MODAL-03: After `openDebtModal()`, `document.body.style.overflow` equals `'hidden'`; after `_closeDebtModal()`, it equals `''`
    - MODAL-04: After `openDebtModal()`, `document.getElementById('debtNameInput').focus()` was called — assert the element received focus
  </behavior>
  <action>
    Create `src/ui/debts.test.js` with the following structure:

    1. First line: `// @vitest-environment jsdom`
    2. Import `vi`, `describe`, `it`, `expect`, `beforeEach` from 'vitest'
    3. Mock `src/ui/render.js` via `vi.mock('../ui/render.js', ...)` (or relative from test location) — mock `modalUI` with `show: vi.fn()`, `close: vi.fn()`, `init: vi.fn()`, and `elements: { overlay: null, close: null }`. The mock's `show` should simulate scroll lock: set `document.body.style.overflow = 'hidden'` inside the mock implementation. The mock's `close` should restore it: `document.body.style.overflow = ''`.
    4. Mock `src/db/repository.js` via `vi.mock` — stub `debtRepository.get` to return a minimal debt object so Edit mode doesn't crash.
    5. Import `debtUI` from `src/ui/debts.js` after the mocks.
    6. In `beforeEach`: reset all mocks, create a minimal DOM — an `#addDebtBtn` button, a `#debtFormContainer` div, and a `#debtNameInput` input — appended to `document.body`. Also reset `debtUI.editingId = null`.

    Four `it` blocks:
    - `'MODAL-01: openDebtModal calls modalUI.show'` — call `debtUI.openDebtModal()`, assert `modalUI.show` toHaveBeenCalledOnce and title arg contains 'Debt'
    - `'MODAL-02: backdrop click calls modalUI.close'` — call `debtUI.openDebtModal()`, get the overlay element, dispatch a click event on it with `target` set to the overlay itself, assert `modalUI.close` toHaveBeenCalled (or `debtUI._closeDebtModal` was triggered — assert `debtUI.editingId === null`)
    - `'MODAL-03: scroll is locked on open and restored on close'` — call `debtUI.openDebtModal()`, assert `document.body.style.overflow === 'hidden'`, then call `debtUI._closeDebtModal()`, assert `document.body.style.overflow === ''`
    - `'MODAL-04: name field receives focus after openDebtModal'` — add a spy on `HTMLElement.prototype.focus`, call `debtUI.openDebtModal()`, assert focus spy was called on the element with id `debtNameInput`

    These tests must FAIL at this point because `openDebtModal` and `_closeDebtModal` do not exist on `debtUI` yet. The test runner should exit with "TypeError: debtUI.openDebtModal is not a function" or similar — this confirms RED state.

    Note: The MODAL-02 backdrop test is necessarily loose at Wave 0 because the backdrop listener is added inside `openDebtModal` after `modalUI.show()` — in the mock, the overlay element is whatever jsdom provides. The test can verify `_closeDebtModal` was wired by checking `debtUI.editingId` is null after a simulated close event. Adjust if needed during Wave 1 green phase.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists. `npx vitest run src/ui/debts.test.js` runs without a config error and exits with failures (not crashes). Four test cases are listed. RED state confirmed.</done>
</task>

</tasks>

<verification>
`npx vitest run src/ui/debts.test.js` runs and reports 4 failing tests (not "no tests found" and not a module resolution crash).
</verification>

<success_criteria>
- `src/ui/debts.test.js` exists with 4 test cases covering MODAL-01 through MODAL-04
- Vitest can parse and run the file (jsdom environment directive present)
- All 4 tests fail for the correct reason: `openDebtModal` / `_closeDebtModal` not yet defined
- Test file is committed
</success_criteria>

<output>
After completion, create `.planning/milestones/v2.5-phases/11-modal-scaffold/11-01-SUMMARY.md`
</output>
