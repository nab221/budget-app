---
phase: 13-save-edit-and-validation
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified: [src/ui/debts.js]
autonomous: true
requirements: [ADD-01, ADD-02, ADD-03, EDIT-01, EDIT-02]

must_haves:
  truths:
    - "Clicking Add in Add mode saves the new debt and closes the modal"
    - "Clicking Save in Edit mode updates the existing debt and closes the modal"
    - "Validation errors appear as inline text beneath the relevant fields"
    - "Edit mode pre-populates all form fields including type-specific sets"
  artifacts:
    - path: "src/ui/debts.js"
      provides: "_saveDebt, _populateEditFields, _showFieldError methods"
  key_links:
    - from: "src/ui/debts.js"
      to: "src/db/repository.js"
      via: "debtRepository.add/update calls"
    - from: "src/ui/debts.js"
      to: "src/ui/render.js"
      via: "modalUI.show(..., buttons) wiring"
---

<objective>
Implement the Save, Edit, and Inline Validation logic for the Debt Modal.

Purpose: Completes the functional loop for debt management in the new modal architecture.
Output: Fully working Add and Edit flows for all debt types in src/ui/debts.js.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v2.5-phases/13-save-edit-and-validation/13-RESEARCH.md
@src/ui/debts.js
@src/ui/debts.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation helpers and wire modal footer buttons</name>
  <files>src/ui/debts.js</files>
  <action>
    1. Add `_showFieldError(fieldId, message)` to `debtUI`.
       - Find element by ID.
       - Remove existing `.field-error` sibling if present.
       - Create `<span>` with class `field-error` and set `textContent`.
       - Insert after the field element.
    2. Add `_clearFieldErrors()` to `debtUI`.
       - `document.querySelectorAll('.field-error').forEach(el => el.remove())`.
    3. Update `openDebtModal(id)` to include the primary action button (Save or Add) in the `buttons` array passed to `modalUI.show()`.
       - Label: `id === null ? 'Add' : 'Save'`.
       - onClick: `() => this._saveDebt()`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js --test-name "ADD-02"</automated>
  </verify>
  <done>Validation helpers exist and footer buttons are wired.</done>
</task>

<task type="auto">
  <name>Task 2: Implement _saveDebt handler with payload building</name>
  <files>src/ui/debts.js</files>
  <action>
    Implement `async _saveDebt()` in `debtUI` following Pattern 1 and Pattern 6 from RESEARCH.md:
    1. Call `this._clearFieldErrors()`.
    2. Read `name` and `type` from `FIELD_IDS`.
    3. Validate `name` is not empty; if empty, call `this._showFieldError` and return.
    4. Branch on `type` to build the payload for all 4 types (credit-card, mortgage, loan, other).
    5. Use `parseFloat(el.value) || 0` for money fields (repository handles `toPence` conversion).
    6. For `mortgage` and `loan`, ensure `payload.apr` is synced with `interestRate` for strategy sorting.
    7. Branch on `this.editingId` to call `debtRepository.update(this.editingId, payload)` or `debtRepository.add(payload)`.
    8. On success: `triggerHaptic('success')`, `this._closeDebtModal()`, `this.render()`, and `window.app?.renderAll()`.
    9. On error: log to console and show top-level inline error using `_showFieldError(FIELD_IDS.name, ...)`.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js --test-name "ADD-01|EDIT-01"</automated>
  </verify>
  <done>Add and Edit save paths are fully functional.</done>
</task>

<task type="auto">
  <name>Task 3: Implement field pre-population for Edit mode</name>
  <files>src/ui/debts.js</files>
  <action>
    1. Implement `_populateEditFields(debt)` in `debtUI` following Pattern 3 from RESEARCH.md.
       - Set `.value` for `name` and `type`.
       - Branch on `debt.debtType` to set values for all type-specific fields in `FIELD_IDS`.
       - Use `fromPence()` for `currentBalance`, `creditLimit`, `originalPrincipal`, `fixedMonthlyPayment`, and `earlyRepaymentFee`.
       - Set other fields (apr, rates, terms, dates) directly.
    2. Update `openDebtModal(id)`:
       - After `this._onTypeChange()`, call `this._populateEditFields(debt)` if `id` is not null.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js --test-name "EDIT-02"</automated>
  </verify>
  <done>Edit mode correctly pre-populates all form fields.</done>
</task>

</tasks>

<verification>
Run all tests:
`npx vitest run src/ui/debts.test.js`
All 14 tests (9 existing + 5 new) should pass.
</verification>

<success_criteria>
- ADD-01 through EDIT-02 requirements are met and verified by tests.
- Form opens fresh for Add mode and pre-populated for Edit mode.
- Save works correctly for all 4 debt types, calling the correct repository method.
- Validation errors appear inline, not as alerts.
- Repository pence conversion is respected (no double-conversion bugs).
</success_criteria>

<output>
After completion, create `.planning/phases/13-save-edit-and-validation/13-03-SUMMARY.md`
</output>
