---
phase: 16-debt-history-ux-refinement
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - src/ui/debts.js
  - src/ui/debts.test.js
autonomous: true
requirements:
  - HIST-01
  - HIST-02
must_haves:
  truths:
    - "History modal table renders with fixed column widths totalling ~665px and a horizontal scroll wrapper"
    - "Date column sticks to the left edge, Actions column sticks to the right edge during horizontal scroll"
    - "A scroll hint indicator is visible when the modal opens and fades after ~2 seconds"
    - "Statement rows display Edit as a pencil icon (✏️) instead of the text 'Edit'"
    - "Date values display as '08 Mar' format, large Opening/Closing values display as '£1.2k'"
  artifacts:
    - path: "src/ui/debts.js"
      provides: "_buildHistoryModalHTML with column widths, sticky styles, scroll wrapper; renderStatements with pencil icon and formatted values"
    - path: "src/ui/debts.test.js"
      provides: "Tests asserting table structure, column presence, pencil icon in rendered HTML"
  key_links:
    - from: "debtUI.openHistoryModal()"
      to: "_buildHistoryModalHTML(debt)"
      via: "returns HTML string injected via modalUI.show"
      pattern: "_buildHistoryModalHTML"
    - from: "debtUI.renderStatements(debtId)"
      to: "stmtBody-modal tbody"
      via: "innerHTML assignment with safeHTML"
      pattern: "renderStatements"
---

<objective>
Refine the history modal table layout (HIST-01) and replace text "Edit" buttons with pencil icons (HIST-02) in statement rows.

Purpose: History modal table wraps badly on mobile and the text "Edit" is inconsistent with the pencil icon used on debt card rows.
Output: Correct column widths, sticky Date/Actions columns, scroll edge shadows, fade-in scroll indicator, "08 Mar" date format, "£1.2k" abbreviation for large values, and ✏️ icon in every statement row's action cell.
</objective>

<execution_context>
@C:/Users/nab221/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-debt-history-ux-refinement/16-CONTEXT.md
@.planning/phases/16-debt-history-ux-refinement/16-RESEARCH.md
@.planning/phases/16-debt-history-ux-refinement/16-01-SUMMARY.md
@src/ui/debts.js
@src/ui/debts.test.js

<interfaces>
<!-- Current _buildHistoryModalHTML structure (debts.js ~line 793) -->
The method returns a safeHTML`` template string. Key container:
```html
<div style="overflow-x:auto">
  <table class="tbl sm">
    <thead>
      <tr>
        <th>Date</th>              <!-- needs: width:80px, sticky left -->
        <th class="r">Opening</th> <!-- needs: width:70px -->
        <th class="r">Closing</th> <!-- needs: width:70px -->
        <th class="r">Int</th>     <!-- needs: width:50px -->
        <th class="r">Fees</th>    <!-- needs: width:50px -->
        <th class="r">Min Due</th> <!-- needs: width:65px -->
        <th>Due Date</th>          <!-- needs: width:80px -->
        <th class="r">Paid</th>    <!-- needs: width:60px -->
        <th>Paid On</th>           <!-- needs: width:80px -->
        <th></th>                  <!-- needs: width:60px, sticky right -->
      </tr>
    </thead>
    <tbody id="stmtBody-modal">...</tbody>
  </table>
</div>
```

<!-- Current renderStatements row (debts.js ~line 851) -->
```javascript
<td class="r nw">
  <button class="sm ghost" onclick="debtUI.editStatement(${s.id}, ${debtId})">Edit</button>  // ← change to ✏️
  <button class="sm danger" onclick="deleteStatement(${s.id}, ${debtId})">✕</button>
</td>
```

<!-- Scroll indicator pattern from RESEARCH.md -->
After modalUI.show() in openHistoryModal():
```javascript
const wrapper = document.getElementById('stmtTableWrapper');
wrapper?.classList.add('scroll-hint-visible');
setTimeout(() => wrapper?.classList.remove('scroll-hint-visible'), 2000);
```

<!-- CSS sticky pattern from RESEARCH.md -->
On <th> and <td> for Date column (first col, sticky left):
  position: sticky; left: 0; z-index: 2; background: var(--bg);
On <th> and <td> for Actions column (last col, sticky right):
  position: sticky; right: 0; z-index: 2; background: var(--bg);
The overflow wrapper must use overflow-x: auto; overflow-y: visible (NOT overflow: auto on both axes).
Give the wrapper id="stmtTableWrapper" for the scroll indicator setTimeout.

<!-- Date format helper -->
"08 Mar" format:
const fmtDate = (d) => d ? new Date(d).toLocaleDateString('en-GB', { day:'2-digit', month:'short' }) : '—';

<!-- Currency abbreviation helper -->
Inline in renderStatements (not a shared utility):
const abbrevGBP = (pence) => {
  if (!pence) return '—';
  const pounds = pence / 100;
  return pounds >= 1000 ? `£${(pounds/1000).toFixed(1)}k` : formatGBP(pence);
};
Apply to Opening (openingBalance) and Closing (amount) columns only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: History table layout — column widths, sticky columns, scroll UX (HIST-01)</name>
  <files>src/ui/debts.js, src/ui/debts.test.js</files>
  <behavior>
    - Test: _buildHistoryModalHTML HTML contains 'stmtTableWrapper'
    - Test: _buildHistoryModalHTML HTML contains 'sticky' (for sticky column styles)
    - Test: _buildHistoryModalHTML HTML contains at least 10 <th> elements (all columns present)
    - Test: renderStatements for a statement with date '2024-03-08' renders '08 Mar' in the row HTML
    - Test: renderStatements for a statement with openingBalance=150000 (£1,500 pence) renders '£1.5k' in the row HTML
  </behavior>
  <action>
    Write tests first (RED). Then implement:

    In _buildHistoryModalHTML:
    1. Change the outer div to: `<div id="stmtTableWrapper" style="overflow-x:auto; overflow-y:visible; position:relative">`
    2. Inject a <style> block inside the modal body (before the table div) with:
       - .stmt-tbl th, .stmt-tbl td — base: white-space: nowrap; padding: 4px 6px
       - .stmt-tbl th:first-child, .stmt-tbl td:first-child — position:sticky; left:0; z-index:2; background:var(--bg)
       - .stmt-tbl th:last-child, .stmt-tbl td:last-child — position:sticky; right:0; z-index:2; background:var(--bg)
       - Explicit widths on each <th> via style attribute (not CSS class):
         Date=80px, Opening=70px, Closing=70px, Int=50px, Fees=50px, Min Due=65px, Due Date=80px, Paid=60px, Paid On=80px, Actions=60px
       - .scroll-hint-visible::after — a pseudo-element showing a subtle "scroll →" indicator
         (e.g. position:absolute; right:0; top:50%; content:'→'; opacity:1; transition:opacity 0.5s)
       - .scroll-hint-visible-remove — opacity:0 (or use setTimeout to remove class)
    3. Add class="stmt-tbl" to the <table> element
    4. In openHistoryModal(), after modalUI.show(), add:
       ```
       const wrapper = document.getElementById('stmtTableWrapper');
       if (wrapper) {
         wrapper.classList.add('scroll-hint-visible');
         setTimeout(() => wrapper.classList.remove('scroll-hint-visible'), 2000);
       }
       ```

    In renderStatements:
    5. Add above the stmts.map() call:
       ```
       const fmtDate = (d) => d ? new Date(d).toLocaleDateString('en-GB', { day:'2-digit', month:'short' }) : '—';
       const abbrevGBP = (pence) => {
         if (!pence && pence !== 0) return '—';
         const pounds = pence / 100;
         return pounds >= 1000 ? `£${(pounds/1000).toFixed(1)}k` : formatGBP(pence);
       };
       ```
    6. In the row template:
       - Date cell: use fmtDate(s.date) instead of s.date
       - Opening (openingBalance) cell: use abbrevGBP(s.openingBalance)
       - Closing (amount) cell: use abbrevGBP(s.amount)

    Keep all other columns using formatGBP as before.
    Use safeHTML for all row HTML — fmtDate and abbrevGBP return static strings with no user content, safe to interpolate.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js</automated>
  </verify>
  <done>
    Tests pass. HTML output of _buildHistoryModalHTML contains stmtTableWrapper and sticky styles.
    Statement rows show "08 Mar" dates and "£1.5k" for large Opening/Closing values.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace text "Edit" with pencil icon in statement rows (HIST-02)</name>
  <files>src/ui/debts.js, src/ui/debts.test.js</files>
  <behavior>
    - Test: renderStatements row HTML does NOT contain '>Edit<'
    - Test: renderStatements row HTML contains '✏️'
  </behavior>
  <action>
    Write test first (RED). Then implement:

    In renderStatements (debts.js), find the Actions <td> in the safeHTML row template:
    ```
    <button class="sm ghost" onclick="debtUI.editStatement(${s.id}, ${debtId})">Edit</button>
    ```
    Change to:
    ```
    <button class="sm ghost" title="Edit statement" onclick="debtUI.editStatement(${s.id}, ${debtId})">✏️</button>
    ```

    Keep class="sm ghost" and the onclick unchanged — only the button label changes.
    The title attribute provides tooltip text for accessibility.
  </action>
  <verify>
    <automated>npx vitest run src/ui/debts.test.js</automated>
  </verify>
  <done>
    Tests pass. No ">Edit<" string appears in renderStatements output. ✏️ appears in its place.
    Existing editStatement() wiring unchanged.
  </done>
</task>

</tasks>

<verification>
Run full suite after both tasks: `npx vitest run`
Manual: Open history modal, scroll the table left/right — Date column stays pinned left, Actions column stays pinned right. Observe scroll hint visible on open, fades in ~2s. Statement rows show ✏️ edit button.
</verification>

<success_criteria>
- History table renders with all 10 columns at specified widths (total ~665px)
- Date column sticks left, Actions column sticks right during horizontal scroll
- Scroll indicator visible on history modal open, gone after ~2s
- Statement date values display as "08 Mar" format
- Opening/Closing values >= £1,000 display as "£X.Xk"
- Edit button in statement rows is ✏️ icon, not text "Edit"
- All automated tests pass (npx vitest run)
</success_criteria>

<output>
After completion, create `.planning/phases/16-debt-history-ux-refinement/16-02-SUMMARY.md`
</output>
