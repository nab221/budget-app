# Milestone v1.5 Roadmap: Automatic Recurring Transactions

## Phase 1: Foundation & Schema (V12)
- [x] Update `src/db/schema.js` to version 12.
- [x] Implement migration logic in `upgrade()` to port `recurringTemplates` to `recurrentExpenses`.
- [x] Add `isRecurring`, `frequency`, `recurrenceId`, and `parentDate` to both expense tables.

## Phase 2: Recurrence Utility
- [x] Create `src/utils/recurrence.js`.
- [x] Implement `generateInstances(base, frequency, count)` using `date-fns`.
- [x] Implement `RecurrenceManager.checkAndGenerate()` for rolling updates on app boot.

## Phase 3: UI - Monthly Navigation
- [x] Implement `MonthPicker` component in `src/ui/expenses.js`.
- [x] Refactor `renderRecurrent()` and `renderOneOff()` to use strict month filtering.
- [x] Persist selected month in `localStorage` per sub-tab.

## Phase 4: UI - Form Updates
- [x] Update Recurrent and One-off forms with "Recurring" and "Frequency" fields.
- [x] Update `handleSaveExpense()` to use the recurrence generator.
- [x] Add visual 🔁 indicators to table rows.

## Phase 5: Edit/Delete Lifecycle
- [x] Implement `recurrenceModal` for "This vs All Future" choice.
- [x] Update `delete` handlers to handle series removal.
- [x] Update `update` handlers to handle series modification.

## Phase 6: Cleanup & Polish
- [x] Remove all Template-related code from `src/ui/templates.js` and `Settings`.
- [x] Update `README.md` with new feature documentation.
- [x] Verify Dashboard calculations correctly handle future pending items.

## Phase 7: UI - Income Monthly Navigation & Filtering
- [x] Implement `MonthPicker` component in `src/ui/transactions.js` (Income tab).
- [x] Refactor `incomeRepository.getByMonth` usage in UI to use local month state.
- [x] Persist selected income month in `localStorage`.
- [x] Add category/search filtering to Income view.
