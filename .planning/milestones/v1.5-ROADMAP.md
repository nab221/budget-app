# Milestone v1.5 Roadmap: Automatic Recurring Transactions

## Phase 1: Foundation & Schema (V12)
- [x] Update `src/db/schema.js` to version 12.
- [x] Implement migration logic in `upgrade()` to port `recurringTemplates` to `recurrentExpenses`.
- [x] Add `isRecurring`, `frequency`, `recurrenceId`, and `parentDate` to both expense tables.

## Phase 2: Recurrence Utility
- [ ] Create `src/utils/recurrence.js`.
- [ ] Implement `generateInstances(base, frequency, count)` using `date-fns`.
- [ ] Implement `RecurrenceManager.checkAndGenerate()` for rolling updates on app boot.

## Phase 3: UI - Monthly Navigation
- [ ] Implement `MonthPicker` component in `src/ui/expenses.js`.
- [ ] Refactor `renderRecurrent()` and `renderOneOff()` to use strict month filtering.
- [ ] Persist selected month in `localStorage` per sub-tab.

## Phase 4: UI - Form Updates
- [ ] Update Recurrent and One-off forms with "Recurring" and "Frequency" fields.
- [ ] Update `handleSaveExpense()` to use the recurrence generator.
- [ ] Add visual 🔁 indicators to table rows.

## Phase 5: Edit/Delete Lifecycle
- [ ] Implement `recurrenceModal` for "This vs All Future" choice.
- [ ] Update `delete` handlers to handle series removal.
- [ ] Update `update` handlers to handle series modification.

## Phase 6: Cleanup & Polish
- [ ] Remove all Template-related code from `src/ui/templates.js` and `Settings`.
- [ ] Update `README.md` with new feature documentation.
- [ ] Verify Dashboard calculations correctly handle future pending items.
