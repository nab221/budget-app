# Milestone v1.4 User Acceptance Testing (UAT)

**Status:** IN PROGRESS
**Browser Requirement:** Desktop Chrome/Edge (File System Access API support)

## Test Session: 2026-03-02
**Tester:** Claude (Gemini CLI) / User

| ID | Test Case | Expected Result | Status | Notes |
|----|-----------|-----------------|--------|-------|
| UAT-1.4.1 | First Launch Detection | Picker modal appears. | PASS | Modal shown on first launch. |
| UAT-1.4.2 | Create New File | Prompts native save dialog with "budget-data.json". | PASS | Native save dialog appeared with correct suggested name. |
| UAT-1.4.3 | Auto-save Mutation | Adding an income/expense record triggers "✓ Saved" status within 2s. | FAIL | "✓ Saved" does not appear automatically after mutation. |
| UAT-1.4.4 | Persistent Handle | Refreshing the page keeps the file connection (shows filename in toolbar). | PENDING | |
| UAT-1.4.5 | Change File | "Change File" button allows selecting a different file and saves current state first. | PENDING | |
| UAT-1.4.6 | Reset Persistence | "Reset" button clears handle from IndexedDB and returns to picker UI. | PENDING | |
| UAT-1.4.7 | Unsupported Fallback | (Simulation/Firefox) App shows legacy Export/Import buttons + Desktop hint. | PENDING | |

---
## Findings & Diagnosis
1. **Auto-save Logic failure (UAT-1.4.3)**: `scheduleAutoSave()` is not triggering `saveToFile()` correctly upon mutation. Analysis of `budget-app.html` shows `scheduleAutoSave()` is called, but `saveToFile()` might be failing due to `queryPermission` returning 'prompt' instead of 'granted' on a refresh, which cannot be automatically requested outside of a user gesture.

---
## Fix Plans
*None yet.*
