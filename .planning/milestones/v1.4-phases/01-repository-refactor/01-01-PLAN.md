---
phase: 01-repository-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/db/repository.js, src/db/repository.test.js]
autonomous: true
requirements: [REPO-01, REPO-03]
must_haves:
  truths:
    - "Category mutations call triggerSync()"
    - "Statement mutations call triggerSync()"
    - "Categorization learning updates call triggerSync()"
  artifacts:
    - path: "src/db/repository.js"
      provides: "Sync-enabled repository methods"
  key_links:
    - from: "categoryRepository.addCategory"
      to: "triggerSync"
      via: "function call"
    - from: "statementRepository.addWithExpense"
      to: "triggerSync"
      via: "function call"
---

<objective>
Update category and statement repositories to call triggerSync() on all mutations. This ensures that changes to categories and debt statements are automatically synced to the local file system.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/db/repository.js
@src/db/repository.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update categoryRepository mutations</name>
  <files>src/db/repository.js</files>
  <action>
    Modify `categoryRepository` methods in `src/db/repository.js` to call `triggerSync()` after every database write:
    - `addCategory`: Call `triggerSync()` after `db.categories.add`.
    - `deleteCategory`: Call `triggerSync()` after `db.categories.delete`.
    - `seedDefaultCategories`: Call `triggerSync()` after `db.categories.bulkAdd`.
    - `ensureOpeningBalanceCategory`: Call `triggerSync()` after `db.categories.add`.
  </action>
  <verify>
    <automated>grep -A 5 "categoryRepository = {" src/db/repository.js | grep "triggerSync"</automated>
  </verify>
  <done>Category mutations call triggerSync()</done>
</task>

<task type="auto">
  <name>Task 2: Update statementRepository and learning rule mutations</name>
  <files>src/db/repository.js</files>
  <action>
    Modify `statementRepository` and `updateCategorizationLearningRule` in `src/db/repository.js` to call `triggerSync()`:
    - `statementRepository.addWithExpense`: Call `triggerSync()` after the transaction.
    - `statementRepository.recordPayment`: Call `triggerSync()` after the transaction.
    - `statementRepository.resetPayment`: Call `triggerSync()` after the transaction.
    - `statementRepository.delete`: Call `triggerSync()` after `db.statements.delete`.
    - `statementRepository.deleteWithExpense`: Call `triggerSync()` after the transaction.
    - `updateCategorizationLearningRule`: Call `triggerSync()` after the loop finishes.
  </action>
  <verify>
    <automated>grep -A 50 "statementRepository = {" src/db/repository.js | grep "triggerSync"</automated>
  </verify>
  <done>Statement and learning rule mutations call triggerSync()</done>
</task>

<task type="auto">
  <name>Task 3: Verify with unit tests</name>
  <files>src/db/repository.test.js</files>
  <action>
    Update `src/db/repository.test.js` to verify that `triggerSync` is called:
    1. Define `global.window = { scheduleAutoSave: vi.fn() }` in a `beforeEach`.
    2. Add assertions to existing tests for `categoryRepository` and `statementRepository` to check if `window.scheduleAutoSave` was called.
    3. Run the tests.
  </action>
  <verify>
    <automated>npm test src/db/repository.test.js</automated>
  </verify>
  <done>Unit tests confirm triggerSync() is called</done>
</task>

</tasks>

<success_criteria>
- [ ] categoryRepository methods call triggerSync()
- [ ] statementRepository methods call triggerSync()
- [ ] updateCategorizationLearningRule calls triggerSync()
- [ ] npm test src/db/repository.test.js passes with sync call assertions
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-refactor/01-01-SUMMARY.md`
</output>
