---
phase: 01-repository-refactor
plan: 02
type: execute
wave: 2
depends_on: [01-repository-refactor-01]
files_modified: [src/db/repository.js, src/db/repository.test.js]
autonomous: true
requirements: [REPO-01, REPO-03]
must_haves:
  truths:
    - "Childcare mutations call triggerSync()"
    - "Balance snapshot mutations call triggerSync()"
    - "Daily balance mutations call triggerSync()"
  artifacts:
    - path: "src/db/repository.js"
      provides: "Sync-enabled repository methods"
  key_links:
    - from: "childcareRepository.saveAccount"
      to: "triggerSync"
      via: "function call"
    - from: "balanceSnapshotRepository.save"
      to: "triggerSync"
      via: "function call"
---

<objective>
Update childcare, balance snapshot, and daily balance repositories to call triggerSync() on all mutations. This ensures that changes to childcare accounts and balance records are automatically synced.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-refactor/01-01-SUMMARY.md
@src/db/repository.js
@src/db/repository.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update childcareRepository mutations</name>
  <files>src/db/repository.js</files>
  <action>
    Modify `childcareRepository` methods in `src/db/repository.js` to call `triggerSync()`:
    - `saveAccount`: Call `triggerSync()` after the update/add.
    - `deleteAccount`: Call `triggerSync()` after the transaction.
    - `addDeposit`: Call `triggerSync()` after the transaction.
    - `addSpend`: Call `triggerSync()` after the transaction.
  </action>
  <verify>
    <automated>grep -A 100 "childcareRepository = {" src/db/repository.js | grep "triggerSync"</automated>
  </verify>
  <done>Childcare mutations call triggerSync()</done>
</task>

<task type="auto">
  <name>Task 2: Update balance and daily snapshot repositories</name>
  <files>src/db/repository.js</files>
  <action>
    Modify snapshot repositories in `src/db/repository.js` to call `triggerSync()`:
    - `balanceSnapshotRepository.save`: Call `triggerSync()` after update/add.
    - `balanceSnapshotRepository.deleteFrom`: Call `triggerSync()` after bulkDelete.
    - `dailyBalanceRepository.save`: Call `triggerSync()` after update/add.
    - `dailyBalanceRepository.deleteFrom`: Call `triggerSync()` after bulkDelete.
    - `dailyBalanceRepository.bulkSave`: Call `triggerSync()` after bulkAdd.
  </action>
  <verify>
    <automated>grep -A 50 "balanceSnapshotRepository = {" src/db/repository.js | grep "triggerSync"</automated>
  </verify>
  <done>Snapshot mutations call triggerSync()</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for childcare and snapshots</name>
  <files>src/db/repository.test.js</files>
  <action>
    Add new test blocks to `src/db/repository.test.js` to verify `triggerSync` calls for:
    - `childcareRepository` (mock `db.childcareAccounts`, `db.childcareLedger`)
    - `balanceSnapshotRepository` (already exists, just add assertions)
    - `dailyBalanceRepository` (mock `db.dailyBalanceSnapshots`)
  </action>
  <verify>
    <automated>npm test src/db/repository.test.js</automated>
  </verify>
  <done>Unit tests confirm sync calls for snapshots and childcare</done>
</task>

</tasks>

<success_criteria>
- [ ] childcareRepository methods call triggerSync()
- [ ] balanceSnapshotRepository methods call triggerSync()
- [ ] dailyBalanceRepository methods call triggerSync()
- [ ] npm test src/db/repository.test.js passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-refactor/01-02-SUMMARY.md`
</output>
