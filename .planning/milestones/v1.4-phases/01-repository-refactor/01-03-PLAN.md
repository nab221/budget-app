---
phase: 01-repository-refactor
plan: 03
type: execute
wave: 3
depends_on: [01-repository-refactor-02]
files_modified: [src/db/repository.js, src/db/repository.test.js]
autonomous: true
requirements: [REPO-01, REPO-03]
must_haves:
  truths:
    - "Expected income overrides call triggerSync()"
    - "Net worth snapshots call triggerSync()"
    - "No async mutations are missing triggerSync()"
  artifacts:
    - path: "src/db/repository.js"
      provides: "Sync-enabled repository methods"
  key_links:
    - from: "expectedIncomeRepository.add"
      to: "triggerSync"
      via: "function call"
---

<objective>
Complete the repository refactor by updating forecast and net worth repositories, followed by a final comprehensive audit of src/db/repository.js to ensure every database mutation triggers a sync.
</objective>

<execution_context>
@C:/Users/nab221/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/nab221/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repository-refactor/01-02-SUMMARY.md
@src/db/repository.js
@src/db/repository.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update expectedIncomeRepository and netWorthRepository</name>
  <files>src/db/repository.js</files>
  <action>
    Update forecast-related mutations in `src/db/repository.js`:
    - `expectedIncomeRepository`: Ensure `add`, `update`, and `delete` call `triggerSync()`. Note: `createBaseRepository` calls it, but overrides must also call it if they don't delegate to `super`.
    - `netWorthRepository.checkAndTakeSnapshot`: Add `triggerSync()` after `db.netWorthSnapshots.add`.
  </action>
  <verify>
    <automated>grep -A 50 "expectedIncomeRepository = {" src/db/repository.js | grep "triggerSync"</automated>
  </verify>
  <done>Forecast and net worth mutations call triggerSync()</done>
</task>

<task type="auto">
  <name>Task 2: Final Audit of async mutations</name>
  <files>src/db/repository.js</files>
  <action>
    Exhaustively audit `src/db/repository.js` for any `db.[table].[mutation]` calls missing `triggerSync()`:
    - Mutation patterns to search: `.add(`, `.put(`, `.update(`, `.delete(`, `.bulkAdd(`, `.bulkDelete(`, `.bulkPut(`, `.clear(`.
    - Exclude patterns that already have `triggerSync()` (like those inside `createBaseRepository`).
    - Specifically check `triggerBalanceRecalc`, `triggerDailyForecastRecalc`, and `updateCategorizationLearningRule`.
    - Check `bankHolidayRepository` base methods are used correctly (it spreads `createBaseRepository` which is fine).
  </action>
  <verify>
    <automated>grep "db\..*\.\(add\|put\|update\|delete\|bulkAdd\|bulkDelete\|bulkPut\|clear\)" src/db/repository.js</automated>
  </verify>
  <done>No async mutations are missing triggerSync()</done>
</task>

<task type="auto">
  <name>Task 3: Complete test coverage and final verification</name>
  <files>src/db/repository.test.js</files>
  <action>
    Update tests to cover `expectedIncomeRepository` and `netWorthRepository` sync calls.
    Run all tests in `src/db/repository.test.js` one last time to ensure everything passes.
  </action>
  <verify>
    <automated>npm test src/db/repository.test.js</automated>
  </verify>
  <done>Comprehensive unit tests pass</done>
</task>

</tasks>

<success_criteria>
- [ ] expectedIncomeRepository calls triggerSync()
- [ ] netWorthRepository calls triggerSync()
- [ ] Every async mutation in src/db/repository.js has a corresponding triggerSync() call
- [ ] npm test src/db/repository.test.js passes with full coverage of sync calls
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-refactor/01-03-SUMMARY.md`
</output>
