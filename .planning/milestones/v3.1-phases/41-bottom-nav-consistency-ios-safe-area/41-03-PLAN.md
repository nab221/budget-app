---
phase: 41-bottom-nav-consistency-ios-safe-area
plan: "03"
type: execute
wave: 3
depends_on:
  - "41-01"
  - "41-02"
files_modified: []
autonomous: false
requirements:
  - BOTNAV-01
  - BOTNAV-02
  - BOTNAV-03
  - BOTNAV-04

must_haves:
  truths:
    - "User scrolls any of the 8 tabs on a mobile device and the bottom nav bar remains fully visible at all times"
    - "User scrolls to the bottom of Transactions or Income tab and the last content item is fully visible above the nav bar"
    - "On an iPhone with a home indicator, the nav bar does not overlap the home indicator"
    - "PWA update bar (when triggered) appears above the nav bar with nav icons remaining tappable"
  artifacts:
    - path: ".planning/phases/41-bottom-nav-consistency-ios-safe-area/41-03-SUMMARY.md"
      provides: "Human-verified confirmation of all 4 BOTNAV requirements"
  key_links:
    - from: "Chrome DevTools 390px verification"
      to: "BOTNAV-01, BOTNAV-02 confirmed"
      via: "manual scroll test on all 8 tabs"
    - from: "Safari responsive design mode or real iPhone"
      to: "BOTNAV-03 confirmed"
      via: "safe-area inset visually respected"
---

<objective>
Human verification that all four bottom nav requirements pass in a live browser environment.

Purpose: BOTNAV-01 through BOTNAV-03 are CSS layout and iOS rendering requirements that cannot be verified by Vitest + jsdom. Chrome DevTools confirms fixed positioning and content clearance (BOTNAV-01, BOTNAV-02). Safari responsive design mode or a real iPhone confirms env(safe-area-inset-bottom) activation with viewport-fit=cover (BOTNAV-03). BOTNAV-04 is confirmed via DevTools console simulation of the update bar.

Output: Human approval that all 4 requirements pass.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-bottom-nav-consistency-ios-safe-area/41-01-SUMMARY.md
@.planning/phases/41-bottom-nav-consistency-ios-safe-area/41-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify BOTNAV-01, BOTNAV-02, and BOTNAV-04 in Chrome DevTools at 390px</name>
  <files></files>
  <what-built>
Plan 01: viewport-fit=cover added to meta viewport; .nav-container moved to direct body child outside .shell; .shell mobile padding updated to include env(safe-area-inset-bottom, 0px).
Plan 02: PWA update bar JS implemented (onNeedRefresh wired, _showUpdateBar/_hideUpdateBar added); mobile CSS override raises update bar above nav.
  </what-built>
  <action>
Serve the app locally (npm run dev or equivalent). Open in Chrome. Present the verification checklist to the user and wait for their results.
  </action>
  <how-to-verify>
Open the app in Chrome. Open DevTools (F12). Toggle device toolbar (Ctrl+Shift+M). Select "iPhone 12 Pro" (390x844). Set zoom to 100%.

**BOTNAV-01 — Bottom nav fixed on all 8 tabs:**
1. Click each of the 8 tab buttons in turn
2. On each tab, scroll down — the bottom nav bar must remain fixed at the bottom of the viewport and never scroll away with the content
3. Expected: nav bar is always visible at the bottom; no tab causes it to disappear

**BOTNAV-02 — Last content item not hidden behind bottom nav:**
1. On the Transactions tab, scroll to the very bottom of the list
2. The last row/item must be fully visible above the top edge of the bottom nav bar — no content cut off
3. Repeat on any other tab with enough content to scroll
4. Expected: no content is hidden behind the nav on any tab

**BOTNAV-04 — Update bar appears above nav (DevTools simulation):**
1. In DevTools Console, run:
   ```javascript
   const bar = document.createElement('div');
   bar.id = 'pwa-update-bar';
   bar.className = 'update-bar';
   bar.innerHTML = '<span>A new version is available.</span><button>Update now</button><button>Later</button>';
   document.body.appendChild(bar);
   ```
2. The update bar must appear above the bottom nav bar — nav icons must still be visible below it
3. Expected: bar sits above the nav; both bar and nav are visible simultaneously
  </how-to-verify>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <resume-signal>Type "chrome-approved" if BOTNAV-01, BOTNAV-02, and BOTNAV-04 all pass. Describe any issues if they fail.</resume-signal>
  <done>Human confirms BOTNAV-01 (nav fixed on all 8 tabs), BOTNAV-02 (last item visible above nav), and BOTNAV-04 (update bar above nav) all pass in Chrome DevTools at 390px</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify BOTNAV-03 on Safari or real iPhone (iOS safe-area)</name>
  <files></files>
  <what-built>
viewport-fit=cover has been added to the meta viewport tag. The .nav-container already had padding-bottom: calc(env(safe-area-inset-bottom) + 8px) — that rule is now active. env(safe-area-inset-bottom) will return the real inset (~34px on iPhone X-series).
  </what-built>
  <action>
Note to executor: Chrome DevTools cannot simulate env(safe-area-inset-bottom) with viewport-fit=cover. Present the Safari/iPhone verification instructions to the user. If neither is available, document as ios-skipped.
  </action>
  <how-to-verify>
IMPORTANT: This check requires Safari or a real iPhone — Chrome DevTools returns 0 for env(safe-area-inset-bottom) even with viewport-fit=cover.

**Option A — Safari Responsive Design Mode:**
1. Open Safari on macOS
2. Enable Develop menu: Safari > Settings > Advanced > Show Develop menu
3. Open the app URL in Safari
4. Develop > Enter Responsive Design Mode (or Shift+Cmd+R)
5. Select "iPhone 15 Pro" (or any Face ID device) from the device list
6. Scroll down on any tab

**Option B — Real iPhone:**
1. Open the app on your iPhone in Safari or as a PWA
2. Scroll down on any tab

**What to check (BOTNAV-03):**
- The bottom nav bar sits ABOVE the home indicator (the thin bar at the very bottom of the screen)
- There is visible clearance between the nav bar's bottom edge and the home indicator
- The nav bar does NOT overlap or touch the home indicator
- Expected: ~34px gap between nav bottom and home indicator for Face ID devices

**If the nav overlaps the home indicator:**
- Check that `viewport-fit=cover` is in the rendered HTML: View Page Source > search for "viewport"
- Check DevTools > Elements > .nav-container > Computed > padding-bottom (should be non-zero)
  </how-to-verify>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <resume-signal>Type "ios-approved" if BOTNAV-03 passes (nav clears home indicator). Type "ios-skipped" if Safari/iPhone is not available. Describe any issues if it fails.</resume-signal>
  <done>Human confirms BOTNAV-03 passes on Safari or real iPhone (nav does not overlap home indicator), or documents as ios-skipped with reason</done>
</task>

</tasks>

<verification>
Phase 41 is complete when:
- BOTNAV-01: Bottom nav stays fixed and visible while scrolling on all 8 tabs (Chrome DevTools confirmed)
- BOTNAV-02: Last content item is fully visible above the nav bar on content-heavy tabs (Chrome DevTools confirmed)
- BOTNAV-03: iOS home indicator is not overlapped by the nav bar (Safari or real device confirmed, or skipped with note)
- BOTNAV-04: PWA update bar appears above the nav bar without blocking nav icons (DevTools simulation confirmed)
- All Vitest tests pass: npm test -- --run exits 0
</verification>

<success_criteria>
All 4 BOTNAV requirements verified by human in live browser:
- BOTNAV-01: chrome-approved
- BOTNAV-02: chrome-approved
- BOTNAV-03: ios-approved (or ios-skipped with documented note)
- BOTNAV-04: chrome-approved
</success_criteria>

<output>
After completion, create `.planning/phases/41-bottom-nav-consistency-ios-safe-area/41-03-SUMMARY.md`
</output>
