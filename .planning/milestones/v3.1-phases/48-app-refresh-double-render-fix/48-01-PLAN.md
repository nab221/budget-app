---
phase: 48-app-refresh-double-render-fix
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/ui/expenses.test.js
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "Three failing tests exist that will pass after the fix: render-once, transactionUI-render-once, no-dispatch"
    - "Tests are RED before any production code changes"
    - "Full test suite still passes (only new tests fail)"
  artifacts:
    - path: "src/ui/expenses.test.js"
      provides: "PERF-01 failing test block for double-render prevention"
      contains: "toggleExpenseStatus render count assertions"
  key_links:
    - from: "src/ui/expenses.test.js"
      to: "expenses.js toggleExpenseStatus"
      via: "spy on expensesUI.render and window.transactionUI.render"
      pattern: "vi\\.spyOn.*render"
---

<objective>
Write three failing unit tests in `src/ui/expenses.test.js` that define the PERF-01 contract before any production code changes.

Purpose: Nyquist Wave 0 — tests must be RED before implementation so that plan 02 has exact contracts to satisfy.
Output: A new describe block in expenses.test.js with 3 failing tests covering render-count and dispatch-suppression.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-app-refresh-double-render-fix/48-RESEARCH.md
@.planning/phases/48-app-refresh-double-render-fix/48-VALIDATION.md
</context>

<feature>
  <name>PERF-01 double-render prevention — Wave 0 failing tests</name>
  <files>src/ui/expenses.test.js</files>
  <behavior>
    Test 1 — expensesUI renders exactly once per toggleExpenseStatus call:
      - Spy on expensesUI.render (or the object the test file uses as expensesUI)
      - Call window.toggleExpenseStatus(id, 'recurring', 'pending')
      - Assert render call count === 1

    Test 2 — transactionUI renders exactly once per toggleExpenseStatus call:
      - Set window.transactionUI = { render: vi.fn().mockResolvedValue(undefined) } in beforeEach
      - Call window.toggleExpenseStatus(id, 'recurring', 'pending')
      - Assert window.transactionUI.render call count === 1

    Test 3 — toggleExpenseStatus does NOT dispatch app:refresh:
      - Spy on window.dispatchEvent
      - Call window.toggleExpenseStatus(id, 'recurring', 'pending')
      - Assert window.dispatchEvent was NOT called with a CustomEvent whose type is 'app:refresh'

    All three tests MUST fail (RED) before plan 02 touches expenses.js.
  </behavior>
  <implementation>
    Add a new describe block to expenses.test.js titled "toggleExpenseStatus — PERF-01 render coordination".

    Pattern to follow: existing tests in expenses.test.js already mock recurrentExpenseRepository and
    set up window.toggleExpenseStatus. Reuse those mocks. Read the top of expenses.test.js to see the
    existing mock setup and vi.mock calls before writing the new block.

    Key mock considerations:
    - window.transactionUI may not exist in the test environment — assign it in beforeEach as
      { render: vi.fn().mockResolvedValue(undefined) } and restore (delete) in afterEach.
    - For Test 3: use vi.spyOn(window, 'dispatchEvent') in beforeEach, restore in afterEach.
    - For Test 1: spy on whatever the test file calls as the expenses render method — follow the
      existing test pattern for how expensesUI.render is invoked/mocked.

    Commit message after tests are RED:
    `test(48-01): add failing PERF-01 render-count tests for toggleExpenseStatus`
  </implementation>
</feature>

<verification>
Run: `npx vitest run src/ui/expenses.test.js`

Expected: 3 new tests FAIL, all existing tests PASS.

The new test block should appear in output with FAIL status. If any new test passes, the test is not asserting the right thing — re-read the current expenses.js dispatch at line 271 to confirm the test is correctly detecting the broken state.
</verification>

<success_criteria>
- 3 new tests exist in expenses.test.js under "toggleExpenseStatus — PERF-01 render coordination"
- All 3 new tests are RED (fail) when run against unmodified expenses.js
- All pre-existing tests remain GREEN
- `npx vitest run src/ui/expenses.test.js` output shows exactly 3 failures, all in the new block
</success_criteria>

<output>
After completion, create `.planning/phases/48-app-refresh-double-render-fix/48-01-SUMMARY.md`
</output>
