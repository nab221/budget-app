---
phase: 49-reconciliation-mode-legacy-button-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified: []
autonomous: false
requirements:
  - RECON-01
  - RECON-02

must_haves:
  truths:
    - "Transactions tab toolbar shows exactly one button (Reconciliation Mode)"
    - "Clicking Reconciliation Mode reveals the reconciliation header with KPI values"
    - "Clicking Reconciliation Mode again hides the reconciliation header"
    - "No broken or orphaned buttons are visible anywhere in the Transactions tab toolbar"
  artifacts:
    - path: "index.html"
      provides: "Transactions tab with only #toggleIncReconBtn in toolbar"
  key_links:
    - from: "#toggleIncReconBtn"
      to: "#incReconHeader"
      via: "transactionUI.toggleReconciliationMode() toggles hidden class"
      pattern: "reconciliationMode.*toggle"
---

<objective>
Human verification that the Transactions tab toolbar is clean (one button only) and the reconciliation mode toggle works end-to-end in the live browser.

Purpose: Vitest covers DOM assertions and function correctness; browser verification confirms visual layout and live data rendering of the reconciliation KPI header.
Output: RECON-01 and RECON-02 confirmed in browser; Phase 49 complete.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/49-reconciliation-mode-legacy-button-audit/49-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 49-01 removed #markAllPaidBtn, #markAllPaidRow, and #triggerRecurrenceBtn from index.html,
    leaving only #toggleIncReconBtn in the Transactions tab toolbar. Vitest tests for RECON-01 and
    RECON-02 were added and are passing.
  </what-built>
  <how-to-verify>
    1. Open the app in a browser (run your dev server if needed)
    2. Navigate to the Transactions tab
    3. RECON-01 check — toolbar layout:
       - Verify the toolbar shows exactly ONE button: "🔍 Reconciliation Mode"
       - Verify there is no "✓ Mark all as paid" button
       - Verify there is no "🔁 Trigger Recurrence" button
       - Verify the toolbar has no empty gaps or broken layout from the removal
    4. RECON-02 check — toggle works:
       - Click "🔍 Reconciliation Mode"
       - Verify a reconciliation header panel appears below the button (showing Cleared Total / Month Total / Difference)
       - Verify the button changes appearance (primary style instead of ghost)
       - Click "🔍 Reconciliation Mode" again (or the same button now showing active state)
       - Verify the reconciliation header panel hides again
       - Verify the button returns to ghost style
    5. Regression check:
       - Scroll through the Transactions list — confirm rows still render correctly
       - Confirm the existing mark-as-paid buttons on expense rows still work
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval confirms:
- RECON-01: No dead or broken buttons in Transactions tab toolbar
- RECON-02: Reconciliation mode toggle shows/hides header panel correctly
</verification>

<success_criteria>
1. Human approves: Transactions tab toolbar shows only "🔍 Reconciliation Mode" button
2. Human approves: Clicking the button opens the reconciliation header; clicking again closes it
3. Human approves: No visual regressions in the Transactions tab after button removal
4. Phase 49 complete — RECON-01 and RECON-02 confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/49-reconciliation-mode-legacy-button-audit/49-02-SUMMARY.md`
</output>
