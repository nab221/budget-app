---
phase: 43-debt-history-modal
plan: 04
type: execute
wave: 4
depends_on: [43-03]
files_modified: []
autonomous: false
requirements: [DEBT-05, DEBT-06, DEBT-07]

must_haves:
  truths:
    - "User opens a loan/mortgage debt card and sees a Payment History button or section"
    - "Payment history modal lists every expected payment date from loan start to today"
    - "Unconfirmed payments show a Confirm Paid button"
    - "User can adjust the amount and confirm — payment appears on the heatmap"
    - "Missing paymentStartDate shows edit-debt hint, not a crash or empty list"
  artifacts:
    - path: "src/ui/debts.js"
      provides: "Complete loan payment confirmation flow"
      contains: "confirmLoanPayment"
  key_links:
    - from: "Debt card History button"
      to: "loan payment history list in modal"
      via: "debtUI.openHistoryModal -> _buildHistoryModalHTML -> _buildAmortisationModalHTML"
      pattern: "loan-history-list"
    - from: "Confirm Paid button"
      to: "heatmap cell colored"
      via: "confirmLoanPayment -> recurrentExpenses -> renderAll"
      pattern: "confirmLoanPayment"
---

<objective>
Human verification that DEBT-05, DEBT-06, and DEBT-07 are functionally complete in the browser. Claude has automated everything; this checkpoint confirms the UI flows work end-to-end on the actual app.

Purpose: Requirements confirmed by a human using the live application.
Output: Phase 43 marked complete, DEBT-05/06/07 checked off in REQUIREMENTS.md.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-debt-history-modal/43-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm green</name>
  <files></files>
  <action>
    Run the full Vitest suite and confirm all tests pass before presenting verification steps to the user.

    ```bash
    npx vitest run
    ```

    If any tests fail, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>npx vitest run exits 0, all tests green including the 5 new DEBT-05/06/07 tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify DEBT-05, DEBT-06, DEBT-07 in browser</name>
  <action>Present the verification steps below to the user and wait for approval.</action>
  <verify>
    <automated>MISSING — visual/heatmap verification requires browser; automated test suite covered in Task 1</automated>
  </verify>
  <done>User has typed "approved" after confirming all browser checks pass.</done>
  <what-built>
    Three functions added to debtUI in src/ui/debts.js:
    - generateHistoricalSchedule: filters amortisation schedule to dates on or before today
    - getConfirmedPaymentMap: loads confirmed payments from recurrentExpenses for a debt
    - confirmLoanPayment: upserts recurrentExpense with status:paid using user-supplied amount

    openHistoryModal extended: loan/mortgage modals now show a scrollable Payment History list below the amortisation summary. Each row shows payment date, scheduled amount, and either a "Paid" badge or "Confirm Paid" button. Clicking Confirm Paid reveals an inline amount input (pre-filled with scheduled amount) and a confirm/cancel pair.
  </what-built>
  <how-to-verify>
    Open the budget app in your browser and carry out these checks:

    **DEBT-05: Payment history list**
    1. Navigate to the Debt tab
    2. Tap or click a loan or mortgage debt card to open its history modal
    3. Confirm: a "Payment History" section appears below the amortisation summary
    4. Confirm: the list shows one row per expected payment date from the loan start date up to today (dates in the past, no future dates)
    5. Confirm: each row shows the payment date and a scheduled amount in £

    **DEBT-05 edge case: missing start date**
    6. If you have a loan/mortgage debt with no Payment Start Date set, open its modal
    7. Confirm: the modal shows a "No payment start date set" message with an Edit Debt link (not a crash or empty list)

    **DEBT-07: Amount adjustment**
    8. Click the "Confirm Paid" button on any unconfirmed payment row
    9. Confirm: an inline input field appears pre-filled with the scheduled payment amount
    10. Change the amount to a different value (e.g. edit it slightly)
    11. Click ✓ Confirm

    **DEBT-06: Payment appears on heatmap**
    12. After confirming in step 11, the modal should refresh automatically
    13. The confirmed payment row should now show a "Paid" badge instead of the Confirm button
    14. Navigate to the Dashboard tab
    15. Confirm: the heatmap cell for the payment date you confirmed is now colored (indicating a payment was recorded for that date)

    **Regression check**
    16. Open a credit card debt card — confirm the existing statement history still works normally (statements still load, mark-paid still works)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms DEBT-05, DEBT-06, DEBT-07 in the live browser. Full Vitest suite green.
</verification>

<success_criteria>
- npx vitest run: all tests green
- Payment history list visible in loan/mortgage modal (DEBT-05)
- Confirmed payment appears on heatmap (DEBT-06)
- User-edited amount is saved, not the scheduled amount (DEBT-07)
- Credit card history modal still works (no regression)
- Human has typed "approved"
</success_criteria>

<output>
After approval:
1. Update .planning/REQUIREMENTS.md — mark DEBT-05, DEBT-06, DEBT-07 as [x]
2. Update .planning/ROADMAP.md — mark Phase 43 complete with today's date
3. Update .planning/STATE.md — Phase 43 complete
4. Create `.planning/phases/43-debt-history-modal/43-04-SUMMARY.md`
</output>
