---
phase: 44-income-tab-cards
plan: "04"
type: execute
wave: 4
depends_on:
  - 44-03
files_modified: []
autonomous: false
requirements:
  - INCOME-01
  - INCOME-02
  - INCOME-03
  - INCOME-04
  - INCOME-05

must_haves:
  truths:
    - "User navigates to Income tab and sees source cards — not a table"
    - "User taps a card and a modal opens showing income entries for that source"
    - "User taps Confirm on an unconfirmed entry and sees date + amount inputs appear"
    - "User saves with default or edited values and the entry shows 'Received' badge on modal reopen"
    - "All 7 income-sources.test.js tests remain green after browser verification"
  artifacts:
    - path: "src/ui/income-sources.js"
      provides: "Fully implemented card layout and modal flow"
      contains: "openIncomeModal"
  key_links:
    - from: "Income tab card click"
      to: "modal with entry list"
      via: "data-action=open-income-modal → openIncomeModal"
      pattern: "open-income-modal"
    - from: "Confirm button click"
      to: "incomeRepository.add() + Received badge"
      via: "showIncomeConfirmPrompt → confirmIncomeEntry"
      pattern: "confirmIncomeEntry"
---

<objective>
Human browser verification that INCOME-01 through INCOME-05 work correctly end-to-end in the app. This plan gates phase completion.

Purpose: Unit tests verify logic; browser verification confirms the UI renders correctly, modal interactions work, and confirmed entries persist across modal reopens.
Output: Human confirms all 5 INCOME requirements satisfied. Phase 44 complete.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-income-tab-cards/44-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated gate</name>
  <files></files>
  <action>
Run the full Vitest suite to confirm all tests pass before requesting human verification. This ensures no regressions were introduced during implementation.
  </action>
  <verify>
    <automated>cd "D:/code/github/budget-app" && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Full vitest suite passes. Exit code 0. No failing tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Income tab card layout (INCOME-01) and per-source confirmation modal (INCOME-02, INCOME-03, INCOME-04, INCOME-05) implemented in src/ui/income-sources.js.

What was changed:
- Income tab now renders source cards (class="card clickable-card") in a grid3 layout instead of a flat table
- Clicking a card opens a modal listing upcoming income entries for that source (±90 day window)
- Each unconfirmed entry has a Confirm button that expands to date + amount inputs
- Saving writes to incomeRepository with the (optionally edited) date and amount
- Confirmed entries show a "Received" badge when the modal is reopened
  </what-built>
  <how-to-verify>
Open the app in a browser (dev server or built). Navigate to the Income tab.

**INCOME-01: Card layout**
1. Verify the Income tab shows source cards — NOT a table. Each configured income source appears as a card with the source name, pay rule pill, monthly amount, and "Click to view income entries" hint. Layout matches the Debt tab card style.

**INCOME-02: Card opens modal**
2. Click one of the income source cards. A modal should appear with title "Income: [Source Name]" and a list of upcoming income entry rows. Each row shows a date, amount, and either a "Confirm" button or a "Received" badge.

**INCOME-03: Confirm entry as received**
3. On an unconfirmed entry, click "Confirm". A date input and amount input should appear with pre-filled values. Click "Save". The modal should refresh and show a "Received" badge for that entry.

**INCOME-04: Date reschedule**
4. Open the modal for a source with an unconfirmed entry. Click Confirm. Change the date to a different date. Click Save. Close and reopen the modal — the entry should show "Received" (the confirmed entry was saved; the original projected date still shows as unconfirmed if it is different).

**INCOME-05: Amount adjustment**
5. Open the modal. Click Confirm on an unconfirmed entry. Change the amount to a different value (e.g., 2500 instead of the shown amount). Click Save. Verify the entry is now marked "Received". Check the Income tab or other views to confirm the saved amount reflects the edited value, not the original projected amount.

**Regression check**
6. Verify the Income tab still shows the "Add Source" button and the add/edit/delete source form works correctly — these should be unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 requirements pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 5 INCOME requirements satisfied:
- INCOME-01: Cards visible, no table layout
- INCOME-02: Modal opens on card click with entry list
- INCOME-03: Confirm button marks entry as Received
- INCOME-04: Date input saves alternate date correctly
- INCOME-05: Amount input saves override amount correctly
</verification>

<success_criteria>
- Human approves all 5 INCOME requirement behaviors in browser
- Full vitest suite green (pre-verification gate passes)
- Phase 44 marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/44-income-tab-cards/44-04-SUMMARY.md`
</output>
