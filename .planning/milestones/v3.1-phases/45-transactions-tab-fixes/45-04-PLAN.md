---
phase: 45-transactions-tab-fixes
plan: 04
type: execute
wave: 3
depends_on:
  - 45-03
files_modified: []
autonomous: false
requirements:
  - TRANS-01
  - TRANS-02
  - TRANS-03
  - TRANS-04
  - TRANS-05
  - TRANS-06
  - TRANS-07
  - TRANS-08

must_haves:
  truths:
    - "All 8 TRANS requirements confirmed by human in a live browser session"
    - "No regressions visible on the Transactions tab"
    - "All existing Vitest tests still green before verification begins"
  artifacts:
    - path: ".planning/phases/45-transactions-tab-fixes/45-04-SUMMARY.md"
      provides: "Verification sign-off with pass/fail per requirement"
  key_links:
    - from: "human tester"
      to: "browser at Transactions tab"
      via: "visual + interactive verification of each requirement"
      pattern: "n/a"
---

<objective>
Human browser verification checkpoint for Phase 45. All 8 TRANS requirements must be confirmed in a running browser before the phase is marked complete.

Purpose: Automated tests cover logic; browser verification catches visual state, CSS classes, UX flow, and cross-module integration that jsdom cannot simulate.
Output: Human sign-off on TRANS-01 through TRANS-08.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/45-transactions-tab-fixes/45-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification automated gate</name>
  <files></files>
  <action>
Run the full Vitest suite to confirm all tests pass before asking the user to open a browser.

`npx vitest run`

If any test fails, fix it before proceeding to the checkpoint. Do not present the checkpoint until the suite is green.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Full Vitest suite exits 0 (all tests pass)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 45 Transactions Tab Fixes — all 8 requirements implemented:
- TRANS-01: Mark Paid / ✓ Paid toggle button on expense rows
- TRANS-02: Confirm / ✓ Received toggle button on income rows
- TRANS-03: Single reconciliation mode button (expense recon button removed)
- TRANS-04: Single "+ Add" button that opens a type-selection modal
- TRANS-05: Sort order toggle button (↓ Newest First / ↑ Oldest First)
- TRANS-06: Expense amounts show − prefix; income amounts show + prefix
- TRANS-07: Search bar placeholder reads "Search transactions"
- TRANS-08: Category filter includes expense and debt-linked categories
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev` (or equivalent). Open the app in a browser and navigate to the Transactions tab.

**TRANS-01 — Mark expense as paid:**
1. Find any expense row (non-debt, labelled OUT).
2. Confirm a "Mark Paid" button is visible in the action cell.
3. Click "Mark Paid" — verify the button changes to "✓ Paid" with a green/success style.
4. Click again — verify it toggles back to "Mark Paid".

**TRANS-02 — Confirm income as received:**
1. Find any income row (labelled IN).
2. Confirm a "Confirm" button is visible in the action cell.
3. Click "Confirm" — verify the button changes to "✓ Received" with a green/success style.
4. Click again — verify it toggles back to "Confirm".

**TRANS-03 — Single reconciliation button:**
1. Inspect the toolbar above the transaction list.
2. Verify there is exactly one "🔍 Reconciliation Mode" button.
3. Verify there is NO "🔍 Expense Recon" button.

**TRANS-04 — Unified Add button:**
1. Verify the toolbar shows a single "+ Add" button (not "+ Add Income" and "+ Add Expense" separately).
2. Click "+ Add" — verify a modal opens with two choices: "+ Income" and "+ Expense".
3. Click "+ Income" — verify the income add form opens.
4. Repeat: click "+ Add" → "+ Expense" — verify the expense add form opens.

**TRANS-05 — Sort order toggle:**
1. Verify a "↓ Newest First" button is visible in the toolbar.
2. Click it — verify the transaction list re-orders (oldest at top) and button label changes to "↑ Oldest First".
3. Click again — verify the list returns to newest-first order.

**TRANS-06 — ± amount prefix:**
1. Scan the amount column in the transaction list.
2. Verify all income rows (IN) show amounts with a + prefix (e.g. +£1,234.00).
3. Verify all expense rows (OUT) show amounts with a − prefix using the proper minus sign (e.g. −£45.00).

**TRANS-07 — Search placeholder:**
1. Click into the search input field.
2. Verify the placeholder text reads "Search transactions" (not "Search income...").

**TRANS-08 — Category filter includes all groups:**
1. Click the "Categories" filter dropdown.
2. Verify you can see categories from expense groups (not only income categories).
3. If there are debt-linked expense rows visible in the list, confirm their categories appear in the filter dropdown.
4. Select an expense category — verify the list filters to show only matching rows.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 requirements pass. If any fail, describe which TRANS-XX requirement failed and what you observed — Claude will create a gap closure plan.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 8 TRANS requirements.
</verification>

<success_criteria>
- Human confirms TRANS-01 through TRANS-08 all pass in browser
- No visual regressions on Transactions tab
- Phase 45 marked complete in ROADMAP.md and STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/45-transactions-tab-fixes/45-04-SUMMARY.md`
</output>
