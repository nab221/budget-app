---
phase: 42-tab-button-uniformity
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified: []
autonomous: false
requirements:
  - TABUI-01
  - TABUI-02

must_haves:
  truths:
    - "User taps through all 8 tabs on a mobile viewport and each button keeps identical height and shape"
    - "User taps the Payoff tab and it does not grow, shrink, gain a pill background, or change border-radius"
    - "No shape transition or flash is visible on any tab tap"
  artifacts:
    - path: ".planning/phases/42-tab-button-uniformity/42-02-SUMMARY.md"
      provides: "Phase 42 verification record — TABUI-01 and TABUI-02 confirmed"
  key_links:
    - from: "css/main.css mobile .tab.active"
      to: "browser rendered tab buttons at 390px"
      via: "Chrome DevTools mobile emulation"
      pattern: "manual inspection"
---

<objective>
Human-verify that the CSS fix from Plan 01 resolves the tab button uniformity requirements on a mobile viewport.

Purpose: Both TABUI requirements are CSS visual rendering requirements that jsdom cannot verify. A human must confirm the rendered output in Chrome DevTools at 390px (or equivalent).

Output: Confirmed visual verification of TABUI-01 and TABUI-02, recorded in the phase summary.
</objective>

<execution_context>
@C:/Users/brito/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/brito/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/42-tab-button-uniformity/42-01-SUMMARY.md
@.planning/phases/42-tab-button-uniformity/42-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm regression suite still passes</name>
  <files></files>
  <action>
Run the full Vitest suite one final time before the visual checkpoint to confirm the CSS changes introduced in Plan 01 have not caused any JS regressions.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
  </verify>
  <done>All 722+ tests pass — exit code 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification — all 8 tab buttons uniform on mobile</name>
  <files></files>
  <action>
Pause for human visual verification. The automated work (CSS fix in Plan 01) is complete. This checkpoint confirms the visual output is correct before marking TABUI-01 and TABUI-02 as done.
  </action>
  <what-built>
    CSS fix in css/main.css: expanded mobile .tab.active rule (border-radius: 0, box-shadow: none, padding: 6px 0, border: none, font-weight: 500), transition: color override on mobile .tab, and .tab:active suppress rule. Together these prevent the desktop active-state pill shape from leaking to mobile on tap.
  </what-built>
  <how-to-verify>
    1. Open the app in Chrome (or run `npm run dev` if on a dev server — otherwise open index.html directly).
    2. Open Chrome DevTools (F12) → click the mobile device toolbar icon → set viewport to 390 x 844 (iPhone 14).
    3. Observe the bottom nav bar. All 8 tab buttons should be flat, equal-height bars with no pill shapes on any tab.
    4. Tap through all 8 tabs one by one. For each tab:
       - The active tab button should show the accent color text and icon — no pill background, no box shadow halo, no border-radius change, same height as inactive buttons.
       - The inactive buttons should look unchanged.
    5. Pay particular attention to the Payoff tab (📈): tap it and confirm it does not grow, shrink, or gain a rounded pill shape on tap.
    6. Tap each tab several times quickly and confirm no shape flash or animation occurs during the tap gesture.
    7. Also verify at 360px viewport (the smallest breakpoint) — tap through tabs and confirm label-hiding does not introduce any height difference between active and inactive.

    Expected result: Every tap through all 8 tabs leaves each button visually identical in size and shape. Active state = accent color only. No pill, no shadow, no height change.
  </how-to-verify>
  <verify>
    <automated>MANUAL — jsdom cannot verify CSS cascade or :active pseudo-class rendering. Human confirmation required.</automated>
  </verify>
  <done>Human confirms all 8 tab buttons pass visual uniformity check at 390px and 360px viewports.</done>
  <resume-signal>Type "approved" if all 8 tabs pass, or describe which tab(s) still show shape changes so a gap-closure plan can be created.</resume-signal>
</task>

</tasks>

<verification>
TABUI-01: Confirmed by human — all 8 tab buttons identical height and shape in active and inactive states at 390px viewport.
TABUI-02: Confirmed by human — Payoff tab does not change shape or size on tap.
Regression suite: npm test -- --run passes with no failures.
</verification>

<success_criteria>
- Human confirms all 8 tabs maintain uniform shape in both active and inactive states
- Human confirms Payoff tab does not grow, shrink, pill, or flash on tap
- npm test -- --run exits 0 (no regressions)
- TABUI-01 and TABUI-02 can be marked complete in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/42-tab-button-uniformity/42-02-SUMMARY.md`
</output>
